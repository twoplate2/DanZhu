[app]

# 启动器中显示的可见名称
title = 跳跳的弹珠机

# 内部包名(小写,无空格,无中文)
package.name = plinko
package.domain = org.danzhu

source.dir = .
source.include_exts = py,png,jpg,kv,atlas,ttf,otf,wav,mp3
# assets/*.png = 中奖玻璃杯表现的三张分层贴图(pile3d 的容器剖面与其同源)。
# 说明: png 本来就在 include_exts 白名单里, 这行是双保险 —— BUILD_APK.md 有
# "子目录资源必须显式列"的历史经验, 加它无害, 真伪由出包后解 private.tar 验证。
source.include_patterns = fonts/*.otf,voice/*.wav,assets/*.png

# 0.6.6(2026-09-11): "弹珠落容器"(装杯演出)的**触发时刻延后 0.15s**(用户定案)。
# 用户给的规格:
#   当前   —— 第0秒进倍率槽: 立即播声音 + **立即(第0秒)触发落容器事件**
#   调整后 —— 第0秒进倍率槽: 立即播声音, **第0.15秒才触发落容器事件**
# 改动面: `RootWidget.settle()` 中奖分支里那句 `win_fx.play_win(...)` 连同紧跟的
#   `_reveal_deadline` 计算一起挪进 `Clock.schedule_once(..., CUP_TRIGGER_DELAY)`。
#   账务 / 指示灯 / 槽位白闪 / **入袋音** 全部留在 t=0 不动 —— 用户明确要求"立即播声音"。
#   演出内部时间轴(WINDUP / 压暗 0.30 / 杯子 0.32 / 落珠 / 回味 / 退场)一律不动,
#   所以整场只是起点后移 0.15s, **总时长不变**。
# ⚠️ `_reveal_deadline` 必须先归零再调度: 兜底判据里带 `and self._reveal_deadline`,
#   不归零的话上一轮残留的非零 deadline 会在这 0.15s 窗口里让兜底提前开火(提前剧透)。
# ⚠️ `park_ball` 不会抢跑: `_land_hold` 最小值 0.3s > 0.15s(注释里写死了这条依赖)。
# 实测(探针): 触地 -> 演出起点 = **0.648s**(= WINDUP 0.50 + 0.15), 阴性对照把延迟改成 0
#   量到 0.507s, 证明探针有分辨力; 触发前 mode 仍是 idle、窗口内 `_reveal_done` 始终 False。
# 冒烟同步更新: settle 那一刻断言 cup==idle(未触发), 过延迟后再断言 cup!=idle(已触发)。
#
# 0.6.5(2026-09-11): 装杯的音量调大(玩家: "弹珠掉落容器的声音, 音量太小了")。
# 病根不是波形, 是装杯这边自己把音量掐窄了:
#   主游戏(球落倍率槽)给的是 clamp(vy/500, 0.3, **1.0**) —— 同一个波形能到满幅;
#   装杯是 max(0.20, min(0.75, gain)), 而两颗球的原始 gain 只有 0.42~0.72 / 0.25~0.45
#   → 实际落在 0.25~0.72。满幅不会削波(bounce 峰值 16383/32767 = -6dBFS, 还有 6dB 余量;
#   Sfx.play 的 lvl=10 就是原始 PCM)。
# 改法: 只加一个 `BOUNCE_GAIN_BOOST = 1.8`(再夹到 [0.35, 1.00]), 两跳的 base/amp 抽成常量。
# ⚠️ **没动 `_sfx_bounce()` 的合成增益** —— 那个波形主游戏共用, 一改就连主游戏一起变响。
# 实测音量(0~10 档): 第一跳 0.42/0.72 -> 0.76/1.00; 第二跳 0.25/0.45 -> 0.45/0.81
#   (主游戏落槽是 0.30~1.00, 现在两者同档)。
# `fx_probe [14]` 钉住: 两跳的最低音量不得低于主游戏那个 0.55, 且夹完不超 1.0。
#
# 0.6.4(2026-09-11): **跑分期间照常播装杯演出**(玩家: "性能测试有bug, 你丢掉了落袋动画",
#   并要求保留; 明确接受代价"保留动画, 接受跑分数字偏低")。
# 改动: `settle()` 里那条 `if _bench_running: 直接揭晓, 不播演出` 的分支**删掉**, 走正常路径。
# 代价(实测, 记档免得以后又被人"优化"掉):
#   · `_auto_launch_tick` 要等 state 回 ready 才发下一发, 而装杯期间 state 是 landed
#     ⇒ 跑分那 3 发的采样窗口从 ~1.5s 拉长到 ~15s, 里面混进大量装杯渲染 ⇒ 平均帧率偏低;
#   · 全程时长 10s -> **实测 25.1 秒**(菜单里那句提示已同步改成"约 25 秒")。
# 旧理由留档: 1) 装杯会盖住置灰层 —— 已不成立(置灰层在 RootWidget.canvas.after, 本来就盖在
# 装杯之上, 所以玩家看到的是**变暗的**装杯); 2) 装杯渲染会污染帧率 —— 就是上面那条, 现在接受。
# ⚠️ 置灰层**没有一起去掉**: 跑分期间整屏仍压 0.72 的灰。要连它一起去掉是另一个决定, 待用户定。
# 冒烟断言同步反过来: 跑分期间**应该**在播装杯(原来断言的是"不该播")。
#
# 0.6.3(2026-09-11): "制作时刻"的年月日写成汉字 + 时间钉死 24 小时制(用户定稿)。
# 格式抽成模块常量 `BUILD_TIME_FMT = '%Y年%m月%d日 %H:%M'`。
# 为什么年月日要汉字: `2026-09-11` 这种全数字写法在中文语境下容易被读反(有人按 日/月 读)。
# 为什么必须 %H: 用 %I 会变成"下午 1:49"那种 12 小时制, 跟历史记录里的口径也不一致。
# `fx_probe [13]` 加了一条**功能性**断言(不是 grep 源码): 直接拿 23:05 与 00:05 两个会暴露
# 12/24 制差异的时刻去格式化 —— 12 小时制下会变成 11:05 / 12:05, 当场红。
# 阴性对照已做(%H->%I 门禁 exit 1)。最终形态: `于 2026年09月11日 01:51 制作`。
#
# 0.6.2(2026-09-11): 性能测试的**成绩面板去掉**版本/制作日期行(用户: "成绩面板别加")。
# 成绩面板只放成绩; 版本/日期留在**长按标题的菜单弹窗**里(那是用户最初要的位置)。
# 成绩面板的高度随之从 320 退回 300(标签 185 -> 160)。
#
# 0.6.1(2026-09-11): 补齐"后层那两半环"的补画 —— 上一版**只补了杯口环, 漏了杯底环**。
# 专家第二轮实测指出: 杯底环的后半(back 层的 base_back, alpha 只有 22)一并被压暗之后,
# 净贡献从 16.7 掉到 5.3(杯内板面 10.7), 读出来就是"杯子底部后半圈没画"。它和杯口远环是
# 同一个根因, 但相对损失更大 —— 杯口环 alpha 68 有余量, 杯底环只有 22。
# 改法: 把"补画哪几段"从单条常量改成 `RIM_BACK_BANDS`(两段: 杯口环后半 贴图行 0..166、
# 杯底环后半 贴图行 694..808), 每段照旧横切成 12 条做纵向渐变。`_rim_band_strips` 改名
# `_rim_back_strips`, 返回可直接画的 (贴图, 纵向起点, 高度, alpha) 列表。
# `fx_probe [11]` 同步扩成两条带: 段数 x 带数、以及**每条带必须铺满该段、底边接到切开线、
# 顶边接到贴图起点**(这条断言当场抓到过一次我自己的几何算错)。
# 另: 在 `CUP_L` 处补了一条防雷注释 —— 杯子矩形比游戏区宽 12.5 逻辑 px, 而压暗矩形 = 游戏区
# 矩形; 今天没穿帮只因为玻璃 PNG 的内容边界留了 3.85px 余量。谁给玻璃加外发光/把杯口画宽/
# 裁掉透明边, 杯壁上就会凭空出现一条竖直硬边。
#
# 0.6.0(2026-09-11): 大版本号进位(用户定案 v0.6)。功能上 = 0.5.30 的两个文案修正:
#   · 那一行改成「于 <日期 时间> 制作」(用户两次定稿: 不用「构建」这个行话, 也不要"制作+日期",
#     要"日期+制作"且前面加个"于") —— 最终形态 `v0.6.0 · 于 2026-09-11 01:35 制作`。
#   · 冒烟夹具修一处**偶发误报**: s9b 跑分前的 `_easter_popup` 没清, 前面某一局真彩蛋留下的
#     引用会让"跑分中不该弹彩蛋窗"这条断言误判(SMOKE-FAIL hold=False 却报了)。清掉即可。
#
# 0.5.30(2026-09-11): 构建日期补上时分, 那一行字号 13sp -> 16sp(玩家: "构建的字太小")。
# 格式: `v0.5.30 · 构建 2026-09-11 01:29`。`fx_probe [13]` 的格式门禁同步收紧。
#
# 0.5.29(2026-09-11): 长按标题的隐藏弹窗加一行"版本 · 构建日期"。
# 版本走 Android PackageManager 的 versionName(就是 buildozer.spec 的 version);
# 日期取 main.py 的文件 mtime。
# **为什么不把日期烘成源码常量**: `tools/build_android_main.py` 是纯字符串拼接、不做任何
# 改写 —— 源码里写死日期, `--check` 每次都报"生成物与源不同步"。运行时取 mtime 与生成
# 过程完全解耦, 生成器一个字都不用改。
# mtime 为什么约等于构建日: p4a 打包时把整个 app 目录塞进 APK 里的 private.tar, 条目时间戳
# = 构建机上文件的写入时间(CI 是新拉代码后立刻构建), 首次运行解包时 tarfile 保留 mtime。
# 两个弹窗都加了(菜单 + 结果); 全程 try/except, 拿不到就少一行, 绝不把弹窗带崩。
# `fx_probe [13]` 钉住: 不抛异常 + 日期格式必须是 YYYY-MM-DD(设备时钟不对时不显示怪日期)。
#
# 0.5.28(2026-09-11): 杯口环从"两个明暗层次"改成**纵向渐变**。
# 玩家: "明暗可以不一样, 但是目前的方案是没有过渡, 只有两个明暗层次"。
# 根因: 后半个杯口环画在 back 层(被压暗)、前半个画在 front 层(不压暗), 两者在杯子
#   左右两侧**直接拼上** —— 0.5.27 整条一个 alpha 补画, 只是把台阶从"25 vs 120"挪成
#   "80 vs 120", 仍然是硬切。
# 改法: 补画那条横切成 `RIM_BAND_STRIPS=12` 段, 每段一个 alpha, 从顶部
#   `RIM_BAND_ALPHA_TOP=0.55` 线性升到杯口分界处的 1.0 —— 相邻两段只差 4%, 看不出台阶;
#   分界两侧都接近 1.0, 接缝自然消失。实测沿环走一圈: 远侧中心 ~42 → 两侧 ~70 →
#   近侧 ~120, 连续变化(改前是 25 / 120 两级)。
# `fx_probe [11]` 补两条: 段数与每段尺寸、以及"补画强度必须严格递增且首尾 == 设计值"。
#
# 0.5.27(2026-09-11): 修 0.5.26 引入的两处**边缘**问题(玩家实测反馈)。
#
# (1) **压暗层没铺到屏幕边**(玩家: "新容器的边缘的什么的似乎有问题")。
#   差分对比"未装杯 vs 装杯中"两张截图实测:
#     · 宽窗口横屏 1400x1000: 只盖住物理 x∈[0,1277], **右边 122px 与顶边 56px 原样亮着**;
#     · 竖屏 540x960: 五行之间的 5 条 spacing(10px)与底部 padding(12px)共 62px 没盖,
#       亮度 20.6 而相邻被压暗的行只有 16.8~19.6 —— 缝成了全屏最亮的东西,
#       横屏反旋转时是**6 条贯通全高的竖亮线**;
#     · 横屏 game_area.y = -78 < 0 ⇒ 下块被 `max(0.0, ga.y)` 算成**高度 0**, 下方两行全没盖。
#   根因: 按 `-self.x` 反推留白宽度算错(Kivy 是单一坐标系, `AnchorLayout` 的矩形才是等效视口)。
#   改法: 从"每行各盖一块"改成"**整块屏幕减去游戏区**"的两块矩形, 视口直接读父容器矩形。
#   修后差分: 行 (0,999) / 列 (0,1399) —— 整块盖满; 竖屏 (0,959)。缝 20.6 -> 11.7。
#
# (2) **杯口上弧(远侧)被压暗 57%**(玩家: "上边缘的内侧和外侧颜色差异较大")。
#   改动 (2) 把压暗挪到后层玻璃之后, 代价是后半个杯口环跟着被压暗:
#   实测峰值 **68.0 -> 29.0**, 前半圈(front 层)纹丝不动 ⇒ 前后对比从 1.76:1 拉到 4.1:1,
#   那半圈读起来像"没画出来"。近亮远暗本身是对的(真实玻璃就是远侧暗, 改前就有),
#   但 4:1 过了。
#   改法: 只把 back 里**后半个杯口环那一条**(贴图行段, 几何切 —— **不是**按 alpha>60 挑,
#   那样会把杯体薄纱一起捞回来)在压暗之上补画一次。实测 29.0 -> 80.7(改前 68.0),
#   而杯内薄纱仍是 10.7, 与不补画时一致 —— "板面被杯子提亮"的元凶没有被放回来。
#
# (3) **回退路径**(分层玻璃图加载失败只画整图)原来画在压暗**之后** ⇒ 回退局里杯子比
#   正常局亮一大截、前后遮挡也没了。现在把整图当"后层"用(画在压暗之前), 正常路径零影响。
#
# (4) 玻璃贴图预热进 `prebake_step`(首次 `_glass_textures()` 要现场解码+上传, 实测 21ms,
#   2x 资产在平板上 40~80ms, 正好落在冷启动后第一次中奖那一帧)。
#
# 配套门禁: `fx_probe [11]` 的接线断言全部改成**几何**判据(两块矩形是否恰好盖满"整块屏幕
# 减去游戏区"、是否与游戏区零重叠、视口变宽时是否跟着铺满), 并新增"压暗必须在弹珠之前"
# 与"杯口环补画必须在压暗之后、弹珠之前"。文本断言改读**出货的 android/main.py**。
# **十条阴性对照全部确认门禁会红**(含三条新盲区: 盖不全 / 只盖 RootWidget 宽 / 球被压暗 /
# 补画位置错)。专家对抗性审查(codex + Claude)跑了三轮: 第一轮挑出的"同帧不同步"、
# 第二轮挑出的"缝没盖到 / 球没被压暗这条门禁是瞎的 / 断言查的是源不是出货文件"全部已修。
#
# 0.5.26(2026-09-11): 装杯演出的**压暗**、**清晰度**、**震动**三件事。
# 玩家原话: ①「在表演弹珠落入容器的时候，背景部分是不是灰化的不够（也就是说之前的
# 游戏界面是不是不够暗？）」②「表演弹珠落容器的时候，是不是感觉弹珠的绘制的精度不够，
# 有点模糊」③「既然说模糊，你看看还有什么界面中的元素/组件也可能是模糊的，一并改了」。
# 定性之后改了三处(全部有实测数字, 全部过了门禁 + 阴性对照):
#
# (1) **HUD 五行也压暗**(覆盖面, 不改 alpha)。
#   板面那块压暗矩形 = GameArea, 而 RootWidget.children 顺序是
#   [顶栏,返奖档,投注档,GameArea,信息行,底行] —— 后 add 的后画 ⇒ **信息行和底行画在
#   GameArea 之上**, 板面压暗永远盖不到它们。横屏反旋转时五行正好落在画面两侧:
#   实测左条亮度 36.6 / 右条 29.6, 而压暗后的板面只有 14~17 ⇒ "板面已黑、两边还亮"。
#   现在五行各自在**自己的 `canvas.after`** 里压一层(`_row_dim`), `_sync_hud_dim`
#   每帧从 `WinPileFX.dim_alpha(HUD_ALPHA)` 取值统一写 —— 生灭曲线真源仍只有 `_layers()`。
#   实测: 五行 65.1/42.8/40.9/36.0/45.2 -> 18.7/12.6/12.4/14.3/12.0, 演出后**精确复原**。
#   横屏实测: 左条 36.6->17.7 / 右条 29.6->15.8, 演出后回到 36.6/29.6。
#
# (2) **压暗的绘制次序**: 原来是 `压暗 -> 后层玻璃 -> 球 -> 前层玻璃`,
#   改成 `后层玻璃 -> 压暗 -> 球 -> 前层玻璃`。
#   后层玻璃(球堆区覆盖 95% 面积、alpha 均值 22.6/255、色近 (166,194,210))会把刚压暗的
#   板面**提亮回去** —— 实测杯子区域只被压掉 12%, 而杯外板面压掉 46%,
#   **玩家盯着的杯子周围反而是全画面压暗最失败的地方**。改后该区域 34.0 -> 25.8。
#   放到前层玻璃之后是错的(会把弹珠本身也压暗)。
#
# (3) **玻璃贴图 800x460 -> 1600x920**(设计层没动, 几何仍是 design 800x460)。
#   真机上(Y700 平板)杯子的屏幕宽是 1612px, 1x 贴图 = 放大 2.02x 采样, 杯壁的结构边/
#   高光发糊。生成器本来就是 SCALE=8 超采样到 6400x3680, 所以**只改了 `_save()` 的
#   降采样目标**(OUT=2), 画法一行没动。新旧 bbox 除以 2 完全一致。
#   **弹珠纹理刻意没动**: 两轮专家各自实测 `d=128->256` 收益 ≈ 0(中位差 1/255、
#   均方根 2.21/255), 而纯 Python 合成耗时 x4 —— 球贴图的边缘羽化写的是**半径百分比**,
#   软边在屏幕上的宽度与纹理边长无关。
#
# (4) **装杯震动**(玩家追加需求: "弹珠落入容器的时候设备应该震动, 震动和落地音效同步")。
#   实现在 `WinPileFX._bounce`: **挂在 `sfx.play(...)` 的返回值上** —— 节流只有一个真源
#   (`BOUNCE_THROTTLE=0.10`), 震动不自己再判一次时间, 否则两处必然漂移(玩家会"听到响
#   但手上没感觉")。副作用有意为之: `音效已关` 时不震(本作只有一个反馈开关, 关了声音
#   的人多半也不想在会议里被震四秒)。参数 10~18ms / 振幅 80~220, 随撞击强弱走 ——
#   **刻意又短又不满**: 脉冲一长就糊成"持续嗡嗡"而不是"珠子一颗颗落进杯子"。
#   桌面测不到真震动, 靠 `fx_probe [12]` 钉住"响一次恰震一次、且时刻逐个相同"。
#
# 配套: `fx_probe` 新开 [11](压暗覆盖/绘制次序) 与 [12](震动同步), [2] 补强(前层尺寸、
# 整图回退尺寸、linear 滤波、前后两层边距一致)。**七条阴性对照都确认门禁会红**。
#
# 审查: 这轮过了专家对抗性审查(codex 外部 CLI), 它挑出的**同帧不同步**已修 ——
# 原来 `_sync_hud_dim()` 排在 `tick_draw()` 之前、且与板面各取一次 `time.time()`,
# 退场结束那帧 HUD 会比板面多黑一整帧。现在 `dim_alpha()` 读的是 `_redraw` 本帧写入的
# `_a_dim_now`(同一个数), 且 `_sync_hud_dim()` 排在 `tick_draw()` 之后。
#
# 0.5.25(2026-09-10): **修 0.5.23 引入的严重回归 —— 中奖揭晓回调丢失**。
# 玩家报: "还没播放落入容器的动画就播放中奖声音" + "完全不播放中奖声音"。
# 根因(三方专家独立复现, 我自己也复刻了 tick 分支语义验证):
#   0.5.23 把揭晓从 `_last_touch` 推到 `_last_touch + REVEAL_DELAY`, 但**判定点仍被关在
#   `if self.mode == "win"` 分支里**, 而 win->result 的切换点是 `_last_settle` ——
#   两者之差 `lag = _last_settle - _last_touch` 实测只有 **0.185~0.323s**(均值 0.254),
#   恒小于 REVEAL_DELAY。于是 mode 先跳走, 这个一次性事件再也判不到:
#     · 回调一路挂到**下一局**才被 play_win 的补执行放出来 —— 上一局的数字/语音画在新一局
#       开头(杯子还没出现), 而新一局自己的大字被 `_reveal_done` 吞掉(实测 258/300 局);
#     · 最惨的是"中奖后不再发射"—— 没有下一局, 中奖音**永久丢失**;
#     · 9s 硬兜底救不了: `busy()` 第一行对 idle 短路, 根本不调 `_abort()`。
#   实测丢账率: 60fps 下 86%(120fps 90%, 30fps 79% —— 越快的设备丢得越多)。
# 修法(三条, 一起上才有意义):
#   ① `WinPileFX.tick()`: 把所有 mode 迁移走完, 末尾统一调 `_pump_reveal(now)` ——
#      揭晓从此**不依赖 mode**, 只认绝对时刻 + `_done_fired` 单向闩; `_balls` 为空那条
#      是**排空**分支(result->idle 那帧会先清空 _balls, 紧接着走到这里), 欠的账必被放掉。
#   ② `_abort()` 补 `_done_fired = True`, 让闩与"回调已原子消费"一致。
#   ③ `RootWidget`: 幂等闸收到唯一一处(settle 的 `_on_settled`, **同时盖住语音** ——
#      原来语音完全没闸); 加 `_win_seq` 序号丢弃上一局的迟到回调; 兜底改调**同一个闭包**
#      `_settle_cb`(原来兜底自己写了一份"揭晓该做什么", 那份没有语音 —— 这就是丢音的直接原因)。
#      `_reveal_win` 自己的 `_reveal_done` 闸删掉: 两处各判同一个东西必然静默脱钩。
# ④ **REVEAL_DELAY 0.3 -> 0.4**(用户定案)。0.3 本来就盖得住最后一颗球的末声
#    (末声在 `触地 + t1`, `t1 ∈ [0.13,0.20]`), 0.4 是给球堆余音留更宽余量。
#    提醒: 用户最初要的语义("等最后几下落地的声音播完再报喜")**本身没错**, 错的是判定位置。
# ⑤ 新门禁 `fx_probe [10]`「揭晓的投递语义」: 受控时钟逐帧驱动真 `tick()`, 断言
#    每局恰好放一次 / 3 帧内 / 收尾不留挂账。**判别力已反向验证**: 把修复退回原样,
#    这条立刻红(269/280 局丢账), 而前面九节仍全绿 —— 缺的就是"投递语义"这个不变量。
#    另修 `[5]` 的种子运气绿(margins 阈值由拍脑袋的 0.5 改成按解析下界推的 0.30) +
#    新增"揭晓必须晚于末声"把用户语义钉成不变量。
# 实测(420 局受控时钟): 丢账 0 / 双响 0 / 挂账 0 / 偏移 0~1 帧(修复前 86% 丢账 + 86% 挂账)。
# 0.5.24(2026-09-10): 修横屏下中奖大字浮字偏左 370px(玩家: "弹珠+xxx 在特殊情况下偏左很多")。
# 病灶: `big_result_text()` / `center_toast()` 里 `cx = self._px(CW/2.0) - self.x`。
# `_px()` 返回的**已经是 Kivy 窗口绝对坐标**(追踪实测横屏 `_px(CW/2)=870.0`), 而本项目的
# 子控件 canvas 就是**绝对坐标、父级不做平移**(CLAUDE.md「坐标系」那条坑, 当年像素实测确认)。
# 于是再减一次 `self.x` = 把大字左移整整一个 `GameArea.x`。
# **竖屏 `GameArea.x` 恒为 0, 减了个 0 所以一直没暴露**; 横屏反旋转时 x=370(1740x1000),
# 大字就偏左 370px —— 这就是"特殊情况下"= 横屏的由来。
# 修法: 横向去掉 `- self.x`(两处)。**竖屏下数学上是恒等变换**(x≡0), 所以对既成观感零影响。
# ⚠️ 纵向的 `- self.y` **本轮没动**: 竖屏 y=122, CLAUDE.md 记着"是被眼睛调过的既成观感"。
#    注意它在横屏下同样是错的(横屏 y=-248, 会反向偏 248px) —— 但改它要重调 `TEXT_CY_WIN`
#    并影响竖屏观感, 留给用户定夺, 不夹带在这个提交里。
# ⚠️ 验证说明: 桌面上没能拿到干净的像素级验证 —— 第一次按颜色找字误检了盘面自己的紫色
#    倍率色块, 第二次帧差混进了飞行的球。本修复的依据是**代码链**(绝对坐标 + 追踪实测的
#    _px/self.x 取值), 不是截图对比。真机确认请开一局中奖看大字是否居中。
# 0.5.23(2026-09-10): 两处玩家反馈。
# ① **揭晓延后固定 0.3s**(用户定案)。中奖音 + "弹珠+xx" 语音原来挂在"最后一颗球**第一次
#   触地**"那一刻, 但那颗球还要再弹 0.20~0.32s 才停住, 而中奖琶音是全库最响的音
#   (-1.4dBFS、带混响、最长 1.8s)—— 一进来就把最后两三下落地声全盖住。
#   玩家原话: "珠子还没完全落进容器就弹提示音, 我甚至听不到弹珠落地的声音"。
#   新增 `REVEAL_DELAY = 0.3`(在 tools/android_part_pile.py), 揭晓 = 触地 + 0.3s。
#   **为什么用固定值而不是跟 `_last_settle` 走**: 那个每局在 0.196~0.315 之间抖, 钉死
#   更好预期, 也避免个别局退化成"揭晓紧跟触地"或"揭晓拖到球停稳之后"。
#   单一真源 = `WinPileFX.reveal_sec()`(相对)/ `reveal_at()`(绝对); tick() 和 ui 的
#   兜底 deadline 都从它取值。
#   ⚠️ 顺带修了一个**改了揭晓时刻才会暴露的坑**: `_reveal_deadline` 原来按
#   `expected_sec - (hold_for + RESULT_FADE)` 反推, 等价于"球停住 + 0.05s"。揭晓改成
#   "触地 + 0.3" 后两者只差 0.196~0.315s, 于是兜底会比真事件**早**最多 0.054s 触发 ——
#   数字/语音在球停稳前就冒出来, 正是当初要消灭的"提前剧透"。现在直接吃 `reveal_at()`。
#   `fx_probe` [5] 补两条门禁: 揭晓必须 = 触地+REVEAL_DELAY、且离淡出起点还剩 >=0.5s
#   (实测最紧一档 0.55s)。七档揭晓时刻实测 1.26~2.83s。
# ② **弹窗标题居中**。Kivy 的 `Popup.title_align` **默认是 'left'**, 而本项目五处弹窗
#   一个都没设过 —— 标题贴左边缘, 与主界面严格居中的标题栏不一致, 玩家报"弹窗文字没居中"。
#   修在 `_popup()` 里 `kw.setdefault("title_align", "center")`(调用方仍可覆盖)。
#   ⚠️ 附一条排查结论, 免得下次重走: 这个**不是横屏特有**。用 `--landscape`(1740x1000)
#   逐控件量过几何: 弹窗中心与窗口中心偏差 (0.0, 0.0), 弹窗内所有 Label/Button 的
#   相对位置与竖屏逐项一致 —— 两种方向下排版完全相同。
# 0.5.22(2026-09-10): 两件事。
# ① **中奖装杯的落杯音试过换掉, 已回退**(用户定案)。曾经把装杯的 `bounce` 换成 4 个变体的
#   合成"玻璃音" `cup0..3`(1960~2540Hz 非谐分音 + tau 0.15 长衰减), 动机是实测 `bounce`
#   96% 能量在 300Hz 以下、过手机喇叭低切掉 15dB。**用户听完的判决: "这个新的是电子音,
#   而不是小球撞击的声音" -> 全部回退。** 回退面 = plinko.py 的 CUP_TONES/_sfx_cup/
#   CUP_LAND_TONE/_sfx_cup_land + iter_bank 两条 yield + 试听表 5 条; tools/android_part_pile.py
#   的 CUP_VARIANTS/CUP_THROTTLE/CUP_HOLD_GAIN/CUP_A*/CUP_G*/_cup_gain/_cup_land + 球里的
#   cup 字段 + tick() 的落位音; tools/fx_probe.py 的 [9] 段; 删 tools/cup_demo.py。
#   音效数回到 41, `bounce`(0.110s/峰值 0.500)与改动前逐位一致。
#   ⚠️ **可复用的教训**(写进两处代码注释了, 别再走一遍): 撞击感来自**攻击瞬间的宽带噪声**,
#   不是分音 —— 主导成分只要是几个正弦分音 + 长衰减, 无论基频放哪、用不用非谐倍数,
#   听起来都是"电子音/音调"。要撞击声, 噪声瞬态必须比振铃更响且够宽(到 8kHz), 分音只当尾巴;
#   库里现役的 `_sfx_tink`(撞钉)就是这个正例, 该照它改。
# ② **最高档(360%)的盘面有奖格 5/6/7 -> 6/7/8**(K_DIST[3.60]), 实测中奖率 65.6% -> 78.6%。
#   起因: 300%->360% 那次只改键名、格数没动, 中奖率和 300% 档一模一样, 玩家察觉不到换档。
#   RTP 仍精确 3.600000(_solve_p2 闭式反解自动配平), 代价是 x2 权重 0.274 -> 0.439,
#   每次中奖平均赔付 5.475 -> 4.558。中奖率与每次赔付是同一个旋钮的两端。
#   ⚠️ 另注: 改最高档盘面后必须重跑自测 RTP 门禁(本次已跑, 结果 OK)。
# 0.5.21(2026-09-10): 最高返还档 300% -> 360%(用户定案)。改动面 = 数值表 + 盘面生成 +
# UI 按钮 + 语音 + 防沉迷白名单 + 自测门禁, 全部同步(用户要求的"同步其他调整"):
#   VALUE_SHAPE / K_DIST 的键 3.00 -> 3.60; VALUE_DIST 构建循环; roll_multipliers 的调用方
#   (`_boards` 两处 + PC/安卓各一处); 按钮 ("360%", 3.60); 配置文件白名单; sim_balance.py;
#   新增语音 voice_rtp_360("百分之三百六十"), 删掉孤儿 voice_rtp_300; 文档四处。
#   RTP 是闭式反解的(_solve_p2): 改键之后 x2 权重自动重新配平到精确 3.600000, 不用手调。
#   ⚠️ 自测门禁复查过: 最高档 σ=9.25, n=40000 时 3σ=0.139, 离 0.15 门禁只剩 0.011 ——
#   以后再往上加档(比如 400%)必须先同步放宽门禁, 否则每次自测约千分之一概率假失败。
# 0.5.20(2026-09-10): 中奖杯球径顶到容量上限 —— x100 实 r 42.8 -> 46.5, 堆顶从 design y=61
# 抬到 41(正好顶到杯口), 杯子明显装满且球贴壁。N<=60 那档 21 -> 23(容量上限 38.5, 安全)。
# 球径不是审美选的: build_pile 装不下就把球缩 6% 重试, 所以 N=100 的球径被卡在
# "刚好塞得进杯口以下"(实测 120 个种子一律收敛到 ~46.5)。
# 真"溢出"做不到, 是几何: 层间距 1.633r≈76 design px, 而杯口到画布顶只有 ~80 ——
# 多铺一层直接冲出画布(y=-51, 球会画到杯子上方像悬在空中); 且缩小兜底会抵消任何
# "收窄杯口以下挤出冒尖球"的尝试(实测 40 组组合无一成功)。详见 pile3d.OVERFLOW_MAX 注释。
# 0.5.19(2026-09-10): 装杯"回味"时长改按倍率分档(用户定案) —— x2 0.6 / x3 0.7 / x5 0.8 /
# x10 0.9 / x20 1.0 / x50 1.1 / x100 1.2 秒(档位线性 +0.1)。用户连报三次"消失太快"。
# 为什么地板选 0.6 而不是更常见的 0.4: x2 占中奖场次 55%(x2+x3 占 80%), 而 0.35->0.40
# 这种 +0.05s 低于可辨差(~10~15%) —— 改了等于没改。为什么按档位线性: 倍率逐档约翻倍,
# 按档位 +0.1 等价于按 log(倍率) 线性(行业标准做法, US20070010315A1: 10分->2s, 100分->10s)。
# 真源收敛成 hold_for(m) 一个函数(RESULT_HOLD 常量删除): 原常量被 expected_sec / tick /
# _layers(3处) / ui 的 _reveal_deadline / fx_probe 共 7 处读取, 按档后若漏改一处就是**静默**脱钩
# (已经踩过一次: _reveal_deadline 硬编码 0.45, 尾巴改成 0.60 后兜底提前触发, 数字提前剧透)。
# fx_probe [5] 新增三条门禁: 最小档停留 >=0.50s、七档严格递增、最高档尾巴离 9s 硬兜底有充裕余量。
# 0.5.18(2026-09-10): 跑分期间不弹窗/不播装杯 —— 跑分时弹珠回到发射槽会弹彩蛋窗, 打断灰屏;
# 且装杯最多画 100 颗球, 那份渲染开销会污染正在采样的帧率。_on_easter_settled 的兜底必须走
# _easter_finish 解锁(只 return 会软锁)。顺带修回归: 弹窗改回等装杯整场播完再弹。
# 0.5.17(2026-09-10): 落定判据改"第一次触地" + 回弹改纯竖直 —— 一石二鸟。
# ①主游戏: 进入 landing 时 `b.vx = 0`(回弹纯竖直)。landing 循环里只有重力/横向弹簧/地板,
#   **没有隔板碰撞**, 球带飞行末段横速入槽 -> 回弹期能滑过隔板停到隔壁槽(结算槽仍是本槽
#   = "钱算对、球停错地方")。清零横速后横向只剩 LAND_K 弹簧, 而弹簧只把球拉向本槽中心,
#   拉不出去 —— 跳槽从物理上不可能(实测落定全程球心越界量 -11px, 只挪 0.2px)。
# ②主游戏结算提前到第一次触地(原来 88.7% 的落定走 0.5s 超时兜底、平均晚 0.3~0.5s)。
# ③装杯 T 改 `_last_touch`(最后一颗第一次触地), 不再等 max(end) —— 整场短 0.22~0.31s
#   (x2 1.71->1.46s, x100 3.30->3.08s)。弹跳照播: tick() 的 result 段也调 _advance_balls,
#   否则未落定的球会冻在半空。
# fx_probe 补 [8] 落定横向不越界门禁, [5] 加"弹跳尾巴能在解锁前播完"门禁。
# 0.5.16(2026-09-10): 中奖杯"淡入淡出效果差"四修 —— ①退场透明度改线性(原三次缓入让
# 79% 的窗口空转、最后 0.15s 从 0.90 崩到 0, 就是"突然消失"), 位移/缩放改二次缓出,
# 上浮 38->52px 缩到 0.90; ②进场位移 22->56px、缩放 0.97->0.92(玻璃贴图极透, 位移是
# 唯一读得出来的通道), 窗口 0.34/0.16->0.32/0.18(和仍 <= WINDUP=0.50); ③整场雨前移
# (RAIN_ANCHOR=0.12)消掉杯子就位后 0.44~0.55s 的空杯静止; ④修 expected_sec 不是上界
# 的真 bug(x100 有 3.9% 的局估得比真实短 -> 兜底 deadline 提前触发 -> 数字提前剧透)。
# fx_probe 补 [6]/[7] 两项曲线门禁(这块原来是零自动覆盖, 改坏了完全静默)。
# 0.5.15(2026-09-10): 修连按音效开关时"声音不太对": 语音互斥改按真实时长判(原来写死
# 3 秒 -> 连按时 3 秒内全静音, 音画脱节); 安卓取消静音不再 autoResume(会把被掐在
# 半路的提示音续播出来)。
# 0.5.14(2026-09-10): 声音开关改两态(去掉了"语音已开"这档, 新的"音效已开"= 老的
# "语音已开"含语音); 声音设置不再持久化; 删孤儿语音 voice_mode_voice;
# 顺带修 PC 侧 off->on 崩溃(enabled=True 而 out=None -> play() AttributeError)。
# 0.5.13(2026-09-10): 中奖杯进场/退场改分层动画(压暗与道具错峰 + 整组位移/缩放),
# 治"突然插入/突然消失"; 退场尾巴内部分配 0.30/0.15 -> 0.20/0.25(和不变, 解锁不动)。
# 0.5.12(2026-09-10): 修复横屏时弹窗宽度错 —— _popup 的宽度改吃 _veq()(等效竖屏),
# 原来用 size_hint 取的是裸窗口宽, 横窗时比界面宽 60~80%。
# 0.5.11(2026-09-10): 彩蛋顺序改为"先播装杯 -> 落定 -> 弹对话框"(原来弹窗在前);
# 删掉装杯落地音的 BOUNCE_BUDGET=24 颗数上限(它让 ×20 后段静音 1.4s / ×100 静音 2.3s)。
# 0.5.10(2026-09-10): 揭晓合流 —— 大字/余额滚动/结果语音 全部推迟到"最后一颗球落定"
# 那一刻一起给(以前 t=0 就报数字, 语音等装杯播完才念, 中间隔一整场); 槽位指示灯改回
# "中了就绿/未中红"(原按倍率取色, 而 slot_color(5) 恰好等于未中的红); PC 大字生命
# 0.85->1.0s、状态栏量词改"个弹珠"。
# 0.5.9(2026-09-10): 彩蛋(弹珠返回发射槽)改中性正式文案 + 补晓晓语音(按投注档 4 条) +
# 弹窗关掉后补播 ×2 装杯; 中奖杯整体放大 16%(470->545 逻辑宽, 球径等比放大)。
# 0.5.8(2026-09-10): 中奖杯启动期分帧预热(球纹理按当前投注档优先 + 7 档球堆) +
# 球堆缓存 LRU 上限 12 + 投放间隔收进 _make_balls 一处算。补 fx_probe 与踩坑文档。
# 0.5.7(2026-09-10): 中奖演出期间锁输入(播完才放行) + 中奖大字上移让位 + 赢音改由
# 落定时刻回调触发 + 杯子位置改到画面中下部。含 FX_MAX_SEC 软锁兜底。
# 0.5.6(2026-09-10): 接线中奖玻璃杯渲染层(WinPileFX 挂进 GameArea, settle 时播放)。
# 0.5.5(2026-09-10): 引入中奖玻璃杯表现层(assets/ 三张 PNG + pile3d 球堆模块)。
# 0.5.4(2026-08-26): 竖屏启动沉浸不生效修复 —— setSystemUiVisibility 从 Python
# 线程直调被安卓线程检查静默拦截, 改投递 UI 线程(runOnUiThread)执行, 转屏自愈
# 的假象消失, 竖屏打开即全屏。0.5.3 是闪退真凶修复(零参签名)。
# ⚠️ 每次出包必须 bump: 版本号是"装的是哪个包"的唯一肉眼证据(APK 文件名含版本)。
# 0.6.7(2026-09-11): 修「弹珠落入1个倍率槽之后跑到其他槽位去了(弹跳又高又斜, 越过倍率槽)」——
#   根因是飞行循环"每轮覆盖 landed": 落袋那一帧只要还有第二个物理步, 那一步返回 None 就把
#   landed 冲掉, 整个落袋分支被跳过(横速没清零/没切 landing/没结算), 球带着横速弹过隔板顶
#   落进隔壁槽。修法两处: ①physics_step 落袋返回处清 vx + 贴地(让"结算槽==落格槽"成为与
#   调用方循环形状无关的结构性不变量); ②两个 GUI 的飞行循环落袋即 break。另外地板矩形不再
#   被当弹性墙撞(它会把真实下落速度弹掉), 并让落地必弹(保底撞击速度 220->440 + 随机系数,
#   旧值实际只弹 4px)。落格分布逐发零变化(3000 发比对), 补 3 条 selftest 门禁 + fx_probe[17]。
# 0.6.8(2026-09-11): 修「玩的时候插耳机, 耳机里没声音(关掉 app 重开才有)」—— SoundPool 的输出路由
#   是建流那一刻定下的, 插耳机不会搬过去。加了 ACTION_HEADSET_PLUG 监听, 插耳机也重建一次
#   (原来只处理拔耳机的 ACTION_AUDIO_BECOMING_NOISY)。
# 0.6.9(2026-09-11): 修「初次安装必然没声音, 关掉 app 再打开就有了」—— 首装走冷路径(先合成整库,
#   好几秒), SoundPool 解码线程被抢 CPU, load() 返回 0; 而 named 是播放闸门, 失败是永久的, 整局
#   一声不响。改成把加载失败的记下来后台重试(0.6s × 12 轮)。
# 0.6.10(2026-09-11): 冷启动加"正在准备音效…"加载页。原来 Sfx(..., sync=True) 是在建 UI **之前**
#   在主线程把整库烘完的 —— 首装那几秒玩家看到的是一块黑屏(窗口在、控件一个没有)。现在改成后台
#   烘焙 + 加载页盖住整屏并吞掉触摸(不吞的话 state 还是 ready, 能按发射出一颗没音效的球)。
# 0.6.11(2026-09-11): 正面修「首次安装必然没声音」—— BUILD_APK.md 早就记过根因: SoundPool.load()
#   是异步的, 返回 sampleId ≠ 解码完, 没解码完 play() 返回 0(静默)。现在加载页改成"探到真的能播
#   再摘"(0 增益试播当探针 + 6s 硬超时), 不再"烘完就摘"; 顺带修 sticky 广播的死守卫(旧代码拿
#   0/1 比 None 恒假 ⇒ 每次启动白重建一次 SoundPool); 隐藏菜单加一行音频体检。
# 0.6.12(2026-09-11): 音频体检那行改成**独立一行**。实测并进版本行是 658px, 而弹窗内容区只有
#   422px —— 会折成两行而标签高度只有 28dp, 第二行被裁掉(玩家看到一句缺尾巴的话)。单独一行
#   334px 放得下; 弹窗高度 425 -> 468 容纳新增的一行。
# 0.6.13(2026-09-11): 音频体检改成一块带字段的分栏(后端/就绪/启动方式/重建次数), 弹窗高度
#   468 -> 620 用满纵向空间(用户定稿: "可以有更多空白来显示, 信息可以多, 高度有空间的");
#   PCM 后端(PC 的 winmm/SoundLoader)不再报"0 / 0"而是说清该字段不适用。
# 0.6.14(2026-09-11): 版本/制作日期 与 音频体检 从性能测试菜单里搬出来, 单开一个「启动信息」
#   按钮(主菜单: 开始测试 / 查看历史 / 启动信息), 用户定稿「版本信息写入启动信息, 不再写入主界面」。
#   字段行改左对齐成栏(4 个汉字标签等宽, 各自居中会错开 = "布局太乱"); 新增「音效等待」字段
#   —— 它接近上限(6s)就说明解码一直没跟上, 是决定要不要做"语音懒加载"的直接依据。
# 0.6.15(2026-09-11): 启动信息大修(两轮专家评审 + 玩家定稿)。① 修一个**已确诊的真 bug**:
#   后端不报数时会**退回闸门值** ⇒ 安卓上 SoundPool 构造失败静默降级时面板报 97/97 全绿
#   (实测那个分支四行有三行假绿, 只有「音频后端」说真话); 现在兜底只能是"未知"。
#   ② 拆「不适用」那一个词覆盖三种处境的老毛病(彻底没后端 vs PC 正常 vs 后端不报数)。
#   ③ 行序按排查优先级重排 + 「后端重建」只在非 0 时出现(玩家定稿: 有唯一预期值的只在偏离时显示)。
#   ④ 弹窗高度改成按内容算(v0.6.12 写死高度被裁过)。⑤ 新增「重放冷启动」按钮: 不丢存档地
#   按需复现"初次安装那种局"。⑥ 加载页加一行实时诊断 —— 它吞触摸, 是唯一覆盖"事发那一刻"的观察点。
# 0.6.16(2026-09-11): ① 「重放冷启动」完成后**停住等玩家点一下**(玩家: 「成功之后没有暂停, 直接回去
#   了, 我啥都没有看清」—— PC 上烘焙 1.2 秒, 自动摘页等于闪一下)。② PCM 后端下那三项**结构上
#   不适用**的行直接不出现, 不再印三行「不适用」(玩家: 「这几个不适用听起来有点奇怪」)。
# 0.6.17(2026-09-11): 修「重放冷启动点下去完全没有反馈」(Windows 与安卓都有)。根因: _replay_cold_start
#   里读的 self.veil 是 **App** 的属性, 而它在 RootWidget 上不存在 ⇒ AttributeError ⇒ 被外层 except
#   静默吞掉; 而且异常发生在"已经删缓存、已经清 named"之后, 所以游戏其实已经哑了却一声不吭。
#   现在: ①先建结果页、成功之后才动音频状态; ②任何失败都 toast 出原因(绝不静默) + 立刻放行。
#   另: 收拾我上一步搬方法时把 _replay_summary/_finish_replay_veil 塞进 _LoadVeil 的错误;
#   新增 hasattr 门禁"关键方法必须长在 RootWidget 上"(这种错能编译, 静态检查拦不住)。
# 0.6.18(2026-09-11): 重放结果页字号放大(标题 20->28, 正文 13->24)。玩家: 「挺好 就是字太小了」。
#   两行**刻意用不同字号**: 烘焙进度那行 sp(13) 是赶时间瞥一眼的; 重放结果是专门停下来给人读的。
# 0.6.19(2026-09-11): PCM 后端上不报「冷/热启动」。玩家: 「是不是每次都必然显示冷启动」——
#   PC 上确实是, 但那是后端性质不是故障: 磁盘缓存(stamp)整个机制只活在 named 路径上,
#   PCM 后端根本不写缓存 ⇒ 每次启动都是现场合成, 冷启动恒为真、零信息量。改成只报耗时。
# 0.6.20(2026-09-11): 启动信息**行高自动撑开 + 弹窗高度自动适配**。玩家在手机密度的模拟器上实测:
#   音频后端 那行折了行, 第二行被定高标签裁掉(v0.6.12 的老毛病在窄屏上复发)。根因是可用宽度
#   取决于设备密度 —— 桌面等效宽 540(可用 421px), 手机密度下可能只有 360(可用 ~270px)。
#   所以不能靠写短字符串躲, 只能让行高跟着实际排版走 + 开完弹窗再按真实排版高度对一次。
#   另: 期望值(安卓应为 SoundPool)改成**单独占一行**, 不再并进上一行充宽度。
#   重放页实时进度行字号 13 -> 17(玩家: 这个字体太小)。
# 0.6.21(2026-09-11): **把降级链的异常原文记下来并显示**。玩家的安卓真机上, 面板实测显示
#   音频后端 = Kivy-SoundLoader ⇒ **SoundPool 构造失败、一直静默降级在跑** —— 而原有的
#   `except Exception: pass` 让失败原因一个字都没留下(一块专门用来抓静默的面板, 自己也在
#   做静默降级)。现在每一级失败都记进 _BACKEND_ERRORS, 安卓上非预期后端时把那句原文摆出来。
# 0.6.22(2026-09-11): ①**修 SoundPool 从来没建起来过**: 内部类改用 pyjnius 的规范写法
#   `autoclass('Outer$Inner')`(属性访问 `Outer.Inner` 的解析并不可靠), 两条路都试;
#   真机上面板实测后端一直是 Kivy-SoundLoader ⇒ 降级链第一级就没成过。
#   ②重放结果页: 标题与正文改成**按内容各自撑高、上下排开**, 不再两个满屏标签叠在一起
#   (真机截图: 6 行数据把标题盖住了)。
# 0.6.23(2026-09-11): 重放页那行进度文案改名。真机实测: 进行中显示「等待能播 3.4 秒」而最终结果
#   是「音效等待 95 ms」—— 同一个词指两个数(探针要等烘焙完才开始跑, 合成那几秒全算在前者里)。
#   改成「已用 3.4 秒」。一块专门抓'说不清'的面板, 自己的文案不能含糊。
# 0.6.24(2026-09-11): 「启动信息」新增一行「输出采样率　48000 Hz」。源音频统一是 SR=22050,
#   设备若跑 48000 就要重采样放大 2.18 倍 —— 这是整条解码链路里最大的一块计算量。
#   **没有这一行, 两台设备的毫秒数根本不可比**(等于拿跑 100 米的跟跑 110 米的比成绩, 还不告诉
#   你是哪条道)。起因: PC 模拟器报 372ms、真机报 711ms, 而模拟器的跑分只有真机一半 ——
#   "模拟器会不会压根没走重采样"正是当时的候选解释之一, 没有这一行那组数据无法判读。
#   只在安卓上出现(PCM 后端送的是 22050 的 PCM, 概念不适用); 拿不到时写「未知」、不猜。
#   fx_probe [18] 加 4 条门禁, 并做了阴性对照(去掉 getattr 兜底 -> 正好那 2 条变红)。
# 0.6.25(2026-09-11): 修「热启动加载页多印一行」。玩家原话: 「热加载的时候应该只显示加载界面,
#   但是额外显示了音频载入时间什么的 必现」。**是两个 bug 叠在一起**:
#   ① 热启动走磁盘缓存、烘完不到 0.1 秒, 加载页那第二行没有任何进度可报 —— 印出来是一块
#      「已加载 97 / 97 · 已用 0.0 秒」却还杵着不走的屏。改成**热启动整行不印**(冷启动照旧,
#      那一行的立意本来就只在冷启动那几秒成立: 这块页吞掉所有触摸, 作者长按不到标题)。
#   ② 计时起点写成了 App 的 `self._load_veil_t0`, 而读它的 `_frame` 长在 RootWidget 上 ⇒ 取不到
#      ⇒ 兜到 `or time.time()` ⇒ **wait 恒等于 0.0**, 那一行永远显示「已用 0.0 秒」。
#      (重放冷启动路径有自己的赋值, 所以那边显示的是真数 —— 这就是它一直没被发现的原因。)
#   顺带把这段逻辑从 `_frame` 内联抽成纯函数 `_veil_status_line()`: 它以前**零自动覆盖**
#   (`_frame` 要整个 App 才跑得起来, 夹具造不出), 而"加载页显示错字"正是本项目最典型的
#   静默故障形态。fx_probe 加 6 条门禁; 阴性对照: 拿掉 cached 闸门 / 把起点改回 App 上,
#   各打掉对应那条, 正好 2 条变红。
# 0.6.26(2026-09-11): 加载页改成「**一张图, 一个字都没有**」。玩家定稿三句原话:
#   「我要的是只显示一个加载界面即可」「这个加载页面 啥字都没有」
#   「可以搞一个加载页的图片, 从小到大的变大过程, 看起来是一个动态的」。
#   ① 删掉实时诊断那一行(「已加载 42 / 97 · 已用 1.0 秒」) —— 那些数「启动信息」里全都有,
#      而且更全, 不必在加载页上再占一行。(v0.6.25 那版把它收窄成"只冷启动印", 玩家打回:
#      「别乱改」「只显示一个加载界面即可」⇒ 整段删除, 连带 _load_veil_t0 一起清掉。)
#   ② 加载页放**系统 presplash 那张图本身**(同一个文件) ⇒ "系统 splash 撤掉 -> 本页接上"
#      中间没有跳变。底色用 VEIL_BG(#0b1220) = 那张图自己的底色(实测四角一致) ⇒ **图的边界
#      完全看不见**, 只有内容在长大(用游戏的 COL_BG #0e1524 会让它显示成一个方块)。
#   ③ 进场 0.72 -> 1.0(0.55s 三次缓出), 长满后 ±1.8% 缓慢呼吸(冷启动那几秒不至于死气沉沉,
#      又不假装有进度)。用 AsyncImage: 同步解码会把第一帧顶后几十~几百毫秒, 而这块页存在的
#      全部意义就是"第一帧别是黑屏"。**不做循环进度条** —— 玩家问过, 结论是不合适: 进度条
#      承诺"我能告诉你还剩多少", 而这个等待没有可报的进度(热启动半秒/冷启动几秒, 没有中间态)。
#   fx_probe: 6 条旧门禁换成 11 条新门禁(无字 / 无 set_status / 进场单调不减 / 呼吸有界 /
#   图 == presplash.png / 底色 == VEIL_BG / _frame 驱动 tick), 并用真窗口截图肉眼复验过。
# 0.6.27(2026-09-11): 修加载图的尺寸算法。玩家问「371x660 这个是不是不合理啊 我记得手机屏幕
#   都是很高分辨率的?」—— 371x660 是**桌面测试窗口**的值(那里 1 逻辑 px = 1 屏幕 px), 手机上按
#   真实像素算; 但那一问戳出一个**真 bug**: 原来写的是"**只按屏幕高度铺**", 而手机屏幕比这张图
#   更瘦长(典型 1080x2340 = 0.46, 而图是 1080:1920 = 0.5625) ⇒ 宽度算出来 1316 > 1080,
#   **图的两侧被裁掉了**。改成 contain(`_veil_fit`): 整张图完整放得下, 上下各留一条边 ——
#   那条边正好是它自己的底色, 肉眼看不出来。
#   fx_probe 加 3 条门禁: 瘦长手机上放得下 / 基准尺寸保持原图比例 / 桌面上仍按高度铺满。
# 0.6.28(2026-09-11): 给加载图的"呼吸"**留出余量**。玩家问「变到100%之后 怎么动」时顺手查出来的:
#   长满之后是呼吸(周期 1.9s、幅度 ±1.8%, 一直动到音效就绪), 而 `_veil_fit` 是按 contain **铺满**
#   ⇒ 呼吸涨到 +1.8% 时图会比屏幕**大一圈**。溢出的虽然只是背景色(看不出来), 但那是"碰巧看不
#   出来" —— 这张 presplash 恰好全边都是纯色背景, 换一张边缘有内容的图就会当场露馅。
#   改成基准尺寸先除以 (1+AMP) ⇒ 呼吸到最大时恰好等于屏幕, 一个像素都不溢出。
#   fx_probe 多一条门禁专门钉"涨到最大时也放得下"。
# 0.6.29(2026-09-11): 修加载页「变小 + 不动」两件事。玩家原话:
#   「效果差爆了, 新的效果是 打开app显示一个最大的加载, 然后迅速变小, 然后啥也没有动 就进入app了」。
#   ① 尺寸: `_veil_fit` 从 contain(完整放下) **改回 cover(铺满整屏)**。系统 presplash 是拿这张图
#      当 windowBackground **铺满**画的, 所以"打开 app"第一眼是铺满那版; 我们做 contain 就比它
#      小一圈 ⇒ 那一下"变小"。铺满之后两者尺寸一致(溢出裁掉的全是它自己的底色 #0b1220)。
#      ⚠️ 这是 v0.6.27 我改坏的 —— 当时把"铺满"误判成 bug 改成了 contain。
#   ② 动作: 删掉"进场从小到大"(0.72->1.0 / 0.55s)。两个原因: (a) 它和系统 splash 的铺满尺寸
#      冲突(起点一定比 splash 小); (b) 它在真机上**根本来不及播** —— `_t0` 从构造算起, 而第一帧
#      要等 presplash 撤掉才画出来, 中间早过了 0.55 秒 ⇒ 一睁眼就是终态(这就是"啥也没有动")。
#      现在只剩呼吸(±1.8% / 周期 1.9s), 从第一帧起就在动。
#   ③ 玩家要求删掉「启动信息」里的「输出采样率」一行 —— 原话「去掉音频输出中的 输出采样率 这个
#      其实是个固定数值」。实测真机与模拟器**都是 48000**, 属于"永远不变的那一行"(面板的成文
#      规则: 有唯一预期值的只在偏离时才有信息)。
#   fx_probe: 采样率那 4 条换成 1 条"已删除"; 加载页门禁改成 cover 口径 + "t=0 就是 1.0" +
#   "进场那一段已删除"; 比例区间门禁(15:10~21:9)保留, 判据由"放得下"改成"铺满"。
# 0.6.30(2026-09-11): PC 上的「启动信息」补齐玩家要的东西 —— 原话:「我在pc上也需要知道版本号
#   也需要知道那几个时间 别tmd自作主张」。
#   ① 版本号: PC 没有 PackageManager, 改成**读 main.py 旁边的 buildozer.spec** —— 和出货打包
#      用的是同一个文件, 不另抄一份(抄的那份早晚忘改)。安卓继续走包信息(buildozer.spec 不进
#      APK), 两边用 `if not parts:` 兜底, 互不干扰。
#   ② 「启动方式」PC 上也用「冷启动 XXXX ms」(原先印「合成耗时 …」)。PCM 后端不写磁盘缓存
#      ⇒ cached 恒为 False ⇒ PC 上永远显示冷启动 —— 那是**事实**, 不是标签错。
#   ③ PC 上补「音效等待 0 ms」一行。winmm 没有 probe_all、`_await_ready` 见到就放行, 所以这个 0
#      是**真的**(PC 上根本不存在"等解码"这件事), 耗时全在上一行的「冷启动 XXXX ms」里。
#   fx_probe: PC 那两条断言从"三项都不出现"改成"必须有那几个时间"; [13] 的版本号从可选变必选。
# 0.6.31(2026-09-11): 「启动信息」弹窗改标题。玩家定稿: 「启动信息调整  从启动信息改为
#   跳跳的弹珠机v0.x.x」+「去掉其他地方的版本号」。
#   ① 标题: 「启动信息」 -> **跳跳的弹珠机v0.6.31**(新增 `_startup_title()` 与 `_app_version()`)。
#   ② 正文那行只剩制作时刻(「于 2026年09月11日 16:14 制作」)—— 原来的版本前缀删掉。
#      **版本号全工程只在标题出现一次**, 来源不变(安卓走 PackageManager, PC 读 buildozer.spec),
#      不另抄一份常数。
#   fx_probe [13]: 正文行从"版本可选"改成"**不许出现版本**", 并新增一条钉标题格式
#   (^跳跳的弹珠机v\d)。
# 0.6.32(2026-09-11): 修加载页「**突然变成小图**」。玩家真机原话:「固定一个启动画面静止不动
#   一段时间, 突然变成小图, 然后突然就进游戏了」。
#   根因: `_sync` 绑在控件自己的 `size` 上, 而 Kivy 里控件**被布局之前** `size` 是默认的
#   `(100, 100)` —— `_sync()` 在 `__init__` 结尾就会跑一次(那时还没进树) ⇒ 算出 **100x178 的小图**,
#   而那一帧正好是"系统 splash 撤掉后玩家看到的第一眼"。实测(432x936 窗口):
#   构造后 base=(100,177.8) img=(100,178) -> 第一帧后 base=(526.5,936) img=(527,937)。
#   修法: 新增 `_pick_box()` / `_veil_box()` —— 尺寸来源**优先父容器**(它由 LandLayer 在
#   App.build 里就摆好了, 比第一帧早), 并且**每帧在 tick() 里现算**, 不再依赖"布局什么时候
#   通知我"(那是个时序依赖: 玩家实测「PC 上会缩放, 是个动态的」而真机上那页不动)。
#   图在尺寸算出来前**藏着(0x0)**, 不给它闪方块的机会; 尺寸同时走布局驱动与每帧驱动两条路,
#   任何一条通就够, 不会出现"图永远不显示"。
#   fx_probe 加 3 条门禁(优先父容器 / 无效时退回自己 / 初值是 0x0 且走 _veil_box), 全绿。
# 0.6.33(2026-09-11): 「启动信息」弹窗标题**加一个空格** —— 「跳跳的弹珠机v0.6.32」改成
#   「跳跳的弹珠机 v0.6.32」(玩家: 「跳跳的弹珠机和后面的v0.6.32 加一个空格」)。
#   只动 `_startup_title()` 一处, 版本号仍然只在这一个地方出现。
# 0.6.34(2026-09-11): 「启动信息」面板两处调整(玩家定稿)。
#   ① 删掉「音效开关　已开」那一行 —— 原话「去掉音效开关： xx 这个行 没有意义」。它是恒定行,
#      属于这块面板自己那条成文规则「有唯一预期值的, 只在偏离时才有信息」的反面。
#      连带把 `_replay_summary` 里那道 `if not r.startswith("音效开关")` 过滤删掉(已成死代码)。
#   ② 最后一行「加载失败　N 个　·　语音 XX / YY」拆掉, 语音那半独立成
#      「语音就绪：XX / YY」并**挪到「音效就绪」紧后面** —— 原话「把加载失败 x个 语音xx/xx
#      改为 语音就绪：xx/yy, 放在音效就绪的后面」。成对摆开, "音效响、语音不响"这种半死状态
#      才一眼可见(单看「音效就绪 97 / 97」它是隐形的 —— 那个数含语音)。
#   现在的面板(安卓): 音效就绪 / 语音就绪 / 音频后端 / 启动方式 / 音效等待
#   fx_probe: 相关断言全部改成**行为断言**(直接问"产出的行里有没有它")—— 文本断言在这条上
#   被自己的注释/文档串绊红了两次, 那是本轮的一个真实教训。
# 0.6.35(2026-09-11): 修**加载页那张图的大小与"不动"**。
#   玩家真机原话:「固定一个启动画面静止不动一段时间, **突然变成小图**, 然后突然就进游戏了」,
#   以及「重放冷启动, 整个画面也不会缩放」。K90 Pro Max(屏 1200 宽)上实测的病根是两层:
#   ① **画出去的不是我们设的尺寸**。Kivy 的 `<Image,AsyncImage>` 画的是 `norm_image_size`
#      (`kivy/data/style.kv`), 而 `AsyncImage.fit_mode` **默认 `"scale-down"`(只许缩不许放)**:
#      只要盒子在某一维超过贴图原生 1080x1920, 画出去就被**夹死在 1080x1920**。而 `_veil_fit`
#      给的是 cover 盒(1467x2608) ⇒ 系统那张(FIT_CENTER = 1200x2133)与我们的差 **0.90 倍**,
#      交接那一下就是"突然变小 10%"。
#   ② **呼吸被夹成 0 像素**。呼吸只是把盒子 ×1.018, 盒子本来就大于贴图 ⇒ 放大缩小都被夹回
#      1080x1920 ⇒ 画出去的矩形一个像素都不变 = 玩家说的"静止不动"。
#   改法(**必须两件一起做, 缺一个都是空操作**):
#      · `_veil_fit` 返回**整窗**(摆放交给 fit_mode);
#      · `AsyncImage(..., fit_mode="contain")` —— 与系统那层 `FILL_PARENT + setScaleType(FIT_CENTER)`
#        同规则。
#      只改 `_veil_fit` = cover 盒照样被夹(这就是 v0.6.27/29/32 三次改动全部"无效"的原因:
#      在 1200 宽的屏上 contain 盒与 cover 盒画出去是**同一个** 1080x1920);
#      只加 contain = 放大 1.25 倍再裁掉两边, 把 PC 那台唯一正常的机器改坏。
#   附带修掉解码期的占位图: AsyncImage 贴图到位前画的是 Kivy 的 32x32 占位图(`image-loading.zip`),
#      改成 contain 后它会被放大成 **1200x1200 的一整屏糊斑**(实测), 所以在原尺寸下就是屏幕正中
#      一个 32px 的点。现在加了 `_tex_ok` 门(`on_load` + `_proxy.loaded` 兜底): 没到位就把
#      `_img.size` **归零**(不是裸 return —— 画的是 `norm_image_size`, 控件 size 不归零它照画)。
#   已知不治(留作待办): ① 安卓 12+ 的**系统启动图**那一层(targetSdk=33 + 零自定义主题 ⇒ 平台
#      默认图标 splash), 它与 `_veil_fit` 怎么改无关; ② **横拿冷启动**时加载页在 `LandLayer` 里
#      被 `Rotate(90)`, 而系统那张是正的 —— 真机上没人验过。
#   实测(src/工具链侧, 桌面): `--selftest` OK; `fx_probe` 新增的**几何门禁**(真 Image + 真
#      presplash.png, 10 档机型断言"画出去的矩形逐像素等于系统 FIT_CENTER")全绿, 阴性对照
#      (改回 cover + 默认 fit_mode)红 3 项, 其中 `K90: 画 1080x1920, 系统 1200x2133` 正是玩家那一跳。
# 0.6.36(2026-09-11): 加载页**去掉所有动效, 固定显示一张图**(玩家定稿)。
#   原话:「这样吧, 不折腾了, 启动的时候固定显示一个图片。**不做任何变化**」。
#   删掉的是 v0.6.29~35 那一段"呼吸"(`LOADVEIL_BREATH_SEC/AMP` + `_veil_scale` 里的
#   `sin` + `_LoadVeil._t0`)。它本意是"让这一页看着是活的", 但在真机上被 `fit_mode` 的
#   夹断冻成 **0 像素**(玩家报的「静止不动」); v0.6.35 把夹断修好之后它才第一次真的动起来 ——
#   而玩家的结论是**不要动**。
#   现在这一页只做两件事: 尺寸与系统 presplash **逐像素一致**(v0.6.35 修的, 交接处看不见)
#   + 盖住整屏吞掉触摸。图是死的, 从抬起到进游戏一个像素都不变。
#   实测(src/工具链侧, 桌面): 逐帧读渲染真正用的矩形, 全程恒为 `540x960`(改之前是 530~548 在摆);
#   `--selftest` OK; `fx_probe` 全绿, 其中"动作"那组门禁改成 **一个动效都没有** ——
#   `LOADVEIL_GROW_*` 与 `LOADVEIL_BREATH_*` 都必须不存在、`_veil_scale` 在 10 秒里恒等于 1.0、
#   `_LoadVeil` 不许再持有 `_t0`。
#   ⚠️ **已知还在的一跳**(与本版无关, 不折腾): Android 12+ 那一层**系统启动图**(应用图标居中,
#   实测 208x208) → p4a presplash(满屏棋盘), 约 2 倍放大。它由 targetSdk=33 + 零自定义主题决定,
#   改 `_veil_fit`/`fit_mode` 永远动不了它; 要关掉只能给 app 换自定义主题
#   (`windowSplashScreenBackground=#0b1220` + 透明图标), 玩家 2026-09-11 决定不做。
# 0.6.37(2026-09-11): 启动页 = **黑屏 + 一行汉字**, KTV 歌词那种"填充线扫色"。
#   玩家定稿的原话(按说的顺序):
#     ·「启动的时候逐渐渲染五个比较大的汉字: 跳跳的弹珠机 …… 反正最多 6 秒钟」
#     ·「**总之这个东西不能是突变**」
#     ·「这个直接黑屏吧。不用之前的背景图了」「棋盘图 没有用了 可以删了」
#     ·「跳跳的弹珠机刚开始是都能看到的, 然后逐渐改变颜色」「效果类似KTV歌词的变化效果 你懂的」
#     ·「跳跳的弹珠机 跳.跳的弹珠机 跳跳.的弹珠机 应该是这个效果, **圆点左边的字体是新颜色**
#        甚至颗粒度可以很细什么的」(那个「圆点」后来被玩家自己否掉: 「**扫描的时候, 那个小球
#        应该是隐藏的啊**, 这个界面中, 只能看到那6个字」⇒ 页面上**没画任何标记**, 只有字在换色)
#     ·「应该是**默认是银色的**, 第1次变为黄色, 再变是绿色, 再变是银色」⇒ 色表顺序就是它
#     ·「这个红色不是很好, 是不是**绿色**更好?」⇒ 第 3 种颜色用灰绿(鼠尾草绿), 不用红
#     ·「高级不是土味审美」「别tmd加奇怪的描边了 阴影了」「每次变更字体颜色就行」
#     ·「无限演 …… 比如设计三种颜色的歌词, 颜色1结束播放颜色2 颜色2结束播放颜色3 结束播放颜色1」
#     ·「启动信息中的冷启动, 仍然可以无限播放这个启动界面 跳跳的弹珠机的 播放歌词版本」
#     ·「把确定按钮放在重放冷启动下面」
#
#   真机录屏(红米 K90 Pro Max 冷/热启动各一段, 逐帧量的)查出**两个突变**:
#     ① 冷启动 0.15~0.22s: 应用窗口已经开了、presplash 还没画上去, 屏幕是一片**中性灰黑**
#        (实测 #181314) —— 系统 starting window 的默认底色;
#     ② 热启动 1.10~1.18s: 我们那张加载图还没解码完(4 帧 / 0.14 秒**空暗**, 实测 #101725)。
#
#   最终形态 —— **把"接得上"从"同图同尺寸"换成"同色"**:
#     · `presplash.png` 从那张 700KB 的棋盘图换成**纯 #0b1220**(8.7KB); 加载页里那条图片路径
#       (`AsyncImage`/`fit_mode`/`_veil_fit`/`_veil_box`/`_pick_box`/占位图门/`LOADVEIL_ASPECT`/
#       `_veil_img_alpha`)**整段删除** —— 一致性现在由颜色保证, 那张图多余了;
#     · 加载页 = 一片底色 + 一行"跳跳的弹珠机"(六个字): 一上来**整行都在**(灰蓝, 一眼能读清),
#       然后**从左往右扫**: 扫过的那一段换成这一轮的新颜色、还没扫到的还是上一种颜色 ——
#       粒度是**像素级**的(两层同字叠着 + 一条 `Stencil` 裁剪), 能切在某个字的中间;
#     · **三种颜色循环演**: **银(默认) -> 黄 -> 绿 -> 银…**(玩家定稿的顺序与色系), 一轮扫完接
#       下一轮, 所以它能**无限演下去**;
#       「重放冷启动」那一页也演同一套(那边不自动淡出, 一直演到玩家点一下);
#     · 音效就绪 + 那行字**完整淡入**(0.22s, 见 0.6.42)-> **整页硬切**露出游戏
#       (**0.6.38 起就没有淡出了**, 一直是硬切 —— 这里原来写的是"淡出 0.4s", 已更正);
#       硬上界是"等音效就绪"的 6s 超时(`Sfx.SFX_READY_TIMEOUT`);
#     · 逐帧**只动两样**: 那条裁剪矩形的宽度和圆点位置; **颜色只在换轮时赋值**
#       (Kivy 的 `Label.color` 一变就重烘文字纹理, 每帧赋是白烧), 也绝不碰 `font_size`。
#     · 那一行字**没有描边/阴影/辉光/缩放** —— 只有颜色(第一版做的"金色 + 厚光晕"被玩家打回:
#       暗底上一圈亮边 + 金色大字 = 土)。
#   ⚠️ **三处底色必须永远同值** —— 它现在是"没有突变"的唯一保证, `fx_probe` 会去另外两个文件里
#      把值读出来对: `VEIL_BG` == 本文件的 `android.presplash_color` == `p4a/hook.py` 注入的
#      `windowSplashScreenBackground`。改一个忘一个, 用户就会在一次启动里看到两种颜色。
#   ⚠️ 顺带更正: 游戏名是**六个字**(跳跳的弹珠机), 不是玩家口述的"五个" —— 代码按 len() 自适应。
#   ⚠️ 「启动信息」弹窗里两个按钮顺序调了: **重放冷启动在上、确定在下**(纵向 BoxLayout 的添加
#      顺序就是几何顺序)。
#
#   改动(p4a/hook.py): 补一个只改底色的主题 `@style/PlinkoTheme`
#   (`parent="@android:style/Theme.NoTitleBar"`, 即 p4a 原来那个)把 `windowSplashScreenBackground`
#   设成 #0b1220, 消掉上面那个中性灰黑。图标**故意不动**: 系统把图标从小放大到内容区本身就是
#   一段动画, 底色对齐之后"同一个图案放大"是最顺的接法。hook 已离线单测(产物可解析 + 幂等)。
#
#   实测(src/工具链侧): `--selftest` OK; `fx_probe` 全绿 —— 那一组门禁: t=0 全黑无字、整行一起
#   淡入且单调、**填充线一轮之内只增不减 + 这轮扫完到 1 + 下轮从 0 重来**、圈次只增不减且
#   **取模回到第一种颜色**、每轮配色是 (暗色,亮色) 一对且至少两种、**没有描边/阴影/辉光/缩放**
#   (查 `_LoadVeil` 里不许出现 `PushMatrix`)、**粒度是像素级**(`StencilPush` + `_clip_a`)、
#   最短停留 >= 一轮、最长停留 = 音效硬超时、末尾有淡出; 另加"底色三处同值"与"presplash 仍是纯色"。
#   `--check` 同步。桌面上逐帧截图肉眼复验: 填充线推进 + 三种颜色各演一轮 + 回到第一种。
#
#   ⚠️ **还没修的一条**(留作待办): 录屏里顶部那条状态栏全程是黑的 —— 应用在启动阶段没进沉浸
#   (`_enter_immersive` 是 Kivy 起来之后才调的)。
# 0.6.38(2026-09-11): 两处修正(玩家当天接着报的)。
#
# (1) **加载页不许有淡出** —— 到点整页直接消失。
#   玩家原话: 「成功打开游戏后, 游戏界面有一个**发光效果**(原来的启动界面 loading 界面的,
#   跳跳的弹珠机的文字的**覆盖发光**), 没有立即消失, 持续了0.x秒。**这里应该是啥都看不见,
#   消失得干干净净, 而不是有淡出**」。
#   病根: 0.6.37 给这一页留了 `VEIL_TITLE_FADE_OUT=0.40` 的整页淡出 —— 那 0.4 秒里标题是
#   **压在游戏画面上**的(玩家看到的"发光"就是它)。现在**整段淡出机制删掉**:
#   `tick()` 在"音效就绪 + 走完最短停留"那一刻直接返回 True, `_frame` 当帧摘页。
#   `fx_probe` 的门禁也反过来了 —— 从"末尾有淡出"改成"**没有淡出**"(`VEIL_TITLE_FADE_OUT`
#   与 `_fade_t0` 都不许存在)。
#
# (2) 「重放冷启动」那一页只留两块东西 + 详细统计挪进弹窗。
#   玩家原话: 「重放冷启动界面**不应该显示各种文字**, 只显示 **跳跳的弹珠机** 和 **最下面的
#   测试已经完成**(如果没有完成, 就不显示)。还有完成之后**点击屏幕任意位置, 弹出的是详细
#   统计信息**, 有个新窗口专门展示(带确认按钮的这种)」。
#   改动: 那一页现在只有 —— 正中的标题(同一套 KTV 扫色动画, 无限演) + **贴底**的一行
#   「测试已经完成」(只在跑完后才写, 之前是空的); 点击 → 摘页 + `_show_replay_detail()`
#   弹出带「确定」的统计窗(内容仍复用 `_replay_summary()`, 不另写格式化)。
#   ⚠️ 顺带删掉了一个 `_lbl`(原来印「正在重放冷启动…」那种进度文字)—— 玩家要的是"各种文字
#   都不显示", 它会一直挂在页面底部。构造参数 `text` 保留, 但**只当"这是不是重放页"的标记**。
#
# 实测(src/工具链侧): `--selftest` OK; `fx_probe` 全绿; `--check` 同步。
# 桌面逐帧截图肉眼复验: 重放中只有标题 -> 完成后底部一行 -> 点击弹出统计窗(标题 + 3 行 + 确定)。
# 0.6.40(2026-09-11): **回退 0.6.39**(把标题画进 presplash 那一版)。
#   玩家: 「不行 回退吧」+ 「**因为有闪屏**」。
#   0.6.39 干的事: 为了不让冷启动那 ~2 秒是一片空白, 把「跳跳的弹珠机」也画进了 `presplash.png`。
#   **为什么必然闪**: presplash 那张图是 **PIL** 画的, 加载页那行字是 **Kivy** 画的 ——
#   同一个字在两个渲染器里的字形/抗锯齿不一样, 交接的一瞬间字会"变一下"。这不是调参数能
#   解决的, 是"两个渲染器"这件事本身决定的。**别再试这条路。**
#   所以回到: `presplash.png` = **纯 #0b1220**(和 `VEIL_BG` / 主题底色三处同值),
#   加载页那行字照旧**淡入**(`VEIL_TITLE_IN_SEC=0.22`)。
#   ⚠️ 代价明摆着: 冷启动那 ~2 秒(应用在解包/导入, Kivy 还没画第一帧)屏幕上是一片深色,
#      玩家 0.6.39 之前报过「打开界面的时候是黑屏的, 持续了好一会」。**这条没解决, 是取舍**:
#      要么"空白 2 秒", 要么"交接闪一下" —— 玩家选了前者。
#      想真解决只能让 **Kivy 早一点画第一帧**(缩短那 2 秒), 那是另一件事(优化启动耗时)。
#   `fx_probe` 的 presplash 门禁跟着回到"**还是那张纯色图**"(尺寸 1080x1920 + 体积 <= 32KB)。
# 0.6.41(2026-09-11): 关掉系统启动图上那个**应用图标**(玩家报「打开 app 后有一个奇怪的图标」)。
#   真机录屏(xiaotu.mp4, 1200x2608)逐帧量: 点开 app 后屏幕上先出现一个 **415~426px 的方块**
#   (白色圆角方块里一个迷你棋盘 = 我们的 launcher 图标), 亮约 1 秒 -> 变灰淡出 -> 一片空暗
#   -> 才轮到那行字。那就是 Android 12+ **强制**的系统启动图(`targetSdk=33` + 没自定义
#   `windowSplashScreen*` 时, 平台默认画应用图标)。
#   改法: `p4a/hook.py` 注入的主题里再加一条 ——
#     `<item name="android:windowSplashScreenAnimatedIcon">@drawable/plinko_blank</item>`
#   并在 `res/drawable/plinko_blank.xml` 写一张**全透明的 1dp 方块**。图标不画了, 整层就是
#   一片 `windowSplashScreenBackground`(#0b1220) —— 与系统 presplash、加载页三处同色。
#   ⚠️ **为什么不用它来显示"标题"**: 那个图标的尺寸由平台定死(288dp), 跟我们那行字对不上;
#      而且 0.6.39 已经证明了"两个渲染器画同一行字"必然闪(PIL vs Kivy)。所以只能是透明。
#   ⚠️ 风险: 主题写错会让 app 起不来。这里 parent 沿用 p4a 原来那个 `Theme.NoTitleBar`,
#      只加 3 条 item, 改动面压到最小。hook 已离线单测(产物 XML 可解析 + 幂等)。
# 0.6.42(2026-09-11): 加载页的"最短停留"从 **1.9s 改成 0.22s** —— 玩家报「我期望的是启动的时候,
#   **加载完成就进入游戏界面**, 实际上是播放了跳跳的弹珠机这几个字才进去」。
#   ⚠️ 那个 1.9s 是 **v0.6.37 实现 KTV 动画时自己加的地板, 玩家从来没要过** —— 他的原话只有上界
#      (「渲染完成后, 再次重复渲染 或从高级渲染变成超高级渲染, **反正最多 6 秒钟**」), 而更早的
#      v0.6.36 及以前摘页判据就是"音效一就绪**当帧**就摘"。它也不是从"一轮 1.47s"算出来的
#      (算出来该是 1.47): 它与 v0.6.29~35 那个**已删掉的呼吸周期** `LOADVEIL_BREATH_SEC = 1.9` 同值。
#   实测代价(桌面端到端跑真 App 三次): 音效在 1.19~1.33s 就绪, 加载页却停到 1.90~1.91s —— 白等
#      1.44~1.64s; 把"就绪"从第 1 帧强行置真, 停留**仍是 1.90s**(差 <3ms) ⇒ 这 1.9s 与加载无关。
#      真机录屏(2026-09-11, 1200x2608)逐帧量: 那行字在屏 **1821ms**, 反解摘页 = 1.91s; 而这 1.9s 里
#      约 **0.98s(52%) 画面完全静止**(淡入完到扫色起步 0.13 + 轮间隙 0.50 + 第二轮前摇 0.35) ——
#      录屏里只看得清一次完整变色(银->金), 第二次变绿刚 4.6% 就被切走 ⇒ 观感是"演一半卡住 -> 进游戏"。
#   改法: `VEIL_TITLE_MIN_SEC` 取 **整行淡入的时长**(`VEIL_TITLE_IN_SEC` = 0.22s) —— 保证那六个字
#      **完整亮起来过**一次, 又不让加载快的机器白等。⚠️ **不是删成 0**: 那样字会**亮到一半就被摘**,
#      半截动画比没有动画更像出 bug。
#   代价(已知且接受): 加载快的机器上只看得到"银色的字亮一下", 看不到 KTV 扫色(扫色要
#      `VEIL_TITLE_LEAD` 之后才起步 —— 当时 0.35s, v0.6.43 起 0.25s); 加载慢的机器照旧一轮轮演下去。
#   改后实测(桌面三次): 加载页停留 0.648 / 0.683 / 1.095s, 其中"白等"只剩 **0.012~0.019s(一帧)**;
#      改前是 1.908 / 1.899 / 1.911s(白等 1.44~1.64s)。点开到能玩约 2.8s -> 约 1.2s。
#   ⚠️ **没动** `Sfx.SFX_READY_TIMEOUT`(等音效的 6s 硬超时) —— 那是"绝不软锁"的兜底; 地板只是地板。
#   ⚠️ 另一处发现(**未改**): `VEIL_TITLE_MAX_SEC = 6.0` 是**死常量** —— 全文件只有定义与注释、
#      零处使用, 注释里"最长 6 秒"没有任何代码兑现(真正兜底的是上面那条 `SFX_READY_TIMEOUT`)。
# 0.6.43(2026-09-11): 扫色的"前后摇"压短 —— 玩家嫌一轮里停太久(原节奏一轮 1.47s 里只有 0.62s 在动,
#   其余 58% 画面完全静止)。玩家自己挑的"温和"档:
#     `VEIL_TITLE_LEAD` 0.35 -> **0.25**(前摇) · `VEIL_TITLE_ROUND_GAP` 0.50 -> **0.30**(后停) ·
#     `VEIL_TITLE_SWEEP` 0.62(扫)不动 ⇒ 一轮 **1.47 -> 1.17s**, 静止占比 **57.8% -> 47.0%**
#     (稳态一轮静止 = 前摇 0.25 + 后停 0.30 = 0.55s)。
#   ⚠️ 前摇只比整行淡入(`VEIL_TITLE_IN_SEC` 0.22)多 0.03s = "亮完就开始扫"。**不能再往下压** ——
#      那六个字会一边淡入一边变色, 而玩家定稿的是「刚开始是都能看到的, **然后**逐渐改变颜色」。
#      为此加了一条门禁(`fx_probe`: **前摇 >= 整行淡入**), 另加一条"一轮时长 == 前摇+扫+后停"的自洽门禁。
#   ⚠️ 这次**没动** `VEIL_TITLE_MIN_SEC`(那个 0.22 的地板, 见 v0.6.42) —— 它管"别让字亮到一半被摘",
#      与节奏无关。两者现在**同值**但语义不同: 一个是地板, 一个是淡入时长。
#   ⚠️ 有一个**没人保护**的中间地带(已知, 接受): 音效恰好在 0.25~0.87s 之间就绪的机器会看到"扫到
#      一半"(左黄右银)然后整页硬消失。这不是新问题(1.9s 时代摘页也切在第二轮扫色的 4.6%), 而地板
#      不能为了它抬到 0.87 —— 那等于"必须看完一轮", 正是 v0.6.42 要消灭的东西。
#   实测(桌面, 真函数逐帧, 原样抽取 main.py 里的 `_veil_title_state`): 淡入在 t=0.216 完成、
#      扫色在 t=0.250 起步、第一个字整个变完 t=0.411; 相邻两次"整行换色"间隔 1.17s, 换色本身是
#      0.62s 的连续像素扫过(不是整行突变), 无闪烁风险。
# 0.6.44(2026-09-11): 冷启动那个"探针"的轮询间隔 0.15s -> **0.05s**(`Sfx.SFX_READY_POLL`)。
#   它决定的是**摘页的发现延迟**: 音频是异步解码的, 探针只负责轮询"好了没有" —— 音频若在探针
#   刚过去 1ms 时就绪, 玩家要白等满一个间隔(原 0.15s, 平均 75ms)。压到 0.05 后最坏 50ms、平均 25ms。
#   ⚠️ 原来的 0.15 也没有依据(注释里就写着"探测间隔"四个字), 与 1.9s / 0.35 / 0.50 是同一类魔数。
#   ⚠️ 加密**不贵**: `probe_all()` 遇到第一个"还不能播"的 sample 就早退, "没就绪"期间每次探测通常
#      只花一两次 play 调用; 只有最后一次(全就绪)才需要遍历完(41 音效 + 57 语音 = 98 个 sid)。
#   ⚠️ 与它相邻的 `SFX_READY_TIMEOUT = 6.0`(硬超时, "绝不软锁"的兜底)**没有动**。
#   ⚠️ 这个常量**只在安卓生效**: 桌面后端没有 `probe_all`, `_await_ready` 直接放行走人(所以
#      `--selftest` / 桌面预览量不出它)。
#   ⚠️ 这一版**没量到真机收益**(手上没设备): 按理论它是 0~100ms 的改善, **不是那 600ms 的大头**
#      —— 大头更可能在"57 条语音(3.85 MB)每次启动都要重新加载"那一块, 待真机数据确认。
# 0.6.45(2026-09-11): 中奖装杯的球**变大且更满** + 冷启动探针间隔**回退**。
#
# 【一】球变大: 根子是"每层只填 89% 的位置"那个松量。玩家报「珠子不够大 / 100 个珠子的时候
#   不够满」。查下来球径**不是审美选的, 是容量上限的产物**: `pile3d._enumerate` 的格子间距
#   系数 `k_p = 1.12`(那是"给抛光留的松量", 不是物理值) ⇒ 每层只填 89% 的位置 ⇒ x100 装不下
#   100 颗 ⇒ 缩球兜底把球从 r=52 一路缩到 46。
#   改动:
#     · 壁模式 `k_p`: 1.12 -> **1.02**(每层多装 13%; 不能再低到 1.00, 跨层距离会正好压到 2r,
#       抖动一上来断言就炸)
#     · 土丘模式(小奖档) `k_p`: 1.0 -> **1.25** —— 玩家新诉求「球少的时候 球和球有一定距离」,
#       原来小球是死贴着的
#     · `_r_dp_for`: 取消按颗数分级(26/24/22/23/28), 统一 **27** —— 玩家定稿「球一样大」。
#       原来那套分级本身就是容量上限的产物, 会让"中得少的档球反而大"(x2 是 48.4、x100 只有 46.0)
#   实测(桌面, 真函数):
#     · x100: r 46.0 -> **50.2(+9%)**, 缩球 **2 次 -> 0 次**; 顶层满度 60% -> **92%**
#       (顶层原来"中间塌一个坑", 这才是"不够满"的真实来源); 堆顶 design y 44 -> 16
#       —— **仍在玻璃轮廓内, 没有冒出杯口**(玩家看过"冒尖"的预览图后否掉了那条路, 原话
#       「一坨屎」: 顶层是满宽圆盘, 一冒出来就横着摊开、比杯子还宽)
#     · x2/x5/x10/x50/x100 全部落在同一个 r=50.2(真·一样大); 只有 x20 因堆法不同落在 44.4
#       (土丘档的容量上限, 不是没调好)
#     · 生成反而更快: 19.6ms -> 13.1ms
#   ⚠️ 已知隐患(**本次未修**): `_polish` 是"30 轮硬上限 + 必须零重叠才收敛", 而浮点下这是
#      渐近的 —— 旧参数下某些 (档, 球径, 种子) 组合会抛断言, 调用方捕获后 `self._balls = []`
#      ⇒ **整场中奖一颗球都没有**, 只打印一行日志。新参数下 112 次扫描没踩到, 但那是运气不是
#      保障。规划建议的修法: "最深穿透 < 1.2px 即停 + 上限 120 轮"(实测既更稳又快一倍)。
#
# 【二】冷启动探针的轮询间隔 **回退** 0.05 -> 0.15(`Sfx.SFX_READY_POLL`)。v0.6.44 压到 0.05 的
#   理由是"能更早发现音频解码完", 但作者未实测到收益 —— 没有实测证据的"理论优化"不留在出货里。
#   要再试先做真机对拍: 同一个包看「启动信息」里的"音效等待"有没有变短。
#
# 【一】球堆变体 **4 -> 12**, 并且把"变体号"从一个数字拆成三个独立维度。玩家反馈原话:
#   「感觉还是几个枚举, 不够随机」—— 4 个变体意味着 `_seq % 4` 玩 4 局就循环一遍, 而且
#   “层间注册 / 摆法 / 旋转”三件事被绑死在同一个号码上, 等于把组合数从 (12 x 12) 压成了 4。
#   现在每个变体号独立派生:
#     · 层间注册: 12 个合法 Barlow 序列(A/B/C 四位, 相邻两层不同位) —— 只改**哪层落在哪个
#       谷位**, 密度一点不降(每层仍密排、层距仍 1.633r)。24 个合法序列里实测 12 个装得下 100 颗
#     · 摆法    : 11 种(6 档"圆环"从最紧铺到贴壁 + 5 种随机散开), 见下
#     · 旋转    : 黄金角步进 137.508 度(**单独用无效**: 只改相位不改形状)
#   ⚠️ 旋转单独用是无效的 —— 实测"去掉绕轴旋转的形状指纹"下距离**恒为 0**。别看到
#      "逐球位移 244px"就以为它有用, 那些球只是落到了**另一颗一模一样的球**旁边。
#
# 【二】小档(x2/x3/x5/x10, 占中奖场次约 90%)改成**自由摆放**。实测这四档的球本来只用到
#   离轴 60~88, 而地板可用半径有 245 —— **只用掉三分之一的空间**。现在 4 种摆法能甩到 ±230。
#   ⚠️ "偏心"这种摆法对 2 颗球是**伪自由度**(两颗球挤在一侧 == 两个挨着的球, 转一下还是),
#      所以摆法改成"把间距从最紧铺到最松"。相位步子也必须用黄金角: 固定步长 0.7 弧度
#      正好是 3 颗球间隔(120 度)的整数倍, 会让 n=3 的两个变体重合。
#
# 【三】大档(x20/x50/x100)加**层颗数配额**。光靠层间横移, 大档的轮廓几乎不动
#   (实测两两 Hausdorff 中位只有 0.47~0.55 球径, 小档是 0.59~1.05) —— 因为大档是**填满杯子**
#   的堆。配额改的是"哪层多、哪层少" ⇒ 层接缝的位置和每层的明暗台阶全变。
#   ⚠️ 大档的差异**仍然弱于小档**(最差 33% vs 小档 20%) —— 100 颗球填满杯子, 轮廓本来就没有
#      自由度, 这是几何事实不是没做。已知的下一步是"顶层留白", 但那是产品决策(会改变
#      "看起来装满了"的观感), 要玩家点头。
#
# 【四】球堆坐标**离线烘焙**: 从"玩家手机每次启动现算"改成"开发机算好、写进包里"
#   (`tools/bake_pile_data.py` -> `android_part_piledata.py`, 34.6KB)。实测:
#     单次 x100: 22.07ms -> **0.09ms**;  84 个堆全部就绪: 381ms -> **1.9ms**
#   ⚠️ **程序化生成的回退路径一行没删** —— 表坏了不能表现为"那一局没有球"。
#   ⚠️ fx_probe 加了"表 vs 现算逐位对拍"门禁守着脱钩(改球堆参数忘了重烘), 阴性对照实测:
#      把表里一个数挪 0.3px 立刻报红并精确指出是哪颗球漂了多少。
#
# 【五】彩蛋(长按"期望返还比例"5 秒)从「开/不开」改成**四选一**:
#   不开启 / 1000% / 2000% / 5000%。**三档全部无空军**(K_DIST = 9 格全有奖)。
#   ⚠️ "必中"把每格均值**恒等锁死在档位上**, 所以 x2 占比不是自由参数, 它由 _solve_p2 反解:
#      想要大倍率就必须拿更多 x2 平衡。实测落格:
#        1000%: x2 56 / x5 16 / x10 12 / x20 8 / x50 5 / x100 3
#        2000%: x2 44 / x5 6 / x10 11 / x20 18 / x50 15 / x100 7
#        5000%: x2 23 / x20 16 / x50 31 / x100 31   (5000% 故意不放 x5/x10: 掺小倍率反而
#               要更多 x2 去平衡, 大奖更少见)
#   ⚠️ fx_probe 原来对彩蛋档**零覆盖** —— 顶层那条 assert 只管"RTP 配平成功", 而空军是
#      K_DIST 管的, 是另一件事。玩家 2026-09-11 报的「5000% 还tmd空军」就漏在这一半。
#
# 【六】彩蛋弹窗的档位文字改成**从 RTP_HIDDEN 取**。这处写死过一次(5000%), 后来档位定稿成
#   1000% 时漏改, 弹窗一直显示上一版的数字(玩家 2026-09-11 截图报的就是它)。
#
# 【v0.6.47 修闪退】玩家报「选择 2000% 和 5000% 档次会闪退」—— 根因是我自己在 v0.6.46 里
#   **只改到一半**:
#     · 档位清单在源码里**有两份手抄本**: `_boards` 的**初始化**一份、`park_ball` 里的
#       **重刷**一份。两处的行尾注释不同(「切换不刷新」/「重新发射…刷新」), 我用批量替换时
#       把注释一起写进了匹配串 ⇒ **只替换到第一处**, 第二处原样留着 5 档。
#     · 于是切到 2000%/5000%(或切完**发射一次**, `park_ball` 是每次发射都会调的)⇒
#       `self._boards[self.rtp_target]` 抛 KeyError ⇒ 闪退。
#     · 1000% 一直没事, 纯粹因为它早就在那行手抄列表里 —— 这也是这个 bug 能躲过四道门禁的原因:
#       `--selftest`/`--smoke` 根本不会切到隐藏档。
#   修法: 新增 `RootWidget._all_rtp()` 作为**唯一真源**(常驻档 + 彩蛋档), 两处都从它取,
#   不再有任何手写的档位元组。这不是"把漏的那处补上"而是把"有两份"这件事本身消掉。
#   门禁: fx_probe 新增「档位清单没有第二份手抄本」—— 扫**出货文件**里有没有
#   `for r in (0.80, 1.20` 这种手写元组。阴性对照实测: 改回手写立刻报红, 恢复即绿。
#   复现验证: 起 App 逐个档位跑「切档 + park_ball()」, 0.8/3.6/10.0/20.0/50.0 五档全过。
# 【v0.6.48 装杯顺序】玩家报「弹珠落入杯子的顺序好像不随机, **总是先内后外**。体验比较奇怪。」
#   根因(对抗性评审 10 次子代理查实, 结论都带实测): `_make_balls` 原来是 `for p in proj`,
#   而 `proj` 是 `project_pile` 按 **z 降序**排的画家序 ⇒ 每一局、每一个变体都从杯子最里侧
#   扫到最外侧。12 个变体只改**位置**、从不改**顺序**, 所以"变体加到 12 个"也救不了。
#   连带查实: 相邻上下两颗球里 **65%** 是上面那颗先落定(它出发更早、落得更快), 屏幕上就是
#   "从杯子深处往外铺"; 而且球会**先停在半空**(底下是个空坑, 等 0.22s、最长 0.68s 才被填上)。
#
#   改法一(顺序) 新增 `_topo_deal()` = **随机拓扑序**: 先用 `_support_map()` 算出"谁压在谁
#     上面"(h 更低 且 **水平** 距 < 一个球直径), 再每次从"身下已就位"的那批里**均匀随机**
#     挑一颗。偏序图挂在球堆缓存上算一次(运行期不重建, 只跑一遍 Kahn)。
#     ⚠️ 判据必须是**水平**距离: 球是竖着落的, 水平距小于一个球直径就必然相交。用 3D 距会
#       漏掉约 2/3 的阻挡关系 —— 专家实测按 3D 图跑出来 x100 一局 PEN 708, 比不改还差 34 倍。
#     ⚠️ 为什么不是"按层发牌": 层序把整层当一块落, 屏幕上留"一层一块"的施工缝 —— 把
#       "永远先内后外"换成"永远先下后上", 在玩家眼里还是同一个固定套路。拓扑序允许
#       "这层角上先落、中间后落", 才是真随机(同层落序跨度 1.00 -> 2.62)。
#     ⚠️ 层号从投影反推(`h = FLOOR_Y - sy - K2*z`), 不用 beads 的 layer 字段 ——
#       离线烘焙表把 `"layer"` 硬写成 0, 按它分层会静默失效(评审实测 2280 颗球 0 误差)。
#
#   改法二(绘制, **必须和改法一一起上**) 原来"飞行球一律置顶"之所以不出事, 唯一原因是
#     发牌序 = z 降序 ⇒ 任意时刻已落定的恰好是最远的那批 ⇒ 飞行球永远比它们近, 画在上面
#     **是对的**。顺序一随机这个巧合就没了。现在球按深度**一趟**画完(含飞行球)。
#     代价: 飞行球可能被前面的球短暂遮住 —— 物理上正确(像球陷进堆里), 别改回去。
#
#   改法三(连带) `RAIN_SHIFT_MAX` 0.55 -> 0.80。发牌随机之后**第一颗球不再是最远那颗**
#     (它现在是随机挑的底层球, 下落路径最长), `min(t0+f_enter)` 从约 0.67 涨到 0.73~0.80,
#     撞上 0.55 的封顶 ⇒ 首球要等 0.25~0.29s 才露头(设计值 0.12)。只影响原本就封顶的
#     x2/x3 两档(它们占中奖场次约 80%), 整场提前约 0.13s, 无副作用。
#
#   门禁 fx_probe [18a/18b/18c], **三条都做了阴性对照**:
#     · [18a] 拓扑合法: 每颗球发牌时正下方的球都已发牌。改回 z 序 -> 违规 **435 对**。
#     · [18b] 顺序每局不同: 逐位比对两局第 i 颗球的落点, 重合率必须 < 0.20。
#       改回 z 序 -> 重合 **1.000**(正是玩家抱怨的那件事)。
#     · [18c] 飞行球也按深度画: 判据是**真 `_redraw` 指令表里两颗球谁先画**(行为判据,
#       不是数调用次数)。改回"飞行球置顶" -> 飞行球 @91 > 已落定球 @85。
#
#   实测结果: 拓扑违规 **0 对**(x20/x100 x v0/v7, 共查 4377 对);
#     跨局顺序相关度 **1.000 -> 0.04~0.06**; 与深度相关度 **+1.000 -> -0.44~-0.63**。
#   四道门禁全绿(--check / fx_probe 全部通过 / --selftest OK / --smoke SMOKE-OK)。
# 【v0.6.49 隐藏档弹窗】玩家 2026-09-12 定稿的一串改动:
#   ① 长按"期望返还比例"的时间 **5s -> 3s**(`RTP_UNLOCK_HOLD`)。
#   ② **每次长按都弹** —— 原来 `_rtp_unlocked` 只置真、从不置假, 一局只能解锁一次。
#      这个标志**整个删除**: 删掉守卫后它只剩写入、零读取, 留着就是第二个状态源, 必然和
#      "那排上有没有隐藏档按钮"漂移。"有没有隐藏档"的唯一真源 = `rtp_btns` 的键 ——
#      冷启动重建界面时它是空的, 天然表达"未解锁", 与"隐藏档不持久化"自洽。
#   ③ 弹窗改成**四横排 + 确认**: [关闭隐藏][1000%][2000%][5000%], 下面「确定」。
#      点选项只**选中**(高亮), 点确定才生效 —— 旧版点了立即生效、没有回头路。
#      **默认选中当前档位**(开着 2000% 就高亮 2000%; 常驻档则高亮「关闭隐藏」)。
#   ④ 新增**关闭隐藏档**的能力(旧版只有"加按钮"、没有"减按钮"): `_close_rtp_hidden()`
#      摘掉全部隐藏档按钮 + 返还比例回到最高常驻档。⚠️ 两个细节是玩家点名的:
#      · **本来就是常驻档时什么都不做**(不跳 360%);
#      · 切到第二个隐藏档时**只留当前一个**(开过 1000% 又选 2000% 就把 1000% 摘掉)。
#   ⑤ 语音: 关闭时播 `voice_rtp_hide_{1000,2000,5000}`("百分之两千的隐藏倍率已关闭,
#      返还比例调整到百分之三百六十")。⚠️ **必须 `set_rtp(..., silent=True)`**:
#      "切回 360%"那条常规语音与新语音同族, 两条都走普通路径会被 `Sfx.play` 的
#      `voice_rtp_` 互斥按上一句真实时长**静默挤掉一条**(靠调用顺序兜底太脆)。
#   ⑥ 「确定」用 `COL_GREEN`, **不和选中态同色**(玩家报的"确定和选择按钮颜色一致";
#      选中态必须保持 COL_BTN 才能和主界面那排档位高亮对上, 所以换的是「确定」)。
#   ⑦ 防重入: 弹窗开着时再长按会叠第二个(`_on_title_touch_down` 是 **Window 级触摸
#      观察者**, 模态弹窗拦不住它) —— 在**建弹窗那一端**加 `_rtp_popup` 闸门。
#   ⑧ 弹窗尺寸 0.92 / h_dp=250 是**量出来的**: Kivy 的 Popup 外壳还有一层 12dp 内边距
#      (`kivy/data/style.kv` 的 `GridLayout(padding:'12dp')`), 横向可用宽 =
#      `hint_w*vw - 24 - 2*pad - 3*spacing` 再 /4。360dp 机器上旧的 0.86/14/5 差 3.3dp
#      —— 而 Kivy 的 Button **不会自动换行**(它没有 text_size), 超了就是溢出到隔壁。
#      实测: 按钮宽 110.2, 最长文字("关闭隐藏")64.0, 无溢出。
#   ⑨ 顺手加固: `_load_config` 的读档白名单从**手抄元组**改成 `_regular_rtp()` 派生 ——
#      它是同一份档位清单的**第三处**副本(v0.6.47 闪退事故的同款形状)。派生之后
#      "隐藏档读不回来"从"靠人记得改那行"变成结构性事实。冷启动仍落 80%、切任务保留
#      (玩家点名的约束, 行为一字未变)。
#
#   门禁 fx_probe [19a~19h] + [3e] 补一条, **每条都做了阴性对照**(改回坏的样子实测全红):
#     · 19a 每次都弹(AST 判 `_rtp_unlocked` 零引用) —— 改回守卫: 引用 2 处。
#     · 19b 不叠弹窗 —— 去掉闸门: 开着时连建 2 个。
#     · 19c 选项文字跟着 RTP_HIDDEN 走(临时换成 777% 必须跟着变)。
#     · 19d 默认选中(两套夹具 + "恰好一个高亮"); 19d-2 确定≠选中色(改回: 两色逐位相同)。
#     · 19e 关闭隐藏档(4 条断言) —— 去掉 was_hidden: 常驻档关闭后跳到 3.60。
#     · 19f 语音只播新的那条 —— 去掉 silent: 出现 voice_rtp_360(撞车重现)。
#     · 19g 语音清单三方一致(PHRASES == voice/*.wav == 出货文件里的字面名, 双向)。
#     · 19h 新方法都长在 RootWidget 上(掉错类能编译、--check 也绿)。
#     · [3e] 补"读档白名单必须走 `_regular_rtp()`"——用**正面形态**而不是"禁止字面量":
#       后者会被解释这段历史的注释误伤, 而且 plinko.py 里还有两处既有的档位元组
#       (VALUE_DIST 配平表、selftest 校验循环), 放宽正则只会把门禁扫红却没人该改。
#   四道门禁全绿(--check / fx_probe / --selftest / --smoke), 弹窗布局实测无溢出。
# 【v0.6.50 隐藏档弹窗收尾】v0.6.49 上线后玩家看过实机提的两条:
#   ① 「确定」按钮的绿色不合理 -> 换成**暗砖红**。项目配色表里**没有暗红**, 新增
#      `COL_DARKRED = "#8f3a2e"`(在 plinko.py 的颜色表里)。不复用 `COL_FIRE`(偏亮偏橙,
#      而且已经是"蓄力发射"按钮的颜色), 也不挪用 `COL_x[10]`(那是槽位倍率色体系)。
#      ⚠️ 选中态**保持 `COL_BTN` 蓝不变** —— 主界面那排档位高亮用的是同一个色, 玩家要能
#      把两处对上; 该换的永远是"确定"这一侧(`fx_probe [19d-2]` 钉着"确定 != 选中色")。
#   ② 提示文案「（不保存 —— 重新打开游戏要重新解锁）」有两处不准, 改成
#      **「（重启游戏后隐藏返还率失效，需重新激活）」**:
#      · 「重新打开游戏」->「重启游戏」: 切到后台再回来隐藏档**还在**(内存没丢,
#        `on_resume` 不重跑读档), 只有**进程被杀/重启**才没。旧文案会让玩家以为切出去
#        回来就丢了, 与实感不符。
#      · 「不保存」->「失效」: 它其实**写进了存档**(`_save_config` 照常写 `rtp_target`),
#        只是**读的时候被白名单丢掉**(`_load_config` 走 `_regular_rtp()` 派生)。
#        说"失效"既贴玩家感受, 也不暴露实现。
#   门禁不用改: [19d-2](确定≠选中色)换色后仍成立; [19c](选项文字集合)只看选项按钮、
#   不看提示行。四道门禁全绿(--check / fx_probe / --selftest / --smoke);
#   弹窗布局按新文案重测, 无溢出。
# 【v0.6.51 隐藏档弹窗排版】v0.6.50 上线后又两条:
#   ① **说明行挪到"选项"和"确定"之间**(玩家定稿): 从上到下变成 选项 -> 说明 -> 确定,
#      跟玩家"先选、再往下看到说明、再确认"的动作顺序一致。原来它在最上面(标题下面),
#      容易被当成副标题跳过。
#   ② **字号整体放大**: 选项/确定 16sp -> 18sp, 说明 13sp -> 14sp。
#      ⚠️ 放大字号会撞宽度: 四个汉字("关闭隐藏")要 4x18 = 72dp, 而按钮宽 =
#        `hint_w*vw - 24(外壳内边距) - 2*pad - 3*spacing` 再 /4 —— 360dp 机器上旧的
#        0.92/10/4 只给 68.8dp, **直接放大就会溢出**(Kivy 的 Button 没有 text_size,
#        不会自动换行, 超了直接盖到隔壁按钮上)。所以同步把弹窗加宽、内边距收紧:
#        hint_w 0.92 -> 0.98、padding 10 -> 8、h_dp 250 -> 265、选项行高 50 -> 54、
#        确定 48 -> 52。改后 360dp 给 75.2dp(余 3.2)、411dp 给 87.7dp(余 15.7)。
#      **实测**(不只是算): 桌面 540dp 窗下四个选项等宽 119.3、最长文字 72.0、无溢出;
#        并额外按 360dp / 411dp 外推校验过。
#   门禁不用改([19c] 只看选项按钮文字、[19d-2] 只看颜色)。四道门禁全绿。
# 【v0.6.52 装杯改成"等点击才退场"】玩家 2026-09-12 定稿, 原话:
#   「当弹珠落入容器后，界面不再消失，当玩家手动点击屏幕任意位置的时候才消失。
#     当弹珠没有下落完成+0.6秒后，点击也不消失。」
#
#   ① **装满后不再自动退场** —— 画面停在装满状态, 等玩家点屏幕任意位置才收走。退场起点
#      从 `_settled_at + _hold` 改成**玩家点击那一刻**(`_closing_at`)。
#   ② **最短停留 0.6s**: 最后一颗球落定 + 0.6s 之前点击无效(球还在往下落时 mode 还不是
#      result, 同样不受理)。判据全在 `request_close()` 里。
#   ③ 停留时长**统一 0.6**(不再按倍率分档) —— `HOLD_TIERS` / `HOLD_STEP` 已删,
#      `hold_for(m)` 恒返回 HOLD_BASE。签名不改: 5 个消费者 + 6 条探针判据都写它。
#   ④ 一直不点就一直停着。
#   ⑤ 彩蛋路径**同样等点击**(规则单一, 玩家不用记两套)。
#   ⑥ **跑分例外**: 跑分是自动连续发射的, 等人点击会把整轮跑分卡死 —— 那一路仍到点自己走。
#      用显式参数 `play_win(..., auto_close=...)` 从界面传下去, 不让演出自己去猜界面状态。
#
#   ⚠️ `FX_MAX_SEC` 9.0 -> 60.0: 退场改成等点击之后, 9 秒对"看够了再点"太短, 会把画面
#      **当场清掉**(`_abort` 是 mode=idle + 清空球)。它是**安全网**不是产品行为 —— 防的是
#      "某个 bug 让点击失效 -> 真卡死"; 触发时画面会突然没了, 跟"一直等"矛盾, 两害相权
#      宁可画面没了也不能锁死玩家。
#   ⚠️ `_layers()` 的退场分支多了个前置: 没收到关闭请求时**恒返回装满静止态** `(1,1,1,0)`
#      —— dy 必须是 0, 否则整组会停在"上浮过的"位置(那是退场终态的姿势, 不是装满的)。
#
#   门禁:
#     · [5] "七档停留严格递增" 改成 "**七档停留一致**"(分档已废, 改回递增 = 又加回来了)
#     · [6] 夹具改成设 `_closing_at`; 新增"没点击时恒返回装满静止态"
#     · [10]/[11] 的 tick 循环**必须模拟点击**才回得到 idle; 上界也重算 —— 按 FX_MAX_SEC
#       (60s) 算跑不完, 而按"停留+淡出"算**又太短**(实测 x100 的揭晓在 _t0+2.9s, 2.85s
#       的窗口根本放不出来, 会误报"丢账")
#     · 新增 **[20] 等玩家点击**: 20a 装满后不自动退场 / 20b 最短停留 / 20c 点击后走现成的
#       0.25s 淡出曲线 / 20d 跑分例外
#   **四条阴性对照都实做了**(把改动分别改回坏的样子, 实测全红):
#     20a 改回自动退场 -> 12s 后 mode=idle
#     20b 去掉最短停留 -> 早 0.05s 的点击被受理
#     20c 去掉"等待期返回静止态" -> 等待期被渲染成退场终态 (0.0, 0.0, 0.9, 52.0)
#     20d 摘掉跑分例外 -> auto_close 之后 0.85s 仍停在 result
#   四道门禁全绿(--check / fx_probe / --selftest / --smoke)。
# 【v0.6.53 兜底收窄 + 冒烟夹具修复】v0.6.52 的两处收尾:
#   ① **`FX_MAX_SEC` 回到 9.0, 硬兜底收窄到只覆盖 `pending`/`win`**。v0.6.52 把它提到 60
#      是想让"等点击"更安全, **方向反了**: `_abort()` 触发时会把杯子和整屏压暗**当场擦掉**,
#      那正是"一直等"要消灭的东西; 而且提到 60 之后, 卡在"球还在落"那一段就要等一分钟
#      才放手。现在:
#        · `pending`/`win`(球还在进场/下落, 玩家没有话语权) -> 9s 兜底照旧, 卡住必须放手
#        · `result`(装满静止) -> **不设任何时间兜底**。出口只有玩家的点击
#          (`request_close`, 挂在 Window 级触摸观察者上, 每一下触摸都会到), 比任何算术可靠
#      副作用是好的: 切后台一分钟回来, 杯子**原样留着**(旧行为会在回来那一帧擦掉)。
#   ② `_abort()` 顺手把 `_closing_at` 归零("清干净"的语义, 别留半局状态给下一局)。
#   ③ `request_close()` 的调用点包 `try/except`: Window 的事件派发**不捕获异常**, 漏出去会
#      冒到事件循环。用裸 `return`(实测观察者返回值只是 OR 聚合、**不短路**, 没有证据
#      `return True` 能吞掉后面的 widget 树 —— 没必要冒那个险)。
#
#   ⚠️ **冒烟夹具两处修复**(都是这次改动暴露的, 原来的绿是"会骗人的绿"):
#     · `when_ready` 的轮询 `state == "ready" and not busy()` —— 装杯现在要点击才退场, 而
#       冒烟里没有人点 ⇒ **永远等不到**, 每次都走超时分支, 在"杯子还立着"的状态下截图,
#       日志里只有一行超时。现在过半还没等到就**模拟一次玩家点击**, 走同一个
#       `request_close()`(别在冒烟里另写一份"点击该干什么")。
#     · `bench-win` 那段没清上一局残留的装杯(它也停在 result), 导致"settle 那一刻 mode
#       本该是 idle"被残值误判成 FAIL。补一句 `_abort()` 清理。
#     实测修复后: s8/s9/s9b 全部 `state=ready`、`bench-win: cup=idle busy=False`、零 FAIL。
#   四道门禁全绿(--check / fx_probe / --selftest / --smoke)。
# 【v0.6.54 注释去谎 + 死字段清理】v0.6.53 的收尾, **零行为改动**:
#   ① 文件头的红线 3「绝不软锁」现在是半个谎 —— 它写"任何异常路径都必须回到 idle", 而
#      result 段(装满静止)**故意没有时间兜底**。改成"出口分两种": pending/win 走
#      FX_MAX_SEC 兜底; result 走玩家点击(request_close)。并写明"谁要给它加超时, 先问玩家"。
#   ② `_on_easter_settled` 那句"(轮询有上界: win_fx 自己有 FX_MAX_SEC=9s 硬兜底, 不会空转)"
#      —— **现在是假的**(result 段没有时间兜底 ⇒ 轮询没有算术上界)。改成真话, 并写明
#      "别改成'到点自己弹', 那会让弹窗盖在**还立着的杯子**上"。
#   ③ `_frame` landed 分支的"硬兜底…不会锁死"补上"**只覆盖装杯期**; 装满后的出口是玩家点击"。
#   ④ `expected_sec` 的 docstring 说"给 _result_until **和兜底 deadline** 用" —— 后半句早
#      就过时了(兜底 deadline 现在走 reveal_at())。改成"只给 _result_until", 写明它算的是
#      "**最短**上屏时长"(结果段可以无限长), 并记一个**既有缺陷**: settle() 调它的时刻早于
#      play_win(后者被 CUP_TRIGGER_DELAY 延后调度) ⇒ 线上实际走估算分支。顺手把那句已被
#      证伪的"调用顺序有保证"删掉(它自 CUP_TRIGGER_DELAY 引入起就不成立了)。
#   ⑤ 删掉死字段 `self._hold`: 退场改由点击触发之后它**只剩写入、零读取**(最后一处读取在
#      v0.6.53 的 auto_close 支路里, 改用 HOLD_BASE 常量)。
#   零行为改动, 四道门禁全绿(--check / fx_probe / --selftest / --smoke 零 FAIL)。
# 【v0.6.55 窄屏布局自适应】玩家 2026-09-12 报「很多界面在红米 K90 Pro Max 上有布局的换行问题」,
#   点名两处: 彩蛋弹窗、顶栏右侧那行状态文字(最短是"未中", 长的会折行)。逐控件实测后确认
#   **不是"字体小 / 界面小"**, 是三类结构性硬伤:
#   ① **宽度是写死的, 加起来比屏幕还宽**。例: 「期望返还比例：」写死 dp(115) 而它那行字只要
#      98 —— 白占 17dp; 整行 = 24(左) + 115 + 4x56(按钮) + 25(间距) + 24(右) = 388dp, 而手机
#      只有 360~400dp ⇒ 最右那个档位按钮**被推出屏幕**(360dp 上右溢 47dp)。
#   ② **Kivy 的文字只有"换行"和"溢出去盖邻居"两种行为, 没有"缩到放得下"这一档**。而 HUD 上
#      几乎每个位置都是定宽 + 定高 ⇒ 窄屏或长文案必然折行, 折出来的第二行还被定高**裁掉**
#      (比折行更狠: 字直接没了 —— v0.6.12 / v0.6.20 记过两次同款)。
#   ③ **弹窗高度是拍的固定值, 正文却给了弹性** ⇒ 正文一折行就顶出格子、压在按钮上
#      (彩蛋弹窗 400dp: 正文要 253px 而只给 248px ⇒ 折成 4 行 96px 塞进 56px 的格子)。
#   修法(四处机制, 全在 tools/android_part_ui.py):
#   ① 新增 `text_px()` / `fit_font_size()` + `RootWidget._fit1()`/`_install_fit()`: **单行**
#      控件在"文字或宽度一变"就重挑一个"恰好放得下"的最大字号(FIT_SCALES 1.0~0.70), 不再
#      折行、不再溢出。挂在 text/width 上而不是去二十个赋值点各调一次 —— 漏掉一个的表现是
#      "某个状态又折行了", 只有截图看得见。实测开销: 未命中缓存 24µs / 命中 1µs。
#   ② `_apply_row_budget()`: 顶栏左块改**显式定宽**(原来它与右块**平分**剩余宽, 而内容要
#      142dp 却只分到 87~123dp, 溢出去盖标题 —— BUILD_APK §3.5 记过同一个坑); 返还率/投入
#      两行的按钮宽**按行宽反算**; 标签宽**按字量**(115 -> 字宽 + 4); 两行左右空白 24/24 ->
#      14/10; 力度标签(全工程唯一赋值是空串的**死控件**)宽按空串给 0 —— 它白占 100dp, 正是
#      340dp 以下底行被挤出屏幕的原因。
#   ③ `_fit_line()` / `_auto_h()` / `_popup_fit_content()`: 弹窗单行标签自适应; 正文高度跟着
#      **真实排版**走(折行只让弹窗长高, 不裁字); 弹窗高度 = content.minimum_height + **实测**
#      非内容区(`popup.height - content.height`, 不写死常数, 与 Kivy 版本无关)。
#   ④ `_reflow_row_budget()`: 隐藏档按钮**增删之后**重跑一遍预算。原来 `_add_rtp_button` 把新
#      按钮宽写死 dp(56), 而常驻档按钮是按行宽反算的(360dp 上 45dp) ⇒ 一解锁, 那一行从
#      "恰好铺满"变成**右溢 44dp**, 最右那个隐藏档按钮**被推出屏幕**。玩家报的「隐藏返还率几乎
#      看不到」就是它 —— 不是"看不清", 是在屏幕外面。⭐ 这条顺手就修掉了, 玩家原以为"搞不定"。
#   共 21 处: 主界面 11(状态栏 / 标题 / 顶栏按钮互盖 / 两条档位行右溢 / 两处标签宽 / 信息行三处 /
#   隐藏档 / 底行) + 弹窗 10(彩蛋 / 跑分菜单 / 跑分历史 / 每轮次数 / 隐藏档 / 本轮结束 / 启动信息 /
#   跑分成绩 / 重放详情)。其中「跑分菜单说明在所有宽度下都被裁」「跑分历史每行都被裁」是玩家
#   没提到、但一直存在的。
#   证据: 新写 `android/temp/layout_probe.py`(逐控件量 折行 / 折行后被裁 / 溢出盖邻居 / 越出容器),
#   360 / 400 / 417 / 457dp 四档 + 横屏(960x540) + 320/340dp **全绿**; **阴性对照** —— 同一套
#   判据跑改动前那版, 400dp 上 5 处红(跑分菜单裁字 / 彩蛋弹窗裁字 / 顶栏状态折行+裁字 /
#   余额折行 / 隐藏档按钮右溢 44dp), 证明判据有分辨力, 而不是"怎么改都绿"。四道门禁全绿
#   (--check / fx_probe / --selftest / --smoke)。
#   ⚠️ 边界(如实记): 只在桌面做了 **dp 等价**模拟(把窗口按目标 dp 设, 桌面 density=1 所以
#   窗口像素即 dp), **没上真机**; 320dp 以下顶栏状态会缩到 8px 并折成两行(那行实在塞不下
#   5 字标题 + 两个按钮 + 状态; 现代机型不会低于 360dp)。
# 【v0.6.56 布局自适应收口】v0.6.55 上线后, 玩家自己看图抓出 3 个新毛病, 送去两轮独立复核
#   (一轮对抗性找茬 31 个 agent, 一轮 QA 审报告 35 个 agent), 又挖出 5 条真 bug。全部修完。
#   ---- 玩家抓到的(都是 v0.6.55 自己引入的) ----
#   ① 弹窗高度算坏: `_popup_fit_content` 用 `popup.height - content.height` 反推"非内容区",
#      而**同一个 tick 里**读到的 content.height 还是上一轮布局的旧值 —— 彩蛋弹窗被压到 110dp
#      (要 125), **分隔线压在标题字上**; 跑分菜单反被撑到 259dp, 顶上留一大块空白。改成按
#      **结构**算(GridLayout 内边距 + 空标题行 + 分隔条), 与"布局跑到第几帧"无关, 调几次都一样。
#   ② 跑分历史同一列出现 34.0/31.96/30.0 **三种字号**(逐行各自缩) —— 新增 `_fit_uniform`,
#      同格式的列表**共用一个字号**, 按最宽那条定档。(返还率那一排按钮后来也被 QA 抓到同款,
#      一并按整排统一了。)
#   ③ 「纯 Python 执行…」在**空格处**断成孤行 —— 空格就是 Kivy 的断行点。拆掉空格, 并把
#      那一句的断行**写死**(两段 93/210px, 360 和 400 都一次放得下)。
#   ---- 对抗性复核挖出来的(v0.6.55 的真 bug) ----
#   ④ **回调自我复制(严重)**: `_apply_row_budget` 每次调用都给两条档位行 `bind(width=...)`,
#      而那条回调又调回它自己 —— observer 数实测 21->85->341->1365(**每变一次宽 ×4**),
#      一次宽度变化要跑几千遍(单遍 0.046ms) ⇒ 转屏 / 分屏拖拽 / 折叠屏开合时**整帧卡 1~4 秒,
#      只增不减、越玩越糟**。改为只绑一次(实测 7 次改宽后仍是 2/2)。
#   ⑤ 运行期新增的隐藏档按钮**没有 `_fit_base`**, `_fit1` 回退成裸 `16.0` 并**缓存**下来 ⇒
#      就它一个的字号跟同排四个常驻档不一致, 直到下次转屏才纠正。
#   ⑥ **内容超出视口时被画到弹窗外面**(Kivy 不裁剪控件): 实测 320x640 + 大字体时弹窗标题
#      整行飘到屏幕外。这是把"定高裁切"换成"自动撑高"时引入的新失败模式。现在先缩正文字号、
#      再压高度兜底, 并补了"够用就释放钳位"(钳位只设不清会让第一帧的压缩永久留下)。
#   ⑦ `fit_font_size` 六档试完**无条件返回 0.70**、不检查是否真塞得下 ⇒ 退回折行/盖字两态。
#      改成**二分搜**(不能按比例估一次: 实测字宽**不随字号线性变** —— 同一串在 14.95 和
#      14.05 下量出来都是 139px, 字形步进被取整)。
#   ⑧ 系统字体放大这条路径**我先前判断错了**: Kivy 在安卓上 `dp = 值 × scaledDensity`、
#      `sp = 值 × scaledDensity × fontScale` —— **sp 把 fontScale 乘了两遍**, 所以文字相对
#      盒子确实是按 fontScale 放大的, 大字体就是会挤。(我一度以为"dp 也缩放所以没事"。)
#      修完实测: **1.0 / 1.15 / 1.3 倍全部干净**; 1.5 倍下跑分菜单说明还差一行, 记为已知边界。
#   ---- QA 审报告时抓到的(v0.6.55 的 UI 不一致 + 我报告里的错数字) ----
#   ⑨ 彩蛋弹窗的**分隔线飘在标题上方** —— 它是六个弹窗里唯一一个"传了 title='' 却没关分隔条"的,
#      另外三个无标题栏的都显式传了 `separator_height=0`。对齐。
#   ⑩ 启动信息弹窗自己手抄了一份 `_refit`(`content.minimum_height + dp(64)`), 比结构值 44dp
#      多 20dp, 而那 20dp 全落在正文上方。改用统一的 `_popup_fit_content`。
#   ⑪ 上限定成 `_vh*0.92` 是**我拍的余量**, 不是硬约束(真正的要求只是"放得上屏幕")。定 0.92
#      的代价实测: 360dp + 1.3 倍字体下跑分菜单多出 11px 就被压掉一行, 而它明明放得下。提到 0.96。
#   ⑫ 报告本身写错的数: 「说明行 22 个汉字要 320px」(实际 17 个汉字 + 括号、14sp 下 280px)、
#      「改之前按钮 45.3dp」(两版都是写死的 56dp); 两张卡片标了 400dp 而症状只在 360dp;
#      一张卡(启动信息)的 before/after 在默认字体下**看不出差别**(那一对图还因为夹具目录缺
#      `buildozer.spec` 而少了版本号) —— 删卡, 并把其余的宽度与前提逐条改正。
#   ---- 联想 Y700(平板) ----
#   ⑬ "按行宽反算按钮宽"在**宽**屏上走向另一个极端: Y700 的等效内容列有 **792dp**, 实测按钮被
#      撑成 127/160dp(设计值 56)。加**上界** `dp(72)` —— 手机(≤437dp)完全不受影响, 平板上富余
#      交回给"吃余量的空白弹簧", 回到原本的紧凑左对齐。
#   ---- 新增两道闸(v0.6.55 的盲区) ----
#   · `temp/popup_gate.py`: 逐弹窗断言"高度 == 内容真正需要的高度"(挤了/空太多都红) ——
#     老探针只看"文字放不放得下", **弹窗框对不对是盲区**, 上面 ①②⑩⑪ 它全绿。
#   · `temp/regress_check.py`: 行宽回调数不许涨 / `_fit_base` 必须一致 / 同排字号必须一致 /
#     弹窗孩子不许越界。四条都做过阴性对照(改回坏的样子实测全红)。
#   四道门禁 + 布局探针(360/400/417/457/Y700) + 弹窗门禁 + 回归检查 全绿;
#   大字体 1.0/1.15/1.3 全绿。⚠️ 边界: 1.5 倍系统字体下跑分菜单说明会少一行; 只在桌面做了
#   dp 等价模拟, **没上真机**。
# 【v0.6.57 跑分菜单文案定稿】玩家 2026-09-12 逐条点的三件事:
#   ① 「去掉这个界面的空行」 —— 正文里那两个空行(概述/两项测试/结论 之间的分段空行)删掉,
#      正文 9 行 -> 6 行。
#   ② 「去掉纯python执行后的文字」 —— 「，反映设备跑弹珠的实际流畅度。」整段删掉。
#   ③ 「润色纯python执行, 告诉我你润色为什么」 —— 4 个透镜(自然度/事实准确性/上下文一致/极简)
#      各拟候选 + 定稿人评分排序, 定稿 = **「物理引擎是纯 Python 写的。」**(192px, 评审 9/10)。
#      定稿人列出的原句毛病(逐条核过):
#        · **无主语**: "纯Python执行"是名词性偏正短语, 没有施事者。它原来靠后半句把它锚在
#          "第 2 项"上; 后半句一删、空行也删掉之后, 它直接跟在「…算力。」的句号后面,
#          读者会读成"整个 app 是纯 Python 执行的"—— **而那是假的**(渲染走 SDL2/OpenGL、
#          音频走 SoundPool, 都是原生层)。
#        · **中英之间缺空格**: 同一屏里「第 2 项主要吃 CPU 单核浮点算力。」「自动发 3 颗球」
#          「每轮游戏 100 次已结束」全都留空格, 只有「纯Python」不留 —— 破本文件自己的惯例。
#          (当年是我把空格删掉的: 空格就是 Kivy 的断行点, 而那句当年 303px 一行放不下,
#          于是被断在逗号后面。现在这句只剩 192px、离断点很远, 空格不会再变成断行点。)
#        · **「执行」是对译腔**: pure Python execution 的逐字对译; 同屏其它动词是 跑/吃/测。
#        · **半句收不住**: 有谓语没主语, 不能独立成句, 而它的新位置(正文最后一行)恰恰要求它独立。
#        · **句内歧义**: "纯 + 动词"既可读成方式状语也可读成偏正, 后半句在时压着这层歧义,
#          半句删掉就浮出来了。
#      为什么选「物理引擎」这个主语: 它接住上一行(第 2 项说的就是「物理引擎全力跑」),
#      而且把断言**锁在物理引擎上** —— 不会溢出成"整个 app 没有原生代码"。⚠️ 评审专门警告:
#      **千万别写成「没有原生加速」**(渲染/音频都是原生的), 那是从"可核实"滑到"错"。
#   ④ 顺带清掉两处**过期注释数字**: 那段注释写着"最宽那句要 327px / 可用 246~307px" ——
#      327px 量的是**改文案之前**那版(那时这句还带空格), 246~307 是弹窗加宽(0.84->0.88)之前的
#      可用宽。现在实测: 6 行, 最宽一句「全程约 25 秒(含完整的中奖装杯演出)。」= **259px**,
#      可用 360dp 上 261px / 400dp 上 296px —— **只差 2px**。新增 `temp/check_desc.py`
#      从源码读字面量逐行量, 以后改这段文案直接跑它。
#   ⑤ ⚠️ 末段那两行**必须保留手写 \n**: 「第 2 项主要吃 CPU 单核浮点算力。」232px + 新末行
#      192px = 424px > 可用 261px, 交给 Kivy 自动折的话断点会落在"算力。"后面那个**空格**上
#      —— 那正是当年「纯 Python 执行」被断成两行的老坑。
#   门禁: --check / --selftest / fx_probe / --smoke 全绿; 布局探针 360/400dp 全绿;
#   弹窗高度门禁 8 个弹窗全过; temp/check_desc.py 报"全部一行放得下(360dp 起)"。
# 【v0.6.58 列表字号统一】玩家问「测试历史这个字是不是太大了, 你看看所有界面 是不是有没有
#   必要的过大的文字」, 并给了判据:**主界面不动; 一个东西设计出来是为了放更多内容的,
#   能放得下时字体大一些无所谓, 但放大之后让可用信息行数大幅下降就不合适**。
#   把**全部界面**的文字按界面拉了一张字号表, 结论:
#     · 主界面**严格踩着自己定的 6 级字阶**(Hero48 / Key18 / Act16 / Body14 / Aux13) —— 很整齐,
#       按玩家的话**一个都没动**(今天早些时候加的只是"装不下才缩"的兜底, 设计字号没改)。
#     · 但**弹窗飘了**: 三个**长得几乎一样**的列表用了三种字号 ——
#         跑分历史 每行 17sp(全 app 最大的正文, 比主界面正文 14~15 还大一档)
#         启动信息 每行 16sp
#         每轮次数 每行 15sp
#   改动:
#     · 跑分历史: 行字号 17 -> 15sp, 行高 30 -> 26dp。行距 32 -> 28dp ⇒ **同样高的滚动框里
#       行数 +14%(约多放两行)** —— 正好命中玩家那条"让可用信息行数大幅下降"的判据。
#       (这一项是滚动列表, 省下来的高度直接换成行数, 收益最实在。)
#     · 启动信息: 行字号 16 -> 15sp。⚠️ 它**不是滚动框**(弹窗按内容自适应高度), 所以收益
#       不是"更多行"而是"弹窗更矮 + 三个同类弹窗终于同一个字号"。
#   按玩家判据**没动**的(都不吃行数, 列出来备查):
#     · 彩蛋弹窗标题 28sp(其它弹窗标题 20sp) —— 一次性"彩蛋时刻", 弹窗按内容自适应, 不占行数;
#     · 隐藏档弹窗选项按钮 18sp —— 玩家先前特意从 16sp 提上去的(还为此算过按钮宽);
#     · 弹窗按钮 16 vs 17sp、弹窗标题 19 vs 20sp 这类 1sp 的差 —— 观感瑕疵, 不吃行数。
#   顺带两件:
#     · 新增 `temp/check_desc.py`: 从源码字面量里读出一段文案, 逐行量宽并报"哪一行会在
#       360/400dp 折"。以后改这类多行文案直接跑它, 别再靠估。
#     · 报告(android/output/layout_report.html)的图片 URL 加了 `?v=<时间戳>`: 本地 file://
#       打开时浏览器会把图缓存得很死, 只换 HTML 看不到更新(玩家为此问过两次), URL 变了才会重取。
#   门禁: --check / --selftest / fx_probe / --smoke 全绿; 布局探针 360/400dp 全绿;
#   弹窗高度门禁 360/400/457dp 各 7 个弹窗全过; 回归检查全过。
# 【v0.6.59 顶栏标题居中 + 文案遍历】玩家 2026-09-12 两条:
#   ---- ① 「跳跳的弹珠机应该居中」 ----
#   病根(实测): 顶栏原来是 [左 定宽126][标题 flex][右 flex] —— 两边**不等宽**, 于是标题随窗口
#   变宽越来越偏左: 400dp 上偏 2.5px(看不出来), **540dp 的桌面窗上偏 38.5px**(一眼就歪)。
#   改成 [左 flex(1)][标题 定宽][右 flex(1)]: 两边**强制等分**剩余宽 ⇒ 标题落在屏幕正中。
#   实测偏差: 360/400/457/540/800dp 全部 0.0~0.1px。
#   代价是左侧两个按钮的宽必须由那份份额反算(上限仍是设计值 58/62): 窄屏上收的是按钮, 不是
#   标题字号 —— 标题在 360dp 上也保持 18sp。
#   顺带(玩家: 「我宁可右侧信息出现换行 你把那几个换行的文字精简下不就可以了」):
#   状态栏两条最长的文案精简掉冗余 —— 「命中 x%d · 结算中…」去尾省略号(128->112px),
#   「中奖!  +%d (x%d)」那个双空格是笔误, 收成单空格(119->116px)。
#   ---- ② 「遍历所有文案的文字, 看看还有哪个可能也需要精简」 ----
#   新增 `temp/scan_copy.py`: 遍历**所有会渲染到屏幕上的文案**(88 条), 用**客观判据**挑候选 ——
#   "哪条是靠自动缩字号才塞下的"(`_fit_installed` 的标签, 实际字号 < 基准字号)。结论:
#     · **400dp(玩家机器): 0 条**; **360dp(最窄档): 从 4 条降到 0 条**(那次精简之后)。
#     · 「余量<8px」那张表里的每一条都是"盒子按字量算出来的"(字号+4~6dp 内边距, 余量本来就小),
#       不是缺陷, 已在脚本里写明。
#   新增 `temp/scan_punct.py`: 只查**真的渲染过**的文案的标点/空格一致性(不查注释, 注释里的
#   半角标点是作者自己的书写习惯)。查出的**真硬伤只有两处**, 已改:
#     · 跑分菜单说明「全程约 25 秒(含完整的中奖装杯演出)。」—— 半角括号里是中文, 该用全角。
#       ⚠️ 直接换全角会 259->279px, 超过 360dp 的可用 261 ⇒ **必须连带缩短**。实测改成
#       「全程约 25 秒（含中奖装杯演出）。」= 234px: 一致性修好的同时, 最宽行的余量
#       从 **2px 变成 20px** —— 那条"差 2px 就折"的脆弱账清了。
#     · 彩蛋弹窗两句里的半角逗号夹在中文之间 ⇒ 换全角「，」(249->256 / 253->260, 仍放得下)。
#   (日期里的 `2026-09-11 08:59`、`(x100)`、`(369%)` 这些**是对的** —— 半角括号里是数字/拉丁,
#    不换算, 已排除在报告之外。)
#   门禁: --check / --selftest / fx_probe / --smoke 全绿; 布局探针 360/400/457/540/800dp 全绿;
#   弹窗高度门禁 360/400dp 全过; 回归检查全过; check_desc 报"全部一行放得下(360dp 起)"。
# 【v0.7.13 换盘面熄灭投中指示灯】玩家 2026-09-14 报:
#   「进入倍率槽的时候, 有一个红点和绿点代表有没有命中, 这个点应该在结算之后就消失,
#     实际并没有消失」。
#   ---- 病根: 不是灭灯的代码写错了, 是 v0.6.91 提速时**顺手把灭灯弄丢了** ----
#   `park_ball(reroll=True)`(球落定后重掷盘面)一直走整块 `_redraw()`, 而 `_redraw()` 重建
#   `_lamp_cols` 时用的是 `COL_LAMP_OFF` —— **顺手就把灯灭了**, 灭灯其实一直靠这层遮盖,
#   没人注意到。v0.6.91 为省 9 毫秒把它换成增量 `_update_slots()`, 而后者只改 9 个槽的
#   颜色与文字, **不碰灯** ⇒ 灯每局点亮一格、从不熄灭, 几局下来槽上攒出一片红绿点。
#   `else`(哑火)那条路上的 `lamps_off()` 只管哑火, 救不了正常拿钱这条路。
#   (PC 版 plinko.py 没这个毛病: 它的灭灯写在 if/else **外面**, 无条件执行。安卓版当初把
#    灭灯塞进 `else` 时就埋了雷, 被 `_redraw()` 盖了几个月。)
#   ---- 修法 ----
#   `_update_slots()` 里补 `self.lamps_off()` —— 它的定位本来就是"重掷时 `_redraw()` 那份
#   工作的增量版"(同族语义里已经补了 `_pulse`)。改这一处两条路都管住, **不碰** v0.6.91
#   那个省 9 毫秒的优化。`_lamp_cols` 的条数由 `_redraw` 建, 这里只按现有条数熄灭, 所以
#   不参与那个结构守卫(守卫不通过时会退回完整 `_redraw()`, 那边本来就灭灯)。
#   门禁: fx_probe 新增 **[21] 换盘面熄灭投中指示灯**(接线 + 行为 + 槽色照常更新 + `_pulse`)。
#   ⚠️ 写这条门禁时自己踩到一个洞, 已修并写进注释: 第一版用文本断言找 `lamps_off()`, 阴性
#      对照把它改成注释 `# self.lamps_off()` 时**照样报绿**(字符串还留在函数体里) ——
#      现在先剥掉整行注释再找。
#   验证: fx_probe [21] 六条全绿, 摘掉修复 → 红两条(接线 + 行为); --selftest OK;
#   --smoke SMOKE-OK; 截图对照(重掷前第0格红点/第4格绿点 → 重掷后全灭且槽色照常换新),
#   并做了**截图阴性对照**(摘掉修复后重掷, 红绿点原样赖着不走)。
# 【v0.7.14 慢帧归因: 补上分母 + 帧率曲线阶段条 + 复制逐帧日志】
#   三件事一个目的: **让"哪一段在慢"变成能读出来的数, 而不是靠猜**。
#
#   ---- ① 成绩页那行"1% Low 定位"原来读不出结论(分母错了) ----
#   玩家 2026-09-14 报「其他 AI 说这统计没有用」。查下来那行是
#   `最慢 56 帧平均 11.9 毫秒（装杯 37帧 · 飞行 9帧 · 待机 5帧）` —— 那是"最慢 1% 里各阶段的
#   **计数**", 分母是共用的 56。而跑分是**特意把装杯演出一起采样进去**的(玩家要求保留落袋
#   动画), 一个阶段**占的时间越多**, 它出现在最慢 1% 里的帧数就越多 —— 这是分母决定的,
#   不是性能决定的。**装杯 37 帧可能只是因为它时间占比最大。**
#   改成条件概率 `P(该帧进最慢 1% | 该阶段) = 该阶段进 1% 的帧数 / **该阶段总帧数**`,
#   而且**分子分母一起印**(`待机 4.2%（5/120）`) —— 分母小的阶段会出现 100% 这种噪声读数,
#   不印原始分数就没法一眼分出噪声和信号。顺序按**比例**降序, 第一个就是最该查的阶段。
#   ⚠️ 分母 `out["groups"]` 原来**已经算了但没印**(被随详细诊断一起精简掉了) ⇒ 本次改动
#      零新增采集、零跑分开销。
#
#   ---- ② 帧率曲线加"阶段条"(玩家 2026-09-14 的主意) ----
#   玩家原话:「每 1 帧 1 个点, 你再额外记录一个东西 —— **这 1 帧游戏在做什么**, 就很容易
#   看出问题了」。关键点: 这个数据**本来就在记** —— `_on_flip` 一直在往 `_bench_frames` 里存
#   `(帧间隔, 场景标签)`, 曲线只取了前半截。现在把标签接到曲线下方画成一条彩色带,
#   与曲线**同一根时间轴**: 曲线往下掉的尖峰, 正下方那段就是它当时在演什么。
#   ⚠️ 按**连续同标签合并成段**再画, 不是一帧一个矩形 —— 34.5 秒 5600 帧铺在 300~700px 上是
#      8~19 帧/像素, 逐帧画会画出一大堆亚像素矩形(又慢又糊); 合并后通常只剩几十段。
#   ⚠️ 标签与帧间隔长度对不上时**退化成不画**(曲线照画, 绝不崩) —— 老记录/异常兜底会走到。
#   ⚠️ 图例只列**这一轮真出现过的**阶段(没哑火就不列), 且底部留白按图例行数算 —— 360dp 上
#      六个阶段会折成两行, 写死留白会盖住时间轴。
#
#   ---- ③ 帧率曲线加"复制逐帧日志"按钮(玩家 2026-09-14 要的) ----
#   曲线只能看、带不走。复制出来的文本 = 头部(机器/版本/关键数 + **各阶段统计表**) +
#   每行一帧 `帧间隔毫秒,阶段`。头部刻意放最前面且自带各阶段慢帧率 —— **即使粘贴被截断,
#   结论仍在开头**。复制失败会把原因写在按钮上, 不静默失败(静默失败 = 让人白等一次出包)。
#
#   门禁: fx_probe 新增 **[23] 成绩页慢帧归因必须按各阶段总帧数归一化**、
#   **[24] 帧率曲线阶段条 + 逐帧日志复制**。两条都做了阴性对照:
#   [23] 把分母退回共用的 56 → 红 3 条(并复现出误导读数「装杯 66.1%（37/56）」);
#   [24] 掐断 `tags=tags` 接线 → 红 1 条。
#   验证: --selftest OK / --smoke OK / fx_probe 仍为已知 5 项(球贴图 mipmap, 9/13 未采纳的改动,
#   本版无新增) / 360dp 上成绩弹窗(654/730 放得下、一行不缺)与曲线弹窗(含六阶段图例折行)
#   逐张截图验证 / 复制路径真剪贴板往返逐字一致。
# 【v0.7.15 逐帧日志加"慢帧 × 文字重建"交叉表 + 发声/震动归因】
#   起因: 玩家真机跑 v0.7.14 拿到 fps1.txt(25102RKBEC / Android 16 / 120Hz),
#   数据分析把矛头指向**文字纹理重建**, 这一版把"哪一帧在还文字的账"逐帧记下来验证它。
#
#   ---- 上一轮数据读出来的东西 ----
#   中位数**所有阶段都一样**(8.29~8.32ms, 即满 120Hz) ⇒ **没有哪个阶段整体更慢**,
#   慢帧是**绑在事件上的一次性尖峰**, 不是渲染压力:
#     · 发射那一刻(蓄力→飞行) ~21ms, 5 次里 4 次   ← 最大且最可复现
#     · 装杯/待机中的某几帧      ~17ms, 7 次
#     · 飞行→落袋                11~13.6ms, 4 次
#   形态指纹: 最慢的 8 帧**每一帧后面都紧跟一帧 2~4ms 的超短帧**; 最短的 12 帧里 9 帧
#   紧跟尖峰之后 —— "一帧把活干完、后面几帧空转" ⇒ 一次性开销, 不是持续负载。
#   装杯当场洗清: 占 35.1% 的帧只贡献 12 个慢帧, 与占 60.1% 的飞行**完全一样多**
#   (慢帧率 1.01% vs 0.59%)。旧口径"装杯 37 帧"就是被分母骗的。
#
#   ---- 这一版加的埋点 ----
#   `_on_flip` 每帧记 `_TEXUPD[0]` 的增量, 存进**平行表** `self._bench_tex`
#   (⚠️ 不往 `_bench_frames` 的元组里加字段: 那个元组的下标被 `_bench_collect_diag`
#    到处按号取, v0.6.82 就因为把 [7]/[8] 写反让整块诊断静默返回空串 —— 平行表零风险)。
#   `_TEXUPD[0]` 数的是**真的 `Label.texture_update`**(由 Kivy 用 Clock **延后**执行,
#   跑在 `_frame` 外面), 所以它落在"那笔账被还上"的那一帧 —— 正是要比对的东西。
#   代码里早就写着这条: "主线程上还有一大块在 `_frame` 外面: Kivy 自己的渲染;
#   **Kivy 用 Clock 延后去做的 Label.texture_update**(重测+重光栅化+重建纹理+上传)"。
#
#   日志头部因此多三行:
#     · `慢帧当帧发生文字重建: N/M (X%)  ·  其余帧: ...`  ← **判据本身**, 一次跑就能定案
#     · `全程文字重建 N 次(其中 M 次落在慢帧上)` + `重建来源`
#     · `发声 N 次 / 震动 N 次 · 单次最慢 X 毫秒` ← 覆盖另一条假设("关音效 1%low 就回升",
#        而关音效会**连震动一起关**, 两条分不开)。这两组埋点一直在跑, 只是很多年没显示了。
#   日志正文改成 `帧间隔毫秒,阶段,当帧文字重建次数`。
#
#   ⚠️ 判据自洽修复(门禁抓到的自己的 bug): 慢帧集合改用 `>= 门槛` 之后, 分母还留着 `_n1` ——
#      门槛上并列时会印出 `20/1 (2000%)`。现在分母取慢帧集合的真实大小。真机毫秒值是浮点、
#      几乎不会并列, 两者相等, 但判据必须自洽。
#
#   门禁: fx_probe [24] 扩到 12 条(交叉表数值 / 正文三列 / 发声震动两行 + 原有接线与退化)。
#   验证: --selftest OK / --smoke SMOKE-OK / fx_probe 仍为已知 5 项(球贴图 mipmap, 本版无新增)。
# 【v0.7.16 v0.7.15 的收尾: 给"发射那一刻"补上计时 + 印出慢帧子步骤】
#   v0.7.15 推上去之后拿到玩家的**关声音对照**(video/no_shengyin.txt), 结论:
#   **声音被排除** —— 关掉声音 1%Low 73.2 -> 72.9(无改善), 发射尖峰一个不少
#   (18.61 / 22.53 / 19.15, 开声音是 21.01 / 20.79 / 20.71), 连"后面跟 2~6ms 追帧"
#   的形状都一样。玩家历史上唯一的因果线索("关音效 1%low 就回升")在这台机器上复现不出来。
#
#   ---- 补的两件事 ----
#   ① `_brk_wrap(RootWidget, "launch", "发射")`: 发射那一刻**原来完全没有计时** ——
#      `_brk_wrap` 只包了 板面/重掷/字号/装杯 四处, 而"蓄力→飞行"那一帧是**全轮最大的
#      尖峰**(~20ms, 两份日志各 4/4 次), 却是唯一没有任何子步骤能归因的。包上之后:
#      这一格很小而整帧很大 ⇒ 钱花在 `_frame` 外面(Kivy 延后的文字重排 / 渲染), 不是我们的代码。
#   ② 日志头印出**最慢三帧的 `_frame` 子步骤**: `帧953 21.0ms[发射1.3 / 板面0.2]`。
#
#   ⚠️ 顺手修掉一个自己踩到的脆弱点: 那段子步骤格式化**没有兜底**, `_bench_frames[i][9]`
#      形状一意外(比如是个扁平元组)就 TypeError, 而外层 `_copy_bench_log` 会把异常吞成空串
#      ⇒ 玩家只看到"没有可复制的数据", **整份日志白跑**。现在退化成 `[无]`, 日志照发。
#
#   门禁: fx_probe [24] 扩到 15 条(含"形状意外时退化成『无』且整份日志不消失")。
#   验证: --selftest OK / fx_probe 仍为已知 5 项(球贴图 mipmap, 本版无新增)。
# 【v0.7.17 逐帧日志改成"保存成 txt"(剪贴板在安卓上会截断)】
#   玩家 2026-09-15: 「你能把这个复制改为下载 txt 吗? 这样就不缺少东西了」。
#   实测证据: v0.7.16 的日志文件头写着 **3778 帧**, 而剪贴板实际只贴出 **425 帧**
#   (约 3.5 秒 / 31.4 秒) —— 安卓剪贴板走 Binder, 有大小上限。所以必须落盘。
#
#   ---- 落盘顺序(逐级降级, 每一级都把结果**说出来**) ----
#   ① 安卓 10+(API 29+) → **MediaStore 写公共 Download 目录**, 不需要任何权限
#      (buildozer 的 permissions 仍然只有 VIBRATE, 没加新的);
#   ② `getExternalFilesDir` → `/sdcard/Android/data/<pkg>/files/`;
#   ③ `user_data_dir`(最后手段);
#   ④ 全失败 → 退回剪贴板, 并**在提示里明说剪贴板可能截断**(否则玩家会以为成功了)。
#   ⚠️ **不能只写 `user_data_dir`**: 那是应用内部目录, 文件管理器看不见 = 存了等于没存。
#   ⚠️ 写流必须用 `java.lang.String.getBytes("UTF-8")` 拿**真正的 byte[]** —— 直接把 Python
#      bytes 交给 `OutputStream.write` 时 pyjnius 可能挑中 `write(int)` 重载, 于是只写进去
#      一个字节(静默截断成一个字符)。
#   ⚠️ 保存结果**单独一行常驻显示**(按钮上的字 2.5 秒就变回去, 而"存到哪了"是要照着去找的)。
#   桌面可验的部分: 存出来的 txt 与内存里那份**逐字一致**(fx_probe [24] 新增断言)。
#   安卓那条路桌面**测不了**, 已另开一轮对抗性审查专门打它; 其结论会进下一版。
#
#   门禁: fx_probe [24] 扩到 20 条(含"保存出来的 txt 逐字一致"/"走 MediaStore"/"getBytes"/
#   "降级到底复用剪贴板")。验证: --selftest OK / --smoke SMOKE-OK / fx_probe 仍为已知 5 项。
# 【v0.7.18 破案: 1% Low 的头号来源是"改 Button.disabled 逼 Kivy 重排文字"】
#
#   ---- 证据链 ----
#   玩家真机 v0.7.17 的**完整**日志(存盘那条路通了, 2577 帧一帧不少)显示:
#   每一发都产生**两次 15~16 个文字纹理重建的爆发**(间隔正好一个蓄力时长 0.1 秒),
#   每次卡 **12~21 毫秒**; 而"最慢三帧的子步骤"显示我们自己的代码只占 0.4~2.8 毫秒。
#   桌面点名: 那 15~16 个里 **12 个是按钮**(发射/重置/4 个投注/4 个返奖/每轮/音效),
#   另两个是两个标题标签 —— 正好是 `_set_controls_enabled` 碰的那些控件。
#   桌面二分(12 个按钮):
#       `disabled=True`                                  -> **12 次重建**
#       `disabled_color` 对齐成 `color` 后再 disabled=True -> **12 次**(Kivy 无条件重排, 没用)
#       `background_color`                               -> **0 次**
#
#   ---- 机制(Kivy uix/label.py 的 `_trigger_texture_update`) ----
#       elif name == 'disabled':
#           self._label.options['color'] = self.disabled_color if value else self.color
#   **Kivy 把文字颜色烘进纹理** ⇒ 改一次 `disabled` 就要重排一次文字纹理, 而 `Button` 就是
#   `Label` ⇒ 12 个按钮 = 12 次; Kivy 又用 Clock(`create_trigger(..., -1)`)把这些重排
#   **攒到同一帧**一起做 ⇒ 那一帧 12~21 毫秒(占该帧 95%+)。这就是玩家报的
#   "球在飞的时候一卡一卡的", 也是 1% Low 上不去的主因。
#
#   ---- 改成什么 ----
#   变灰照旧走 `background_color`(免费), 输入锁改由 `RootWidget.on_touch_down`
#   在**触摸层**统一吞掉。实测同一次发射的重建 **19 次 -> 7 次**(剩下的是状态栏/余额/统计
#   这些本来就该变的, 加两个标题标签的变灰)。
#   ⚠️ 守卫**只挡 `on_touch_down`, 不挡 `on_touch_up`** —— 松手走 Kivy 的 touch grab
#      (发射键按下时就 grab 了), `on_release -> launch()` 必须照常送达。而锁**只在
#      `launch()` 里上**(`start_charge` 不加锁), 那时手早松开了, 两者不冲突。
#   ⚠️ 变灰必须保留: 万一状态卡住, 玩家**看得见**按钮是暗的, 比"看起来正常却点不动"强。
#   ⚠️ 弹窗是 Window 的直接子控件、不挂在 RootWidget 下, 所以锁输入期间弹窗按钮照常可点。
#
#   ---- 同一版收掉的 4 个存盘路径问题(来自一轮对抗性审查, 48 个子代理) ----
#   M1 insert 成功后任一步抛会**在公共 Download 留下 0 字节/半截的同名 txt**, 玩家会抓到
#      坏文件 → except 里 `resolver.delete(uri)` 清掉(API 29+ 删自己插的行不要权限)。
#   M2 ④ 那一级才显示 `_err1` ⇒ 降级到 ② 时玩家**看不到 ① 为什么失败**, 而 ② 的目录在
#      Android 11+ 系统文件管理器根本进不去, 他会白找一轮 → 现在把原因一起写进提示。
#   M3 `_copy_bench_log()` 在**无参调用**时剪贴板失败也返回 True(假报成功) → `return False`
#      挪到 `if btn is not None` 外面。
#   M4 `tempfile.gettempdir()` 裸在 try 外, 一抛整个函数冲出、连 ④ 都到不了 → 包上。
#
#   门禁: fx_probe 新增 **[25] 输入锁: 触摸层吞掉, 不再动 Button.disabled**(5 条,
#   含"整个文件零 `.disabled =` 赋值"); 阴性对照: 把一处 `.disabled` 加回去 → 红 2 条。
#   ⚠️ 已知缺口: "真手指按下→松手→球发出去"这条端到端链路**没在桌面验成**(构造 Kivy touch
#   有三个坑没趟平), 依据是结构性的(见上)。真机首次跑请重点确认球能正常发出去。
#   验证: --selftest OK / --smoke SMOKE-OK / fx_probe 仍为已知 5 项。
# 【v0.7.19 v0.7.18 收尾: 保存提示行改成自动撑高(长文案会被裁)】
#   对抗性审查的 A2 提过"提示行高度写死 dp(30), ② 那条长文案会溢出", 当时标的是"未验证"。
#   随后 v0.7.18 我又把 ② 的文案**写得更长**(加了 ① 的失败原因), 于是必须一起改:
#   现在走 `_auto_h(_note, dp(30), dp(4))`, 按真实排版撑高。
#   桌面 360dp 实测: 提示标签 高 112 / 文字实际需要 108(放得下), 弹窗 500 未触上限 729.6;
#   截图确认那段 4 行长提示**完整显示、一个字没裁**。
#   ⚠️ 尾巴里正是"① 为什么没成" —— 裁掉它, 降级到 ② 的玩家就不知道该去插线用 adb。
#   验证: --selftest OK / --smoke SMOKE-OK / fx_probe 仍为已知 5 项。
# 【v0.7.20 日志加"逐帧 CPU 账": 把没说清的那批慢帧分流掉】
#   起因: v0.7.18 的真机日志里, 25 个慢帧只说得清一部分 ——
#     A 锁输入爆发(14~16 次文字重建) 6 帧 · 中位 17.3ms   <- v0.7.18 已修
#     B 零星重建(2~4 次)             5 帧 · 中位 12.6ms
#     C **完全没有重建**            14 帧 · 中位 11.5ms   <- 解释不了
#   C 组全在 11.1~12.3ms(中位帧 8.29 的 1.35 倍 = 多干约 3ms 的活), 间隔乱七八糟
#   (211/97/22/25/50/105/286/10/85/187/1/770/188)—— 像事件驱动, 不像节拍;
#   而"有没有文字重建"这条判据对它们完全失效。
#
#   这一版把**每一帧本来就在采、只是没印**的三样印出来:
#     `_bench_frames` 的第 6 个字段 = `_frame` 自算、第 8 个 = 主线程(线程级 CPU)、
#     第 10 个 = 当帧最大的两笔子步骤(板面/重掷/字号/装杯/发射)。
#   正文每行加三列: `帧间隔ms,阶段,文字重建,_frame自算ms,主线程ms,最大子步骤`
#   头部直接给分流结论(判据写死在代码里, 免得各读各的):
#       `# 慢帧 CPU 账(中位): 帧间隔 X / _frame 自算 Y / 主线程 Z 毫秒`
#       `#   我们的代码(自算>=1ms) N 帧 · _frame 外面(主线程明显大于自算) N 帧 · 在等(两个都小) N 帧`
#   三堆的读法: 自算大 = 我们自己的代码; 主线程大而自算小 = Kivy 渲染 / 延后的文字重排;
#   两个都小 = 在等(GC / 显卡 / 驱动)。
#   ⚠️ 桌面 `thread_time` 精度只有 15.6ms, 主线程那一列在桌面上基本是台阶 ——
#      **分流只能在真机上看**, 桌面只验证"这三列确实印出来了、格式对"。
#
#   门禁: fx_probe [24] 扩到 23 条(新增 CPU 账那两行 + 正文三列)。
#   验证: --selftest OK / --smoke SMOKE-OK / fx_probe 仍为已知 5 项。
# 【v0.7.21 让"没人认领的主线程 CPU"报名 + 分流判据改成按比例】
#
#   ---- v0.7.20 真机日志读出来的东西 ----
#   慢帧不是一个现象, 是**两个**(按"主线程占帧长的比例"分):
#     A 主线程没在跑(<30%帧长) 17 帧 · 帧间隔中位 11.09 · 主线程中位 **1.90** · 重建中位 0
#       ⇒ 帧在**等**(节拍/合成器/调度), 不是算出来的
#     B 主线程在算(>=30%)    15 帧 · 帧间隔中位 13.79 · 主线程中位 **7.94** · 重建中位 3
#       ⇒ 真在算, 而且**每发球稳定产生 4 个**(相邻间隔 252/89/21/423 帧, 周期性极强),
#         签名固定 = 字号 / 板面 / 重掷 / 发射 各一个
#   常态帧主线程只占帧长 21%(拿它当尺子)。
#
#   ---- B 堆的账对不上, 这是本版要修的 ----
#   主线程烧 13 毫秒, 而 `板面/重掷/字号/装杯/发射` 五个**已计时**的加起来只有 **2 毫秒** ——
#   剩下 **11 毫秒不在我们任何一处已计时的代码里**, 而这几帧**每帧正好 3 次文字重建**。
#   (帧1569 未认领 11.17 · 帧2318 未认领 11.45 · 帧3117 未认领 11.37)
#
#   ---- 改了两处 ----
#   ① `_texupd_wrap` 里给 `Label.texture_update` **本身也计时**(`_brk_add("文字", _t0)`)。
#      下次跑, 那 11 毫秒会在子步骤里自己报名字(出现「文字8.3」这种), 不用再推。
#      ⚠️ 只在跑分采样期记(`_TEXUPD_ACTIVE`), 平时没人读 `_FRAME_BRK`。
#   ② 头部的分流判据从"自算是否 >=1ms"改成**按主线程占帧长的比例**分三堆
#      (没在跑 <30% / 半跑半等 / 一直在算 >=70%), 并印出"常态帧占多少"当尺子。
#      ⚠️ 旧判据是**错的**: 它把 A 堆那 17 个"主线程只跑 1.9ms、帧却走 11ms"的帧也算进
#      "`_frame` 外面", 读起来像"钱花在 Kivy 上" —— 而那些帧主线程大部分时间根本没在跑。
#      两者要修的东西完全不同(A 是节拍/合成器, B 是算力)。
#
#   投影(用 v0.7.20 那份数据算): 只修 A -> 77.8→80.6 fps; A+B 都修 -> **96.0 fps**。
#
#   门禁: fx_probe [24] 扩到 26 条(两堆都覆盖的夹具 + "文字也进了子步骤计时")。
#   ⚠️ 自己踩的坑: 新夹具的循环变量起名 `_x`, 把上面"逐帧重建次数"那个 `_x` 覆盖了,
#      在很远的下面报了一个看起来毫不相干的 TypeError —— 已改名并写进注释。
#   验证: --selftest OK / --smoke SMOKE-OK / fx_probe 仍为已知 5 项。
# 【v0.7.22 把"卡的那一下是不是正在响/正在震"也印出来(玩家提的假设)】
#   玩家 2026-09-15 问: 「有没有可能是音效的锅? ... 记录下卡的那个时间有没有播放音效/震动?」
#
#   先说已有的证据: 玩家**自己跑过一版关掉声音的对照**(no_shengyin.txt), 1%Low
#   73.2 -> 72.9, 几乎没变。⚠️ 但那一版(v0.7.14)的日志里**没有发声计数那一行**,
#   所以"那次到底有没有真关掉"**没有证据** —— 这正是这一版要补的。
#
#   好在**埋点早就在跑**: `_on_flip` 每一帧都记了发声次数(`_bench_frames[3]`)与
#   震动次数(`[4]`), 而且发声那个计数在**节流闸门之后** —— 数的是"真的播了", 不是"想播";
#   记的时刻是**投递那一刻**(主线程), 所以能归到具体某一帧。
#   旧诊断里连"慢帧有几帧在发声/震动"都算好了(`slow_snd`/`slow_vib`), 只是那一行被精简掉了。
#
#   这一版把逐帧发声/震动加进正文, 并印出交叉表(与「慢帧 × 文字重建」同一形状, 便于横向比):
#     # 慢帧当帧在发声: N/M (X%)  ·  其余帧: N/M (X%)
#     # 慢帧当帧在震动: N/M (X%)  ·  其余帧: N/M (X%)
#   ⚠️ 判据**两边都要印**: 发声本来就密(约 4 次/秒 ⇒ 每 25 帧一次), 只印"慢帧里 40% 在发声"
#      读不出超配 —— 得看它比其余帧高多少。
#   正文 8 列: `帧间隔,阶段,文字重建,_frame自算,主线程,发声,震动,最大子步骤`
#
#   顺带回答玩家的另一半问题("音效已经在缓存里了?"): 是。音效是启动期**预烘**成 PCM、
#   常驻缓存(`Sfx`), 播放走 `SoundPool` 且**在工作线程**上, 主线程只做一次入队 ⇒
#   架构上它不该卡帧。但架构上讲得通不等于实测如此, 所以还是要量。
#
#   门禁: fx_probe [24] 扩到 28 条(发声/震动交叉表 + 正文两列)。
#   ⚠️ 自己踩的坑: 新断言插在了 `_log_cpu` 定义**之前**, 报了个 UnboundLocalError —— 已挪后。
#   验证: --selftest OK / --smoke SMOKE-OK / fx_probe 仍为已知 5 项。
# 【v0.7.23 发声/震动相关性加"滞后窗口"那一版(玩家提的机制需要它)】
#   玩家 2026-09-15 问「卡的那一下是不是正在响/正在震」。同一帧的视角**不够** ——
#   声音/震动的计数记在**投递那一刻**(主线程), 真正的 JNI 调用发生在其后; 而 JNI 会在
#   JVM 里分配对象, JVM 的 GC 是**停全世界**的(把主线程一起按住)。若卡是这么来的,
#   它会出现在发声的**下一两帧**, 只看当帧必然漏掉。
#   所以除了「慢帧当帧在发声/震动」, 再加一行「慢帧**前3帧内**有过发声/震动」。
#   自证分辨力: 夹具把发声放在慢帧的**前一帧**时 —— 「当帧」那行报 0/2、「前3帧内」报 2/2。
#   (顺带一提: A 堆那 17 帧"主线程只跑 1.9ms、帧却走 11ms"的形状, 正是"线程被按住了"的样子,
#    与"停全世界 GC"的假设吻合 —— 这正是这一版要验的。)
#   门禁: fx_probe [24] 扩到 29 条。
# 【v0.7.24 补五个数 + 修两处"日志在印假数"(玩家说"先补两个数")】
#   ---- 起因: 玩家把同一颗 SoC 的两台机器分别设成 60/120/165Hz 各跑了一轮 ----
#   三份日志摆在一起, 冒出一件谁都没预料到的事:
#     档位       中位ms   1%Low   慢帧均   =几个刷新周期   1%Low/中位   常态帧主线程占比
#     60Hz       16.93    42.2    23.69      1.40            71%          17%
#     120Hz       8.30    83.3    12.00      1.45            69%          19%
#     165Hz       6.08    86.9    11.51      1.89            53%          34%
#   ⇒ **常态帧跟着刷新率缩, 慢帧的绝对时长却几乎不动(11.5~12ms)**。也就是说慢帧不是
#     "周期不够", 而是一笔**与刷新率无关的固定开销**; 刷新率越高它占比越大。
#   165Hz 那台玩家用第三方工具看到的: **采样窗口前期 CPU 只跑 1.1GHz, 后期纯 CPU 的物理
#   跑分才升到 4.5GHz** —— 而"主线程ms"量的是 `time.thread_time()` = **真实 CPU 秒**,
#   主频差 4 倍 ⇒ 同一个函数量出来差 4 倍。这条一口气解释掉了上面三件怪事。
#
#   玩家给的优化目标很明确(165Hz 那张曲线): **全窗口只有 13 帧低于 90fps**, 把那几根尖刺
#   去掉就够了。实测这 13 帧: **13/13 主线程都在真算(没有一帧是"在等")、13/13 都伴随文字
#   重建、0/13 与震动有关**。把这 13 帧压到 11.1ms ⇒ 1%Low **86.9 → 101.6**(只压最差 4 帧 ⇒ 95.0)。
#
#   ---- 这一版做了什么(五格数 + 两处真 bug) ----
#   ① **节拍真值**: `屏幕X Hz · 请求高刷模式 · Kivy上限 maxfps · Clock._max_fps · vsync · multisamples`。
#      ⚠️ **跨日志比较的唯一前提**。实测教训: 同一份代码同一个版本, 屏幕 120Hz 那轮 1%Low=83.3,
#      60Hz 那轮只有 42.2。所以此前"75.1 → 78.2 → 83.3"那种跨版本对比**根本不知道是不是同一把尺子**。
#      (这几个字段 `_bench_collect_diag` 早就采好了, 只是 `_bench_frame_log` 一次都没引用 —— 采集 60 个
#       key, 只用了 40 个。)
#   ② **等屏幕(swap 阻塞)** 逐帧新列 + 头部聚合 + 「慢帧 vs 其余帧」交叉表。
#      ⚠️ 桌面实测定案: Kivy 的**绑定回调 `on_flip` 跑在真正 swap 之前**(事件序列恒为 `CB SWAP`),
#      所以日志里那个时间戳打在"画面还没交出去"的那一刻 —— 帧间隔里含着等屏幕的时间却分不出来;
#      而 `time.thread_time()` 也看不见它(阻塞在驱动 fence 上**不算 CPU 时间**)。
#      那批"主线程只烧 1 毫秒、帧却走 11 毫秒"的帧到底在等谁, 以前一个字都答不上。
#      ⚠️ 判据**两个方向都写进了日志**: 桌面 vsync 下大部分帧 swap 只要 0.3ms, 而排到队的那一帧
#      要等 15.9ms —— 那帧"等屏幕高"是因为它**本来就来晚了、撞上队列**, 不是被屏幕拖慢的。
#      所以必须配合「慢帧 CPU 账」一起读(CPU 也高=我们交晚了; CPU 低=真在等屏幕)。
#   ③ **CPU 频率(采样期)**: 中位/最低/最高/上限 + 低于上限一半的采样占比。
#      ⚠️ 采样**必须放工作线程** —— 读 sysfs 每次几十微秒, 而这一格恰恰是用来解释 `主线程ms` 的,
#      埋点自己抬高被解释的那个数就自相矛盾了。⚠️ 读不到(桌面/SELinux)时**整段不印**, 不印 0 冒充量到。
#   ④ **第二趟文字渲染(`CoreLabel._texture_fill`)** 接进子步骤计时(日志里是「填纹X.X」)。
#      ⚠️ 查源码才明白: Kivy 的文字**两趟画** —— `refresh()` 里 `render()` 只量宽高(第一趟),
#      真光栅化在 `_texture_fill`(由 Texture 回调在**纹理下次被用到时**触发, 跑在 `texture_update` 外面)。
#      `_texupd_wrap` 只包了第一趟 ⇒ 真正贵的那趟一直没人认领, 而那正是 165Hz 那 13 帧里
#      "主线程 17.1ms 而 `_frame` 自算 4.3ms"的那 11 毫秒的候选。
#      桌面实测确认挂上了(改一次文字: 文字 0.51ms / 填纹 0.61ms), 且 `__name__` 保住了。
#   ⑤ **另外几格采了没印的**: GC 次数/累计/最慢 + 实际生效的后端 · 进程态 utime/stime · JNI 主线程与
#      后台两笔 · 最差 20 帧距上次发射多少帧 · 配置写盘。真机 165Hz 有两段 0.8 秒掉到 136~152fps,
#      段内飞行帧主线程 CPU **占比与全局同(34%)**、**绝对量涨 20%** ⇒ 那段**不是被外面挡住,
#      是自己多干了 20% 的活**; 而"多干的是什么"当时一个数都没有。纯打印, 零新埋点。
#
#   ---- 顺手修的两处「日志在印假数」 ----
#   ⑥ **重建来源被静默截断**: `sorted(...)[:4]` 只留前 4 名 —— 真机 v0.7.23 印"全程文字重建 76 次"
#      而四项相加只有 68, 差 8 次(约 10%)且**日志里没有任何提示**。现在补「其余N类合计」+「[合计 N]」。
#   ⑦ **第 0 行是跨窗口残值**: 发声/震动/预热三个逐帧计数(`_FRAME_PROBE[0..2]`)在采样期之外照常累加,
#      而 `_on_flip` 只在 `if prev is not None` 里复位 ⇒ **第一行背的是"上次采样以来"的全部累计**。
#      真机铁证: `0.74,待机,4,0.03,0.60,6,0,...` —— 一帧只有 0.74 毫秒却背着 `发声6`(全窗口次大 3)、
#      `字号1010.0`(全 3069 行里唯一 ≥100ms 的, 次大 4.9ms), 物理上装不进去。
#
#   ⚠️ **没有改任何游戏行为**: 全是埋点与打印。唯一的"新采样"是 CPU 频率(工作线程)与
#      `_texture_fill`(只在采样期开计时, 沿用现成的 `_TEXUPD_ACTIVE` 闸门)。
#
#   门禁: fx_probe [24] 扩到 209 条(新增 25 条, 全部做过阴性对照 —— 拿 HEAD 版跑, 这 25 条全红)。
#      ⚠️ 自己踩的坑(比报红更危险的形态): 新加的 `_fn_body` 工具函数写在了引用它的判断**之后**,
#      于是 `UnboundLocalError` 让门禁**在中间炸掉**, 后面 20 条一条没跑, 而输出里只看到
#      "总检查数从 205 悄悄掉到 185"。已挪到 `main()` 顶部并写进注释。
#      另: 我第一版把 `_on_flip` 的归零判据写成 `split("def _on_flip")[1][:2000]`, 而注释太长
#      真正的归零那行落在 2000 字符之外 —— 门禁自己报了个假红, 已改成按函数体取。
#   验证: --selftest OK · fx_probe 209 OK / 5 项已知(球与玻璃杯贴图, **HEAD 版上同样红, 预先存在**)
#      · 桌面端到端真跑一次跑分, 整条链(11 元组 _on_flip → _swap_wrap 真 swap → 诊断 → 打印)无异常。
#   已知遗留(**不在本次范围**): 玻璃杯/球贴图那 5 项门禁失败 —— 贴图是 64×64 而门禁要 128×128、
#      且没开 mipmap。与本次改动无关, 但值得单独查一次(生成器重跑过? assets 里是旧图?)。
# 【v0.7.25 文字纹理缓存: 把 4~10.7 毫秒/次的重复重建变成 0】
#   起因是 v0.7.24 那份真机日志(165Hz Y700, 采样期 CPU 只跑 1.133GHz):
#   44 个慢帧里 16 帧的最大一笔是 `填纹`(`CoreLabel._texture_fill`, 第二趟真光栅化)
#   **4.1~10.7 毫秒**; 把这笔全拿掉, 1%Low 从 81.1 → 95.2。而"低于 90fps 的全压到 90"
#   这个理论上限是 94.2 ⇒ **只做文字这半就能吃满玩家定的目标**。
#
#   机制: Kivy 的文字**两趟画** —— `refresh()` 里 `render()` 只量宽高(第一趟),
#   真光栅化在 `_texture_fill()`(第二趟), 由 Texture 回调在"纹理下次被用到时"触发,
#   跑在 `Label.texture_update` 外面。工程里原来的埋点只包了 `texture_update`
#   ⇒ 一直在量第一趟(0.5ms), 真正贵的第二趟直到 v0.7.24 才被量到。
#
#   做法: 给打了 `_texupd_tag` 的标签加一层**按标签自己的文字纹理缓存**。
#   命中时直接把烘好的纹理交给标签 ⇒ **第一趟、第二趟、纹理上传三样全跳过**。
#
#   ⚠️⚠️ **第一版是错的, 靠逐像素比对抓住了 —— 这一段是本版最值钱的经验**:
#     一开始直接缓存 `Label` 自己那张 Texture。计数器证明命中 12 次, 但把命中纹理的
#     `pixels` 和"该句现烘一张"比, **4 句里 3 句内容不对**(尺寸全对 78/39/30/66,
#     所以只看尺寸永远发现不了)。病根: `CoreLabel._texture_fill(self, texture)`
#     **忽略传进来的 texture 参数**, 方法体只有 `self.render(real=True)` —— 它画进的是
#     **那一刻标签的 `self.texture`**, 而填充推迟到 Texture 第一次 bind 才发生,
#     这段延迟里谁先被 bind 内容就写进谁 ⇒ 缓存下来的对象张冠李戴。
#     **改成正解**: 每个指纹配一个**专用 `CoreLabel`**, 它的 text/options 从此永不改变
#     ⇒ 延迟填充只发生一次、填的必然是它自己的内容。这条范式工程里本来就有
#     (`slot_text_tex` 的槽位倍率贴图), 这次把它推广到动态文字。
#
#   第二个坑(同样只能靠"打印中间状态"抓): `_texex_bake` 里漏了 `cl.refresh()`
#     ⇒ `cl.texture` 恒为 None ⇒ 缓存永远存不进去。**画面完全正常**(退回 Kivy 老路渲染了),
#     只有打印"缓存里有几项: 0"才看得出来。光看画面一辈子发现不了。
#
#   顺带: 缓存**只对打了 tag 的标签生效**(普通按钮/弹窗不受影响)。为此给
#   `mute_btn` / `round_btn` / `_rtp_title_lbl` / `_bet_title_lbl` 补了 tag ——
#   后两个**文字永不变、只是每发球被 `_set_controls_enabled` 改 `.color`**,
#   而 Kivy 把颜色烘进纹理 ⇒ 那是**每发球 6 次重建**, 打标 + 预热两种颜色之后归零。
#
#   预热: 在已有的分帧预热链(`prebake_step`)里加一步, **一帧只走一个组合**
#   (挤一帧就是自己造一记长帧, 这个坑字号预热那里已经踩过)。预热表只列**固定文案**
#   (状态栏 10 句 / 音效按钮 2 句 / 两个标题标签 x 2 色); 带数字的列不进来。
#
#   桌面端到端实测(同一次跑分): **全程文字重建 76~80 次 → 26 次**, 其中
#   **状态栏 37 次 → 5 次**(剩下 5 次正是那两句带数字的), 缓存命中率 66%。
#   与计划里"约 2/3、剩下的主要是带数字的"预估吻合。
#
#   门禁: `fx_probe` 加到 226 条(新增 17 条, 全部做过阴性对照 —— 拿改动前的版本跑全红)。
#     配套探针 `temp/texcache_probe.py`(**逐像素比对这个判据就是它立的功**)与
#     `temp/texwarm_probe.py`(验收预热: 采样期切 12 次状态栏, miss 必须为 0)。
#   验证: --selftest OK · fx_probe 226 OK / 5 项已知(球与玻璃杯贴图, **预先存在**)
#     · 桌面端到端真跑一次跑分, 日志头部齐全、缓存命中率正常。
# 【v0.7.26 统计刷新挪出"落袋那一拍"】
#   v0.7.25 的文字纹理缓存上线后, 真机日志(165Hz Y700)显示: 全程文字重建 80 → 22 次,
#   1%Low 81.1 → 92.4。但**低于 90fps 的帧还剩 10 个**, 复核(3 透镜 + 对抗验证)把它们
#   逐个归因后发现: **7 个落在"落袋那一拍"上**, 而 `settle()` 里就有一句裸的
#   `self._refresh_stats()` —— 写文字 = 重排一次字形纹理(真机 4~7 毫秒), 而那一帧还同时
#   扛着 灯/槽闪/入袋音/余额/揭晓。
#
#   这一版把统计刷新**延后 0.20 秒**:
#     Clock.schedule_once(lambda _dt: self._refresh_stats(), 0.20)
#   * 为什么是 0.20: 这段时间 HUD 正被装杯压暗、玩家也还在看落袋, 数字晚 0.2 秒无感;
#     而 `_land_hold` 最小 0.3 秒 ⇒ **挪不到下一发去**。
#   * ⚠️ **不能改成 0**(下一帧)—— 下一帧仍在落袋窗口里, 白挪。
#   * ⚠️ **也不能挪进 `_reveal_win`** —— 那是揭晓合流的地方, 统计跟它没关系, 混进去会
#     重演"揭晓时序"那类事故(本模块历史上丢过中奖音、提前剧透过数字)。
#
#   ---- 同时**主动退掉**了一个改动, 记在这里免得以后有人再试 ----
#   复核建议的另一半是"把 `_set_controls_enabled` 里那 3 处 `.color` 赋值换成画布染色
#   (`_lbl_canvas_color` / `_set_lbl_tint`), 零重建"。**试过了, 退掉了**:
#   `temp/tint_visual_probe.py` 做了逐像素对照(老做法 `label.color = COL_GRAY+0.6`
#   vs 新做法 `label.color = COL_TEXT` + 画布乘 `COL_GRAY/COL_TEXT`) ——
#   **最大差 53/255**, 不是抗锯齿噪声。而收益只有 `stats_lbl` 约 2 次/球
#   (两个标题标签的文字与颜色都固定, **靠 v0.7.25 的缓存已经免费了**)。
#   收益小、画面差异没吃透 ⇒ 不值, 已回退。想再试的人先回答"为什么逐像素对不上"。
#
#   门禁: fx_probe 加到 229 条(新增 3 条, 做过阴性对照 —— 拿改动前跑全红)。
#   验证: --selftest OK · fx_probe 229 OK / 5 项已知(球与玻璃杯贴图, 预先存在)
#     · 桌面端到端真跑一次跑分, 日志头部齐全、正文 9 列。
# 【v0.7.27 两件便宜的: 画布染色 + 状态栏带数字那两句的枚举预热】
#   ---- 先记一笔:v0.7.26 那版**实测无效**, 别再走那条路 ----
#   v0.7.26 把 `_refresh_stats` 延后 0.20 秒(想着"搬出落袋那一拍")。真机实测:
#   1%Low 92.4 → 92.2, 低于 90fps 的帧 10 → 10。**配比标准化后差 0.02ms** —— 就是没效果。
#   为什么: 落袋慢帧确实 4→2(搬走了), 但装杯/待机各 +1, **净零**。
#   **根因: 慢帧和文字重建"同源"** —— 都由落袋/发射/结算触发 ⇒ **"延迟"躲不开**,
#   只是把重活从一个慢帧搬到另一个慢帧。而且那个 0.20 秒是**盲挪**(不看目标帧空不空),
#   实测搬到的待机帧自己也有 13~14ms。
#   ⚠️ 所以**别再往"延迟渲染"这个方向投**。要干净只能"消灭"。
#
#   ---- 这一版做的两件 ----
#   ① **`_set_controls_enabled` 里那 3 处 `.color` 换成画布染色**(零纹理重建)
#      病根: Kivy 把颜色**烘进字形纹理** ⇒ 写一次 `label.color` 就是一次 4~7 毫秒的重排,
#      而它每发球要改 3 个标签(`_rtp_title_lbl` / `_bet_title_lbl` / `stats_lbl`)、
#      **正好落在"回 ready"那一拍** = 每发球 6 次。
#      做法: 照工程里现成的 `_lbl_canvas_color` / `_fade_set` 那套(中奖大字淡出就是它),
#      给画布那条 `Color` 乘一个系数。
#      ⚠️ **系数必须逐通道算**(`_tint_from` = `target/baked`)—— 三个通道比例不同
#         (0.39/0.45/0.56), 拍一个"0.5"会偏色。
#      ⚠️ 拿不到画布 Color **必须退回写 `.color`** —— 绝不静默不染色(那会变成
#         "状态卡住时看不出按钮是暗的")。
#      ⚠️ `_TINT_BRIGHT`/`_TINT_DIM` 是模块级调用 `_tint_from`, **必须排在它之后**, 否则 NameError。
#
#      **数学与画面都验过了:**
#      · 数学: 直接烘 `CoreLabel` 读 `texture.pixels` 最亮像素 ——
#          color=GRAY@0.6 ⇒ rgba (90,106,140,153)  ← **RGB 仍是 GRAY, 只有 A 乘了 0.6**
#          color=TEXT@1.0 ⇒ rgba (232,238,252,255)
#        ⇒ **Kivy 不预乘 RGB** ⇒ "烘 TEXT + 乘 GRAY/TEXT + alpha 0.6" **精确等于**老做法。
#      · 画面: `temp/tint_visual_probe.py` **同一标签、同一位置**前后各截一张、比同一块区域,
#        25200 点**最大差 1/255**(量化噪声)。
#
#      ⚠️⚠️ **这个探针第一版是错的, 它报了假红, 而我据此退掉过这个正确的改动 —— 记在这里:**
#        第一版把两个标签放在**不同的 y**(120/200), 采样按 `(h-y)/h` 映射到截图像素。
#        只要截图尺寸 ≠ 窗口尺寸(HiDPI), 两个 y 的取整就差一像素 ⇒ 取到字形不同位置,
#        抗锯齿边缘错一像素就差 **53/255**(实测 32/1848 = 1.7% 的点超差, 正是"只有边缘对不上")。
#        ⇒ **验收"画面是否一致"时, 必须是"同一个东西、同一个位置、两次运行", 不能"并排两个"。**
#        另外它自己还踩了两个坑: `sys.argv` 被覆盖后才读模式(于是 `cmp` 永远进不去)、
#        Kivy 的 `screenshot` 会在扩展名前插计数(`tint_old0001.png`)导致比错文件。
#        这两个都是**静默跑错**, 已一并修好并写进注释。
#
#   ② **状态栏带数字那两句的枚举预热**
#      v0.7.26 的 `重建来源` 里 "状态栏 6 次" 全是这两句 —— 缓存吃不到(每次数字都变),
#      但它们的**取值域有限**: `命中 x%d · 结算中`(倍率 7 种)+ `中奖! +%d (x%d)`(投注档 x 倍率)。
#      ⚠️ **倍率与投注档从代码里派生**(`VALUE_SHAPE.values()` ∪ {2} / `PRESETS`),
#         **不许手抄** —— 抄的那份迟早和真值脱钩, 而脱钩的表现是"预热白做、不报错"(静默失败)。
#      ⚠️ 只烘**当前投注档**(7 条)而不是四档全铺(28 条): 一局里投注档通常不变,
#         全铺要多花 1 秒启动。别的档第一次用到时走懒缓存(一次 4~7 毫秒, 之后命中)。
#      ⚠️ 枚举不全**不会出错** —— 失败模式是"没赚到", 不是"画面坏了"。
#      ⚠️ **`_TEXTEX_PER_LABEL` 从 12 调到 64**: 状态栏要存 10 固定 + 35 带数字 = 45 项,
#         上限留在 12 会**自己把自己挤掉**(烘一个丢一个)而且不报错。
#      启动代价: 预热链 +14 步 x 0.05s ≈ **+0.7 秒**(全部 29 步 ≈ 1.5 秒)。
#
#   ---- 桌面端到端实测 ----
#     全程文字重建 27 → **18 次**; `重建来源` 里 **`状态栏` 整个消失**
#     (v0.7.26 是 状态栏 6), 剩下的是 **余额 6 · 统计 4 · 结算大字 4 · 结算阴影 4**
#     —— 全是不可枚举的(或瓶颈在板面的)。缓存命中率 67%。
#
#   门禁: fx_probe 237 条(新增 9 条, 全部做过阴性对照 —— 拿改动前的版本跑全红;
#     另**改掉一条过时判据**: 它钉的是"两个标题标签烘两种颜色", 而那正是这次改掉的东西,
#     不跟着改它会**阻止正确的改动**)。
#   验证: --selftest OK · fx_probe 237 OK / 5 项已知(球与玻璃杯贴图, 预先存在)
#     · 两个探针全绿(tint 同位置 0 差异 / 预热 miss=0)· 端到端日志头部齐全。
# 【v0.7.28 跑分改成"放录像" + 三处口径/埋点改动】
#
#   ---- ★ 主改动: 跑分 = 放录像(玩家提的) ----
#   病根: 每轮球的落格是随机的 ⇒ 中奖次数不同 ⇒ **装杯时长不同** ⇒ 内容配比每轮都不一样。
#   实测连续三轮的装杯占比是 **25.2% / 30.6% / 38.2%**, 而 1%Low 是 92.4 / 92.2 / 88.4 ——
#   差的那 3.9 **全来自内容配比**, 不是代码变差。**追了三轮噪声。**
#
#   四处一起钉(缺一不可, 每一处都是实测出来的):
#   ① **固定种子**: 开跑 `random.seed(BENCH_SEED)`, 并把 `getstate()` 存下来,
#      跑完 `setstate()` **原样还回去**。
#      ⚠️ 不还的话正常游戏的球路会被钉死、每局一模一样 —— 那比"跑分不可比"严重得多。
#   ② **固定盘面**: 每发的所有格子填成**同一个倍率**, 依次 5/10/20/50/100
#      (`BENCH_BOARD`; 相邻约 2 倍, 且 100 保证**最重那档每轮都被量到**)。
#      ⇒ "球落在哪一格"不再影响结果 ⇒ 装杯长度确定。
#      `park_ball` 在跑分期间**不重掷盘面**, 否则会把刚钉好的覆盖掉。
#   ③ **碰撞随机流**: `launch_ball(..., rng=按球号派生的 Random)`。
#      物理段所有随机(撞钉散射角 490 / 反弹高度 502 / 高斯扰动 506 / 天花板旋钮 /
#      弧面旋钮)都写成 `rng = getattr(b, "_rng", None) or random` —— 正常发射 `_rng` 是 None
#      ⇒ **走全局 `random`, 而全局流的抽取顺序受"球什么时候落袋"(真实时间)影响**,
#      所以光 seed 一次**不够**(实测: 连跑两轮帧数与装杯帧数都不一样)。
#      给它一条自己的流, 顺序就与时间无关。
#      ⚠️ 种子按**球号**派生, 不是每发同一个 —— 否则 5 发走同一条轨迹、落同一格, 只测了一种球路。
#   ④ **装杯内部的下落时序**: `_make_balls` 的种子按**倍率**派生。
#      ⚠️ 只对跑分生效(`auto_close` 就是跑分的标志) —— **正常游戏的杯子必须随机**, 那是观感。
#
#   **验证(两次独立进程, 干净环境)**:
#     第1次  飞行 2720 · 装杯 2394 · 落袋 102 · 待机 34   n=5334   落格=[8,8,1,3,3]
#     第2次  飞行 2710 · 装杯 2395 · 落袋 102 · 待机 37   n=5329   落格=[8,8,1,3,3]
#     **装杯差 1 帧、落袋一帧不差、总帧差 0.09%** —— 从 25%→31%→38% 的乱跳变成逐帧级一致。
#
#   ---- 另外三件 ----
#   ⑤ **日志头部加两行**(`★ 低于 90fps 的帧数` / `★ 内容配比`): 以后每一轮日志**自己就能
#      说明白能不能和上一轮比**, 不用事后分析。判据用"低于 90fps 的帧数"而不是 1%Low ——
#      后者被配比带得晃。
#   ⑥ **历史面板口径**: `平均/1%Low` → `平均/1%Low帧`、`步数` → `中位跑分`
#      (那一格是 `_phys_sorted[len//2]`, **中位数不是均值**), 底部加一行说明:
#      波动 = 100 x (最慢−最快) / 中位跑分 —— 分子是**极值**、分母是**中位数**, 两次口径混用,
#      而且只看两个样本点 ⇒ **跑分次数少时一个异常值就能把它带飞**。
#   ⑦ **字号预热扩到 HUD 基准**: 真机抓到 `字号`(`_fit1`)一次 **16.5 毫秒**, 把帧634 从
#      6.05ms 推到 22.55ms —— **单笔就造出一个低于 90fps 的帧**。而桌面同一件事只要 0.46ms,
#      **桌面测不出来**(我此前据此否掉过这条, 真机数据推翻了)。
#      原来只烘 `sp(36/48/26/30)`(大字/飘字), HUD 的 `sp(13/14/15/16/18/19)` 一个都没烘。
#      基准值**从标签自己的 `_fit_base` 读**, 不手抄 `sp(N)` —— `sp()` 在真机上是非整数,
#      手抄差 0.01 就是另一个 fontid, 预热全白做且**不报错**。
#      这一步的间隔从 0.05s 收到 **0.02s**(真机实测冷开一次约 1.4ms, 26ms 那个数不成立):
#      **启动预热 5.82s → 4.45s**。
#
#   ⚠️ **本次实踩并已修的两个静默坑(都写进注释了)**:
#     · 替换预热那一段时把 `Clock.schedule_once(self.prebake_step, 0.02)` **整个吃掉** ——
#       自链式预热走不下去、`_PREBAKE_DONE` 永不置真。表现是"启动预热 5.8 秒变 90 秒",
#       而**画面上什么都看不出来**, 靠探针才发现。
#     · 日志头部那两行第一版排在了 `tex`/`tags` 定义**之前**(而且插错了函数),
#       NameError 被外层 `try/except` **静默吞掉** ⇒ 日志里根本没有那两行。
#       同一个形状 v0.7.24 踩过一次。现在那段**不包在 try 里**, 且就地取数据。
#     · 另: `1%%Low` 的双百分号不能省 —— 写成 `1%Low` 会被当成格式符 `%L`,
#       报错信息指向 `%d` 的个数, 真正的原因在后面那个 `%`。
#
#   门禁: fx_probe 252 条(新增 15 条, 全部做过阴性对照 —— 拿改动前的版本跑 20 项红)。
#     配套探针 `temp/bench_replay_probe.py`(跑分可复现性: 两次独立进程比落格与各阶段帧数)。
#   验证: --selftest OK · fx_probe 252 OK / 5 项已知(球与玻璃杯贴图, 预先存在)
#     · 端到端日志头部齐全(★ 两行 + 节拍 + 等屏幕 + 缓存命中率)。
#
# 【v0.7.29 预热到底有没有烘到纹理 + 「字号」那一格拆开印】
#   起因: v0.7.28 的真机日志(165Hz Y700)把那 10 帧「低于 90fps」拆开看 —— 7 帧卡在「填纹」,
#     其中 **3 帧 `文字重建 = 0` 却各付 3.1~4.4 毫秒**; 另 2 帧卡在「字号」(15.5 / 50.7 毫秒)。
#
#   ① **预热其实一张纹理都没烘**(本次最大的发现)
#      机制(桌面探针 `temp/texfill_force_probe.py`, 直接数 `LabelBase._texture_fill` 被调几次):
#        `CoreLabel.refresh()` 之后填纹 **+0** —— 它只量宽高、挂了个回调; 真正的光栅化
#        要等**这张纹理第一次被 bind**。而预热链从头到尾没 bind 过 ⇒
#        **预热没有消灭填纹, 只是把它推迟到"这句话第一次真的出现在屏幕上"的那一帧。**
#      量化(`temp/texwarm_fill_probe.py`, 在 `_PREBAKE_DONE` 变真那一刻立刻数缓存):
#        阴性对照(不 bind): 缓存 39 项里 **32 项从未进过 `_texture_fill`**(状态栏 26/27)
#        修之后:            **0 项**
#      改法: `_texex_bake` 在 `refresh()` **之后**主动 `bind()` 一次。
#        ⚠️ 用 `bind()` 不读 `.pixels`: 两者都能触发, 但后者要把整张纹理拷进 Python 侧
#           (45KB/项 x 上百项); `bind()` 只是把它设为当前 GL 纹理 —— 而 Kivy 每次画 canvas
#           都会重设自己的 GL 状态, 不留副作用。触发**只做一次**(回调随即被摘掉)。
#      这同时解释了 v0.7.26 那次"延迟渲染"为什么实测是 0: **填纹跟着纹理走, 不跟着
#      `texture_update` 走** —— 把它从这一帧挪开, 它还是会在同一张纹理第一次上屏时冒出来,
#      只是换了个人付钱。
#
#   ② **「字号」那一格拆开印**(叫了几次 / 量了几回)
#      同一个名字下藏着两种**修法相反**的病: 「叫 1 次 / 量 12 回」= 一次 `_fit1` 走完
#      阶梯 + 二分, 每回都是冷字号 ⇒ 该收敛字号集合; 「叫 20 次 / 量 20 回」= 一帧里好几个
#      标签同时换字 ⇒ 该错峰合并。光有毫秒分不出来, 所以这两个数必须和毫秒一起印。
#      现在印的是 `帧723 57.6ms[字号50.7(1次/12测) / ...]`, 外加 ★ 那行多一列「带字号 N」。
#      ⚠️ 「带字号」用**分解计数**判, 不用子步骤名 —— 那两帧的 `_fit1` 跑在别的回调里,
#        最大子步骤那一栏未必写着「字号」。
#
#   ③ **上一条的测量更正**: v0.7.28 的 changelog 里写了「真机实测冷开一次约 1.4 毫秒」——
#      那是拿 16.5 ÷ 12 硬除出来的。现在有两帧独立数据(15.5 / 50.7), **那个除法没有意义**,
#      该说法作废。也正是因为算不出来, 才有了 ② 这个埋点。
#
#   门禁: fx_probe 263 条(新增 11 条)。阴性对照做了 4 组(A / A2 / B / C), 每组都确认对应
#     门禁**真的变红**, 且还原后与改动前逐字节一致。本次抓到两条**门禁自己的毛病**:
#     · 一条第一版是**恒真的假绿灯**: `"带字号" in main_src` —— main.py 里另有一句早就存在的
#       注释「缓存键**必须带字号**」, 把 ★ 那行整个删掉它照样绿(阴性对照时它纹丝不动)。
#       现在改成从 `_code_only(_bench_frame_log)` 里取, 只看代码行。
#     · 另一条写成 `_bk_c.split("_cl.texture.bind()")[1]` —— 阴性对照把 bind 摘掉时那个
#       split 只有一段, `[1]` 当场 IndexError ⇒ **门禁在中间炸掉、它后面 22 条一条都没跑**
#       (OK 总数 263 → 239), 而输出里只看到"少了一批 OK", 没有任何报错。
#       **门禁自己静默少跑, 比门禁报红危险得多。** 已改成先判长度。
#
#   画面: 最小夹具同位置前后对比 **0 差异**(`temp/texex_bind_visual2.py`)。
#     ⚠️ 真 App 的截图**量不出这件事**: 同一个实现连跑两次就差 1.72% 的像素(动画/状态在漂),
#       而 old-vs-new 只差 1.18% —— 那个尺子只会给出假答案。去掉 Kivy 自己的 FPS 叠加层
#       之后, 最小夹具的自比与 old-vs-new 都是 0。
#
#   验证: --selftest OK · fx_probe 263 OK / 5 项已知(球与玻璃杯贴图, 预先存在)
#     · 端到端日志头部同时印出 `★ ... 其中带文字重建 16 / 带板面 578 / 带字号 44` 与
#       `帧259 31.1ms[字号29.7(1次/1测)]` —— 分解真的落到纸面上了。
#
# 【v0.7.30 「字号」的根因: 二分产出的字号永远烘不到, 每次冷开字体表约 40 毫秒】
#   起因: v0.7.29 真机(TB323FU)「低于 90fps」11 帧里, 有 2 帧卡在 `字号` 上 ——
#     `帧31 54.6ms[字号48.6(2次/1测)]` / `帧716 49.1ms[字号42.8(1次/1测)]`。
#     **只量了 1 回就烧掉 42 毫秒** ⇒ 那笔钱不在"量了几回", 在"量的那个字号是冷的"。
#
#   证据链(两条, 都是桌面可复现的):
#     ① `temp/fit_warm_match_probe.py`(跑一轮真跑分后逐个比对预热表):
#          预热表里的 72 个字号**运行期全都用到了**(一个没白烘);
#          而运行期冷量到的 83 个字号里 **有 11 个不在表里**, 它们全部长这样 ——
#            7.2800 / 7.7350 / 7.9625 / 8.0194 / 8.0762 / 8.1900 / 8.6450 / 8.8725 / 8.9862 / 9.0431
#          **一眼就是二分产出的任意值。**
#     ② 直接计时: 冷字号一次 `text_px` = **30.0 毫秒**; 同一字号热了以后 **1.49 毫秒**;
#        同字号换文字 **0.02 毫秒**。
#        ⚠️ 这也**推翻了本工程另一处旧结论** —— `glyph_fifo_probe.py` 当年量到"全新字号
#        0.458ms", 据此否掉过"字号是瓶颈"。那个数是在**已经开过的字号**上量的。
#        桌面 30ms / 真机 42.8ms, 量级吻合。
#
#   改法: `_fit_font_size_slow` 的第二段(原来在 [0.42, 0.70] 之间**二分 6 次**)换成
#     **固定细分档** `FIT_FINE = (0.66, 0.62, 0.58, 0.54, 0.50, 0.46, 0.42)`。
#     二分在这个引擎里是**结构性错误**: 它保证产出"这辈子只用一次"的字号, 而每个新字号
#     都是一次冷字体表打开。换成固定档之后, 全 app 的字号集合塌缩成
#     "每个基准 x 13 个固定倍率", 预热表一次全盖上。
#     实测: **不在预热表的字号 11 → 1**(剩的那个是 `_fit_uniform` 用"最长那一行的 bold"
#     去度量造成的 bold 组合缺口, 每轮 1 次, 已记在案)。
#
#   ⚠️⚠️ **这一版有真实代价, 必须说清**: 预热步数 72 → 155, 而每步都是一次真正的
#     字体表打开(桌面实测 30 毫秒/次) ⇒ **启动预热 4.45 秒 → 7.27 秒**(桌面实测)。
#     换句话说: 这一版是**把 40 毫秒的账从游戏帧挪到启动期**, 账面总工时没省, 省的是
#     "它落在哪一帧"。要不要付这个启动代价, 是玩家的决定 —— 如果嫌启动长, 可以只对
#     小基准(<=20)烘 `FIT_FINE`, 大基准留懒热。
#
#   视觉验收(`temp/fit_ladder_probe.py`, 12 种文字 x 9 个基准 x 2 bold x 7 档可用宽 = 1512 组合):
#     · **旧的塞得下、新的塞不下 = 0**(这条是回归红线)
#     · 塞不下次数: 新 336 / 旧 336(连硬下限都放不下时新旧都只能给地板档, 本来就一样)
#     · 与旧值最大差 **1.86 px**, 出现在 `+5000` / 基准 48 —— **只在溢出时才走到这一段**,
#       而 1.86px 是最大基准上的上限(0.0387 x 48); 小基准上不足 1px。
#     · 「新值比旧值大」有 20 组, **不是错误**: 旧的二分返回的是 `_lo`(验过能放下, 但
#       不保证是**最大**的), 新的固定档挑的是"最大的、放得下的那一档"。
#
#   门禁: fx_probe 267 条(+4: 走两段固定阶梯 / 二分已删干净 / FIT_HARD_FLOOR 单真源 /
#     预热不漏 FIT_FINE)。阴性对照 D 组一次改回四处, **四条全部变红**; 还原后逐字节一致。
#
#   验证: --selftest OK · fx_probe 267 OK / 5 项已知(球与玻璃杯贴图, 预先存在)。
#
# 【v0.7.31 冷字号收尾 + 四个新埋点 + 测试历史面板三处排版修复】
#   v0.7.30 真机(TB323FU): 低90 = **8**(v0.7.29 是 11)。卡在「字号」的那两帧从
#   54.57 / 49.06 毫秒降到 22.13 / 27.18, **但没消失** —— 签名仍是 `(1次/1测)`, 说明还剩冷字号。
#
#   ① **同类行/同类钮的度量 bold 统一**(`_fit_uniform` / `_fit_buttons_uniform`)
#      原来用"**最长那一行自己**的 bold"去度量, 而 `_longest` 是按"谁最宽"选的 ⇒ 同一组
#      换一段文字就可能换人 ⇒ 同一个基准**今天按粗体量、明天按细体量**。而预热表是按
#      "每个标签自己的 bold"烘的 ⇒ 撞上另一个组合就是一次冷字体表打开。
#      改成"组里只要有一个粗体就按粗体量"。**零预热成本**。
#      ⚠️ 顺带修掉一个既有隐患: 原来若最宽那行是细体、组里另有粗体行, 会**按细体定字号**
#         ⇒ 粗体那行可能溢出。粗体更宽, 按它量是保守且正确的。
#      ⚠️ 桌面探针实测这条**不是**那个残留的唯一来源(还有一条 `_apply_row_budget` 在
#         `_apply_sizes` 里, 那时预热表还没算出来, 属于鸡生蛋问题) —— 所以没有把话说完。
#
#   ② **四个新埋点**: `尾` / `自动发` / `起蓄` / `槽面`
#      三份真机日志里都有一个**同形状的怪帧**(每份恰好 1 个, 都在"待机 -> 蓄力"那一拍):
#        v0.7.28 帧16 28.73 · v0.7.29 帧1184 48.74 · v0.7.30 帧1237 25.26
#      `_frame` 自算只有 0.03~0.25 毫秒、子步骤栏空着, 主线程却烧 15~47 毫秒 ——
#      钱花在**不在 `_frame` 里**的 Clock 回调上。补上:
#        · `尾` = 从 `_frame` 算完到真正交画面之间那一段(渲染 + 其余回调), 墙钟;
#          与 `主线程`(CPU 时间)一起读就能分流"卡在渲染/驱动" vs "有回调在烧 CPU"。
#        · `自动发` / `起蓄` / `槽面` = `_auto_launch_tick` / `start_charge` / `_update_slots`。
#      **上线的当天就在桌面逮到一个**: `帧445 32.2ms[自动发30.3 / 槽面30.2]` ——
#      `_update_slots` 一次 31 毫秒(桌面), 是它第一次跑时的冷字号/冷纹理。
#
#   ③ **冷字号榜**: 逐帧那行只印得出「字号21.7(1次/1测)」—— 知道"一次冷测量烧了 21.7 毫秒",
#      却**不知道是哪个字号、哪个标签**, 而修法完全取决于这个。现在日志头部直接印
#      `最慢的冷字号测量: 21.7ms fs=15.0000 bold=1 [统计] · ...`(四元组: 毫秒/字号/bold/标签),
#      没有就明说「一次都没有」。**这一项我已经猜错过两次**(先说"冷开只要 1.4ms"、又把桌面
#      探针测到的那个当成了真机的), 所以这次不猜了 —— 让下一份日志直接给答案。
#
#   ④ **测试历史面板三处排版修复**(玩家截图):
#      · 表头「平均/1%Low帧」折成了两行。实测那一串在 12sp 下**正好要 82.0 px**, 而列宽是
#        `dp(82)` = 82.0 px —— **零余量**(原来是「平均/1%Low」70px, 加"帧"之后吃光了)。
#        两处一起修: 列宽 82 -> 94(恢复原来那 12px 余量), 且三列表头改走 `_fit_line` ——
#        原来绑的是 `text_size = w.size`(**两维都绑**) ⇒ 宽度不够就折行, 而这一排只有 dp(22) 高,
#        第二行直接被顶出格子。真机上还有第二个放大器: `12sp` 跟**系统字体缩放**走、`dp()` 不跟
#        ⇒ fontScale > 1 时光靠加宽永远救不回来, 必须能缩。
#      · 底部口径里 `**中位数**` 的星号是**照搬 markdown 的残留**, Kivy 不认, 屏幕上原样显示。
#      · 同一段的高度写死 `dp(88)`, 而折行后真实要 ~6 行 ⇒ **最后一行被「关闭」按钮压掉一半**。
#        改成 `_auto_h`(高度跟真实排版走, 本工程所有多行正文的标准做法, 这里是漏掉的一个)。
#      · 文案精简(玩家: 「大家都知道什么是平均帧和1%low帧就不用你教学了」+「难点是中位跑分和
#        波动是什么」): **删掉平均/1%Low帧的解释**(常识), 只留这个面板特有的两条, 并手工折行 +
#        全角空格做悬挂缩进(自动折出来的续行没有缩进, 三条会糊成一片)。
#
#   门禁: fx_probe 285 条(+18)。阴性对照 E 组一次改回 8 处, **8 条全部变红**, 还原后逐字节一致。
#   ⚠️ 本次又踩到一次"门禁自己静默少跑": 新写的一条引用了 `_code_only`, 而它**定义在 1878 行**、
#      比引用点晚一千行 ⇒ `UnboundLocalError` 当场炸掉, **后面两百条一条没跑**(OK 从 283 掉到 79),
#      输出里只有一行 Traceback。已改成裸字符串并把这个坑写进注释。
#   ⚠️ 另有一次**探针假绿**: 历史面板的表头挂在 `Window` 上、**不在 `rootw` 下**, 从 rootw 走
#      一个都找不到 —— 而"找不到坏的那几个"会让判定**直接变绿**。已加"必须恰好找到 3 个"的断言。
#
#   画面: `temp/hist_head_probe.py` 截图 + 逐行量宽验收(表头一行; 口径四行全部 ≤ 408px 可用宽,
#     最长 332px; 不再有星号; 不再被「关闭」按钮压住)。
#   验证: --selftest OK · fx_probe 285 OK / 5 项已知(球与玻璃杯贴图, 预先存在)。
#
# 【v0.7.32 字号真凶: Kivy 的字体缓存上限是 64, 预热自己把自己挤掉了】
#   v0.7.31 真机(TB323FU): 低90 = **10**(v0.7.30 是 8, 配比可比) —— 装了冷字号榜之后,
#   第一次就抓到了 5 个"冷字号", 而且**全是 `57.2375` 这种带小数尾巴的**, 与 v0.7.30
#   "已把二分的任意值换成固定档"矛盾。顺着这条线查到底, 结论与前面的假设**完全不同**。
#
#   ① **真凶: Kivy 的 SDL2 字体缓存上限 = 64**(桌面实测: 开过 64 个不同字号后, 回头访问
#      最早那个会重新变冷, 30 毫秒)。而 v0.7.30 把预热加到 **155 项**(12 基准 + 4 大字基准
#      x 13 档) ⇒ **预热自己把自己挤掉**。
#      真机日志的铁证: `fs=57.2375 bold=1 [余额] 最近预热档 57.2375 差 0.000000000
#      预热时量过=是` —— **表里有、也真的开过, 运行期仍然是冷**。三个条件同时成立,
#      就把"四舍五入对不上"和"基准不是一个数"两种解释都排除了, 只剩**被挤掉**。
#      ⇒ 这解释了 8 -> 10 的倒退: **预热不是越多越好, 上限是硬的**。
#
#   ② 改法: 预热**只烘 6 档基础阶梯, 且只烘 HUD 那批** —— 48 项, 在上限以内还留 16 个空位。
#      · HUD(状态栏/余额/统计/按钮)每局用几十次 ⇒ 必须热;
#      · 大字/飘字(sp(36/48/26/30))一局只用几次、且第一档多半就放得下 ⇒ 首次冷一次
#        (真机约 20~30 毫秒, 落在第一局装杯那一帧), 之后 LRU 会留住。
#      桌面实测坐实: 余额/统计 不再出现在冷字号榜上(改之前它们各 30 毫秒)。
#      **副作用也是收益: 启动预热 7.27 秒 -> 3.70 秒**(比 v0.7.28 的 4.45 秒还短)。
#
#   ③ 冷字号榜升级成**能自证**的三件事: 字号印**原值**(`%r`, 不再四舍五入到 4 位)、
#      「离最近的**同 bold** 预热档差多少」、「预热时**真的量过**吗」。并把**读法**直接印进
#      日志(差=0 排除四舍五入; 量过=是 而仍冷 ⇒ 被挤掉)。这一项我已经猜错三次, 所以
#      这次的目标不是"修对", 是"让日志自己把话说死"。
#      ⚠️ 中途踩到一个自相矛盾: `_FONT_WARM_ALL`("最近预热档"的来源)是从**旧的**大字基准表
#         建的, 而预热链已经不烘它们了 ⇒ 印出 `fs=48.0 [状态栏] 最近预热档48.0 差0` 而
#         同一行又说 `预热时量过=否`。**比我什么都不知道还糟**。现在那份清单只收真的进了
#         预热链的。日志头部同时印预热项数 + 上限。
#
#   ④ **`_auto_launch_tick` 逐句埋点** —— 四版日志里那个"主线程烧 15~53 毫秒、子步骤栏
#      空着"的怪帧, 这一版**抓到并命名了**: `帧1201 55.92ms [自动发51.4]`。而它调的两个
#      子函数(`槽面`/`起蓄`)**都没进前二**(=各自 ≤0.2 毫秒) ⇒ 那 51 毫秒花在这个方法
#      **自己身上**。现在按 `发·盘面` / `发·起蓄` / `发·排程` / `发·整段` 分段计时,
#      下一份日志直接指到那一句。
#
#   ⑤ **测试历史口径**(玩家给的稿子): 每项一行 —— 「中位跑分：物理引擎每秒模拟步数的中位数」
#      /「波动：（最大 − 最小）÷ 中位跑分，百分比」。门禁**真去量宽度**(按最窄的 400px 窗口、
#      可用 288px 算), 不是数关键字。
#      ⚠️ 别看第二行没写 ×100 就以为和代码对不上: `main.py:9600` 是 `100.0 * (max-min)/fps`,
#         屏幕上印的是 `1.9%` —— 那个 `%` 就是 ×100 的结果, 只是写在单位里。
#
#   ⑥ **修了一个真 crash**: 新加的"预热项数"那行直接 `len(_FONT_WARM_SIZES)`, 而它的初值是
#      `None`(预热链还没跑) ⇒ TypeError —— 外层 `_copy_bench_log` 会把异常吞成**空串**,
#      玩家看到"没有可复制的数据"。是门禁的夹具(不跑预热直接出日志)当场逮住的。
#
#   门禁: fx_probe 296 条(+11)。阴性对照 F 组把预热改回"全都烘上" ⇒ 2 条变红, 还原逐字节一致。
#   ⚠️ 本轮门禁自己又踩两次: ① `_fn_body("prebake_step")` 因为它是所在类最后一个方法,
#      会一路吞到模块末尾(43801 字符), 把**运行期**那条 `FIT_SCALES + FIT_FINE` 也算进来,
#      报了一条**假红**; ② 判据字符串里用了直引号 ⇒ 语法错, 整份门禁 0 输出。
#   验证: --selftest OK · fx_probe 296 OK / 5 项已知(球与玻璃杯贴图, 预先存在)。
#
# 【v0.7.33 冷字号的两个真修法: 预热不许 round + 大字不能一个都不烘】
#   起因: v0.7.32 真机(TB323FU)低90 = 9, 但冷字号榜**一次就说清了三种不同的病**:
#     144.60000610351562 bold=1  49.2ms  最近预热档 57.2375  差 87.3625  预热时量过=否
#      45.1875              bold=1  37.4ms  最近预热档 45.1875  差 0        预热时量过=是
#      57.23749923706055    bold=1  20.5ms  最近预热档 57.2375  差 0.000000763 预热时量过=否
#      53.80324928283691    bold=1  16.6ms  最近预热档 53.8032  差 0.000049283 预热时量过=否
#      26.329249649047853   bold=1  14.0ms  最近预热档 27.4138  差 1.0846   预热时量过=否
#
#   ① **预热里 `round(_b * _k, 4)` 是错的**(修掉上表第 3、4 条, 各 20.5 / 16.6 毫秒)。
#      运行期算的是 `_fit_base * _k` 的**原值**, 而预热 round 到 4 位 ⇒ 两个值差 1e-7~5e-5;
#      而 Kivy 的字体缓存按**全精度**索引 ⇒ **两个 fontid**, 预热等于白烘。
#      差 0.000000763 就要多付 20 毫秒 —— **预热必须和运行期算同一个数**, 这是整套东西的前提。
#
#   ② **大字那批不能一个都不烘**(修掉上表第 1 条, 49.2 毫秒 —— 全表最贵的一笔)。
#      v0.7.32 我为了压到 48 项, 把 `sp(36/48/26/30)` 整批删成了空。结果 `sp(48)`
#      (真机上 = **144.6**)第一次用要冷开 49.2 毫秒 —— 而大字是在
#      `_frame -> tick_draw -> _pump_reveal` 里造的, 所以**记在「板面」头上**:
#      `帧1103 60.32ms[板面50.4]` 看起来像板面变慢, 其实是字号。
#      ⇒ 改成**只烘第一档**(k=1.0): 大字一局只用几次、第一档多半就放得下; 4 项, 总数 52。
#
#   ③ 剩下那两条(45.1875 差 0 且"量过=是" / 26.329 差 1.08)是**另外的病**, 这版没动:
#      前者是被 Kivy 的字体缓存挤掉(上限 64), 后者是某个没进表的基准。已记在案。
#
#   桌面实测(修完之后): **`冷字号测量: 一次都没有`** —— 第一次出现这一行;
#     预热表 52 项(< 上限 64), 启动预热 3.94 秒。
#
#   门禁: fx_probe 302 条(+6)。阴性对照 G 组把两处改回坏的样子 ⇒ 2 条变红, 还原逐字节一致。
#   验证: --selftest OK · fx_probe 302 OK / 5 项已知(球与玻璃杯贴图, 预先存在)。
#
# 【v0.7.34 成绩面板: 两个按钮到底部 + 只能靠关闭关 + 诊断块瘦身; 文字纹理加全局缓存】
#   ① **成绩面板的两个按钮挪到底部**(玩家定稿): 左「帧率曲线」右「关闭」, 同一行。
#      原来「帧率曲线」是一条通栏按钮夹在成绩块和诊断块**中间**, 而**根本没有关闭按钮**,
#      关闭全靠 `auto_dismiss`(点面板外面)—— 玩家点空白处就把成绩面板关掉了。
#      现在 `auto_dismiss=False`: **这个界面只能通过「关闭」关掉**。
#      ⚠️ 按钮那行**必须最后 add** —— 竖向 BoxLayout 按 add 先后自上而下排。第一版加在
#         成绩块后面(那是「帧率曲线」原来的位置), 结果两个按钮卡在面板**中间**, 截图为证。
#      ⚠️ 横向 BoxLayout 里**先 add 的在左边**(Kivy 按 `reversed(children)` 摆位)。
#
#   ② **底部诊断块瘦身**(玩家 2026-09-15 点检: 「这个信息是不是没有用了? 如果真没有用就
#      删掉, 如果有用就保留有用的」)。那是**我加的开发者埋点**, 而玩家 2026-09-11 早就定过
#      「成绩面板只放成绩」(版本/日期都因此挪去了菜单弹窗)。逐行判下来:
#        · `1% Low 定位：最慢 N 帧平均 X 毫秒` —— **删**(在解释 1%Low 是什么 = 教学,
#          而玩家明说过「大家都知道什么是平均帧和 1%low帧就不用你教学了」);
#        · `文字纹理：N 次（余额 8 · 统计 4 · 结算大字 4）` —— **删**(纯埋点, 玩家没用;
#          数据在保存的 txt 里一条不少);
#        · `最慢帧：… · 主线程 9.8 / 游戏逻辑 8.5 毫秒` —— **删掉"主线程/游戏逻辑"**
#          (开发者术语), 「最慢一帧多少毫秒、发生在哪个阶段」那一半留着 —— 玩家看得懂;
#        · `各阶段出慢帧比例` 与 `低于 60 FPS 的帧：N / M` —— **留**(前者回答"卡在哪一段",
#          后者就是判据本身)。
#
#   ③ **文字纹理加一层全局(跨标签)缓存** `_TEXEX_G`。起因: 结算大字/阴影是**每次中奖现新建的
#      Label**, 而 `_texex` 存在标签自己身上 ⇒ 每个新标签的缓存都是空的 ⇒ 每局都稳定产生
#      2 次重建(实机各 5~10 毫秒, 落在同一帧)。
#      ⚠️ **为什么全局共享在这里安全**(按标签存本身是有理由的, 必须说清): 当初按标签存是因为
#      "直接把 Kivy 给标签建的那张纹理交出去"会被**原地重画**; 而现在烘的是**专用 CoreLabel,
#      它的 text/options 从此一个字都不动** ⇒ 那张纹理再也不会被重画, 多给几个标签用没问题。
#      ⚠️ 诚实说明: 这条**消不掉跑分里那 4 帧** —— 跑分 5 发的倍率各不相同(5/10/20/50/100),
#      大字的文字本来就是 4 个不同的串。它的价值在**正常游戏**(同一笔赔付反复出现)。
#      要真正干掉那 4 帧得上**数字图集**(从预烘的字形图集里拼数字, 永不新建纹理)。
#
#   门禁: fx_probe 308 条(+6)。阴性对照 E 组 13 条变红, 还原逐字节一致。
#   ⚠️ 本轮门禁又踩两次: ① 查"某串在不在"时**没先去掉注释** —— 而解释这件事的注释里
#      也写着那三句 ⇒ 假红; ② "按钮在底部"的判据写成 `y > 0.5*视口`, 而 Kivy 的 y 是
#      **下原点** ⇒ 那是"在上半屏才报错", 按钮卡在面板中间它照样绿, 靠截图才发现。
#   验证: --selftest OK · fx_probe 308 OK / 5 项已知(球与玻璃杯贴图, 预先存在)。
#
# 【v0.7.35 给「装杯板面持久指令表」取基线: 把 _redraw 拆成 杯层/球层】
#   这是**纯埋点版**, 不改任何行为 —— 目的是让下一份真机日志回答"值不值得做持久指令表"。
#
#   ① **`WinPileFX._redraw` 拆成「杯层 / 球层」两段计时。**
#      该函数是 `canvas.clear()` + 整块重建, 其中**固定 56 条**(接地阴影 2 + 后层玻璃 2 +
#      压暗 2 + 杯口环补画 48 + 前层玻璃 2)**每帧重来一遍**; 球层是 `5 x N` 条。
#      要把固定层改成"建一次、之后只改 rgba/pos/size", 就能清掉低90 里那两帧
#      **只超线 0.15 毫秒**的帧(帧4926/4934, 板面 2.6/3.4 毫秒)。
#      但原来那句"固定层真机约 2.8ms"是**推算, 不是实测** —— 所以先量。
#      桌面基线(拆完立刻量到): **杯层 max 0.71 / 中位 0.32; 球层 max 1.64 / 中位 0.36**。
#      按主频差(1.133GHz vs 4.5GHz)折算真机约 4 倍 ⇒ 杯层 ~1.2ms —— 正好够清那两帧。
#      ⚠️ `装杯` 嵌套在 `板面` 里、`杯层/球层` 又嵌套在 `装杯` 里, 这几个数**不能相加**。
#
#   ② **每帧存的子步骤从「前二」放宽到「前四」。**
#      这条不是可有可无: 拆完第一版**一个都看不到** —— 因为嵌套关系决定了
#      `板面 > 装杯 > 杯层/球层`, 只存前二时它们**永远进不了榜**, 而榜单就是日志能印的东西。
#      (与"截断要明着说"是同一个教训: 采集端截断了, 打印端再全也没用。)
#
#   ③ **顺带记下两条这次探查摸到的、下一步要用的事实**:
#      · **本工程从没用过 `tex_coords`**(全文件零匹配)。取"纹理里的一块"用的范式是
#        `Texture.get_region(x, y, w, h)` 切子纹理 + 普通 `Rectangle`
#        (`_rim_back_strips()`, 中奖玻璃杯那条, 含"比例 -> 像素 y 要翻转"这个唯一易错点)。
#        做数字字形图集照抄它, 不要另发明。
#      · 屏幕上那个 `弹珠：273300` 是**两个相邻 Label 拼的**: `_bead_lbl` 永远只画
#        「弹珠：」三个字(硬编码, 没有 `_texupd_tag`、从不改文字), 数字是它右边的
#        `balance_lbl`。所以图集要覆盖的是 `balance_lbl`, `_bead_lbl` 不用管。
#
#   门禁: fx_probe 312 条(+4)。阴性对照 H 组改回坏的样子 ⇒ 4 条全部变红, 还原逐字节一致。
#   ⚠️ 本轮门禁自己又踩两次(都是老坑): ① `_mod_fn` 只认顶格的 `def`, 而 `_redraw` 是方法
#      ⇒ 切出空串, 三条假红; ② 阴性对照只删 `_brk_add(...)\n` 会留下行首 12 个空格、和下一行
#      缩进叠成 24 格 ⇒ IndentationError ⇒ **门禁根本没跑起来**(控制组无效, 这是第二次踩)。
#   验证: --selftest OK · fx_probe 312 OK / 5 项已知(球与玻璃杯贴图, 预先存在)。
#
# 【v0.7.36 判据统一到「卡顿帧」= 帧率 < 中位帧率的 50%】
#   玩家 2026-09-14 定稿的两档口径: **<50% 要尽量消除; 50%~70% 只作参考**。
#   (交接文档 `jiaojie.md` 早就把"门槛要相对本机中位帧、不写死绝对帧率"定死了 ——
#    原来硬编码 `1000/90`, 而实证过: 1%Low 贴到 89.7 时同一份数据反而报"变差"。)
#
#   ① **日志印两行**(原来是"低于「中位帧×70%」"一行):
#        `★ 低于「中位帧率 50%」(X fps) 的帧数: N · 其中带文字重建/带板面/带字号 …`
#        `★ 参考带「中位帧率 50%~70%」(X~Y fps): M 帧 · 中位帧 Z 毫秒 ← 这不是指标`
#      ⚠️ 参考带**单独一行并明说它不是指标** —— 混进硬指标会让跨版本比较失真。
#      ⚠️ 写法沿用既有约定:「中位帧×70%」= **中位帧率的 70%**, 阈值 = `p50 / 0.7`。
#
#   ② **成绩面板那三行**(玩家截图点检 + 自己定的文案):
#        `卡顿帧分布：落袋 2.8%（1/36）· 装杯 0.5%（4/879）· 飞行 0.1%（1/959）`
#        `最慢一帧：43 毫秒（装杯）`
#        `卡顿帧（帧率 < 中位帧率的 50%）：共 6 帧`
#      · 最后一行是玩家要的原话; 原来的「低于 60 FPS 的帧：971 / 1925」**删掉** ——
#        绝对门槛, 而且它的分子分母是全窗口, 与"卡顿帧"根本不是同一批帧。
#      · **分布那一行的口径也统一到卡顿帧**(原来是"最慢 1%"), 否则两行对不上,
#        读数的人会以为哪里算错了。
#      · 卡顿帧为 0 时**不许退回** 1% 那批 —— 那会印着"分布"却写着"共 0 帧"。
#      · 分隔符统一成**全角冒号**(上一版三行里两行全角一行半角, 截图上一眼看出来不齐)。
#
#   门禁: fx_probe 321 条(+9)。阴性对照 I 组把判据改回绝对 90fps ⇒ **11 条变红**,
#     还原逐字节一致。旧夹具也跟着更新了(它原来喂的是 `low1_groups`, 现在是 `jank_groups`)。
#   ⚠️ 本轮门禁自己又踩两次(都是老坑的变体): ① 断言串里用了直引号 ⇒ 语法错, 0 输出;
#     ② 查"某串在不在"时**没先去注释** —— 而我把"为什么删掉这一行"的注释也写在同一个函数里,
#        裸文本断言被自己的注释喂饱(这是本文件第三次踩: "带字号"、"1% Low 定位"、这次)。
#   验证: --selftest OK · fx_probe 321 OK / 5 项已知(球与玻璃杯贴图, 预先存在)。
#
# 【v0.7.37 门槛调到 55%/75% + 屏幕刷新率按阶段采样 + 历史面板两处观感】
#   ① **判据门槛 50%/70% -> 55%/75%**(玩家定稿)。卡顿帧 = 帧率 < 中位帧率 **55%**;
#      参考带 = 中位帧率 **55%~75%**。日志两行、成绩面板那一行、采集端 `_jank` 一起改。
#   ② **采样期屏幕刷新率按阶段采样**(回应玩家「**发射的时候必然会降低帧率上限**」)。
#      这件事必须量, 因为它会**同时污染两个地方**:
#        · 屏幕上真降到 60/120Hz 的话, 那一段的帧间隔会**自然变长**(60Hz=16.7ms) ⇒
#          那些帧会被判成"卡顿", 而其实**画面是满帧的**, 不是我们的锅;
#        · 新判据的分母是**中位帧间隔**, 窗口里混进一段低频会把它往大推 ⇒ 判据线跟着动。
#      原来只印**采集那一刻**的 Hz(一个点), 答不了"窗口中间变没变"。现在每 0.5 秒记一次
#      「阶段 + Hz」, 日志印 `⚠️ 屏幕刷新率(采样期)变过: 165.0Hz{飞行 40 装杯 18} · 60.0Hz{蓄力 6}`
#      这样的分解; **恒为同一个值就明说"没变过, 长帧与降频无关"**;
#      **采不到也明说**(桌面预览 / 无权限) —— 本工程反复踩过「没印」被读成「没发生」。
#      ⚠️ `_screen_hz()` 在安卓上走 JNI, **绝不能每帧调**; tick 在采样结束时**自己摘掉**。
#   ③ **历史跑分面板两处观感**(玩家定稿):
#      · 开头那三个字「口径：」**删掉** —— 那两行本身就是注释, 前面再挂个标签是废话;
#      · 说明文字从最闷的次级色 `COL_SUB` 换成**金色 `COL_BALL`**。选金色是因为它本来就是
#        本作「主数字」的颜色(见代码里 `弹珠金色主数字` 那条注释), 在深蓝底上最抓眼,
#        又和近白的成绩正文天然分开。
#
#   门禁: fx_probe 327 条(+2 净增; 另有 9 条跟着 55%/75% 与「口径：」一起更新)。
#     阴性对照 J 组摘掉刷新率采样 ⇒ 6 条变红, 还原逐字节一致。
#   ⚠️ 本轮门禁又踩三次老坑: 断言串用直引号(语法错, 0 输出); 查字符串没先去注释(假红);
#     阴性对照摘 `try` 体里唯一的语句时**不摘 try/except** ⇒ IndentationError ⇒
#     门禁根本没跑起来(控制组无效 —— 这个坑本文件第四次踩)。
#   验证: --selftest OK · fx_probe 327 OK / 5 项已知(球与玻璃杯贴图, 预先存在)。
#
# 【v0.7.38 冷字号榜加「基准 / 倍率」两栏 —— 把最后 5 条冷字号指名道姓】
#   来由: v0.7.35 真机日志(TB323FU) — 低90 = **9**(与 v0.7.32 同), 但**构成变了**:
#     · 最慢的三帧 44/60/23 毫秒 -> **21.8/21.1/20.4 毫秒**, 1%Low 77.9 -> **92.7**
#       ⇒ **v0.7.33 的冷字号修复确实起作用了**(那个 `60.32ms[板面50.4]` 的大头没了);
#     · 但**填纹帧从 4 涨到 6**, 把坑填回来了(配比一样: 45.3% vs 45.4%)。
#   而冷字号榜还剩 **5 条**, 且"离最近预热档的差"是 **0.18~3.37** —— 差得**不像四舍五入**,
#   说明它们压根不在表里。可只印一个 `fs` 分不出两种可能:
#     (a) 它是 `基准 x FIT_FINE` —— v0.7.32 为了压到 64 上限以内**故意不烘**的那 7 档;
#     (b) 它是"某个没进表的基准"。
#   修法完全不同((a) 是硬取舍要减字号总数, (b) 是补表)。
#
#   ① 现在记下每次冷测量用的**基准字号**, `_fit1` 里顺手写进 `_COLD_FS_BASE[0]`。
#   ② 日志那一行加两栏: `fs=37.7767 bold=1 [Label] **基准57.2375 倍率0.6600** 最近预热档… 差…`。
#      实测样例已验: `37.7767 / 57.2375 = 0.6600` —— **正是 FIT_FINE 的第一档**。
#      ⇒ (a) 成立: 那不是漏烘, 是"**要么超上限自己挤自己、要么多付一次冷开**"的硬取舍在还债。
#   ③ 日志里补一段「读法2」把这个结论写死, 免得下次再从头推一遍。
#
#   门禁: fx_probe 330 条(+3)。
#   验证: --selftest OK · fx_probe 330 OK / 5 项已知(球与玻璃杯贴图, 预先存在)。
#
# 【v0.8.62 「就绪检查」收尾: 让它会修 + 池数对拍实验档】
#
#   v0.8.61 的真机结果(联想 Y700 二代 / 8+ Gen 1 / Android 13):
#     冷启动「音效等待」4343 ms → **233 ms**; 热启动 2940~3555 ms → **747 ms**(总耗时 880 ms)。
#     同一会话对照把账结清: 老探针 4970 ms vs 闸门 793 ms, 而探针那一轮 **101 个样本全部就绪**
#     (一次都没等到解码) ⇒ 那 4.97 秒 100% 是 play() 自己烧的。**热启动剩下的 747ms 是纯解码。**

#   ① 检查/修复阶梯(A 段) —— 把"只会等"改成"会修"
#     三种失败形态: 甲 load() 返回 0(从没进池, 已有 `_retry_failed`) / 乙 没解码完(闸门等着) /
#     丙 解码完但建不出流(**以前完全没人管**)。闸门到点没开时按这个顺序修, 每层都有上限:
#       1) **点名**: Java 侧 `firstMissing()` ⇒ Python 反查名字(带池序号) ⇒ 印日志与面板
#          —— 在此之前只知道「100/101」, 不知道是谁。
#       2) **重 load 它**: 塞进现成的 `_retry_failed` 队列(后台反复重试)。
#       3) **转老探针兜底**(原样保留)。
#       4) **重建两个池 + 全部重新 load**(只在一个都没就绪时做, 且**每进程只一次**)。
#       5) **绝不重新合成资源** —— 甲乙丙没有一种是"资源坏了", 而冷路径合成**正是甲的原因**
#          (2.2 秒纯 Python 抢解码线程), 失败后再合成 = 再制造一次争抢。真正需要重生成的情况
#          (缓存 WAV 损坏/截断)早就有专管: 启动时逐个核对指纹+字节数 + 原子写盘。
#
#   ② 池数对拍实验档(B 段, **不碰出货路径**)
#     唯一剩下的杠杆: 解码 755ms ÷ 4 条解码线程(2 池 × 2)。临时池做 2 池 vs 4 池的**交替对拍**
#     (4 池 = 合成音组与语音组各自对半 = 8 条解码线程), 每池一个闸门取**最晚**那个, 全程不 play()。
#     判据: 4 池≈2 池 ⇒ 解码不随线程数缩放, 750ms 就是这个平台的地板, 这条杠杆到此为止。
#
#   ③ 收尾(C 段)
#     · `ARM_LADDER` 四档 → 三档(出货形态 / 老探针对照 / 池数对拍)。`only` 与 `48k` 两档退休
#       —— 48k **结论已得**(实测 22050 中位 56.3ms vs 48000 55.7ms、字节比 2.16x ⇒ 采样率不是
#       杠杆), 代码全保留、加回元组即可复活。
#     · 修 `off` 档那行**误报**: 它原来印「闸门不可用」, 而闸门好好的、只是本档不用它。
#     · 启动日志新增「闸门差件」与「后端没播成 N 次」(后者是运行期静默不响唯一的探照灯,
#       以前只在面板上; **0 也印** —— 那是"全程一声没漏"的正面证据)。
#     · 面板**不新增行**: 那块面板有成文硬预算(内容区 376px ÷ 每行 38px), 而 `_popup_fit_content`
#       在装不下时会按比例压标签高度裁掉尾巴 ⇒ "差哪一个"折进现有「就绪闸门」那一行。
#     · `sfx._arm_busy` 接进重放那一页的收尾判据: 实验档跑在 `_audio_ready = True` 之前,
#       而 veil 看的是更早的 `sfx.baked` ⇒ 以前玩家手快会点开一个没有实验结果的详情弹窗。
#
#   ④ 顺带查实的两个既有毛病(只说实话 + 留痕, 不修行为)
#     · `_rebuild` 的 docstring 在撒谎: 它写着"仍补不上的交给 `_retry_failed` 去重试", 而那一支
#       是裸 `except: pass` —— 全文件往 `Sfx._failed` 里 append 的**只有两处**。
#       ⇒ 插拔耳机后某个样本重载失败, **没有任何人有第二次机会, 也没有痕迹**。现在留一行日志。
#       (真要让它们进重试队列 = 后端→Sfx 的新接线, 单独立项。)
#     · 重放详情弹窗有两套封顶口径: `_refit` 用 `_vh * 0.92` 而 `_popup_fit_content` 已用 0.96,
#       且 `_refit` 在 0.06 秒后**覆盖**前者 ⇒ 统一到 0.96(实验结论正是往这块弹窗里塞)。
#
#   验证: py_compile OK · --selftest OK · --smoke OK · fx_probe 19 项 = 基线(逐条同名, 一条不新增)。
#   回退: `SFX_GATE = False`(闸门整段失效, 等价 v0.8.60); 修复阶梯与池数对拍各自整块 try/except。
#
# 【v0.8.61 「音效就绪」可行性实验包 —— 换成 Java 层的加载完成回调(闸门)】
#
#   要解决什么: 老机器(骁龙 8+ Gen 1 / Android 13)每次启动「音效等待」2.9~3.6 秒。
#   实测那 3 秒里 **96% 是"逐个试播 101 个样本"这件事本身**(29~33 ms/次), 不是解码慢
#   (同机热启动的解码等待≈0; 纯 CPU 的烘焙三台机器只差 1.57 倍)。
#
#   根因(AOSP 已核): Android 11~14 的 `StreamManager.cpp` 里 `kPlayOnCallingThread = true`
#   ⇒ `play()` **在调用线程上建并启动 AudioTrack**; 而 `Stream.cpp` 只对**同一个 soundID**
#   复用 ⇒ 101 个不同样本 = 101 次建流。Android 15+ 该常量改成 false(所以新机器 2.99ms
#   与老机器 33ms **不是同一把尺子**)。
#
#   做法: 用 `SoundPool.setOnLoadCompleteListener` 当就绪信号, 但它住在**纯 Java** 里
#   (`java/com/plinko/SoundGate.java`, 经 `android.add_src` 进包), Python 只做下行查询。
#   ⚠️ 当年否掉它的理由("PythonJavaClass 代理失灵/被 GC ⇒ 全库永久静音")否的是**那座桥**,
#      不是这个接口。逐条核过 AOSP/SDL:
#        · `setOnLoadCompleteListener` **自己**建 Handler: 优先**调用线程**的 Looper, 取不到
#          用 main Looper; 该 Handler **强引用**监听器(android13 分支没有 WeakReference)
#          ⇒ 监听器只要是 Java 对象就没有 GC 风险。
#        · p4a/SDL2 里 `SDL_main`(Python/Kivy)跑在**独立的 SDLThread**, 不是安卓 UI 线程
#          ⇒ 从 Python 线程 attach 拿到的是 main Looper, 而 UI 线程正常跑 `Looper.loop()`
#          ⇒ 回调送得到。
#      **唯一的实测确认点是回调线程名**(日志里那行 `就绪闸门 … 线程=`), 空 = 送不到。
#
#   ⚠️ **判据的这一处变化要记在账上**: 闸门证明的是"**已解码**", 不是"**建流播出成功**"。
#      它仍是**逐个**确认(不是抽查), 但确认的是解码那一环 —— 玩家当年那条硬约束的原话是
#      "每个样本必须被真正试播确认过至少一次"。**这一条需要所有者点头**, 而实验档 A(只跑
#      老探针)会把"闸门自记的时刻"一起印出来, 两者一致就说明这一处变化是安全的。
#
#   兜底(本包的核心安全设计): 放行判据 = 「闸门打开」**或**「老探针说好」, 谁先到听谁的。
#     · 闸门可用时才走闸门优先: 先等它 `SFX_GATE_FIRST_SEC=1.5` 秒, 到点不开 ⇒ 转老探针;
#     · 闸门不可用(类取不到 / 无 jnius / 桌面) ⇒ 与 v0.8.60 **逐字相同**;
#     · 6 秒硬超时、绝不软锁 —— 一个字没改。
#     ⇒ **最坏情况 == 今天**, 唯一代价是"闸门挂着但打不开"时多等 1.5 秒。
#
#   四档实验(点「重放冷启动」往上换一档, 循环; 档位写在**后端**上, 不是 RootWidget):
#     A `off`   只跑老探针 = v0.8.60 那条路(基线; 并且**同时**印出闸门自记的时刻)
#     B `only`  只等闸门, 老探针一次都不跑(闸门的纯读数, 没有探针抢 CPU)
#     C `first` 出货形态(闸门优先 + 1.5s 后老探针兜底)
#     D `first` + 48k 对拍
#   ⚠️ 「重放冷启动」现在会**把两个池整个换新**(`replay_reset`)再做冷烘焙 —— 不换的话
#      `_ok_sids` 已记满(探针空转)、闸门已闩上(量不出它多快), 而且"冷启动"名不副实。
#
#   48k 对拍(第 4 档, 外部模型建议里唯一还有救的那条): 不改 APK 资产 —— 拿缓存里 10 个
#   合成音在 Python 里当场重采样成 48k(线性插值)并与 22050 对拍 `play()` 单价, 交替跑 5 轮,
#   报**中位数与最小值**(单价有 143ms 那种离群点)并把**字节比 2.18x** 一起印出来
#   (否则"按字节定价"和"按采样率"分不开)。
#
#   ⚠️ 外部四份答案里被判死的两条(别再试):
#     · 「自己用 MediaCodec 解一遍」—— SoundPool 有**自己的**解码线程与内存池, `play()` 查的是
#       它自己的就绪标记; 在外面解一遍既不改它的状态、还得让它再解一遍 ⇒ 不可行。
#     · 「冷路径做完 probe、热启动复用 SoundPool」—— 我们每次启动都是**全新进程**
#       (`_await_ready` 每次 `_bake` 之后都跑, 而热启动日志里是满满 101 次点名) ⇒ 用不上。
#     · 「自己写混音器 / 单条长驻 AudioTrack」—— 换掉整条播放路径, 风险最大, 本包不做。
#
#   回退(一行): `SFX_GATE = False` ⇒ 闸门永不挂载, 全链路等价于 v0.8.60。
#
#   未验实的(真机上要看的): ① Java 类能不能进包(`android.add_src`); ② 回调从哪个线程来
#   (日志那行 `线程=`); ③ 实时是不是真如预期 —— 全在启动日志与重放详情弹窗里。
#
#   验证: py_compile OK · --selftest OK · --smoke OK · fx_probe 19 项 = 基线(逐条同名, 一条不新增)。
#
# 【v0.8.60 按对抗复核修 v0.8.59 的实验包 —— 含一条**致命**: 档位根本没传到后端】
#
#   ⚠️⚠️ **别用 0.8.59 做那四组对照**: 它把档位写在 RootWidget 上、读在 `_SoundPoolOut` 上
#      (两个对象!) ⇒ 四档全走 A(串行), 而日志会写着「档位 4/4」⇒
#      四组一模一样的数会被当成实验结论。
#
#   对抗复核(3 路: 并发正确性 / 实验可信度 / 回归面)抓到 10 条, 逐条修:
#    ①(致命) 档位写到**后端**对象上(`sfx.out._probe_mode`); 门禁也改成从后端侧断言。
#       ⚠️ 为什么原来那条 26/26 的门禁看不见它: 夹具是**直接往后端塞属性**的 ——
#          "复制品只会测它自己", 本项目踩过这个形状。
#    ②(致命) **线程起不来时不许声称"全部就绪"**: 先预登记槽位(None = 这条没起来),
#       合并层看到 None 就把这一段判成"从没验过" ⇒ 返回 False, 让 `_await_ready` 下一轮补。
#       改之前: 起不来一条 ⇒ 该池剩下全不创建, 合并只回放已有的 ⇒ **返回 True**
#       (一个样本没验就摘加载页 = 玩家报过的「初次安装必然没有声音」)。
#    ③ `play()` **抛异常**与**返回 0** 分开: 串行版对异常是"整轮放行", 并行原来折成 0
#       判"没好" ⇒ 老机上白等 6 秒。现在异常 ⇒ 记 `_err` ⇒ 合并层照串行约定放行。
#    ④ boost 提到**分派之前** ⇒ B 档 ((1,1), True) 才真的是"串行 + 钉核";
#       原来它会偷偷变成"两池各一条线程 + boost"(同时改两个变量, 归因就废了)。
#    ⑤ `join` 加 3 秒上限 —— 6 秒护栏的判据在**调探针之前**, worker 卡在原生 play() 里
#       就永远等不到; 后面"槽位还是 None ⇒ 不放行"恰好接住这条。
#    ⑥ **代次守卫**: 拔耳机重建会清空确认集, 而新池的 sampleId 又从 1 编号 ⇒
#       旧池结果回写会**假绿**(本项目头号失效形状)。
#    ⑦ 线程数钳到 ≤4(超过池的 maxStreams 就拿不到流 ⇒ 全员被判未就绪 ⇒ 假阴性)。
#    ⑧ 「重放冷启动」加 **in-flight 闸门**(两次重放叠着跑会让两轮 probe_all 互相覆盖诊断字段)。
#    ⑨ 优先级常量注释纠正(AOSP: `THREAD_PRIORITY_AUDIO = -16`, `URGENT_AUDIO = -19`)。
#    ⑩ **门禁自己抓到的**: 槽位字典必须**先注册再起线程** —— 否则主线程那句
#       `_res[_pi] = _slots` 会覆盖 chunk 已写好的结果(非确定性: k=1 常侥幸、k>1 基本必现)。
#
#   验证: `temp/_probe_parallel059.py` **12 组全过**, 含复核建议的两条盲区测试
#        (线程起不来 ⇒ False; `play()` 抛异常 ⇒ 两条路都 True), 另加 boost 分派顺序、
#        线程数上界、以及"档位写/读都在后端"的断言。
#        py_compile OK · --selftest OK · fx_probe 19 项 = 基线(逐条同名, 一条不新增)。
#   ⚠️ 复核还指出一条**要记在账上**的: 并行每轮多付约 20% 的 play(每个分块各一个断裂点
#      + 各付一次补试, 实测 117 → 141 次/局) ⇒ 看四组数时**把总 play 次数与墙钟并排看**,
#      别只比单价 —— 否则会把这项成本读没。
#
# 【v0.8.59 探针并行**实验包**(两条路并存, 默认走老的) —— 给"老机那 3.4 秒"做的对照组】
#
#   为什么做(实测把问题定死了):
#     · 二代(Android 13): 热启动 音效等待 3555ms, **点名单价 33.05 ms/次**(探针自身 3405ms / 真扫 103 次)
#     · 5 代(Android 16): 音效等待 518ms, **点名单价 2.99 ms/次** ⇒ 差 11 倍
#     · 而这个差**与 CPU 无关**: 纯 Python 合成那一段两台只差 1.57 倍(2118 vs 1345);
#       v0.8.58 的埋点还显示探针起手时三簇已经顶到 2995/2745/1804(满频 3187/2745/2016),
#       既没降频也没被丢小核 ⇒ 贵的是"等"(Android <=14 在调用线程启 AudioTrack +
#       AudioFlinger 全局锁 + HAL 唤醒), 而"等"正是**并行能重叠**的东西。
#     · pyjnius 在 JNI 调用期间 `with nogil`(源码逐字核过) ⇒ 多线程**真能同时**进音频栈。
#
#   做法(**两条路并存, 这是回退的保险**):
#     · `probe_all` 变成**分派层**; 老代码**逐字**搬进 `_probe_serial` ⇒ 默认档走的就是那条路,
#       **正常启动零改动** —— 实验档只在「重放冷启动」之后生效。
#     · 新增 `_probe_parallel(mode, boost)`: 每池派 mode[池] 条线程, 各扫**下标连续**的一段;
#       合并时按下标排序回放 ⇒ 三个对外读数(连续就绪前缀 / 真扫次数 / 卡在谁)与串行版**同义**。
#     · 新增 `_probe_chunk()`: 撞到第一个未就绪的**只停自己这一段**; 每项仍补试一次(B1);
#       已确认的不重扫(K5)。
#     · 新增 `_probe_boost_thread()`: 只把**当前线程**钉大核簇 + 提"迟延敏感"优先级(失败静默)。
#     · 新增 `_cpu_groups()`(按 max_freq 分组的 CPU 下标, 从快到慢); `_cpu_shape()` 改调它。
#
#   怎么用(四组对照, 一台机器一次安装): **每次点「重放冷启动」自动往 `PROBE_LADDER` 下一档换**,
#   并把档位写进启动日志(`探针实验档 n/4: 每池线程 ...`) ⇒ 连点四次拿到:
#     A 基线(1,1) / B (1,1)+钉核提优先 / C (1,2) / D (2,3)
#   读数全现成: 启动日志里的 `点名单价` + `音效等待` + `心跳` + `探针起手/收工 CPU`。
#
#   验证: `temp/_probe_parallel059.py` **26/26** 全过 —— ①全就绪四档都对;
#     ②**(1,1) 与串行版四项读数逐字等价**(返回值/确认集/卡在谁/真扫次数/连续前缀);
#     ③池 0 卡住时池 1 照常被扫(并行的收益所在); ④**真并行**: 假池每次 play sleep 20ms
#       ⇒ k=1 = 122ms / k=2 = 61ms 且池里 max_live=2; ⑤空池/单池/乱给线程数不炸;
#     ⑥boost 在 PC 上是静默 no-op; ⑦四档阶梯格式对、默认档 = 基线。
#     `_cpu_shape()` 改造后用假 /sys 复核: 输出与改造前**逐字一致**。
#     py_compile OK · --selftest OK · fx_probe 19 项 = 基线(逐条同名, 一条不新增)。
#   ⚠️ 另有一轮对抗复核(并发正确性 / 实验可信度 / 回归面)在跑 —— 若它指出要改的, 另开一版。
#
# 【v0.8.58 三条埋点: 「远侧环层」计时 + 主线程心跳 + 探针起手/收工读 CPU 频率】
#
#   背景: v0.8.57 的真机日志(二代 / Android 13 / SM8475)把模型坐实了 ——
#   `点名单价  33.05 ms/次（探针自身共 3405 ms ÷ 真扫 103 次）`, 探针自己占「音效等待 3555ms」的 **95.8%**。
#   但同一条时间轴暴露出一段**没人算过的时间**:
#      856.1  [prebake] 玻璃贴图 134.2 ms
#     3552.9  [prebake] 字体 45 bold 0.3 ms      ← 中间 2.65 秒, 一行都没有
#   而**旧的小米日志里有同样的洞**(446 -> 3166, 也是 2.7 秒) ⇒ 不是这台机器特有的。
#   那个位置唯一**没被计量**的调用, 就是紧跟其后的 `_glass_over()`(远侧环独立层)。  ← ❌ **这句后来被证伪**, 见下面「当天查清」。
#
#   三条埋点:
#   ① **`_glass_over()` 计时** -> 新日志多一行「远侧环层 X ms」。桌面实测 **12ms**。
#      平板上若是 ~2600ms, 洞就找到了; 若也是十几毫秒, 就说明是**主线程被别的东西卡住**了。
#   ② **主线程心跳**(0.5 秒一条, **只在启动窗口内记**: 加载页还挂着、或模块加载后 12 秒内)
#      -> 把"主线程被卡住"与"预热自己停了"分开(预热是主线程驱动的)。
#      ⚠️ 必须设窗口: `_boot_log` 只增不减, 游戏期一直记会把日志撑爆。
#   ③ **探针起手/收工各读一次各簇当前频率** -> 日志头部那个「当前」是**导出日志那一刻**读的
#      (机器闲着), 对判断"探针期间有没有被降频/丢到小核"没用; 改成探针两端各读一次, 两条成对。
#
#   ⚠️ **当天查清(还在同一个 v0.8.58 里): 那 2.7 秒不是空洞, 是两段没写日志的预热。**
#      玻璃贴图之后还排着「震动预热 / 守卫预热」和「文字纹理预热」—— 它们**一行日志都没有**,
#      于是时间轴上看着像"主线程被卡住 2.7 秒"。三条证据:
#        · 桌面**同样复现**(玻璃贴图 1385 -> 第一条字体 4097, 间隔 2691ms)
#        · 主线程**心跳全程 55~60 帧/秒** ⇒ 主线程根本没卡(这条埋点当场还了自己清白)
#        · 三台机器的"空洞"是 2.70 / 2.72 / 2.72 秒 —— 长度由**步数 x 0.05 秒**决定,
#          **与设备快慢无关**(自链式分帧预热的固有形状, 这正解释了它为什么那么一致)
#      ⇒ 给这两段补上日志(每项一行): 桌面最大间隔从 **2691ms 降到 102ms**, 时间轴不再有洞。
#      ⇒ 顺带两条事实: ① `_glass_over()` 桌面 21.3ms —— 不是元凶;
#        ② 文字纹理预热是 **52 项**、桌面每项 15~16ms ⇒ 整段约 3 秒, 但它在**加载页摘掉之后**
#          才跑(分帧预热的设计如此) ⇒ **不占玩家的等待**, 只影响"第一次中奖之前烘没烘完"。
#
#   为什么先查这个再谈并行: 探针跑在**另一条线程**上 —— 如果它真能把主线程卡住 2.6 秒,
#   那"多开线程并行探"只会更糟。因果必须先分清。
#
#   验证: `temp/_probe_hb058.py` 全过 —— 桌面: 远侧环层 12.0ms、心跳 55.5~60.0 帧每秒共 9 条、
#        PC 无 /sys 时两条 CPU 行**不出现**(不印空行)、启动窗口内心跳没撑爆日志。
#        py_compile OK · --selftest OK · fx_probe 19 项 = 基线(逐条同名, 一条不新增)。
#
# 【v0.8.57 修「语音池从来没被暂停过、也没被释放过」—— pause/resume/close 只管了合成音那个池】
#
#   来源: 「4 个池可不可以」那轮评审顺手挖出的**既有缺口**, 我逐行核实 + 写了门禁。
#
#   现象(玩家听得见): 按静音、或切后台时, **正在念的那句语音会继续念完** —— 别的音(合成音池)都停了,
#   只有 61 条语音那个池的流照常在跑。而语音恰恰是最长的那批(1~3 秒), 所以听得出。
#   退出时语音池也从来不释放(资源漏着)。
#
#   根因: `_SoundPoolOut.pause()` / `resume()` / `close()` **全程只碰 `self._sp`**,
#   而 `_sp2`(v0.8.40 加的语音池)一次都没被管过。
#
#   改法: 新增 `_each_pool(fn)` —— 遍历 (`_sp`, `_sp2`), **每个池各自 try**(一个池出事不拖累另一个),
#   `_sp2 is None`(关掉分池 / 第二个池构造失败)时跳过。三个方法改成调它。
#   ⚠️ **不动 `_sp`/`_sp2` 这两个字段的形状** —— 重构成列表是另一件事(一个一个来);
#      `close()` 里也**不把字段置 None**: `play_named`/`probe_all` 都在判 `is not None`, 置 None 会改行为。
#
#   验证: 新门禁 `temp/_probe_pool2_gaps.py` **11/11**, 且**先跑过阴性对照**(改前 3 项红:
#        池1 的 pause/resume/release 一个都没收到)。含「`_sp2=None` 时三个方法都不抛异常」那条。
#        py_compile OK · --selftest OK · fx_probe 19 项 = 基线(逐条同名, 一条不新增)。
#
# 【v0.8.56 恢复「保存加载日志」按钮 + 日志写清设备/SoC/CPU 形状 + 直接印「点名单价」】
#
#   玩家: 「你接下来准备怎么做新的log文件 这次要恢复那个保存记录按钮了 记得log中写明手机/设备型号」。
#
#   一、**恢复「保存加载日志」按钮**(v0.8.48 按玩家要求删过的那一个):
#      一字不差还原(git show 1ed10c9 取原文): 按钮 + 回调(结果写进按钮文字、3 秒后复原) + n_btn 2->3。
#      ⚠️ 高度公式**不用改** —— 那里的 n_btn 本来就是变量(v0.8.48 之前一直这么用)。
#      ⚠️ 屏幕顺序仍是「保存加载日志 / 重放冷启动 / 确定」(纵向 BoxLayout 的 add 顺序即上下顺序)。
#      ⚠️ 它不违反这个弹窗的铁律: 按钮**只写文件**, 不碰 Sfx / 音频栈 / 游戏状态。
#
#   二、**日志里补三样**(全是 v0.8.53~55 那周卡住我们的):
#      · **SoC 型号** —— Build.SOC_MANUFACTURER + SOC_MODEL, 老版本退回 Build.HARDWARE。
#        光写「8 核」看不出 **1+3+4** 与 **2+6** 的区别, 而那正是同一个 play() 单价差 26 倍的来源。
#      · **CPU 簇结构 + 各簇频率** —— 新 `_cpu_shape()`: 读 cpuN/cpufreq/cpuinfo_max_freq **按值分组**
#        (同频即同簇, 不需 root), 再抽每簇一个核读 scaling_cur_freq 看有没有降频/野兽模式生效。
#        日志头部因此变成: `CPU       8 核  1+3+4  1x3187M + 3x2745M + 4x2016M　当前 …`
#      · **点名单价** = Σ探针自身 ÷ Σ真扫 —— 也就是「这台机器一次 play() 有多贵」。
#        以前只能从逐轮日志手算; 现在一眼分档(新机约 1.5ms, 老机 26~43ms)。
#        数据来自新累计量 `_PROBE_COST`(**只写、不参与任何判据**, 与 `_PROBE_TRACE` 同类)。
#      · 设备/系统/型号本来就有(`设备  Xiaomi 25102RKBEC`), 没动。
#
#   验证: 新探针 `temp/_probe_log056.py` 全过 —— PC 降级路径(无 jnius / 无 /sys)不抛异常、
#        单价行按 Σ÷Σ 算对(660.5/128 = 5.16)、面板三按钮齐全且屏幕顺序正确、
#        弹窗高 414 ≥ 内容 370 + 44(chrome) 不裁尾巴。
#         `_cpu_shape()` 另用**假 /sys** 验过: 1+3+4 与 2+6 两种形状、各簇频率、读不到给空串(不编数)。
#        py_compile OK · --selftest OK · fx_probe 19 项 = 基线(逐条同名, 一条不新增)。
#
# 【v0.8.55 补两条「静默盲区」(评审交叉繁殖那轮挖出) —— 探针只停本池 + 「后端没播成」终于可见】
#
#   来源: 「音效等待下一步怎么办」那轮多专家评审, 第二轮(把对方的点子喂给另一位)挖出两条
#   **代码事实**, 我逐行核实过; 第一条还在桌面假后端上**实测复现**了。都不是推测。
#
#   一、`_SoundPoolOut.probe_all` 的 break 是**双层**的: 内层"撞到第一个未就绪就停"之后,
#      外层还有一句 `if _stuck: break` ⇒ **池 1(40 个合成音)没确认完之前, 池 2(61 条语音)
#      一轮都不被扫**。桌面假后端实测(改前): 池 1 全不就绪 + 池 2 全就绪 ⇒ 池 2 的 play 次数 = **0**。
#      ⇒ 外层那句删掉, 改成「**只停本池**」; 「连续就绪前缀」的语义用一个 `_seq_open` 保住
#        (前缀一旦在某个池断掉, 后面的池照扫、只是不再计入「连续就绪个数」—— 与串行时代的
#        语义逐字一致, 而探针的自动判定读的正是这个数)。
#      ⚠️ 放行判据一个字没变: 仍是"每个 id 都得有一次 play 返回非 0";
#         「每池同时最多一条流」这个前提也仍成立(B1 的补试逻辑靠它排除"拿不到流")。
#      收益: 池 2 的确认不再被池 1 拖着走 ⇒ 少几轮 ⇒ 少几段轮询等待(慢机上轮询间隔就是几十毫秒)。
#
#   二、`play_named` 把 `play()` 的返回值**丢了** ⇒ 「后端没播成」(没解码完 / 拿不到流)
#      **完全不可见**: `Sfx.n_missed` 那个写着"静默的正面计数"的字段, 只在"名字压根不在池子里"
#      时才加, 而且**全工程只写不读**(从来没上过屏); 而运行期所有发声都走 `_drain` 工作线程的
#      异步路径 ⇒ 装杯/撞钉那一声没响, 没人知道。
#      ⇒ **返回值保持 True 不动** —— 它牵着装杯震动(`WinPileFX._bounce` 的
#        `if sfx.play(...)` → `_vibrate_tick`), 改成"后端说了算"会让流一忙就**整段没震**
#        (`Sfx.play` 里那段注释早就写明这一点, 而且是有意为之)。
#      ⇒ 只在后端里记一笔 `_play_missed / _play_missed_last`, 由「游戏信息」面板**在非 0 时**
#        印出来: `后端没播成　N 次(最后一次 xxx)` —— 唯一预期值是 0, 常态印它只是噪音
#        (与「后端重建」同一规矩)。字库已验: 后端没播成/次/最后/一 全在库。
#
#   验证: 新门禁 `temp/_probe_two_blindspots.py` **13/13** 全过, 且**先跑过阴性对照**
#        (改前 4 项红: 池 2 的 play 次数 = 0、`_play_missed` 不存在) ⇒ 这个门禁有分辨力。
#        面板行另测: `_play_missed=3` 出现那一行且数字/名字都对, `=0` 时面板仍是 3 行。
#        py_compile OK · --selftest OK · fx_probe 19 项 = 基线(逐条同名, 不新增)。
#
# 【v0.8.54 删掉「热启动，总耗时」中间那个逗号 + 「详细统计」弹窗那一行不再折行】
#
#   一、玩家: 「删掉启动和总耗时中间的逗号」 —— 「热启动，总耗时 506ms(音效等待 482ms)」
#       -> **「热启动总耗时 506ms(音效等待 482ms)」**。那一行在窄机器上会折行, 省一个全角字宽。
#
#   二、玩家接着要求「(这样)肯定不换行了」。**实测: 光删逗号做不到**, 所以要一起改那个弹窗:
#       量法: 用工程自己注册的字体(NotoSansSC-Medium.otf)按 16sp 量各行宽度(比值与屏幕密度无关)。
#         · 截图里那一行**放得下的只有 245px**(它放得下「冷启动，总耗时 2586ms(音效等待」= 245px,
#           却塞不下后面那块「 167ms)」= 56px) ⇒ 可用宽度 A ∈ [245, 301) px
#         · 整行-旧(带逗号) 305px; **删逗号 289px**(省 16px); 放不下的是 56px
#         ⇒ 删逗号只能指望"那台机器右边刚好还剩 ≥16px", 大概率(≈3/4)照旧折行。
#         ⇒ 「肯定不折行」要求整行 ≤ 245px, **还差 44px**。补在两处(都只动这一个弹窗):
#             ① 弹窗宽 `0.86 -> 0.94`(≈ +9%, ≈ +24px)
#             ② 正文 `16sp -> 15sp`(≈ -6.7%, ≈ -19px; 顺带与「启动信息」面板统一 ——
#                项目里本来就有"这几个弹窗长得几乎一样, 不该用几种字号"这条规矩, 一直没改到它)
#           合计 ≈ 43~45px ⇒ 压到 245 以内(留一点余量)。
#       ⚠️ 折行**不会裁字**(那个 body 标签的高度是按真实排版长的, `_refit` 还会再校正一次弹窗高)
#          —— 所以这纯是观感问题, 不是故障。
#
#   三、**充电时那行不能叫「功耗」**(玩家报的: 「当我给手机充电的时候, 面板显示的是 功耗：18W」;
#      玩家当场裁决: 「耗电 vs 充电」):
#      ⚠️ 根因不只是文案 —— `_power_from` 里**写死了 abs()**(理由: AOSP 说"正 = 充入电池",
#         但厂商实现两极分化 ⇒ 只报大小、符号只作诊断) ⇒ 这个数**天然分不出充放电**。
#      ⇒ 判据换成 `_battery_snapshot()` 的 `plugged`(EXTRA_PLUGGED, 非 0 = 接着电源):
#         与厂商约定无关, 而且**高压测试那边判"测试期间在充电"用的就是它**(`power_plugged`)
#         —— 两处同口径, 不另写第二份判据(同一件事两份清单必然漂移, 这项目踩过多次)。
#      ⇒ 插着电印「充电：X W」、用电池印「耗电：X W」。数值格式与 `abs()` 判据**一个字没动**。
#      ⚠️ 已知边界(与高压测试同口径): 接着纯数据线 / 供电不足时仍叫「充电」。那种场景电流本来就小,
#         而"真正的充放方向"需要厂商符号知识(即高压测试里的 `inverted`), 不在这一行里猜。
#      字库: 「耗」「电」「充」三字已验在库(1602 字形的子集, 缺的只有「瓦」「熵」)。
#      实测: 新探针 `temp/_probe_power_label.py` **8/8 全过** —— 把两个读数打桩后跑的是
#            main.py 里出货的那份函数(PC 上 `_battery_snapshot()` 恒返回 None, 不注入就一行都测不到),
#            覆盖: 4.2V/4300mA = 18.06W 那一档(充电/耗电两种标签都对)、无线充 `plugged=4`、
#            µA 单位换算、读不到电压/读不到电流/整机无电池三条降级路径。
#   验证: py_compile OK · --selftest OK · fx_probe 19 项 = 基线(逐条同名, 一条不新增)。
#
# 【v0.8.53 修「音效已关 + 点重放冷启动 ⇒ 画面没有任何反馈」(v0.8.52 自己带进来的回归)】
#
#   玩家: 「0.8.51 可以正常重放冷启动(但是耗时那个数不对), 0.8.52 点击重放冷启动后,
#          画面没有任何反馈」。
#
#   根因: 上一版给"重放完成"那一支加了 `and self.sfx.baked` 条件(为的是别在烘焙跑完之前
#   就报耗时 —— 那个意图是对的)。但**条件不成立时会掉进下面 `elif not _veil._hold:` 那条
#   "自动摘页"** —— 那条是给**普通启动页**写的; 重放页原来靠"`_hold` 一置真就轮不到它"
#   躲过去, 现在条件先不成立, 于是**重放页在第一帧就被摘掉了**。
#   而 `audio_ready()` 在**音效关掉时恒为真**(`not self.enabled or ...`) ⇒ 「音效已关 + 点重放」
#   必然踩中。音效开着时 `audio_ready()` 要等探针, 所以那一半是好的 —— 这也正是上一版
#   只手测"音效开着"会漏掉它的原因。
#
#   ⇒ 改成**先按「这一页是不是重放结果页」分岔**: 重放页**绝不走自动摘页**, 出口只有玩家
#      点击(见 `_LoadVeil.on_touch_down`); "报结果"仍然等**这一次重放自己的**烘焙收工。
#   ⇒ 新增 `REPLAY_BAKE_MAX_SEC = 15.0` 作**防软锁**兜底: 重放页吞掉所有触摸, 万一烘焙线程
#      永远不回来, `_hold` 就永远不置真 ⇒ 玩家被锁在启动页(项目红线: 绝不软锁)。
#   ⚠️ 上一版那条"别在烘焙跑完前报耗时"的意图**没有被撤销**, 它现在长在正确的位置。
#
#   集成探针(新: `temp/_replay_probe.py` —— 驱动的是**出货的真链路**: 真 `_replay_cold_start`
#   → 真 `_frame` → 真 `_LoadVeil` → 真烘焙; 只用 Clock 替玩家按了那一下):
#     · 修前 `off`: 点击后 **0.25s 页就没了**(`_load_veil=None`) 而烘焙照样跑了 2849ms
#       ⇒ 玩家看到的就是"没有任何反馈"。
#     · 修前 `on` : 全过(耗时 882ms) ⇒ **阴性对照成立**, 确认触发条件就是"音效已关"。
#     · 修后 `off`: 全过(耗时 1259ms / 2868ms 两次) · `on`: 全过(786ms / 915ms)。
#     · 另外钉住"**普通启动页照旧会被摘掉**"—— 我动过的那条 `elif` 是它唯一的出口, 摘不掉
#       就是软锁。
#   ⚠️ 这条 bug `--selftest` 与 `fx_probe` **都测不到**: 前者不建界面, 后者的门禁全在数值与
#      文本上(`_frame` 的分支形状它一条都没覆盖)。所以这个探针是这条路上唯一的门禁。
#
#   ⚠️ 已知副作用(接受): 音效关掉时重放报出的"耗时"是**烘焙**那一半(`_bake_pcm` 真的在跑,
#      手机上仍是几秒的纯 Python 合成), 「音效等待」那半是 0 —— 两处读的是同一份真源,
#      不会打架。音效关着本来就不存在"音效启动开销"这回事。
#
#   验证: --selftest OK · fx_probe 19 项 = 基线(逐条同名, 一条不新增)。
#
# 【v0.8.52 面板三个微调 + 温度/功耗数字金色 + 修「重放冷启动」耗时读到旧值】
#
#   一、**玩家(他有强迫症)的三个视觉微调**:
#      · `536 ms` -> **`536ms`**(数字与单位不留空格; 只改耗时那两处 ——
#        「温度：33.2 度　功耗：0.98 W」里的空格是**分隔两个字段**的, 不动)
#      · 中文括号 `（）` -> **半角 `()`**。顺带: 原来那个「（音效等待 472 ms）」在真机上
#        **折行**了(折成"（音效等待 472" / "ms）"), 半角括号 + 去空格省下两三个字符宽。
#      · 页边距: `padding 16->12` / `spacing 12->8`。
#        ⚠️⚠️ **高度公式(`need`)同步改了** —— 那里的 `dp(32)` 是 `2×padding`、`dp(12)` 是
#           `spacing`。只改布局不改公式, 弹窗就会短一截把内容裁掉尾巴(v0.6.12 踩过)。
#        ⚠️ 只改**这一个**弹窗: 同样两个参数在跑分菜单 / 高压历史里也有, 那两处没动。
#
#   二、**温度 / 功耗的"数字"改成金色**(玩家: 「数字的颜色都改为金色」, 标签不动):
#        温度：[金]33.2[/金] 度　功耗：[金]0.98[/金] W
#      用 Kivy 的 markup 做(只给数字上色), 颜色取现成的 `COL_BALL`(#ffd451) —— 与中奖大字
#      同一个金, 不新造一个色值。`_mk_lbl` 加了个 `_markup` 参数(默认 False, 别的行不受影响)。
#
#   三、**修「重放冷启动」耗时读到旧值**(玩家报: 「第1次显示18毫秒, 第2次超过3000, 后续都是」):
#      根因: `audio_ready()` 在**音效关掉时恒为真**(`not self.enabled or ...`) ⇒ 重放页
#      **烘焙还没跑完就**进了"完成"那一支 ⇒ `_replay_cost_text()` 读到的是**上一次的
#      `bake_ms`**(18ms 是上一次热启动的) 配上刚被清零的 `ready_ms`。
#      ⇒ ① **治本**: 重放页完成判据加 `self.sfx.baked`(必须烘焙真的跑完才报耗时);
#         ② **保险**: `_replay_cold_start` 把 `bake_ms` 也清零(原来只清了 `ready_ms`)。
#
# 【v0.8.51 回退哨兵查尾(v0.8.47), 回到 v0.8.44 那个实测状态】
#
#   玩家把两个方案摆一起算过账:
#       现状(间隔 150 + 全扫 + K5)   103 次 play   ~656ms
#       查尾版(哨兵 + 间隔 30)       131 次 play   ~662ms
#   **打平**。而且查尾多一个"白扫"风险: 队尾先好会**提前触发全扫**, 万一中间还没好,
#   那一轮就白花 ~220ms(乱序时才会碰上, 但它是纯亏)。
#
#   ⚠️ 安全性上两者**功能等价** —— 放行判据都是"全扫通过", 哨兵只是**预检**、只会让
#      放行更晚, 不会更早。差别在**工程安全**: 少一个机制、出错面更小、后人只需理解
#      一条主路; 而且 **v0.8.44 的 593ms 是真机实测**, 查尾版只有我的估算。
#      ⇒ 玩家定: **回退**。
#
#   ⚠️ 删干净, **不留 `SENTINEL_TAIL = False` 这种死开关** —— 那正是本项目点名过的
#      死代码形状(`PITY_RATIO` 那次)。`_probe_round` 一起删(它只被哨兵的"每 4 轮"用)。
#
#   回退后: `SFX_READY_POLL` 回到 **0.15**。
#   ⚠️ **别单独把它改小**: 没有哨兵时每轮都要扫到第一个未就绪, 而那个位置随解码往前爬
#      ⇒ 每一轮都比上一轮扫得多, 轮数越多总开销越大。按实测估:
#          间隔 100ms(5 轮)  ~200 次 play   ← 几乎翻倍, 净亏 ~250ms
#          间隔 150ms(3 轮)   103 次(实测)
#          间隔 200ms(2~3 轮) ~103 次       ← 已经持平, 再大只是发现更晚
#      150 正好在拐点上。想再快只能去动"最后一轮那 101 次全扫"(≈220ms), 而那要么
#      去掉确认(依赖顺序性, 证据已被证伪)、要么换事件通知(有全库静音风险)。
#
#   **保留**: K5(已确认的不重扫)、两个 SoundPool 池、按速率自适应。
#
# 【v0.8.50 实时温度行避开字体子集里没有的字(摄氏度->度, 瓦->W)】
#
#   玩家报(附截图): `温度: 31.1 □□度　功耗: 1.04 □` —— **"摄" "氏" "瓦" 三个字是豆腐块**。
#   根因: 这个项目的字体是**子集化**的(`temp/NotoSansSC-sub*.otf`), 只含项目**已经用到**的字;
#   新加的中文不在里面就显示成 □, 而且**不报错、不留痕**。
#   ⇒ 玩家定的解法: **避开新字, 不动字库** ——
#        「摄氏度」->「**度**」(常用字, 子集里有) 、「瓦」->「**W**」(半角, 不受字库影响)。
#
#   ⚠️ 与 `no-emoji-in-code` 那条 memory 同源(NotoSansSC 不含 emoji 字形)。
#      以后界面上再加中文, 先用常用字/半角单位; 拿不准就查子集或先在真机看一眼。
#
# 【v0.8.49 「启动总耗时」简化为「总耗时」】
#
#   玩家 2026-09-18: 「简化为 热启动，总耗时xms（音效等待xms）」。
#   ⇒ `mode_row` 里 `启动总耗时` -> `总耗时`(「热启动」在前, 再说一次「启动」是重复的)。
#      最终: `热启动，总耗时 1203 ms（音效等待 593 ms）`
#   ⚠️ `fx_probe` 里那条「返回字段行」的断言跟着改了关键字(它查"启动总耗时"字样)。
#      `tools/fx_probe.py` 不在任何 git 仓库里, 改动无版本记录(老问题)。
#
#   ⚠️ 附: 哨兵查尾(v0.8.47)经玩家核算「和现状差不多(656 vs 662ms)」⇒ **不再优化**,
#      保留原样。两边的账: 现状 103 次 play / 哨兵版 131 次, 估算总时间打平。
#      真正的大头(最后一轮全扫 101 次 ≈ 220ms)没动 —— 砍它要么去掉确认(依赖顺序性,
#      而那个证据已被证伪)、要么换事件通知(有全库静音风险), 两条都不划算。
#
# 【v0.8.48 「游戏信息」面板重排(时间一行 / 加载进度一行) + 删「保存加载日志」按钮 + 改名】
#
#   玩家 2026-09-18 逐条定的:
#   一、**「启动信息」改名「游戏信息」**(只改界面上那个按钮的文案; 弹窗标题早就不是它了 —
#      玩家 2026-09-11 定稿改成「跳跳的弹珠机v0.x.x」; 源码注释里那些「启动信息」是给维护者
#      看的行话, 不动)。
#   二、**删掉「保存加载日志」按钮**(玩家: 「你可以只删客户端按钮」)。服务端的
#      `_startup_log_text()` / `_save_startup_log()` **保留** —— 不挂按钮就不会被调用,
#      留着是为了"想导出日志时把按钮加回来即可"(那套导出降级链是现成的)。
#      只删**这一个界面**的, 功率记录那边的「保存记录」不动。
#   三、**时间一行、加载进度一行**(玩家: 「其实就是把时间放一起，把加载进度放一起」):
#        PC:   `音频后端　winmm(8声道)`
#              `热启动，启动总耗时 1203 ms（音效等待 593 ms）`
#              `音效加载 41/41，语音加载 61/61`
#        安卓: 同样两行, 进度那行的名字是「音效就绪 / 语音就绪」。
#   四、**「启动总耗时」= 烘焙 + 等待**。玩家先问「启动耗时是否包含等待的耗时」——
#      **不包含**: `bake_ms` 在 `_await_ready()` **之前**就结算完了(见 `Sfx._bake`),
#      两者是先后两段。弄清之后玩家要求改成"总耗时", 于是两段相加;
#      后面的「（音效等待 …）」是它的**明细**(不重复, 它本来就是总数里的一段)。
#      括起来是为了让"它是总时间的一部分"这件事一眼可见。
#   五、**去掉「（上限 6000）」** —— 那是项目内部的红线值, 对玩家没有信息量。
#   六、**「音效等待」在 PC 上也从变量来**(原来在 PC 分支里写死了 0) —— 两边同一口径。
#
#   ⚠️ `fx_probe` 里那条「audio_detail() 返回字段行」的断言跟着更新了: 它的条件原来只是
#      `len >= 3`, 而并成两行、且音效关掉时"加载进度"那行不出现 ⇒ 只剩 2 行。**改成直接查
#      内容**(音频后端 / 启动总耗时 / 音效等待三个关键字都在) —— 比数数字更能抵制"整段变空"。
#      ⚠️ `tools/fx_probe.py` 不在任何 git 仓库里, 这次改动**没有版本记录**(老问题)。
#
# 【v0.8.47 哨兵查尾 + 轮询间隔压到 30ms(玩家批准的第 1 处, 安全那半)】
#
#   ⚠️ **先说清楚: 这一版不会有大幅收益**(算过账)。
#   真机(v0.8.44): 第1轮 1 次 play / 第2轮 25 次(前缀24) / 第3轮 77 次 ⇒ 共 103 次。
#   只加哨兵: 第1轮 1 次 / 第2轮 1 次(尾没好) / 第3轮 **101 次**(尾好了要全扫确认) ⇒ 也是 103 次。
#   **哨兵省下的"中间扫描", 被"确认集没增长 ⇒ 最后一轮多扫"抵掉了。**
#   ⇒ 它的价值只在**配小间隔**时才兑现 —— 每轮便宜了, 才敢把轮询压密、更早发现解码完成。
#     所以这一版是**两件事一起做**。
#
#   一、**哨兵查尾**: 每轮先只看**每个池的队尾**(1~2 次 play)。
#       ⚠️ 这是**预检, 不是判据** —— 尾部好了**仍然要全扫确认**。所以即使某次解码乱序
#          (尾部先好、中间还差一个), 最坏也只是**白扫一次**(多花百来毫秒), **绝不会放行
#          没就绪的样本**。护栏一个字没动 —— 这是玩家只批"安全那半"的原因。
#       ⚠️ **两个池各查各的尾**: 两个 SoundPool 实例是**并行解码**的, 池 2 的尾巴好了
#          **不代表**池 1 都好 —— 只查一个尾是错的。
#       ⚠️ **每 4 轮保留一次全扫** —— 否则「连续就绪个数」(判顺序性的唯一来源)在哨兵
#          轮次算不出来。
#
#   二、**轮询间隔 0.15 -> 0.03**: 有了哨兵, 每轮只要 1~2 次 play(原来要扫到第一个未就绪,
#       实测第 2 轮 25 次), 所以"轮数多 ⇒ 总开销爆炸"那个老问题不存在了 ——
#       评审当年回退 v0.6.44 正是因为这个。按速率自适应仍然在(它会在慢解码时自己调大)。
#
#   三、**顺手修一个认知错误**: v0.8.44 那三个点(0→24→101)曾被当成"顺序性成立"的证据,
#       但**那是错的** —— 「连续就绪个数」按定义就单调(好了的不会变回没好), 它涨不代表
#       "后面都没好"。所以这一版**同时记「总共好了几个」**: 两者相等才是顺序, 总共多则
#       说明有"跳着好的"。判顺序性的正确判据从这一版才真正存在。
#
# 【v0.8.46 「启动信息」三行并一行(玩家 2026-09-18)】
#
#   玩家(附 PC 截图): 「把这3行, 压缩为1行, 用逗号分割。比如 音效等待：xxms,
#   音效加载xx/xx, 语音加载xx/xx」。
#
#   · **PC**: 原来三行(音效等待 / 音效加载 / 语音加载)并成一行:
#        `音效等待：0 ms，音效加载 41/41，语音加载 61/61`
#   · **安卓**: 同样并成一行(与 PC 同一口径) —— 「音效就绪 / 语音就绪 / 音效等待」三者合一。
#     ⚠️ `ready` 里那些「后端缺 N / 满编 N」的偏离提示**一个都没丢**(它们只在异常时出现,
#        正是这块面板存在的理由); 「音效等待」的三种分支文案(有探针 / 无探针 / 未等待)
#        也原样保留 —— 只是算的位置提到了 rows 构建之前才并得进来。
#
#   ⚠️ **这里以前写着"绝不并成一行"** —— 那是 v0.6.12 的教训(合并后 658px > 内容区 422px,
#      折行会被**定高标签裁掉尾巴**, 玩家看到的是一句缺尾巴的话)。但那个前提**已经变了**:
#      `_mk_lbl` 后来改成自适应撑高(绑 width→text_size 与 texture_size→height),
#      **折行只会让弹窗长高, 不会再裁**。所以合并现在是安全的, 而且省行数 ——
#      那块面板的行数是有硬预算的(实测内容区 376px / 每行 38px)。
#
#   ⚠️ `fx_probe` 里那条「语音就绪紧跟音效就绪」的断言**跟着改了口径**(从"下一行"改成
#      "同一行内且在其后") —— 玩家 2026-09-11 的原意(「放在音效就绪的后面」)仍然成立,
#      只是从跨行变成同行, **不是把门禁放松**。改完 fx_probe 回到 19 条基线。
#      ⚠️ `tools/fx_probe.py` 不在任何 git 仓库里 —— 这次改动**没有版本记录**(老问题)。
#
# 【v0.8.45 「启动信息」新增实时「温度/功耗」行(每 0.5 秒刷新)】
#
#   玩家 2026-09-18: 「启动信息界面调整, 新增一行, 内容是, 温度：xx摄氏度, 功耗xx.xx瓦。
#   并且每0.5秒刷新一次」+「如果是PC读不到信息, 就不显示这行」。
#
#   · **全部复用现成的读取器**(`_battery_snapshot` / `_battery_current_raw` / `_power_from`),
#     不新写一份 —— 高压测试那块已经把这套调好了, 两处各写一份必然漂移。
#   · 单位判断沿用 `_pwr_guess_unit()` 的**同一个阈值**(原始绝对值 < 20000 只可能是 mA):
#     那个函数靠"全程分布"判, 这里只有一次快照, 就用同一把尺子判一次(两处一个数)。
#   · **读不到就整行不出现** —— 与 `audio_detail()` 里"不适用的行直接不出现"同一条规矩
#     (印一行"不适用"既是噪音、又把真内容淹掉)。PC 上 `_battery_snapshot()` 恒返回 None
#     ⇒ 自然满足; **只读得到一半时只印那一半**, 不印「功耗：--」那种占位。
#   · 高度预算把它计进 `n_lbl` —— 那块面板栽在"被裁掉尾巴"上两次了(v0.6.12 / 2026-09-11)。
#   · ⚠️ 弹窗一关**必须 `Clock.unschedule`** —— 否则定时器会一直跑下去(每开一次面板再攒
#     一个), 而它每 0.5 秒要 `registerReceiver` 一次。
#
#   验证: 桌面(PC)返回空串; 模拟安卓验了五种情形(两段都有 / 只有温度 / 只有功耗 /
#   电流读不到 / 快照抛异常), 数值按 4.1V × 0.45A 手算对过。
#
# 【v0.8.44 轮询改成「按速率自适应」+ 关掉诊断的全扫】
#
#   诊断版(v0.8.43)给出的上下界: t+16 时一个都没好、t+180 时全部好了
#   ⇒ **解码在 16~180ms 之间完成**, 而固定 150ms 的间隔要等到 t+150 才看第二眼。
#
#   但**不能简单压小** —— 假池模拟(同一个解码模型, 用真的 probe_all 跑完整条等待):
#       场景              固定150ms   固定30ms   按速率自适应
#       解码 180ms          712ms      608ms        608ms
#       解码 1200ms        1636ms     1764ms       1681ms
#   总 play 次数 = **101(每个确认一次) + 轮数(每轮撞一个未就绪的)** ⇒ 间隔小则轮数多、
#   白付更多次"失败 play"; 间隔大则发现得晚。**固定值必然在某一边输。**
#   而真正的最优间隔是 sqrt(8×解码耗时) —— 解码 180ms 时约 38ms、1200ms 时约 98ms,
#   **它随设备变, 定不下来**。
#
#   ⇒ 改成**按速率自适应**:
#        速率 = 本轮新增的连续就绪数 / 距上轮的间隔
#        间隔 = clamp(剩余 / 速率 × 0.3, 30ms, 200ms)
#      ⚠️ 信号必须用**速率**, 不能用"本轮扫了几个" —— 后者与时间无关, 慢解码到中期也会
#         变大, 于是误切小间隔(第一版自适应就是这么输的: 慢解码 1748ms vs 固定 150ms 的
#         1636ms)。这一条有模拟数据撑着。
#      ⚠️ 起手仍用 150ms(那时还没有速率信息); 上界 200ms 防"迟迟测不出速率", 下界 30ms
#         防"速率被噪声放大后疯狂轮询"。
#
#   ⚠️ 密采样还有个我们要的副作用: 0~180ms 里能拿到 5~6 个采样点, 于是
#      「连续就绪个数」的时间序列**本身就够判"解码是不是按加载顺序推进"** —— 那决定
#      "尾部哨兵"(101 次 play 变 1 次, 省 ~400ms)能不能用。
#
#   ⚠️ 同时把 `PROBE_DIAG` 关掉: 那个"前两轮全扫"又贵、采样点又落在两端, 密采样之后
#      不需要它了。`_probe_scan`(连续就绪个数)在普通模式下一直记着, 不依赖它。
#   ⚠️ 日志每轮多印一个「下轮间隔 X ms」, 用它确认自适应真的在工作。
#
# 【v0.8.43 探针诊断版: 前两轮扫完整个列表, 为了判"解码顺序性"】
#
#   ⚠️ **这是一版诊断包, 不是性能改进** —— 装上之后「音效等待」会比上一版**长**
#      (前两轮扫满列表), 别把它当成性能回归。
#
#   为什么需要它: 真机日志里探针**撞到第一个未就绪就停**, 后面那些"根本没看", 于是
#   两件事都不知道:
#     ① 解码**真实多快** —— 现在那个数被探针自己的成本盖住了(从"第 1 轮完全没好 →
#        隔 150ms → 第 2 轮全好"看, 真实解码可能只有 ~150ms, 而 630ms 是探针在折腾自己);
#     ② 解码是不是**按加载顺序**推进的。
#   而 ② 决定一个能省 ~620ms 的做法能不能用: 若"最后一个好了 ⇒ 前面全都好了",
#   那现在这 101 次 play 可以变成 **1 次**(只查尾部)。
#
#   做法: 开关 `PROBE_DIAG`(现在 =True)。打开时探针**前两轮扫完整个列表**, 记一条:
#       诊断轮 N: 全扫 A 个 / 就绪 B / 连续前缀 C / 未就绪 D 个, 首个=X, 分布=[...]
#   ⇒ **判顺序性就看那个"分布"**:
#        · 未就绪**全挤在尾部** ⇒ 按加载顺序推进 ⇒ 尾部哨兵可行(101 次 → 1 次)
#        · **零散分布**        ⇒ 乱序 ⇒ 哨兵会卡在永远不卡的索引上, 这条路划掉
#
#   ⚠️ **返回值与非诊断模式逐字一致** —— 诊断只是"多问几个", 绝不放过任何一个没好的;
#      摘页判据一个字没改。验证: 同一状态在开关两种模式下返回值相同, 且"首个未就绪"
#      记的确实是第一个(不是最后一个)。
#   ⚠️ **定案后必须把 `PROBE_DIAG` 置 False 再出货。**
#
# 【v0.8.42 修 K5 的跨池假绿(真机日志抓出来的)】
#
#   真机日志(v0.8.41, Xiaomi 25102RKBEC)里这一段:
#       第 1 轮(t+0 ms):   探针自身 0.1 ms,  连续就绪 0 个,  真扫 1 个,  卡在 peg0
#       第 2 轮(t+535 ms): 探针自身 384.6 ms, 连续就绪 101 个, 真扫 61 个, 全部就绪
#   **第 2 轮少扫了整整 40 个**(两池共 101 个样本), 而 40 恰好是池 1 的样本数。
#
#   根因: **两个 SoundPool 实例的 sampleId 是各自从 1 开始编号的**(池 1 是 1~40、
#   池 2 是 1~61)。而 K5 的确认集用**纯 sid** 当键 ⇒ 池 2 前 40 个的 sid 撞上了池 1
#   已确认过的 ⇒ 被当成"已确认"直接跳过。数字完全对上: 真扫 61 = 池1 的 40 + 池2 的 21。
#
#   ⚠️⚠️ **这是假绿**: 那 40 个样本**从没被验证过**, 却被当成就绪放行 —— 正是本项目头号
#      失效形状(「闸门放行、后端其实还没就绪 ⇒ 静默不响」)。而且 `_probe_scan`(连续就绪)
#      因此虚高成 101, 导出日志里那个自动判定也跟着失准。
#
#   修法: 键改成 **(池序号, sid)** —— 换池时 `_reset_pools()` 照样一起清, 而不同池的 sid
#   从此不可能互相冒充。
#   ⚠️ 验证: 假池**故意让两池的 sid 都从 1 开始**(复现真机条件), 断言第 2 轮**必须**真扫
#      池 2 的 3 个(修复前是 0 —— 那就是假绿); 另有"池 2 没好时绝不能因为池 1 确认过就
#      放行"的负对照。
#
#   ⚠️ **预期: 修好之后这个数字会变长** —— 上一版那个 535ms 是"少扫 40 个"换来的虚低。
#      真实值 ≈ 535 + 40×6.3ms ≈ 780ms。但它才是对的。
#
# 【v0.8.41 K5 增量探针(不再重扫已确认的) + 修「重放冷启动」耗时显示】
#
#   一、**K5 增量探针** —— 真机日志驱动, 不再是赌。
#      玩家导出的启动日志(Xiaomi 25102RKBEC / Android 16 / 8 核)写着:
#          第 1 轮(t+142 ms): 探针自身 142.2 ms, 扫 27 个, 卡在 win4
#          第 2 轮(t+820 ms): 探针自身 517.6 ms, 扫 101 个, 全部就绪
#          音效等待结束: 820 ms, 共 2 轮
#      ⇒ **探针自己花了 660ms, 而"音效等待"总共 820ms** —— 真正的解码只有 ~300ms
#        (第 2 轮从 t+302 开始扫就全过了 ⇒ 那一刻早已全部就绪)。
#        **探针才是大头, 不是解码**; 而每轮**从头重扫**已确认的那批, 对结果零信息、
#        对成本满贡献(两轮共 128 次 play, 约 5.1ms/次)。
#      ⇒ 换成**增量**: 已经亲眼确认过"能播"的不再重扫, 预期省 ≈130ms。
#      ⚠️ **安全性**: 依据是 `_ids` **只增不减**(唯一写入点是 prime), 启动窗口内没有任何
#         地方 release 单个 sample ⇒「刚才能播」⇒「现在还能播」成立(评审逐行核实过)。
#         双重保险: ① 键用 **sid 的值**(不是名字) —— 换池后 sid 全变 ⇒ 新 id 天然不在
#         集合里; ② 清空点收进**已有的** `_reset_pools()`, 与两张 id 表同生命周期、
#         同一个函数 ⇒ 不新增"需要记得同时改"的地方。
#      ⚠️ 放行判据**一个字没变**; `_probe_scan` 的语义也没变(仍是"从队首起连续就绪的
#         个数", 导出时的自动判定读它), 新增的是 `_probe_played`(这一轮真扫了几个)。
#      ⚠️ 验证: 用假池跑真代码, 7 组全过 —— 含 **乱序到达不误判**(某个后面的先就绪、
#         前面的还没好 ⇒ 仍判"没好")与 **_reset_pools 后 sid 复用不假绿**(负对照)。
#
#   二、**修「重放冷启动」的耗时显示**(玩家 2026-09-18 报):
#      原来算的是"玩家点下去那一刻"到"音效真的能播"那一刻的差值, 真机上却显示成
#      **41ms**, 而「启动信息」面板上写着「烘焙 2587ms + 音效等待 617ms」—— 自相矛盾。
#      玩家原话:「应该显示的耗时是 2571+617=xxxx 的这个 xxxx, 而不是 41ms」。
#      ⇒ 改成直接相加 `bake_ms + ready_ms`: 它们本来就是"这次冷启动花了多久"的两个
#        组成部分, 而且与面板读的是**同一份真源** ⇒ 结构上不可能再和面板打架。
#      ⚠️ 那个差值为什么变成 41ms **这一轮没查到根因** —— 所以 `_replay_t0` 保留并记进
#         启动日志(仅供以后追), 但**不再参与显示**。
#
# 【v0.8.40 解码并行度 2 -> 4: 开第二个 SoundPool 池装语音】
#   玩家 2026-09-18 问「1条8ms 100条800ms 你这个不能并行加载吗」—— 这一问点在要害上。
#
#   **AOSP 正文核实**(不是二手):
#     static const size_t kDecoderThreads =
#         std::thread::hardware_concurrency() >= 4 ? 2 : 1;
#     : mDecoder{std::make_unique<SoundDecoder>(this, kDecoderThreads, ANDROID_PRIORITY_NORMAL)}
#   ⇒ **4 核以上的机器, 每个 SoundPool 实例也只给 2 条解码线程** —— 101 个样本全在排这 2 条队。
#
#   **实测账对得上**(两个独立观测被同一个模型解释):
#     · 101 × ~16ms ÷ 2 ≈ 808ms    —— 热启动观测 ~800ms
#     · 61 条语音 × 16 ÷ 2 ≈ 482ms —— 冷启动"最后一批提交后 500ms 内全部就绪"
#   (那个 16ms 是反推的: 若并行度是 2, 800ms 就必须摊成每样本 16ms。早先说的"8ms"是从
#    500ms 窗口推的**上界**, 不是精确值 —— 两个模型都能解释总量, 只有这个能同时解释两处。)
#
#   ⇒ **再开一个池 = 4 条线程 ⇒ 理论上腰斩到 ~400ms**。收益来自"并行度翻倍"这个硬机制,
#     **不依赖到达曲线的形状** —— 这一点很关键: K1/K4/K5 那三条探针类改动, 同一套代码在
#     七条不同的到达曲线上跑出 −111ms ~ +197ms, **符号都能翻**, 所以它们必须等真机日志。
#
#   实现(安全约束四条都做了):
#     · **分池只按名字前缀**(语音都叫 voice_*), 判断只有 `prime()` 里那一处 ——
#       **不引入第二份清单**(本项目头号杀手就是"同一份清单出现在两处", v0.6.47 闪退即此)。
#     · **两张 id 表(`_ids` / `_ids2`)只能一起清** ⇒ 抽成 `_reset_pools()`, 唯一的一处。
#       改完 `grep 'self._ids = {}'` 全文件只剩 1 处(就在那个函数里), 形式上也是单点。
#     · **每个池各自串行探** ⇒ 探针靠的"池里同时最多一条流"这个前提在池内仍然成立
#       (上一版修的 `play()` 返回 0 的补试逻辑, 正是靠它排除"拿不到流"这个原因)。
#     · **可回退**: 模块级开关 `SFX_POOL_SPLIT`, 置 False 即退回单池(与上一版逐字相同);
#       有专门的验证项覆盖这条回退路径。
#     · 声道上限: 2 池 × SFX_VOICES(8) = 16 条流, 而 Android 9+ 的 client output track
#       上限是 40 ⇒ 安全。
#
#   ⚠️ 这一条的收益**只能真机验证** —— 桌面的 selftest / smoke / fx_probe 都碰不到真
#      SoundPool。装包后看「启动信息」里的「音效等待」有没有从 ~1000ms 掉到 ~500ms 量级。
#   ⚠️ 那份「保存加载日志」会自动把两池的扫描数累加进到达曲线, 判定逻辑不受分池影响
#      (池 1 先扫完 40 个合成音、再扫池 2 的语音, 切分点仍是 40)。
#
# 【v0.8.39 修三条 bug(探针把 play 返回 0 当唯一含义 / 缓存一失败就删光重烘 / 护栏异常出口静默) + 新增启动加载日志】
#   一、**`play()` 返回 0 被当成了「还没解码完」的唯一含义**(三轮评审挖出来的, 已在线上):
#      官方 javadoc 写明另一个**独立**原因是「新流的优先级低于所有在播流 / 当前没有空闲流」。
#      而探针(`probe_all`)撞到 0 就 `break` 并断定它没好 ⇒ 一个**其实已经就绪**的样本也被判成
#      未就绪 ⇒ 继续 sleep 重扫 ⇒ **最坏一路空转到 6 秒硬超时, 玩家白等**。
#      ⇒ 撞到 0 时**单独补试一次**(此刻前面所有流都 stop 掉了、池是空的 ⇒ "拿不到流"这个
#        原因被排除, 再返回 0 才是真的没好)。
#      ⚠️ 摘页判据**一个字没变**: 仍然是"每个音效都得有一次 play 返回非 0"。
#      ⚠️ 阴性对照: 两次都返回 0 时必须仍然判「没好」—— 实测仍然 False, 判据没被放宽。
#   二、**缓存自愈的形状不对称**: `_load_cached` 里**任意一个**合成音 prime 失败就
#      `return False`, 而上层随即 `_wav_wipe(d)` **把 40 个缓存 WAV 全删掉** + 整库重合成
#      3.7 秒 ⇒ 一次**偶发**失败(解码器忙 / 内存紧)的代价被放大成"这一局从热启动变成冷启动"。
#      同一条路上 `_prime_voice` 与冷路径对失败都是"记下来 + 后台重试", 只有这里一票否决。
#      ⇒ **先原地重试一次**: 偶发的过了就保住缓存; 两次都失败才判缓存真坏 ——
#        **保住原来那条自愈路径**(被截断的 WAV 就是这么被发现的)。
#      ⚠️ 阴性对照: 两次都失败时必须仍然返回 False(自愈没被砍) —— 实测仍然 False。
#   三、**`_await_ready` 的异常出口是裸 `pass`**: 循环里任何异常都让 `ready_ms` 保持 0、
#      **启动日志一行不留**, 然后照常硬放行 ⇒ 症状是玩家报过的「初次安装必然没声音」
#      **原样回来**而查不到痕迹。⇒ except 里补一条记录(⚠️ 仍然硬放行 ——「绝不软锁」是红线)。
#
#   四、**新增「启动加载日志」**(玩家 2026-09-18 要求: 「搞个完整的加载log ... 保存为一个
#      txt文件」): 从 **Kivy 导入之前**起算, 逐条记录 —— 每条音效的 load 提交(带**字节数**)、
#      探针每一轮的「自身耗时 / 扫了几个 / 卡在谁」、烘焙起止、预热三处耗时、`build` 进入 /
#      建 UI 完成(含"音效是否已就绪 ⇒ 建不建加载页")/ 第一帧 / 摘页 / 玩家首次触摸,
#      以及**摘页那一刻的四元快照**(重建次数 / 应到 / 已到 / 闸门 + 累计加载失败)。
#      「启动信息」里多了第三个按钮「**保存加载日志**」, 复用现成的导出降级链。
#      ⚠️ 它回答的是面板**回答不了**的问题: 面板只报结果(「音效等待 N ms」), 而"这 N ms
#        花在哪"要看过程。**为什么现在必须要有它**: 三轮评审最大的发现是「`Sfx` 是
#        `build()` 的第一句, 后面隔着整棵界面树 —— 若建树本来就比音频就绪慢, 那这 1000ms
#        玩家根本感知不到, 面板上那个数在骗人」。日志里那三个时刻就是判据。
#      ⚠️ 逐条 load 的 ms **只量"提交"、不代表解码**(`load()` 是异步的) —— 日志里已标明,
#        不要拿它当"每个音效的加载耗时"读。
#      ⚠️ 而且这份日志**自己会算结论**(不用你读曲线): 探针是按 `_ids` 的插入顺序扫的
#        (先 40 个合成音、再 61 条语音), 所以每轮那个「扫 N 个」本身就是"从队首起连续
#        就绪的个数" —— 那已经是一条现成的到达曲线。导出时它自动切成「合成音段 / 语音段」
#        各算速率, 并**判定那 ~800ms 到底是「每样本固定开销」还是「按字节」定价**
#        (两段字节比 7.8 倍、样本比只有 1.5 倍, 两种假设的预测拉得很开, 判据不会糊在一起)。
#        这个判定决定"把数据做小"整族方案(降采样/裁静音/换格式)是活是死。
#        ⚠️ 数据不足时它**明说"不足以判定"**, 绝不硬给一个假结论。
#
#   五、「重放冷启动」完成那行现在是两行: 「测试已经完成」+「**耗时 xxxx 毫秒**」
#      (玩家 2026-09-18 要求)。起点取"玩家点下去那一刻", 终点取"音效真的能播"那一刻
#      (不是"烘焙线程结束" —— 后者早约 800ms, 而玩家等的正是前者)。
#
# 【v0.8.38 修四个 bug(切档只切一半 / 蓄力期误发射 / 坏盘重抽死代码 / 彩蛋球嵌进隔墙) + 主界面按钮配色定案】
#   一、**切档只切一半**(玩家 2026-09-18 点名): `set_rtp` 里盘面交换被
#      `if self.state == "ready"` 挡着, 而入口在非 ready **够得着** —— 蓄力期点档位按钮、
#      装杯等待期长按「期望返还比例」(那个弹窗是 Window 子控件, 输入锁拦不住它上面的按钮)
#      ⇒ **高亮跳到新档、9 个倍率槽还是旧档的**, 这一发按**旧档**赔付, 要等下一发
#      `park_ball` 重掷才同步。而档位按钮是"我在玩哪一档"的**唯一**指示(那行标签只写
#      「期望返还比例：」不带数字)。
#      ⇒ ① `set_rtp` 非 ready **整个不动**(守卫放最前面 —— 连 `rtp_target` 都不先写,
#         先写就已经"切了一半") ② `start_charge` 立刻上输入锁 ③ `_ask_unlock_rtp` 非 ready 不弹窗。
#   二、**蓄力期任意一根手指抬手都会把球打出去**: `_on_title_touch_up` 是 **Window 级**
#      观察者, 屏幕上每次抬手都进来, 里面裸判 `state == "charging"` ⇒ 蓄力期间另一根手指
#      蹭一下再抬起, 球就当场飞出去(力度作废; 不到 `MISFIRE_POWER` 更是直接哑火)。
#      ⇒ `on_touch_down` 记下"按下发射键那根手指"的 `uid`, 抬手时**只认它**。
#      ⚠️ 那条兜底**不能删** —— 它管的是"按着发射键滑出按钮、`on_release` 不触发"。
#      ⚠️ 判定**不要改用 `touch.grab_current`**: 实测验过, `ButtonBehavior` 里那句
#         `touch.grab(self)` 在本 Kivy 上派发走完仍读到 `None`, 照它判定会**静默失效**;
#         用显式 `collide_point`(坐标沿用 `_on_title_touch_down` 那套 `_to_eq` 逆旋转)。
#   三、**隐藏档的"坏盘重抽"是死代码**(1000/2000/5000% 三档判据恒真 ⇒ 每盘白掷 3 遍取
#      第 3 遍), 且 `PITY_RATIO` 这个"保底阈值"常量**全文件从没被读过**(改不动的假旋钮)。
#      ⇒ 坏盘判据抽成**共用的** `_is_bad_board()`(以前是解析式与实际重抽**各写一遍的互补
#        写法**, 改一处忘另一处 RTP 就分家); `REROLL_DEAD` 里的档只掷一遍; 删掉 `PITY_RATIO`。
#      ⚠️ **RTP 一分钱不变** —— 判据恒真时"三掷取末"与"一掷"分布逐位相同。
#   四、**主界面按钮配色定案**(玩家 2026-09-18 逐轮定): 「重置」加一圈**跟着自己底色派生**
#      的描边(同色相, 暗底提亮 x1.75 / 亮底压暗 x0.50), 且**只给重置** —— 音效开关与
#      「每轮N次」的描边都已撤掉。 「音效已关」底色由暖橄榄 `#3d3828` 换成
#      **暗棕红 `#4a2e28`**(从 20 个候选里选的 6 号: 色相 10 度, 离「开」那个绿 152 度最远,
#      区分度最大; 而"红 = 静音"也是通用语言)。
#   五、**彩蛋那发的球会嵌进隔墙**(专家组排查发现的第四条): 约 1% 的发次球翻不过通道
#      隔墙、掉回右侧发射竖井(彩蛋, 按 x2 结算)。那之后球会**自己往左平移 25~35px、
#      压在/嵌进隔墙里**, 而且整个装杯演出 + 等玩家点「确定」期间一直定格在那儿。
#      ⚠️ 根因: 落定目标被算成了**槽中心**。竖井在场区**右边**、隔着一道墙(`FIELD_R`=459 ~
#         `LANE_L`=466), 而按 x 算出来的槽号(球在竖井里会被夹到第 8 格)其中心 **434.2 在墙
#         的左边** —— landing 循环里只有弹簧/重力/地板、**没有墙体碰撞**, 那句 LAND_K 弹簧
#         就把球往左拽、**拽穿隔墙**, 0.5s 超时兜底一到就定格在墙里。
#         (与项目定稿的"落定期间球不可能横移出本槽"直接冲突。)
#      ⇒ 彩蛋那发的目标是**发射槽本身**(`PLUNGER_X`=487, 与弹窗文案「已回到发射槽」一致):
#        球落点 477~498、目标 487, 整段移动都在竖井(466~508)之内, 永不跨墙。
#        结算不受影响 —— `settle()` 在彩蛋分支里本来就提前 return。
#      验证: 真 `_frame` 从竖井 6 个落点(475~500)下落 —— 修复前 6/6 停在 x 约 456(球体压在
#            459~466 的墙里), 修复后 6/6 收在 466~508 且一次没进墙带; 阴性对照: 场区正常落袋
#            (4 个力度)仍各自停在本格内。
#   验证: `--selftest` OK(七档 RTP 全在允差内、无空军) · `fx_probe` **19 条 = 基线、
#        逐条同名同序** · 本次新增 `fx_probe [19h]` 守住"非 ready 切档完全不动"
#        (含阴性对照: 换回旧写法 21 条红) · `REROLL_DEAD = {10.0, 20.0, 50.0}`、
#        七档解析 RTP 与档位差 <=1e-13。
#   ⚠️ 连带: `tools/fx_probe.py` 的 `_rtp_fixture` 已从 `state="flying"` 改成 `"ready"`
#      并补了 `_boards`/`game_area` 最小桩 —— 旧夹具正是靠"非 ready 时 set_rtp 跳过盘面
#      分支"省事的, 不改它会抛 AttributeError **中断 [19] 段**, 后面的基线 FAIL 全被跳过。
#      (`tools/` 不在 git 里, 这处改动没有版本记录。)
#
# 【v0.8.37 修崩溃: 切「每5秒」时 `Label.text` 收到了一个控件】
#   玩家 2026-09-18: 「新增 bug: 切换到每5秒的时候, 系统崩溃」。
#   ⚠️ 根因: `_show_hp_curve` 里 **`title` 这个入参名被函数体覆盖**了 ——
#      `title = self._fit_line(Label(text=...))` 之后它变成了 **Label 控件**; 而给粒度切换
#      拼的 `_kw` 字典是在**那之后**才建的 ⇒ `_kw["title"]` 抓到控件 ⇒ 切粒度时
#      `Label(text=<Label>)` ⇒ `ValueError: Label.text accept only str`。
#      ⚠️ **只有切粒度会崩**, 直接打开没事(那条路用的是原始字符串)。
#   ⇒ 把那个局部变量改名 `_title_lbl`, 不再遮蔽入参。
#   验证: 探针新增三条 —— E15(切到每5秒不崩) / E15b(换成 72 点的新弹窗) /
#         **E16(来回切四个来回都不崩: 1787 → 72 → 1787 → 72, 点数都对)**。
#         ⚠️ E16 是玩家点名的: 「这2个**互相切换都不能报错**」—— 只测一个方向不够。
#   另: 顺带修了**探针自己的**一个 Python 坑 —— 打桩 `staticmethod` 要从 `__dict__` 取
#       descriptor, 否则还原后会丢掉 staticmethod 身份 ⇒ `_txt()` 多收一个 self
#       (`TypeError: takes from 4 to 5 positional arguments but 6 were given`)。
#
# 【v0.8.36 粒度选择改为**保存**, 且它现在也**影响面板的数值范围**】
#   玩家 2026-09-17: 「每5秒和每帧的选择**是保存的**, 他**应该影响**电池功率的输出界面的
#   数值范围」。
#   ① **粒度写进配置**(`plinko_config.json` 的 `power_grain`) ⇒ 冷启动后保持上次选的那档;
#      打开曲线弹窗时也直接用它(不再是硬编码的「每帧」)。
#      ⚠️ 白名单走 `POWER_GRAINS` **派生**(与 rtp 那条同一个规矩), **不手抄** ——
#         手抄一份粒度清单, 将来加档位时漏改一处就静默读不回来(v0.6.47 闪退的同款形状)。
#   ② **面板那三个数(平均/最低/最高)跟着粒度走** —— 切到「每5秒」后**当场重算**
#      (`_recalc_power_stats`), 否则会出现"曲线最高 3.81、面板写着 7.60"的自相矛盾。
#      ⚠️ 这**推翻了 v0.8.33 那条"曲线滤波、统计用原始"的口径分离** —— 玩家要的是**一致**。
#   ③ ⚠️ **`wh`(总耗电)仍走原始序列**, 而且**顺带补上了缺格偏差**:
#      旧式 `sum(有效格) × dt` 等价于"把缺格记成 0 瓦" —— 实测那份 Redmi K90 缺 40/1775
#      ⇒ wh 偏低 **2.25%**, 比整根尖峰的影响(0.15%)大 **6.6 倍**。
#      现在按 **平均功率 × 总时长** 外推(能量守恒要的是"均值 × 时长", 换粒度会让它偏)。
#   验证: selftest OK · 探针 29 全绿 · curveaxis 24 · hpcurve 12 · 截图确认(两行按钮)。
#
# 【v0.8.35 功率曲线加「每帧 / 每5秒」两个粒度按钮】
#   玩家 2026-09-17: 「新增2个按钮: 真实数据, 分别是: **每帧** **每3秒** 这样就能去掉了?」
#   ⚠️ **实测「每3秒」去不掉那些峰**: 那两根挨着的峰合起来是 **10 格连续高值(2 秒)**,
#      3 秒一组(15 格)它占 2/3 ⇒ 均值 5.71 / 中位 5.94, **峰还在**。
#      **5 秒一组(25 格)它只占 40%** ⇒ 中位数被平台拉回来: **max 7.60 → 3.81, 一个不剩**。
#   ⇒ 两个粒度做成「**每帧**」(原样, 保住每一根真实的爆发)+「**每5秒**」(每 25 格取中位数)。
#      当前选中的那个高亮, 点另一个就重开弹窗换粒度; 说明行跟着写「采样 / 5秒段」。
#   ⚠️ 两个粒度**都只影响画曲线** —— 面板统计与导出的 txt 始终是原始值。
#   ⚠️ 切粒度时参数在**闭包里整个捕获**(不能等点击再去读 `self._hp_*`: 那时可能已被
#      下一局覆盖, 而翻历史那条路的数据根本不在 self 里)。
#   验证: selftest OK · 探针 29 · curveaxis 24 · hpcurve 12 ·
#         截图确认(两行四个按钮, 第一行粒度、第二行保存/返回)。
#
# 【v0.8.34 剔除改成「跳过开头无效段 + 再延后 5 秒」, 不再是固定 N 秒】
#   玩家 2026-09-17(看了真机导出的记录): 「你需要忽视这些**无用的数据**, 同时在**有用的
#   数据处延后 5 秒**再开始统计」。
#   ⚠️ 实测那份 Redmi K90(25102RKBEC) 的记录里 **t=3.0~10.8s 的电流全是 `nan`**
#      (CPU 刚冲满载那几秒 Binder 被抢占, 一格都没读到)
#      ⇒ **"固定剔前 N 秒"根本不管用**: N 得随设备变(K90 要 11 秒, 别的机器可能 3 秒)。
#   ⇒ 改成两步: ① 跳过**开头那段读不到的空段**; ② 再从第一个有效读数往后延
#      `HP_PWR_SKIP_SEC`(5 秒, CPU 从 idle 冲到满载的过渡期)。
#   ⚠️ 只处理**开头** —— 中途偶发的 nan 不算"无效段"(那是采样抖动, 该留着)。
#   实测(那份 K90 数据): 第一个有效点在 t=11.0s(5.19W, 正是过渡期值), 新逻辑统计起点
#   = **t=16.0s**; 剔除后 平均 3.45W / 最低 2.47 / 最高 7.60, 只丢掉 1.4% 的有效格。
#   导出的 txt 表头会写明"实际剔掉几格、t 列起点几秒"。
#   ⚠️ 附带一条**真机实证**(支撑 v0.8.33 那个 5 点中位数): 那份记录里 **8 个峰(>4.2W)
#      的游程全是 4~5 格 = 0.8~1.0 秒**, 正是底层一次刷新的宽度
#      ⇒ **每一个峰都是真实读数**, 没有"只闪 1~2 格"的伪影; 而 5 点中位数的门槛是 3 格
#      ⇒ **一个峰都不会被删掉**。滤波器只会动那 3 个 1~3 格的普通段(3.4~3.9W)。
#   验证: selftest OK · 探针 29 · curveaxis 24 · hpcurve 12 · fx_probe 19 条与基线同名。
#
# 【v0.8.33 功率曲线: 每 5 点取中位数 + 前 5 秒不参与】
#   玩家 2026-09-17 定案(原话): 「1、每 5 个点取中位数　2、不用前5秒的功率数据　做完就push」。
#   ① **曲线**改走「每 5 个点一组(不重叠)取中位数」⇒ 1790 点 → **358 点**(= 1 秒 1 点)。
#      ⚠️ 5 点是**结构决定**的, 不是拍的: 底层电量计约 0.96 秒才刷新一次, 而采样是 5Hz
#         ⇒ **一个真实读数必然连续占约 4~6 格**, 于是"只占 1~2 格"在结构上就不可能是真读数。
#         5 点一组的无损门槛恰好是 **3 格** —— 正好滤掉 1~2 格的毛刺、完整保留 5 格的读数。
#      ⚠️ 用**中位数**不用均值: 均值/截尾均值会造出没测到过的值(实测造出 8.8 / 10.4 / 8.7,
#         而原始数据里根本没有这些数); 中位数的输出必然是窗口里存在过的读数。
#   ② **统计(平均/最低/最高)与导出的 txt 一律不滤**, 走原始序列 ——
#      滤波会改 `max`(实测单点尖峰 11.87 → 3.9, 差 3 倍), 而"最高功率"是机器的能力指标;
#      导出的那份 txt 更是**专门**拿原始值去判尖峰真伪的。**曲线滤波、统计/导出原始**, 口径分开。
#   ③ `HP_PWR_SKIP_SEC` 3.0 → **5.0**(玩家把"前3秒"改成"前5秒")。
#   验证: selftest OK · 探针 29 全绿(新增三条机制断言: 单点滤掉 / 持续 5 格完整保留 /
#         只占 2 格的滤掉; 外加一条"`_med5` 只在曲线路径上, 共 3 处") ·
#         curveaxis 24 · hpcurve 12 · fx_probe 19 条与基线逐条同名 ·
#         截图确认(横轴 358 点、说明行写「中位段」、两个按钮并排没溢出)。
#
# 【v0.8.32 高压功率: 加「保存记录」导出原始数据 + 剔掉前 3 秒】
#   玩家 2026-09-17 两条:
#   ① 「新增一个按钮, **保存记录**, 点击后把 txt 文件放在下载目录中, 我发给你」
#      ⇒ 功率曲线弹窗上多了「保存记录」, 点它把**原始采样**写成 txt 落到**公共下载目录**。
#        走的是逐帧日志那条现成的降级链(MediaStore → 外部私有 → 内部 → 剪贴板, 不需权限),
#        `_bench_save_log` 为此**参数化**(默认行为逐字不变)。
#      ⚠️ 导的是**原始值**(整数电流 + 当时的电压), 不是已换算的 W —— "那个尖峰是真读数
#         还是采样毛刺"只能拿原始值回答, 而这两条**之前根本没落盘**。
#      ⚠️ 表头带一行「极值点游程」: 底层电量计约 0.96 秒才刷新一次, 而采样是 5Hz
#         ⇒ **一个真实读数应当连续占约 5 格**。所以"最高值连续占了几格"直接回答了
#         "它像不像真的"; 而且**只用 W 序列就能算**, 翻历史导出的老记录也有这个数。
#   ② 「功率数据的前 3 秒不能用, 需要删掉, 变化很大, 没有啥用」
#      ⇒ 新增 `HP_PWR_SKIP_SEC = 3.0`。那几秒是 CPU 从 idle 冲到满载的**过渡期**
#        (实测从 11.5W 一路掉到 7.5W), 算进平均/最高会把整场读数带偏。
#      ⚠️ **只切一处**(存 `_hp_power_*` 时) ⇒ 面板统计 / 曲线 / 每瓦跑分 / 导出全跟上。
#      ⚠️ **只跳功率**, 不跳温度/频率 —— 玩家点名的是功率; 温度那条是**变长**序列
#        (读不到就不记), 按下标切会切错位。
#   验证: selftest OK · 探针 25 全绿(含"导出真写盘""txt 带原始电流""游程宽度算出来了")
#         · fx_probe 与基线逐条同名。
#
# 【v0.8.31 功率曲线: 纵轴刻度带单位 W】
#   玩家 2026-09-17: 「这个**竖着的轴**先改了, 数字后面加个 **W** 字」+
#   「改为**竖轴单位是W**」。
#   · 纵轴刻度 `6.80 / 9.50 / 12.20` → `6.80W / 9.50W / 12.20W`
#   · 图下那行 `竖轴 = W` → `竖轴单位是W`(三条曲线统一, 两种写法一样长、不会折行)
#   ⚠️ **只有功率曲线能带单位** —— 左留白是 `dp(46)`, 改前实测: `12.20W` = **36px**
#      (与现有最宽的 `102000` 一样宽, 安全), 而 `2425MHz` 要 **44px**、只剩 2px 余量。
#      ⇒ `SpeedCurve.unit` 必须只给功率那条传; 想给频率加得先加宽 `pad_l`,
#        而那会动 `temp/_curveaxis.py` A10 的判据。
#   验证: curveaxis 24 · hpcurve 12 · 功率曲线界面 21(全绿, 含"刻度以 W 结尾"这条新断言)
#         · 截图确认刻度没顶出左边界、说明行没折行。
#
# 【v0.8.30 高压面板再撤一行: 电压/电流】
#   玩家 2026-09-17: 「**电压/电流**：平均 3.85V / 1.23A（3.61~4.21V / 0.30~1.48A）
#   这个删掉吧, 重新 push」。
#   那一行当初是照着"功率的分子分母、零采集成本"加的, 玩家看下来觉得没必要。
#   ⚠️ **采集与落盘照旧** —— `volt_mean/min/max`、`amp_mean/min/max` 还在记录 JSON 里,
#      撤的只是显示(将来想查那两台机器的电压压降, 翻记录还有, 不用重跑)。
#   面板现在是四行: CPU 频率 / 电池温度 / 电池功率 / 每W跑分。
#
# 【v0.8.29 高压面板: 功率曲线独立成图 + 文案三处收尾】
#   玩家 2026-09-17(第二轮, 四条):
#   ① 「**去掉温度曲线**, 只汇报温度, 那个曲线更名为**功率曲线**, 单独的界面标题
#      改为 **cpu高压测试的功率曲线**」⇒ 图里只画功率(单轴); 温度仍然报, 但只在
#      面板文字里。读不到功率的设备**不建按钮**(温度不进图, 没有可降级的东西)。
#   ② 「每?跑分」的「**瓦**」渲染成豆腐块 ⇒ 项目字体 `fonts/NotoSansSC-Medium.otf`
#      是**子集**(实测只有 1602 个字形), `瓦`(U+74E6) 不在里面, 而 每/步/秒/度/到 都在。
#      改用 **每W跑分**(W 是 ASCII, 一定在)。⚠️ 加新文案前先查字形表。
#   ③ 「去掉**总耗电**和下面的这个描述」⇒ 与「电流刷新」两行**从面板撤掉**。
#      ⚠️ **采集与落盘照旧**(`wh` / `power_stats` 还在记录 JSON 里), 撤的只是显示 ——
#         将来想查那两台机器的刷新周期, 翻记录还能翻到, 不用重跑一次测试。
#   ④ 「从**最低xx/最高xx** 简化为 **（x到y）**」⇒ 频率/温度/功率**三行一起改**
#      (原写法在 360dp 上会折行, 实测「最高 4185）」被挤到下一行);
#      另外「每W跑分」后面**不再重复写 `·W`**(标签里已经有 W)。
#   验证: selftest OK · curveaxis 24 · hpcurve 12 · 功率曲线单测 21 · 界面 17(全绿)
#         · fx_probe 19 条与基线逐条同名 · 冒烟 OK · 截图确认版面。
#
# 【v0.8.28 新增「每瓦跑分」+ 高压面板版面修两处】
#   ① **每瓦跑分**(玩家 2026-09-17: 「压力测试需要后面新增一个每瓦跑分功能」):
#      口径 = **全程平均步/秒 ÷ 全程平均功率**。印在结果/详情面板上, 并**存进历史记录**
#      (`ppw` 字段) —— 这个指标本来就是拿来**跨设备对比**的(玩家手上 K90 / Y700),
#      只印在当场那个弹窗里就翻不出来了。
#      ⚠️ 取**全程平均**而不是首窗峰值: 压力测试关心的是持续能效, 首窗那几秒还没热起来,
#         拿它比会把每台机器都高估。老记录没有 `ppw` 时才退回现算。
#   ② **面板版面修两处**(都是截图验证抓到的 —— 门禁全绿、断言全过, 画面照样是坏的):
#      · 「电池功率」原来与「电池温度」**挤在同一行** —— 拼上去的功率块少一个**前导**换行
#        (上面那句「电池温度：…」是末行写法, 结尾本来就没有换行);
#      · 「电压/电流」那行在 360dp 上**折行** ⇒ 改成「平均 X / Y（A~B / C~D）」写法,
#        信息一点没少。
#
# 【v0.8.27 CPU高压测试新增「电池功率」+ 温度功率双轴曲线（测试版）】
#   玩家 2026-09-17: 「CPU高压测试，新增一个测试结果」「把电池曲线按钮改名为温度功率」
#   「把功率曲线和电池温度放在一起，一个是左坐标轴，一个是右坐标轴」。
#   功率 = |电流| × 电压：
#     · 电流 —— BatteryManager.getIntProperty(CURRENT_NOW)(µA)，**不加任何权限**；
#       三条路兜底(CURRENT_NOW → CURRENT_AVERAGE → sysfs)，都读不到就整块不显示。
#     · 电压 —— 与温度**共用同一次** sticky 广播(EXTRA_VOLTAGE)。
#   采样 5Hz(温度仍 1Hz) ⇒ 两条序列长度不同，曲线横轴改成**按秒**画
#   (原来是「第 N 个样本」；没功率的设备自动降级回单轴温度、老记录照旧)。
#   面板新增四行：电池功率 / 电压·电流 / 每瓦性能 / 总耗电；另加一行**电流刷新周期**。
#   ⚠️ CURRENT_NOW 的底层刷新由 fuel gauge 决定(AOSP 记录过 175.8ms~3.5s 不等)，
#      5Hz 只能分辨到 200ms —— 更快时只报「≤0.2 秒」，不编具体毫秒数。
#   ⚠️ **这是测试版**：目的是拿 K90 / Y700 实测两台机器的电流可读性与刷新周期，
#      结论出来前不要当成正式功能对外说。
#
# 【v0.8.26 措辞统一: 「落珠」并入「装杯」】
#   玩家 2026-09-17: 「优化措辞，有的地方叫落珠有的地方叫装杯，统一下」。
#   改动面 = 注释与文档(main.py 10 处 / buildozer.spec 6 处 / CLAUDE.md 4 处 / BUILD_APK.md 1 处),
#   外加**玩家可见的那一句** —— 跑分菜单说明 `_bench_menu_desc`「含落珠动画」->「含装杯动画」。
#   ⚠️ **刻意不动的三类**(不是漏改):
#     ① 玩家原话引用(`main.py:13415`、`buildozer.spec:3111/3113`「不包含落珠动画」)——
#        那是证据, 改了就成伪造;
#     ② 演出内部时间轴的分段名 `WINDUP / 压暗 / 杯子 / 落珠 / 回味 / 退场`(`main.py:9175` 等 3 处)——
#        整场叫「装杯」、段落叫「落珠」; 统一成「装杯」会变成"装杯里面还有装杯";
#     ③ `pending / win(进场 + 落珠)` 那处 mode 说明, 同上。
#   零功能改动: 一个**契约字符串**都没碰 —— `STAGE_ORDER` / `_bench_tag` / `_BRK_KEYS` 本来就是「装杯」。
#   验证: `--selftest` OK(音效体检 41 个 0 异常)。
#
# 【v0.8.25 同一个线宽问题扫尾: 弹窗小曲线 + 成绩面板趋势线也改成真 1 像素】
#   玩家 2026-09-17 看过 1:1 对照图后定案: 「那你也改了，并推送」。
#   v0.8.24 只改了放大图(那两处他当时说「别动」)。复核时把它们**量了一遍**, 发现两张
#   都还是 2.3 像素粗: 弹窗小曲线孤立笔画 中位 **2** 像素 / 成绩面板那条金线中位 **3** 像素。
#   ⚠️ 顺带纠正一条历史: 玩家 2026-09-16 就提过成绩曲线「是不是**太粗**了, 你看看之前的
#      那个帧率曲线用哪个粗细, 这个也那个粗细」—— 而当时"对齐"到的 `1.15` 是**假的**
#      (Kivy 半宽 ⇒ 实际 2.3 像素, 见 v0.8.24), **那次诉求从来没被真正解决**。
#   改法(两个旋钮分开, 不互相连带):
#     ① `FpsCurve.line_w` 默认值 1.15 -> **1.0**(一处真源: 放大图不再显式传 `line_w`);
#     ② `SpeedCurve._draw` 里那条硬编码的 `width=1.15` -> **1.0**。
#   实测(真渲染, 各按自己绘图区几何裁):
#     · 弹窗小曲线: 孤立笔画 中位 2 -> **1**(均值 2.19 -> 1.00); 带内墨迹 17.5% -> **12.9%**;
#     · 成绩面板金线: 金色像素 10771 -> **6304**; 逐行连通段宽度 中位 3 -> 2
#       (新值是 2 不是 1, 因为金线压在深色半透明底上, 抗锯齿的混色像素也被算进来了);
#     · 放大图: 不受影响(它昨天就是 1.0)。
#   ⚠️ 顺手关掉一个隐患: `width != 1.0` 时每点生成 14 个顶点, 撞上 65535 就整段画不出来
#      (v0.8.24 查清的那条)。三张图现在都是每点 1 个顶点 —— 小图 405 点 / 成绩面板 321 点
#      本来就离极限很远, 但 `joint="round"` 是同一个, 点数涨上去一样会中招。
#   ⚠️ `FpsCurve` 那条注释里补了一句交叉引用: 这个 Kivy 约定**本仓库早就记过**
#      (搜索 `真实宽度是 7px` —— 导轨带那条), 坑记录过, 只是帧率曲线这边没照办。
#
#   对照图: `android/output/fps_small_linecmp.png` · `android/output/fps_speedcurve_linecmp.png`
#          · (v0.8.24 的) `output/fps_zoom_before_after.png`
#   验证: --selftest OK · --smoke SMOKE-OK · 三处都出了真渲染量测(见上)。
#
# 【v0.8.24 修「放大图帧率曲线糊成一团实心」的真根因: **Kivy 的线宽是半宽**】
#   玩家 2026-09-17: 「这个放大版本的帧率图还是会出现一团糊在一起的情况」。
#   ⚠️⚠️ **根因不是"每帧占几像素", 是线的宽度**: Kivy 的 `Line(width=W)` 画出来是 **2W** 宽
#      (`kivy/graphics/vertex_instructions_line.pxi`: 四边形两侧**各按 w 偏移**; 只有
#      `width == 1.0` 才走 `build_legacy()` 的 GL_LINES, 那才是真 1 像素)。
#      所以旧代码 `width=1.15` 实际是 **2.3 像素**粗, 再叠加 `joint="round"` 给 5263 个顶点
#      各补一个半径 1.15 的圆盘 ⇒ 点间距不管调到几都被填满。**"1 像素线 + 2 像素留白"
#      从来没成立过** —— v0.8.23 那条里写的「线宽 1.15 比 1 像素宽」正是病根本身。
#   实测(真渲染截图量"曲线带内的墨迹填充率", 探针 `android/temp/fpscov_probe.py`):
#      修复前(每帧 3px + width 1.15) **59.3%** · 新版(每帧 2px + 真 1 像素) **12.3%** ·
#      阴性对照(内存里把线宽顶回 1.15) **65.7%** ⇒ 门禁阈值 35%, 两端离得都很远。
#      ⚠️ 量"糊没糊"必须量**带子内部**, 不能量整块绘图区 —— 绘图区一大半是空白, 曲线
#         再实心也只占百分之几(门禁第一版就踩了这个: 读数 1.5%, 看着像没问题)。
#   改法 3 处(都在 `FpsCurve` / `RootWidget._fps_curve_zoom`):
#     ① `FpsCurve.__init__` 新增 `line_w=1.15`(**默认值 = 老观感**), `_draw` 改用它;
#     ② 放大图 `_fps_curve_zoom` 显式传 `line_w=1.0`(真 1 像素);
#     ③ `ZOOM_PER_FRAME_PX` 3.0 -> **2.0**(玩家看过 1/2/3 三张「线: 真 1 像素」对比图
#        `output/fps_pitch_1to123.png` 之后选的 2; 5265 帧 ≈ 10530px ⇒ 手机约 8.6 屏)。
#   玩家硬要求「别动」的两张图**逐像素验证过**: 弹窗小曲线走 `FpsCurve` 的默认值
#     (1.15); 成绩面板趋势线是 `SpeedCurve` **另一处硬编码**的 `width=1.15`(不是本参数)。
#     新旧渲染按各自绘图区几何对齐后 85890 像素里只差 **1 个** —— 逐像素看过, 它是
#     **曲线自己那条抗锯齿边缘**上的一个半覆盖像素(背景 249 与 34% 覆盖的 188), 来自两次
#     运行弹窗落位差 1 像素的栅格取整, 不是内容差异。同一份代码重跑是 **0 差**。
#   同批带上并行会话(android-7d)的 `frame_spaced`: 放大图横坐标从"按累计真实时间"改成
#     "按帧序号均分"(阶段条同步)。理由: `pw = 帧数 x N` 而 x 按时间走 ⇒ 逐帧间距其实是
#     `N x (本帧间隔/平均间隔)`, 165Hz 上抖动 ±20% 就飘成 1.6~2.4 像素, "每帧 2 像素"名不副实。
#     ⚠️ 代价(有意为之): 长卡顿不再占更宽的横轴 —— 要看"卡顿占多宽"请回小图。
#   ⚠️ 附带记一条 Kivy 陷阱(与 BUILD_APK §3.25"子控件 canvas 是绝对坐标"是两回事):
#     **ScrollView 的子控件 `pos` 恒为 (0,0)** —— 它用 `vp.pos = 0, 0` + `g_translate.xy`
#     的 canvas 矩阵平移(`kivy/uix/scrollview.py:1142`)。探针要取窗口坐标必须读父 ScrollView
#     的 `g_translate`; 用 `c.x/c.y` 会量到屏幕左下角的黑背景(本会话踩过)。
#
#   ⚠️⚠️ **同一处还顺手治好第二条缺陷(复核抓出来的, 已实测坐实): 顶点数 > 65535 就画不出来。**
#      `width != 1.0` 时 Kivy 给**每个点**生成 14 个顶点(4 段 + `joint_precision`=10 个关节),
#      而顶点号写进 **`unsigned short` 索引数组**(同文件 `cdef unsigned short *indices`) ——
#      超了静默回绕, 无异常无守卫(65535 那道守卫只长在 `Mesh` 上)。5265 点 ⇒ **73706 顶点,
#      超 11%**。**实测**: 用 `width=1.15` 把长条滚到最右端, 那一段曲线**整段不见**
#      (带内墨迹 0.9% —— 扣掉网格线就是 0); 换 `width=1.0`(每点 1 个顶点)同一位置正常(2.4%)。
#      ⚠️ 通用教训: **点数上千的折线一律 `width=1.0`** —— 又快(5265 vs 73706 顶点,
#         索引少 34 倍)又不会撞这个上限。小图(降采样后几百点)与成绩面板(300 来点)没撞到,
#         但它们用的是同一个 `joint="round"`, 点数涨上去同样会中招。
#      注: 探针只量得到长条的**第一屏**(长条被 ScrollView 裁在视口内), 上面这条是**专门滚到
#         最右端**才量出来的 —— 老探针对这个缺陷结构性看不见。
#
#   验证: --selftest OK · --smoke SMOKE-OK · 真渲染探针带内填充率 59.3% -> 12.3%
#         (阴性对照 65.7%, 有分辨力) · 小图与成绩面板逐像素不变(见上)
#         · `android/output/fps_zoom_before_after.png` 是 1:1 的前后对照
#           (实心块 -> 细线 + 能逐根数出来的尖刺)。
#   ⚠️ `tools/fx_probe.py`: **本次改动新增失败 0 项**。拿 HEAD 建 git worktree 跑了同一套
#      门禁做 A/B: HEAD 20 项红(1 项是 worktree 缺件的环境项) / 工作区 19 项红,
#      差分 new-old = **空**。那 19 项是之前几次提交留下的(球与玻璃贴图 mipmap 5 项 +
#      成绩面板文案等)。另外 fx_probe 里那条「曲线弹窗把逐帧阶段标签传给 FpsCurve」
#      **早已失效**(它的正则要求 `def _show_fps_curve(self):` 无参, 而现在签名有 7 个
#      参数 ⇒ 恒红) —— 所以这段代码目前只有"真渲染像素探针"一条门禁兜底。
#
# 【v0.8.23 放大图密度 2px -> 3px(玩家 2026-09-17 二次定案)】
#   玩家 2026-09-17: 「把放大后的帧率曲线从 2 个像素 1 组改为 3 个像素一组」。
#   只改一个数: `FpsCurve.ZOOM_PER_FRAME_PX` 2.0 -> 3.0(它是放大图的唯一旋钮, 换粒度
#   不用碰任何绘制代码)。宽度 = 帧数 x 3px: 5263 帧 => 15789px, 手机上约拖 13 屏
#   (2px 那版约 8 屏)。
#   ⚠️ 这个旋钮已被来回拧过四次: 2 -> 6(v0.8.12, 我误判) -> 3(v0.8.13 改回) ->
#      2(v0.8.14) -> 3(本次)。**别再拿"上一版是几"当依据** —— 只认常量当前值。
#   ⚠️ `_grp`(每点合并几帧)**不受影响**: 放大图的宽度是按帧数给的, 恒有
#      "横轴像素数 >= 帧数" ⇒ 仍是每帧一个点, 数据口径一个字没变, 只是间距宽了
#      (多出来的那 2 像素是给两条线之间留的空隙, 线宽 1.15 比 1 像素宽)。
#   ⚠️⚠️ **上面那句「线宽 1.15 比 1 像素宽」是错的, 见 v0.8.24**: Kivy 的 `Line(width=W)`
#      实际画 **2W** 宽 ⇒ 1.15 是 2.3 像素, "留空隙"从来没成立过, 那正是糊成实心块的根因。
#
#   验证: --selftest OK(只改一个浮点常量, 不涉及任何逻辑)。
#
# 【v0.8.19 把「热限制等级 / 本机 thermal zone」两行从界面上删掉(采集保留)】
#   玩家 2026-09-17: 「A + 删掉之前多加的测量文本」—— 曲线**保持阶梯**(A 方案), 那两行删掉。
#   理由: 它回答的那个问题**已经有结论了**, 而用词(`thermal zone` / `batt` / `bms` / `charger` /
#   `cpullc-0-0`…)对**看结果的人**就是噪音。
#
#   **结论留档**(联想 TB323FU / Android 16 实测): 「本机 thermal zone(共 32 个): 没有一个带电池
#   字样(batt/bms/charger/fg/ib); 例如 cpullc-0-0、cpullc-0-1、qmx-0-0、qmx-0-1、qmx-0-2、
#   cpu-0-0-0…」⇒ **这台机器没有比广播更细的电池温度源**, "换数据源"那条路走不通;
#   台阶是 `BatteryService` 的广播限流造成的, **只能保持原样**。
#   热限制等级报 0(无): 跑满 360 秒系统也没触发降频(与当时 CPU 那几个 zone 才 50 多度对得上)。
#
#   ⚠️⚠️ **采集本身留着**(`_hp_battery` 里的 `thermal` / `zones` 照旧写进历史 JSON) ——
#      **只是不显示**。将来换台机器(比如还没测过的手机)想再看一眼, 改 `_hp_result_text`
#      一处就能加回来; 从 `_hp_battery` 里删掉的话就变成"得重新写一遍探测"了。
#   ⚠️ `_THERMAL_NAMES` 现在界面上用不到了, **同样留着**(采集还在), 注释里写明了。
#
#   验证: --selftest OK · `_hp_result_text` 的输出里已不含"热限制等级"/"thermal zone"字样。
#
# 【v0.8.18 修平板跑分黑屏的两处: 右侧露亮边 + 期间能转屏】
#   玩家 2026-09-17 报的两条(都在联想 Y700 上)。
#
# ① **黑屏右侧不是黑色**(玩家: 「黑屏不均匀, 右侧的不是黑色, 可能是覆盖不完整」)。
#    病根: `_relayout_bench_dim` 写的是 `pos = (-self.x, -self.y)` + `size = _veq()` ——
#    作者的意图是"RootWidget 在 anchor 里居中, 把原点挪到视口原点", **但 Kivy 的 canvas 指令
#    本来就是绝对(窗口)坐标、父级不做平移**(CLAUDE.md 里记着这条) ⇒ 那个 `-self.x` 反而把
#    矩形推进了**负坐标**, 右边露出一条 `self.x` 宽的缝。
#    ⚠️ **只有 `self.x != 0` 的机器看得见**: 手机上内容列正好铺满(`self.x = 0`)永远正常 ——
#       所以这个 bug 从上线起就一直在, 直到平板才暴露。平板(Y700 等效竖屏 792dp、内容列
#       780dp)居中后 `self.x ≈ 6dp` ⇒ **右边露约 16px**。
#    ⇒ 改成和 `_relayout_hud_dim`(2026-09-11 踩过**同一个坑**、已修)逐字同款: 直接用
#      **父容器**(App.build 里那个 AnchorLayout)的矩形 —— 它就是等效视口, 横屏反旋转时铺满
#      整块物理屏。白字也跟着用同一个 `vp` 居中。
#    实测(算术): 旧写法覆盖 [-5.95, 786.05] / 新写法 [0, 792], 视口右边 792。
#
# ② **两种跑分期间禁止屏幕旋转**(玩家: 「否则很容易出问题…会出现非黑屏画面, **甚至有原有的
#    弹珠屏幕**」)。病根: 方向守卫对**宽屏设备只在横置(rot 1/3)时重申**, **竖置时什么都不设**
#    ⇒ 平板竖着跑分时方向完全敞开; 而跑分是 **360 秒**的长过程, 中途一转: `LandLayer` 要重排
#    整棵树、`_veq()` 跟着变、黑屏矩形/白字的位置全要重算(全在跑分线程满载时发生) ⇒
#    矩形跟不上就露出底下的游戏画面。
#    ⇒ 新增 `_BENCH_ORIENT_LOCK` + `_set_orient_lock()`, 挂在黑屏的开/关那一对上
#      (`_show_bench_dim` / `_hide_bench_dim`) —— 与 `_set_system_ui` **逐字同款**: 改标志 +
#      **立刻投一次**, 不等那 2.5 秒的周期重申(那 2.5 秒里转一下就白搭)。
#      锁的是 `LOCKED`(14, API 18+)= **锁定当前方向**; ⚠️ **别用 `NOSENSOR`(5)** —— 那个的
#      语义是"用 manifest 声明的方向"(平板横竖都可能), 不是锁当前。
#    ⚠️ 锁**只覆盖黑屏那一段**: 平时照旧按设备分流(给宽屏 `FULL_SENSOR` 本意就是"让用户能转就转")。
#    ⚠️ 两个跑分(物理演算 / CPU高压)**共用这一对开关**, 所以"普通跑分可能也有"这条一起修了。
#
#   验证: --selftest OK · 方向锁开关 True/False · 黑屏矩形新旧写法的覆盖区间(见上)。
#    ⚠️ **真机效果要装了才知道**: 这两条在桌面上**复现不了** —— 桌面 `Window.size` 改不动
#       (SDL 直接拒绝), 所以造不出"RootWidget 比视口窄"那个条件。第一条是**构造性正确**
#       (用视口自身盖视口), 第二条是 API 语义确定的, 但都得以真机为准。
#
# 【v0.8.17 thermal zone 列表改成「只印带电池字样的」】
#   2026-09-17 第一份真机数据(联想 TB323FU / Android 16)回来了, 结果很关键:
#     · **这台机器普通 app 读得到 24 个 thermal zone**(`cpullc-*` / `qmx-*` / `cpu-*`) ——
#       推翻了"普通 app 一定读不到 thermal_zone"那半个假设(**厂商可以开口子, 联想就没挡**);
#     · `热限制等级` 也读到了(报 0 = 无) —— 说明这个 API 在这台机器上是通的;
#     · 但**第一版只印前 8 个**, 而那 8 个全是 CPU/GPU 的 ⇒ **看不出有没有电池的那个**, 白费一次实验。
#   ⇒ 这一版改成:
#     · **名字里带电池字样的全印**(`batt` / `bms` / `charger` / `fg` / `ib`, 通常 0~3 个);
#     · 一个都没有时, 报总数 + 举 6 个例子(实测 135 字, 撑不坏弹窗);
#     · `_thermal_probe` 的扫描上限 24 -> 32(实测 0~23 连续 24 个, 留点余量)。
#   ⚠️ **别改回"全印"**: 24 个 zone 约 517 字, 而结果弹窗那一栏本来就排满到按钮了, 会撑爆。
#   ⚠️ 文案里**不许用 markdown 星号**(Kivy 不认, 会原样显示两个星号) —— 这一版初稿又踩了一次。
#
#   验证: --selftest OK · 两种形态(全是 CPU/GPU / 含 batt)的文本各验一遍(135 字 / 41 字)。
#
# 【v0.8.16 高压测试新增「热限制等级」+ 本机 thermal zone 能力探测(查电池温度台阶的根因)】
#
# 来由: 玩家 2026-09-17 一路追"电池温度曲线为什么是台阶"。**结论(已查证)**:
#   · 电池温度只走 `ACTION_BATTERY_CHANGED`, 而它被 `BatteryService` **限流** ——
#     温度变化 ≥1°C **或** 电量/电压/充电状态等字段变化才发。**台阶是系统给的, 不是画坏的**;
#     (这也解释了实测里那个 **0.61°C** 的跳变: 那一次广播根本不是温度触发的, 温度只是搭了车)
#   · 更底层的源(Health HAL / thermal sysfs)**在架构上存在**, 但普通 app 的 `untrusted_app`
#     域**通常读不到** —— `HardwarePropertiesManager` 要 `DEVICE_POWER`(系统级)权限,
#     `/sys/class/power_supply` 和 `/sys/class/thermal` 都被 SELinux 挡;
#   · **"通常读不到" ≠ "这台读不到"** —— 厂商可以自己开口子 ⇒ 所以加了**实测探测**。
#   ⚠️ 反向佐证: **GameBench 的官方文档写明它在 Android 上就是用 `BatteryManager` 读电池温度** ——
#      一个专业级性能工具接受了同样的限制。PerfDog 的 `BTemp` 不一样是因为它走 **PC + ADB +
#      手机 agent**, 权限边界完全不同 —— **不能拿它的效果反推"普通 app 也行"**。
#
# 新增两条(都印在高压测试的结果/日志里):
#   ① **热限制等级** —— `PowerManager.getCurrentThermalStatus()`(API 29+, **不需要权限**)。
#      这是和温度**互补**的维度: 温度说"多热", 它说"系统开始降频了没有"。
#      ⚠️ 它依赖设备的 **Thermal HAL 2.0**: 不支持的机器**恒返回 0(NONE)**, 而 0 也是"真没热限制"
#         ⇒ **两者分不出来**, 只能和温度一起看。拿不到返回 `None`, **绝不编个 0 冒充**。
#   ② **本机 thermal zone 探测** —— 试读 `/sys/class/thermal/thermal_zone{0..23}/{type,temp}`。
#      ⚠️ 读不到是**常态不是错误**, 全部吞掉; 单位是**毫摄氏度**(除以 1000)。
#      ⚠️ **别只认 type 里带 battery 的** —— 名字厂商随手起, 而且"叫 battery"也不代表它就是
#         BatteryService 用的那个源 ⇒ 全记下来由人比对。
#
# 🐛 **对抗评审抓出一个严重接线 bug(功能等于没上线, 已修)**: `thermal` / `zones` 两个键
#    **采集了、塞进 `self._hp_battery` 了, 但三个消费点谁都没搬它们** —— 现场弹窗(12274)、
#    存历史(12324)、历史详情(15789) 全漏。后果: 那两行**恒印"读不到"**, 在读数成功的真机上也一样,
#    而且印出来的正是"设备不支持 Thermal HAL"这种**假结论**。
#    ⇒ 教训: **"填好了数据源"不等于"接通了"** —— 这个项目里"同一个数要在三处搬运"是常态,
#      加字段必须三处一起加, 光测数据源本身测不出来。
# 另修三条(评审提的): `_act.POWER_SERVICE` → `Context.POWER_SERVICE`(全文件唯一一处
#    "从实例取 Context 常量", 失败还是静默的); 去掉多余的 `cast`; `_thermal_probe` 的**空文件
#    记 None 而不是 0.0**(那是看起来很真的假数); 老记录**缺键时整段不印**。
#
# 验证: --selftest OK · 三种情况(老记录/新记录读不到/新记录有 zone)的日志文本各验一遍。
#   ⚠️ **真机效果要等玩家装了才知道** —— 这段代码只在安卓上走, 桌面测不到。
#
# 【v0.8.15 平板宽度适配(两个历史记录弹窗的表格等比放大) + 修两处文案】
#
# ① **宽度适配**(玩家 2026-09-17: 「你要做的应该是**宽度适配**」)。
#    两个历史记录弹窗 —— 「测试历史(渲染/CPU)」`_show_bench_history` 与
#    「CPU高压测试历史」`_show_hp_history` —— 的表格列宽原来是**写死的 288dp, 而且只缩不放**:
#      · `_show_hp_history`: `_k = min(1.0, _tw_max / sum(_HW))` —— `min(1.0,…)` 封死了放大;
#      · `_show_bench_history`: **连缩放都没有**(`_table_w = sum(_HW)`)。
#    于是平板(等效竖屏 792dp、弹窗 760dp)上表格缩在中间、**两边各空 236dp**
#    (手机 444dp 上只空 69dp) —— 玩家说的"宽度问题"就是这个。
#    ⇒ 抽出 `RootWidget._fit_w(base_w)` 统一处理: 宽屏按可用宽度**等比放大**,
#      列宽 + 字号 + 行高 + 行间距**用同一个系数**(不然会"格子变大、字还是小的"),
#      上限 `_TW_GROW_MAX = 1.6`(玩家看过截图后定的)。
#    ⚠️⚠️ **只在宽屏放大** —— 门槛用 Android 的 `sw600dp` 惯例(等效竖屏窗口宽 > 600dp)。
#       手机上 `_tw_max`(388dp)本来就大于表格(288dp), 无脑按比例放大会把**手机侧本来就
#       不错的观感**一起改掉(玩家要的是「让 2 个界面都显示的不错」, 不是重做手机)。
#       实测: 手机档 `_k=1.0` 表格仍 **288dp**; 平板档 `_k=1.6` 表格 **461dp**。
#    ⚠️ 窄屏照旧**收缩**(`min(1.0, …)`) —— 360dp 那档小屏靠它防表格溢出弹窗
#       (`size_hint_x=None` 的子控件宽度不够时**不会自己缩**, 会直接飘到弹窗外)。
#
# ② **修两处文案**:
#    · 帧率曲线弹窗那行「点一下＝1:1 原始帧」的**全角等号 `＝` 在真机上是个豆腐块**
#      (玩家截图报「无法显示的文字」)。**桌面渲染同一个字符完全正常** —— 别拿桌面截图
#      当"没问题"的证据。已改成半角 `=`。
#    · 「1:1 原始图」这个说法**本身也是错的**(玩家指出): 放大图是**每帧占 2 像素**
#      (`ZOOM_PER_FRAME_PX`), 多出来的那 1 像素是**给两条线之间留的空隙** —— 线宽 1.15
#      比 1 像素还宽, 真按"1 像素 1 帧"排会叠死。"1:1"说的是**数据口径**(每帧一个点),
#      不是像素比, 混在一起就会误导。两处都改成只说数据口径:
#      「点一下看每一帧的原始值」/「左右拖动查看 · 逐帧原始值」。
#
#   验证: --selftest OK · 桌面 `temp/_tbl_probe.py` 模拟两种屏宽量宽度(手机档 288dp
#     与改动前一致 / 平板档 461dp), 截图 `temp/tbl_bench_792.png`。
#
# 【v0.8.14 放大图密度 3px -> 2px(玩家看过 1/2/3/6 四张对比之后定的)】
#   玩家 2026-09-17: 「改为 1:2, push 下」。
#     · 1px 不行 —— 点间距 1 比线宽 1.15 还小, 相邻线段完全重叠, 曲线糊成一条粗毛线;
#     · 2px 是他选的 —— 比 3px 少拖 5 屏(约 8 屏 vs 13 屏), 而空隙够用;
#     · 6px 是**我误判**加上去的(v0.8.12), 当天就被打回(v0.8.13)。
#   四张对比图都在 `output/fps_raw_1to{1,2,3,6}.png`(5263 帧 = 5363 / 10626 / 15889 /
#   31678 px), 玩家就是拿它们定的。
#   ⚠️ 附带一条方法教训(已进记忆): 判断"清不清楚"必须**裁一小块按原尺寸看**, 不许看整张
#      被缩放的图 —— 我拿 Read 看自己渲染的 15889px 宽 PNG, 工具把它缩到约 1000px 显示,
#      于是 3px 的曲线在我眼里糊成一片, 我据此改成 6px; 玩家在原尺寸下看, 3px 完全够。
#
#   验证: --selftest OK(只改一个浮点常量, 不涉及任何逻辑)。
#
# 【v0.8.13 放大图密度 6px -> 3px(改回玩家定的值; v0.8.12 改 6 是我误判)】
#   玩家 2026-09-17: 「3像素挺好, 别改了, 我就要 3」。
#   ⚠️⚠️ **v0.8.12 改成 6px 是误判, 根因是"看图的姿势"**: 我用 Read 看渲染出来的 PNG, 而那张
#      图 15889px 宽、显示时被**缩到约 1000px**, 于是 3px/帧的图在我眼里糊成一片, 我据此
#      断定"要 6px 才看得清"。玩家是在**原尺寸**下看的 —— 3px 完全够。
#      **这个坑本会话踩了两次**(另一次是 `temp/scan_x1.png`)。
#      ⇒ 判断"清不清楚"必须**裁一小块按原尺寸看**, 不许看整张被缩放的图。
#   三张密度对比图留在 `output/fps_raw_1to{2,3,6}.png`(5263 帧 = 10626 / 15889 / 31678 px),
#   玩家是拿这些定的。横向要拖多久也由它决定: 3px ⇒ 约 13 屏(一屏约 2.5 秒)。
#
#   验证: --selftest OK(只改了一个浮点常量, 不涉及任何逻辑)。
#
# 【v0.8.12 帧率曲线: 放大图密度 3px -> 6px + 弹窗新增一行口径说明】
#   ① 放大图里**每帧占的宽度 3px -> 6px**(玩家定)。3px 时点间距只比线宽(1.15)大一倍多,
#      相邻线段挤在一起, 整片糊成一条黑带 —— 实测对比见 `temp/den_1.png`(每帧 1px, 最糊)、
#      `temp/fps_density.png`(3/6/10 三档)。6px 才看得清每一帧。
#      代价: 5263 帧 x 6px = 31618px, 手机上约拖 26 屏(一屏约 1.2 秒)。
#      ⚠️ "每帧 1 像素"是**做不到**的, 别被这个直觉带回去: 那是"每帧画一个点"才成立,
#         而这里画的是**折线**, 线有 1.15 像素粗、纵向还跨 20 多像素(相邻两帧差 15fps),
#         1 像素的间距会让线段完全重叠。
#   ② 曲线弹窗的统计行下面**新增一行口径说明**(措辞是玩家逐字定的):
#      「本图每若干帧合一个点、取最慢的那帧（看着比实际差）· 点一下＝1:1 原始帧」
#      ⚠️ 我原先写的是"每组取最慢", 玩家当场反问「什么叫每组」—— "组"是**我自己造的词**,
#         他从没这么说过。教训: 引入他没讲过的说法之前先跟他对齐, 别当成共识用。
#
#   验证: --selftest OK · fx_probe **18 项**(与 9/16 基线逐条相同)。另多出的 1 项
#     「球贴图 杯中球 画布 128x128 (256, 256)」**与本次无关** —— 是 v0.8.10 把
#     `_CUP_BALL_TEX_PX` 改成 256 之后, fx_probe 里那条断言(还写着 128)没跟着更新。
#
# 【v0.8.9 放大图改成 1:1 原始图(每帧一个点)】
#   玩家 2026-09-17 看过两种粒度的对比后定: 「缩小图用多个帧的最小 / 放大图用 1:1 的图,
#   支持横向拖拉」。小图(每 N 帧取最慢)不动, 只把放大图的粒度从"每 2 帧"改成"每 1 帧"。
#   实现只改一个数: `FpsCurve.ZOOM_PER_FRAME_PX` 3.0(每帧一个点) / 0.5(每 2 帧) /
#   0.34(每 3 帧) —— 它是放大图的**唯一旋钮**, 换粒度不用碰任何绘制代码。
#   ⚠️ 1:1 的观感是**顶部一条厚带**(实测 `temp/zoom_per3.0.png`), 不是 bug: 相邻两点的
#      y 差就是数据本身的逐帧抖动量(那台 165Hz 在 150~165 乱跳)。降采样那版
#      (`temp/zoom_per0.5.png`) 才是一条清晰的线 —— 两者都留了图, 便于以后翻案。
#   ⚠️ 顺手修掉一个**文案与画面不一致**的 bug: 那句"每 N 帧一个点"是按浮点宽度算的,
#      而 `_draw` 按 Kivy 取整后的控件宽算 —— 实测 2671.5 vs 2672 这一个浮点差, 就让
#      文案说"每 2 帧"、画面画"每 3 帧"。现在文案写死「1:1 原始图」(由 PER>=1 保证)。
#   ⚠️ 宽度 = 帧数 x 3px: 5263 帧 => 15789px, 手机上约拖 13 屏(一屏约 2.5 秒)。
#   ⚠️ 常量长在 `FpsCurve` 上而用它的方法长在 `RootWidget` 上 ⇒ 必须写 `FpsCurve.` 前缀,
#      写 `self.` 会 AttributeError。
#
#   验证: 端到端跑真 App(弹窗 / ScrollView / 曲线 width=15829 横轴=15789 每 1 帧一个点 /
#     可滚 15359px); --selftest OK; fx_probe **与改动前逐条相同**(18 项, 全部预存在)。
#
# 【v0.8.8 「点击曲线放大成长条图」(可左右拖) —— 玩家提的, 当天做完】
#   玩家 2026-09-17: 「点击放大 可以变为一个长条图 可以横向滚动不就可以了」。
#   ⇒ `FpsCurve.on_touch_down` 点本体就调 `on_zoom` 回调; `RootWidget._fps_curve_zoom`
#     弹一个 `ScrollView`(横向), 里面是一条**按帧数定宽**的长条曲线。
#   ⚠️ **一行绘制代码都没改**: 本控件的"每组几帧"本来就是 `ceil(总帧数 ÷ 横轴像素数)`
#      算出来的 ⇒ 控件一拉宽, 粒度自动变细。小图横轴约 880 像素 ⇒ 每 6 帧一个点;
#      长条图拉到 `ceil(帧数/2)` 宽 ⇒ **每 2 帧一个点**。
#   ⚠️⚠️ **"每帧一个点"是做不到的 —— 会糊回一块实心**(实测扫过五档, `temp/scan_x*.png`):
#      **糊不糊由"每组几帧"决定, 不由放大倍数决定**。逐帧时相邻两点的 y 差**就是数据本身的
#      逐帧抖动量**(那台 165Hz 实测在 150~165 之间跳), 相邻线段一正一反交替, 直接把整个
#      区间填满 —— 跟 v0.8.6 要修的病根是同一个东西。实测: `_grp=1` 糊 / `_grp=2` 清楚 /
#      `_grp=3` 更清楚。所以放大图的目标是 **`_grp ≈ 2`**(比小图细 3 倍), 不是逐帧。
#   ⚠️ 宽度必须用 `ceil` 反推: 写 `len(gaps) / 2.0` 在 5263 帧时给 2631.5, 差 1 像素
#      `_grp` 当场从 2 掉到 3(实测踩到过)。
#   ⚠️ 标题和"每 N 帧一个点"那句**都是算出来的**, 不许写死 —— 我第一版写死了"逐帧/每帧一个点",
#      而实际是每 2 帧, 截图当场抓出来。
#   ⚠️ 放大图**不带 `on_zoom`**(免得点一下又套一层弹窗); 阶段条照画。
#   ⚠️ 接线: `FpsCurve` 是纯绘制件, **不认识弹窗那套**(`_popup` 长在 RootWidget 上) ⇒
#      回调由 `_show_fps_curve` 传进去, 两处调用点(现场/历史)自动都有。
#
#   验证: 端到端跑**真 PlinkoApp**、调**真 RootWidget 方法**(`temp/_zoom_e2e.py`):
#     弹窗弹出 / ScrollView 在 / 曲线 width=2672 横轴=2632 **每 2 帧一个点** / 可滚 2202 px;
#     视觉见 `temp/zoom_popup.png`(弹窗整体)与 `temp/scan_z2672.png`(曲线本体)。
#   ⚠️ 踩到的测试坑(与出货代码无关, 但下次别再花时间): `App.root` 是 **LandLayer** 不是
#      RootWidget(得递归找); **Popup 挂在 `Window` 上, 不在 `App.root` 那棵树里** ⇒
#      `root.export_to_png()` 拍不到弹窗, 要 `popup.export_to_png()`。
#   门禁: --selftest OK · fx_probe **与改动前逐条相同**(18 项失败, 全部预存在)。
#
# 【v0.8.7 帧率曲线定稿: 每 N 帧取最慢(N 由横轴像素数反推) + 顺手修一个既有崩溃】
#   来由: v0.8.6 的「抖动带 + 中线」**当天就被玩家否了** —— 带子上沿会跟着**追赶帧**一起
#     冲到图表顶。所谓追赶帧就是"两帧之间只隔 8.7 毫秒"那种(165Hz 那轮实测最短 1.53 毫秒),
#     换算出来是 115~650 fps, 可它**根本不是性能问题**(反而是跑太快、上限没压住),
#     却比真的卡顿还抢眼, 整张图就没法看了。
#   ⇒ 玩家在三张对比图里选了**「每 0.1 秒取窗口内最慢的那一帧」**(口径保守, 但绝不漏卡顿;
#     那台设备上平均 164.9 / 中位 165.1, 可 165 的曲线会把 5 次长卡顿全吃掉, 而"最慢"抓得住),
#     随后又**当场纠正了窗口宽度** —— 见下。
#
#   ⚠️⚠️ **窗口宽度不是写死的秒数, 由横轴像素数反推**(玩家 2026-09-17 原话:
#      「不能按照 0.1 秒取最慢, 应该是算 32 秒 × 165 帧 … 看看应该是多少帧合并为 1 帧」):
#        · 真机那一轮 5263 帧 / 31.92 秒, 横轴约 880 像素(屏宽1220 × 0.92 弹窗 − 各处内边距);
#        · ⇒ **每 6 帧合成一个点**(`_grp = ceil(总帧数 ÷ 像素数)`), 取这 6 帧里最慢的那一帧;
#        · 而写死的 100 毫秒 = **16.5 帧** —— 一组宽了 2.8 倍, "最慢"就更容易从十几帧里
#          抓到一个慢帧, 整条曲线被人为压低、抖成一片。实测对比图: 旧版在 145~165 之间摆,
#          新版主体贴着 165, 而那些真卡顿的深尖刺(探到 85~90)一根没丢。
#      ⚠️ `_grp` 用 `ceil` 是为了保证**点数 ≤ 像素数** ⇒ 横轴铺满又不会糊(糊的病根是
#         "一个像素塞好几个点", 这里恒为 1:1); 帧数少于像素数时一组一帧, 点数 = 帧数,
#         更稀, 同样不会糊。
#      ⚠️ x 仍按**累计真实时间**定位(不是按点数均分)—— 一个 200 毫秒的长卡顿在横轴上
#         占的宽度是真实的, 不会被压扁。
#      ⚠️ 也别再"帮"玩家改回中位/平均: 那正是他要避开的(中位会把两帧的卡顿稀释掉)。
#
#   🐛 **顺手修掉一个既有崩溃**(不是本次改出来的): `FpsCurve._draw` 里画阶段条那段的
#      `self._tags[_i]` 是**裸着**的, 而 `__init__` 的注释白纸黑字写着"标签长度对不上就当
#      没有 —— 曲线照画, 只是不画阶段条, 绝不让诊断把曲线本身搞没"。实际是没传标签(或长度
#      不匹配)时直接 IndexError, **整张曲线一起没**, 承诺的行为和代码正好相反。出货路径
#      (`_show_fps_curve`)每次都给等长标签, 所以线上从没撞到过 —— 是画对比图时拿
#      `FpsCurve(gaps, cap_fps=...)` 裸调才炸出来的。已加 `if self._tags:` 闸。
#
#   验证: --selftest OK · fx_probe **与改动前逐条相同**(18 项失败, 全部**预存在**, 系
#     `tools/` 与 `main.py` 分叉所致)⇒ 本次没引入新失败。
#   对比图(同一份真机数据/同一纵轴): 三方案 `temp/fps_3plans.png`、窗口宽度
#     `temp/fps_window_cmp.png`。
#
# 【v0.8.6 帧率曲线改成「抖动带 + 中线」(玩家报"糊成一块实心")】
#   来由: 玩家指着帧率曲线弹窗说「我发现一个帧率曲线的绘制问题」。那一轮在 165Hz 设备上
#     **5263 帧 / 横轴 785 像素 = 每像素列塞 7 帧**, 而逐帧折线是**不降采样**的(一帧一个点)。
#     实测这一列里那 7 个点的纵向跨度中位就有 **9fps(约 16 像素)**, 折线在同一列里上下横跳、
#     自己跟自己交叉 ⇒ **糊成一块实心色块**: 深色从图表顶一路铺到 150 那格, 读起来像
#     「帧率一直在 150 上下剧烈波动」。可那一轮**平均 164.9 / 中位 165.1**(基本满帧跑满),
#     而**真正该被看见的几次长卡顿(伸到 90 以下那几根)反倒被糊掉的地基淹了**。
#
#   改法(三个方案里玩家定的「画抖动带 + 中线」): 每列只留三个数 ——
#     · 最慢~最快 画成一条**半透明抖动带**(同 PresentMon 那类专业帧率工具的画法):
#       带子越厚 = 那一刻抖得越厉害;
#     · **中位**画成实线叠在带子上 —— 它的位置才是"这一刻到底跑得怎么样"。
#     中位是**每列各取各的**, 不是全局中位(全局中位会变成一条平线, 把"哪一段时间在抖"丢光)。
#   两个参数: `FpsCurve.BAND_ALPHA = 0.30`、`FpsCurve.MIN_FRAMES_PER_COL = 16`。
#
#   ⚠️⚠️ **两个坑, 都是实测撞出来的, 别再踩**:
#    ① **`Mesh` 在 Kivy 2.3.1 上根本不渲染**。第一版想用一条 `triangle_strip` 把带子一次
#       画完(最省指令), 结果**画面上一个像素都没有**。最小样例钉死了原因: 同一场景里
#       `Line` 好好在屏幕上, `Mesh` 完全不出 —— 它没绑纹理时 sampler 取不到东西。
#       现在改用项目里到处在用的 `Rectangle`(每列一个; 列宽 `+0.6px` 是**盖缝**用的,
#       相邻矩形共享一条边, 浮点取整会在中间留出 1px 亮线, 整幅宽排下来就是一片竖条栅栏)。
#    ② **列数不能按"横轴像素数"定, 要按"每列至少攒够多少帧"定**。按像素切的时候一列只有
#       7 帧, **连中位数都在 165 和 150 之间反复跳** ⇒ 中线自己就长成一排锯齿, 还把带子
#       整个盖住(实测截图确认过, 等于白改)。`MIN_FRAMES_PER_COL = 16` 才稳得住;
#       列数上限仍是横轴像素数, 所以宽屏不会变粗。
#     另: 空列做了**前向填充再反向补头** —— 低帧率段 + 宽屏时一列一个点, 空列能占七成,
#       留缝的话带子会碎成一片虚线。填充用的是相邻列的**真数据**, 不是插值。
#
#   验证: --selftest OK · fx_probe **与改动前逐条相同**(18 项失败, 全部**预存在** ——
#     `tools/` 与 `main.py` 分叉导致, 见 android/CLAUDE.md) ⇒ 本次**没引入新失败**。
#   另用合成数据做了**分辨力测试**(阴性对照): ①满帧→83fps→满帧 三段式, 中间必须明显
#     下陷(实测下陷到 83, 带子只在两个过渡处变宽); ②满帧 + 每 60 帧插一个长帧, 带子必须
#     戳出**等距**尖刺(实测 9 个等距尖刺, 与阶段条对齐)。两条都正确。
#
# 【v0.8.3 跑分别息屏 + 频率采样掐头去尾且只看跑分核 + 新增「频率曲线」】
#
# ① ⚠️⚠️ **跑分期间不让屏幕息屏**(玩家报的 bug: 「跑分的过程中(**包括 cpu 压力测试和
#    那个 45s 测试**等), 都应该**阻止屏幕进入黑屏(待机)休眠**, 现在没有阻止」)。
#    · 用**窗口标志 `FLAG_KEEP_SCREEN_ON`**, 不是 WakeLock: 前者**不需要权限**
#      (WakeLock 要 `WAKE_LOCK`, 本工程只声明了 VIBRATE), 而且**随窗口可见性自动失效**
#      (切后台/退出就放行), 不会泄漏成「一直亮着吃电」。
#    · 与系统栏那两档**共用同一个 UI 线程任务**(`ImmersiveTask`), 切一次只投一趟 JNI;
#      开关挂在**同一对** `_show_bench_dim` / `_hide_bench_dim` 上 ⇒ 波 1 波 2 自动都覆盖。
#    · ⚠️ **这不是体验优化, 是正确性**: 屏幕一灭, 安卓会**暂停游戏、冻结 Clock**
#      (`BUILD_APK.md` §3.22 那个坑)⇒ **6 分钟的高压测试会被打断, 成绩是假的**。
#    · 门禁 `temp/_sysui.py` 加了 W 系列(W1~W7), 并做了**阴性对照**:
#      手工删掉 `_hide_bench_dim` 里那句 `_set_keep_awake(False)` ⇒ **4 条变红**
#      (W1/W3/W4b/W6), 还原 ⇒ 19 全绿。
#
# ② **CPU 频率采样掐头去尾**(玩家: 「调整为**只对第1秒到第359秒生效, 掐头去尾**」)
#    ⇒ 360 秒的局只统计 **[1, 359] 秒**这一段。两端本来就不该算: **头**那一秒 CPU 才
#    刚被拉起来(还在爬频), **尾**那一秒测试已在收尾、下一轮的调度动作也在动。
#    与跑分成绩那边「掐头 0.5 秒」是同一条思路 —— 只在跑匀了的那一段上量尺子。
#    ⚠️ 窗口用**绝对时刻**表达、与 `_hp_wall0` 同源; **不是**「线程跑起来之后 N 秒」
#       —— 那会差出锁核/探测拓扑那几十毫秒。
#    ⚠️ 采样线程**仍在锁核之前起**: `os.sched_setaffinity` 只改**调用线程**, 而**新线程
#       继承创建者的亲和性** —— 晚起会被钉到同一批性能核上, 反过来跟被测线程抢核。
#       所以只能「先起线程、后填窗口」(传的是个**可变容器**)。窗口没填时**一个都不收**。
#    波 1 不传窗口(它没有掐头去尾这回事), 行为不变。
#
# ③ ⚠️⚠️ **频率只看「跑分核」**(玩家: 「这个频率应该看的是**大核的数据**吧?」)
#    · 原来 `_frq` 装的是 `_cpufreq_mhz()` = **所有核里的最大值** —— 只要任何一个核
#      (哪怕跑分线程根本不在上面)瞬间冲到最高, 那一格就记那个数, 报出来的不是
#      「我这列车跑多快」。
#    · 而跑分线程是**锁核**的(`_bench_pin_fast_cpus` 把 affinity 收到最高性能簇,
#      面板那行「跑分只跑这几个核：cpu6、cpu7」就是它)⇒ 现在按 `_BENCH_CPU_PIN` 里的 actual
#      **过滤**, 只取那几个核的最大值。
#    · ⚠️ 锁核信息**延迟才有** ⇒ 每个 tick **现读一次**, 不缓存; 锁核失败/未锁核时
#      `actual` 为空 ⇒ **退回「全体取最大」**(老行为)。
#    · ⚠️ **面板标签跟着口径走**: 锁上了印「**跑分核频率：**」, 没锁上印「CPU 频率：」
#      —— **别把这两个标签合并**, 它们是两个不同的数(同词盖两义正是这工程栽过的坑)。
#      记录里存 `freq_pinned` 供历史详情复用同一个判断。
#    · 顺带省掉一遍读盘: 原来一个 tick 里 `_cpufreq_mhz()` 与 `_cpufreq_cores()` **各读一遍**
#      sysfs(合计最多 32 次 open), 现在**一次读全、两处共用**。
#
# ④ **新增「频率曲线」按钮**(玩家: 「CPU高压测试, 新增按钮 频率曲线(**规则和之前那个
#    曲线的 y 轴坐标一样**), 放在**成绩曲线和关闭按钮的中间**」)。
#    · **复用同一个 `SpeedCurve`** ⇒ 纵轴那套规则(上下留白 + 向外取整到友好刻度 +
#      保底离底 5%)**自动一致**, 一行都不用抄。`_show_hp_curve()` 因此泛化成
#      「一条曲线」: 标题 / 单位 / 一个点代表什么 由调用方给, 两条曲线共用这一份。
#    · 现场弹窗**与**历史详情**都加**; 位置按玩家点的: 成绩曲线 → **频率曲线** → 关闭。
#      ⚠️ 位置就是**加进 BoxLayout 的先后**, 挪前挪后都会改版面 ⇒ 门禁按**几何判据**
#         (三个按钮的 x 坐标递增)钉住 —— 别用 `children` 下标, 那是**反序**的。
#    · 数据: 现场用内存里那一轮的 `_hp_freq_series`(排序**之前**留的原序副本,
#      `_frq.sort()` 会毁掉时间轴); 历史用记录里新存的 `freq_series`。
#      ⚠️ 老记录没有这个字段 ⇒ **自然不建按钮**, 不印假图。
#      ⚠️ 体积: 实测 ~344 个四位数 ≈ **1.7KB/条**, 与 `windows` 同一量级
#         (历史表上限 100 条 ⇒ 这一项总共约 +170KB)。可接受, 但**别再往里塞第二条序列**。
#
# 验证: --selftest OK · `_sysui` **19**(+阴性对照 4 条变红) · `hp_wall_probe` **20**
#      (新增 T1~T4: 窗口只收 [1,2] 秒内的样本、未填窗口一个不收、窗口与 `_hp_wall0`
#       同源、采样线程在锁核之前起、波 1 不传窗口) · `_hpcurve` **12**(新增 C6/C7:
#       频率曲线按钮存在 + **几何判据**钉住三段顺序 + 点数对得上 + 标题/MHz + 复用
#       同一个 SpeedCurve) · `_verify` **48** · `_benchdetail` **31** · `_curveaxis` **24** ·
#      `_physprog` **8** —— 全绿; 截图看过(频率曲线弹窗: 标题/`3950-3125-2300` 纵轴/
#      `横轴 = 按时间顺序的 321 个采样　竖轴 = MHz`) · 纯 LF(CRLF=0) · 未跑 `tools/` 生成器。
#
# 【v0.8.2 成绩面板: 「飞行平均持续」->「平均持续」】
#
# ① 成绩面板那一行: 「**飞行**平均持续 x.xx 秒」-> 「**平均持续** x.xx 秒」(去掉「飞行」)
#    ⚠️ 这一处**已经改过三次**, 沿革如下(**别再当成"哪版写错了"**):
#       v0.7.109  平均持续 x.xx 秒
#       v0.8.0    飞行平均持续 x.xx 秒   (玩家: 「去掉句号, 同时把平均改为飞行平均」)
#       本次       平均持续 x.xx 秒      (玩家: 「把飞行平均持续x.xx秒改为平均持续x.xx秒」)
#    两版宽度差 30px(sp15), 但那一行**本来就不折**(改后 271px, 面板给 438px)
#    ⇒ 纯粹是措辞偏好, 与排版无关。
#
# 验证: --selftest OK · `_benchdetail` **31**(B1/B1c 两条断言同步: 正文里**不得**再出现
#      「飞行平均持续」、B1c 仍钉硬回车 + 结尾无句号) · 纯 LF(CRLF=0) ·
#      未跑 `tools/` 生成器(工具链与 main.py 已分叉)。
#
# 【v0.8.1 菜单说明重排成 3 条 + 标题改「三项」+ 弹窗加宽到 0.92】
#
# ① 玩家重排菜单说明(原话: 「测试两项设备性能后的文字改为(从 **4 行减少到 3 行**)」),
#    逐字照抄他给的三条:
#      1、累计发射5颗弹珠，测屏幕渲染帧率
#      2、用python物理引擎，测CPU单核浮点
#      3、用高压测试，考验CPU调度和散热
#    ⚠️ 三条的逗号都是**全角 `，`**(半角 0 个)。玩家前后定了四次: 半角 -> 全角 -> 半角
#       -> 最后问「**话说不能把窗口宽度变宽一些来解决吗? 全角很美啊**」⇒ **把弹窗加宽**,
#       全角才放得下(见下)。
#    原来那 4 行(「1. 累计发射…」/「2. 通过后台跑物理引擎…」/
#    「Python 写的引擎，消耗…」/「CPU高压测试：考验…」)被并进这 3 条里,
#    菜单从 6 行变 **5 行**。
#    ⚠️ 玩家给的文本**逐字照发** —— 含「1、」这个**顿号编号**(原来是小圆点)、
#       小写 `python`、数字两侧**不加空格**。别自作主张改标点。
# ② 标题「测试**两**项设备性能：」->「测试**三**项设备性能：」
#    (列表已经是 3 条了 —— 这条是**玩家自己随后抓到的**, 不是我提的。)
# ③ ⚠️⚠️ **菜单弹窗宽度 0.88 -> 0.92**(玩家: 「话说不能把窗口宽度变宽一些来解决吗?」)
#    ⇒ 360dp 上正文可用宽 **261px -> 275px**, 全角逗号那三条这才放得下。
#    ⚠️ **0.92 不是新发明**: 另外两张历史面板早就用 0.92, 菜单是唯一还停在 0.88 的 ——
#       改完几个弹窗的宽度也就统一了。
#    ⚠️ 改这一行**必须同步 `temp/check_desc.py` 的 `AVAIL`**: 那个式子原来把 0.88
#       **手抄**在里面, 不改就会按旧宽度算 ⇒ **假绿**。已改成**从 main.py 正则读**这个数。
#
# ⚠️⚠️ **整件事是"宽度是量出来的, 不是拍的"的又一个实例** —— 三行文字改了四轮、
#    弹窗宽度也动了一次, 每一步都先量再改:
#    · 最初的「2、用python写的物理引擎…」= **288px**, 而 0.88 只剩 261px ⇒ 超 27px。
#    · 问「有空间就改全角」⇒ 全角逗号 15px / 半角 4px(**每换一个 +11px**), 那行要 299px。
#    · 玩家把第 2 条瘦成「用python**物理引擎**」(去掉「写的」)= 半角 258px。
#    · 再问「现在能改全角了吧」⇒ 全角 269px, **还差 8px**。
#    · ⇒ **改宽度**: 0.92 给 275px, 269px 放得下(余 6px)。
#    ✅ 定稿逐行: 219 / 135 / 258 / **269** / 250 px —— **最宽 269px, 360dp 余量 6px**,
#       整整 5 行**一行都不折**。
#
# 验证: --selftest OK · `temp/check_desc.py` 逐行量过(可用宽 275px, 最宽 269px) ·
#      纯 LF(CRLF=0) · 未跑 `tools/` 生成器(工具链与 main.py 已分叉)。
#
# 【v0.8.0 删两处版本号/参数噪声 + 高压曲线说明简化】
#
# ⚠️ **版本号比上一条小**(1.0 -> 0.8.0, 玩家定的), 但**时间在后** ⇒ 本条目排在
#    v1.0 **之上**(这段按时间倒序)。**装包不受影响**: v1.0 那一版已经给
#    `buildozer.spec` 加了 `android.numeric_version = 1000000` —— versionCode 与
#    versionName 从此脱钩, 版本名怎么写都不会让安卓判成降级、拒绝覆盖安装。
#
# ① CPU 高压**历史详情**头一行: 删掉**记录当年的版本号**
#    (玩家截图圈出那个 `v0.7.111`: 「删除这里的版本号」+「也就是后面是 0.7.111 的
#     这个字段」)⇒ 现在只留 **设备 + 时间**, 与现场那个结果弹窗**一致**
#     (那边头一行是 `_device_info()`, 本来就只有设备 / 安卓版本; 版本只在**标题**里)。
#    ⚠️ 记录里的 `version` 字段**照旧存着**(存档格式没动), 只是不上屏。
#
# ② 提权那一行: 删掉 `（-4 → -8）`, 并把冒号改成逗号
#    (玩家: 「删掉这个界面中的（-4→-8）这个**没有啥可读性**, 有前面的中文就够了」
#     + 「把**已提权：比画面**中的**分号改为逗号**」)
#    ⇒ 现在两行是: `跑分线程已提权，比画面渲染更优先` /
#                   `跑分线程没提权，可能被画面抢 CPU（未执行）`
#    ⚠️ 两行**一起**改(同一种句式留两种标点, 正是这项目反复被打回的那种「排版送把柄」)。
#    ⚠️ 那两个数**没丢**: 日志里「物理跑分工作线程优先级: **已提升** -4 → -8」
#       一个字没动, 复盘照样能看。
#
# ③ 高压成绩曲线弹窗: 删**两段说明**
#    (玩家: 「**删掉第1段话**(逐秒/秒 到 最高xxx), **删掉第2段话中括号的内容**
#     就是那个横线等于啥」)
#    · 第一段(`逐秒步/秒 首 x 末 x 中位 x 最低 x 最高 x`)那个 Label **整个删掉** ——
#      连 bind 与 add_widget 一起, 不是清空文字。
#      (那几个数在**详情面板**里照旧有: `_hp_result_text` 头几行就是首/末/最低/平均;
#       所以删它**不丢信息**, 只是不再在图上重复一遍。)
#    · 第二段的 `（横线 = 纵轴刻度）`(就是 v1.0 那次我从「中位」改过来的那句)也删掉
#      ⇒ 现在只剩 `横轴 = 按时间顺序的 N 个窗口　竖轴 = 步/秒`。
#    ⚠️ 删掉一个 Label 之后弹窗矮一行(dp(22) + dp(8) 间距), 高度由
#       `_popup_fit_content` 按内容自己算, 定高控件那一套没动。
#
# 验证: --selftest OK · `_verify` **48** · `_sysui` **10**(含阴性对照) ·
#      `_benchdetail` **31** · `_curveaxis` **24** · `_hpcurve` **6** ·
#      `_physprog` **8** —— 全绿; 纯 LF(CRLF=0) · 未跑 `tools/` 生成器
#      (工具链与 main.py 已分叉)。
#
# 【v1.0 版本号换方案(1.0 起) + 系统栏两档 + 面板排版/字号统一】
#
# ① **版本号方案换成 `1.0` / `1.1` / `1.2` …**(玩家: 「把版本号改为 1.0
#    （后续是 1.1, 1.2, 1.1000）之类」)。上面那些 `v0.7.x` 是**历史记录**, 不改。
#    ⚠️⚠️ **同时给 buildozer.spec 加了 `android.numeric_version = 1000000`** ——
#       原因: `version` 只决定 versionName(显示用的字符串), 管「能不能装上去」的是
#       `android:versionCode`; 那个数**本文件从没定义过**, 一直是 buildozer/p4a
#       自己推的(buildozer 1.5.0 只在 `android.numeric_version` 有值时才把
#       `--numeric-version` 传给 p4a), 很可能与 `version` 字符串有关。
#       ⇒ 版本号从 `0.7.111` 退回 `1.0`, 万一 versionCode 也跟着退, 安卓会**拒绝安装**
#         (`INSTALL_FAILED_VERSION_DOWNGRADE`), 想装新版就得**先卸载** —— 那会把
#         玩家攒的**历史一起清掉**(高压历史 / 性能测试历史 / 弹珠数)。
#       ⇒ 显式钉一个大常数: 不管旧包推到多少, 新包永远更大、永远装得上。
#       **别删那一行**, 也别改成跟 `version` 联动的写法。
#
# ② CPU 高压结果弹窗**去掉一个多余的空行**(玩家 2026-09-16 看截图:
#    「**这里不需要换行, 这里有一个空的换行**」)—— 位置在「平均差系数 x.xx%」
#    与「每10秒连续采样成绩：」之间。
#    实现上原先是 `_n + _n`(两个换行 = 空一行), 现在**只留一个**。
#    ⚠️ 那个空行是 v0.7.109「历史详情改用结果弹窗那套」时**从旧版式带过来的** ——
#       详情原来分「成绩 / 频率 / 过程」三块, 块间空行有意义; 合并成一份渲染之后
#       就是多余的了。
#
# ③ 菜单第 5 行的动词**连着改了三次**: 「吃」-> 「考验」-> 「衡量」-> **「消耗」**
#    (玩家最后一句: 「这个**衡量不妥当**, 改为**消耗**」) ⇒ 现在是
#    「Python 写的引擎，**消耗**单核浮点算力。」
#    ⚠️ 实测 **264px**, 而 360dp 只有 **261px** ⇒ **窄屏折行, 差 3px**(末尾「算力。」
#       会被孤零零甩到下一行)。**换词救不了** —— 考验/衡量/消耗都是两个字, 宽度一样。
#       三个候选已量给玩家(去句号 249px / 去「写的」234px / 「Python 引擎消耗…」219px),
#       **玩家尚未定夺 ⇒ 本版保持原样**。其余 5 行最宽 256px, 都有余量。
#
# ④ 菜单那 6 行**去掉全部句号**(玩家: 「去掉这个界面的所有的句号」)—— 带句号的是第
#    1 / 5 / 6 行, 三处全去掉。⇒ **顺带把第 5 行那个 3px 折行也解决了**(264 → 249px,
#    360dp 可用 261px, 现在最宽一行 256px、余量 5px)。
#
# ⑤ 成绩面板: 「**N 轮**分数依次为：」-> 「**分数依次为：**」(玩家: 「把 5 轮分数依次为：
#    改为 分数依次为:」)。⚠️ 改格式串的同时**必须删掉对应的那个实参**
#    (原来是 `len(_runs) if _runs else ...`) —— 少一个 `%d` 却留着实参, 后面的数会
#    **整体前移一格且不报错**(`%s` 什么都吃得下)。已实测两种记录都不串位。
#
# ⑥ ⚠️⚠️ **系统栏改成两档**(玩家 2026-09-16 分两次说完):
#      · 「打开游戏后会全屏, 需改为**不全屏**(我记得之前的版本都是, 能显示顶部的状态栏)」
#        +「我要**非沉浸式全屏**, 能看到状态栏的」
#      · 「做物理验算的时候**全黑屏**的时候, 这个时候需要**真全屏**(高压和不高压都算)」
#    ⇒ 平时**非沉浸**(状态栏 + 导航栏都在) / 跑分黑屏期间**真全屏**, 由黑屏的开与关切换
#      (挂在 `_show_bench_dim` / `_hide_bench_dim` 上 —— 波 1 波 2 **共用这一对**, 所以
#       "高压和不高压都算"是自动满足的)。新增 `_SYSUI_MODE` + `_set_system_ui()`。
#    ⚠️ **这不是新增 bug**: `git show 8d5af0b~1:main.py | grep -c SYSTEM_UI_FLAG` = **0**
#       —— v0.5.1(`8d5af0b`, 2026-08-26) 之前**根本不碰系统栏**, 那正是玩家记得的样子。
#       而"老版本能看见状态栏"还叠着另一个 bug 的症状(竖屏启动时沉浸没生效、转屏才自愈,
#       见 `BUILD_APK.md` §3.22 —— v0.5.4 `9873ab0` 才修好)。
#    ⚠️ 非沉浸档下 `_enter_immersive` **直接 return**(那一档没有需要重申的东西);
#       切档是**立即生效**的(不等 2.5 秒的周期重申), 否则黑屏撤了还会继续全屏一会儿。
#    ⚠️ 真机效果**桌面测不出**(平台分支直接 return) ⇒ 新增门禁 `temp/_sysui.py`(**10**),
#       量的是**档位状态机**(真行为) + 出货源码的形状; 并做了**阴性对照**
#       (手工删掉 `_hide_bench_dim` 里那句 `_set_system_ui(False)` ⇒ S3/S4b/S7 **三红**)。
#
# ⑦ 成绩面板灰色诊断块: **中文圆点 `·` 换成两个半角空格**(玩家: 「这里的**中文圆点**用
#    **半角空格**来代替」)。两处: 「采样窗口：x 秒 · y 帧 · 中位 z 帧/秒」与
#    `_dist_line_of` 里那句 `' · '.join(...)`(分布行)——**一处改两处都得改**,
#    否则同一个面板里两种分隔符并存。用两个空格而不是一个: 一个会被读成同一个词的一部分。
#
# ⑧ ⚠️ **成绩面板正文字号与历史「详情」统一到 14sp**(玩家: 「在历史记录看到的这个
#    **还不错**, 用这个**代替**之前的那个测试后的数据（这个**字体稍微减少一些** 就可以
#    **不用换行**了）」+ 随后「统一成 15sp: 这个应该改为 **14sp** 吧」)。
#    起因: 同一个 `_bench_score_text`, 跑完那个弹窗用 **17sp**、历史详情用 15sp ⇒
#    前者在 K90 上"各种乱换行"。现在**四个 Label 一个尺寸**(两处正文 + 两处灰字)。
#    实测 14sp 下最宽一行 **300px**(面板可用 438px), 全部不折行。
#
# 验证: --selftest OK · `_sysui` **10**(含阴性对照) · `_verify` **44** ·
#      `_benchdetail` **31** · `_curveaxis` **24** · `_physprog` **8** · `_hpcurve` **6**
#      —— 全绿; 截图逐张看过(菜单无句号不折行 / 高压结果无空行 / 成绩面板 14sp 不折行) ·
#      `check_desc` 最宽 256px · 纯 LF(CRLF=0) · 未跑 `tools/` 生成器(工具链与 main.py 已分叉)。
#
# 【v0.7.111 菜单「吃」->「衡量」· 成绩面板去句号 + 恢复「飞行平均持续」】
#
# ① 菜单第 5 行: 「Python 写的引擎，**吃**单核浮点算力。」
#    -> 「Python 写的引擎，**衡量**单核浮点算力。」
#    (玩家先要「考验」, 随即改成「**衡量**」)
#    ⚠️⚠️ **实测 264px, 而 360dp 只有 261px ⇒ 窄屏折行, 差 3px**
#       (末尾「算力。」会被孤零零甩到下一行)。已把三个候选量给玩家 ——
#       去句号 249px / 去「写的」234px / 「Python 引擎衡量…」219px ——
#       **玩家尚未定夺 ⇒ 本版保持原样**。其余 5 行最宽 256px, 都有余量。
#
# ② 成绩面板那两行(玩家对着截图逐条改):
#    · **去掉末尾句号**(v0.7.110 那个包上是「…4.15 秒。」, 现在只到「秒」)
#    · 「平均持续」-> 「**飞行平均持续**」——把 v0.7.109 那次「少 2 个字」**整个撤回**。
#      ⚠️ 那次省字是为了让整行不折行, 而实测**根本救不了**(要 565px, 面板只给 438px;
#         且字号 sp 与面板宽 dp **同比缩放** ⇒ 换手机也一样折)。折行问题**已由硬回车
#         解决**, 不需要再省字 —— 这条留给以后想再省字的人看。
#    · 「飞行期间可完成」-> 「**弹珠飞行期间可完成**」(第二行开头加「弹珠」)
#    · ✅ 实测(硬回车拆开之后): 第一行 365px、第二行约 306px, 都在面板的 438px 内
#      ⇒ **两行各自都放得下, 没有意外折行**。之前那个 565px 是「两句挤在同一条
#      自动折行的流里」时的数, 不是分行之后的宽度。
#
# 验证: --selftest OK · `_benchdetail` **31**(B1/B1c/B3 三条断言同步: 钉住
#      「飞行平均持续」在第一行末尾、第二行以「弹珠飞行期间」开头、**结尾无句号**) ·
#      `_scorewidth` 逐行量过、全部放得下 · 纯 LF(CRLF=0) · 未跑 `tools/` 生成器
#      (工具链与 main.py 已分叉)。
#
# 【v0.7.110 进度改「第x/45秒」· 屏幕全改 CPU · 曲线纵轴留白 · 采样改每10秒中点】
#
# ⚠️ 本条是**累计**记录: `f7c275b`(v0.7.109) 之后攒着的改动全在这一版, 一次推。
#
# ① 两行黑屏进度 —— 起点都挪到 **1**, 每秒 +1
#    · 波 1: `物理演算进度 x/88` -> **`物理演算第1/45秒`**
#      (玩家: 「x/88 改为 **x/45**, **初始数值是 1**, 每 1 秒增加一次」+「恢复秒字」
#       +「`物理演算 d/d秒` 改为 **`物理演算第x/45秒`**」)
#      分母 = `ceil(44.5)` = **45**(`int()` 会给 44, 差那半秒);「进度」换成「第」。
#    · 波 2: `SOC高压测试 1/360秒` -> **`CPU高压测试第1/360秒`**(同一条规矩, 起点 1)。
#
# ② 屏幕上**所有 SOC 都改叫 CPU**(玩家: 「实际上跑的就是 cpu, 这个更合理」)
#    · 16 处面向玩家的字: 菜单按钮与说明 / 结果弹窗标题 / 历史面板标题 / 空态 /
#      清空确认框 / 详情标题 / 成绩曲线标题 / 「测试历史（渲染 / CPU）」…
#    · ⚠️ **标识符一个没改**(`COL_SOC` / `SOC_*` / `_soc_result_title`): 改它们要动
#      上百处, 还会把 `tools/`(与 main.py 已分叉)和 `temp/` 下的探针一起带偏 ——
#      而那几个名字**玩家一个都看不到**。已在常量处写明「`SOC_` 是历史名」。
#    · 日志里那些「高压…」行本来就没有 SOC 字样, 未动。
#
# ③ 成绩曲线的**纵轴取值规则**(玩家亲定, 逐字实现)
#    起因: 「**不建议把 Y 轴最低点机械地设为成绩最低点** —— 曲线会贴着底边,
#    尤其像图中大量低分段, 会显得拥挤、跳变更刺眼」。
#    · 常规: 下限 = 最小 − max(跨度×5%, 最小值×2%); 上限 = 最大 + max(跨度×5%, 最大值×2%)
#    · 常数: 上下各留 max(值×5%, 最小展示范围/2)
#    · **向外**取整到友好刻度(向内会把真实极值切出画外 —— 那就从「不好看」变「画错了」)
#    · 兜底: 最低点离底**至少约 5% 图高**(解那条不等式, 差多少再往外退一格)
#    · 实测: 玩家给的例子 13280~35173 ⇒ **12000 ~ 36500**(与他手写的那组数逐个吻合),
#      恒为 30000 ⇒ 28500~31500; 跨量级(2600~260000, 差 100 倍)全部不裁切、
#      曲线稳定占高 87~90% ⇒ **换手机不用调任何东西**。
#    ⚠️ 取整粒度玩家没定, 是**照他那个例子反推**的(轴量级×1%): 按「跨度×2%」取也能
#       对上他那个例子, 但窄幅数据(30000~30200)会退化成粒度 5、刻度印出 30805 这种脏数。
#    ⚠️ **顺手改掉一句假话**: 图下原写「（横线 = **中位**）」—— 三条横线画的是轴的
#       上界/中点/下界, 而「中点」不是中位(本局中点 24250 / 真中位 14847, 真中位就印在
#       上面一行) ⇒ 改成「（横线 = 纵轴刻度）」。玩家当时说过这事「先停止」, 这是做纵轴
#       改动时顺手改的**唯一**一处文案(不改的话它就是被我自己弄得更假的一句)。
#
# ④ 成绩面板两处文案 + 一处断行
#    · 「N 轮分数**分别**为」-> 「**依次**为」
#    · 「**飞行**平均持续」-> 「平均持续」(玩家: 「少 2 个字」)
#    · ⚠️ 少这 2 个字**仍然折行**(那一行要 565px、面板只给 438px, 还差 127px;
#      而且字号 sp 与面板宽 dp 同比缩放 ⇒ **换手机也一样折**) ⇒ 玩家改主意:
#      「飞行期间可完成 xxxx **和前面的不在同一行**。**平均持续 x.xx 秒。后接回车**」
#      ⇒ 不再跟自动折行较劲(Kivy 在空格处断, 断点由宽度定、不可控), 改**硬回车**,
#      第一行以**句号**收尾, 第二行整句在后。
#
# ⑤ 高压采样行: 「等间隔连续采样成绩」-> **「每10秒连续采样成绩」**
#    · 玩家: 「改为 每10秒连续采样成绩（**采样的数据数量也会增加**）」——确实变多:
#      由「固定抽 20 个」改成**按时间抽**, 一局 360 秒 ⇒ **36 个**。
#    · ⚠️ 再定一次: 「这个应该是**第5秒、第15秒** 一共 36 个的数值才合理吧?
#      **第0秒和第360秒** 我总感觉不用采纳」⇒ 取的是每 10 秒那一段的**中点**
#      (5, 15, …, 355), 首尾都不采纳 ⇒ 头一个数代表「第 1~10 秒那一段」而不是开局一瞬
#      (段首取值实测偏 **+1.6%**, 中点无偏)。
#    · ⚠️ **窗口不是整 1 秒一个**(实测 321~331 个窗口摊在 360 秒上 ≈ 1.1 秒/窗) ⇒
#      不能拿「每 10 个窗口」当 10 秒, 必须按 `秒数 × 窗口数 ÷ 总秒数` 折算。
#    · 文案里那个「10」与抽点步长**共用同一个常量** `HP_SAMPLE_SEC`, 不会各写一个后脱钩。
#
# ⑥ **修掉一个真 crash**: 空样本的老记录(`windows` 字段没有 / 全 0)在新的抽点逻辑下
#    `IndexError` 直接崩(段数用了 `max(1, …)` 封底 ⇒ 循环照样跑一次 ⇒ `min(len-1,…)`
#    得 `-1`、`max(0,-1)` 变回 0 ⇒ 越界)。抓到它的是 `temp/_hpcurve.py` 的 **C5**
#    (专测「老记录没有 windows」那条路)。现在空样本印「—」。
#
# 探针(都在 `temp/`, **不进 APK**): 新增 `_curveaxis.py`(**24**, 含阴性对照 ——
#   老口径 5/5 组都违反 5% 那条 ⇒ 门禁有分辨力; 且断言的是控件**真画出来的** `_ax`,
#   不是探针里重算一遍) + `_scorewidth.py`(逐行量排版); 改造 `hp_wall_probe.py`
#   (**15**: 去掉已删的「成绩核算中」判据, 改判「面板爬满 -> 弹窗」并把容差从 A1 的界
#   推出来) · `prog_probe.py` 标注**已过期**(它钉的是三代以前的进度口径)。
#
# 验证: --selftest OK · `_verify` **44** · `_benchdetail` **31** · `_curveaxis` **24** ·
#      `hp_wall_probe` **15**(含阴性对照) · `_physprog` **8** · `_hpcurve` **6** —— 全绿;
#      截图逐张看过(菜单/历史/两行白字/成绩面板两行/曲线刻度/详情 36 个采样/360x640
#      小屏详情不溢出) · 纯 LF(CRLF=0) · 展示串缺字形 **0** · 未跑 `tools/` 生成器
#      (工具链与 main.py 已分叉, 跑了会把被否的观感改动一起带出来)。
#
# 备注: 本批新增/改动的文字**不做任何预烘**(玩家点名:「这些文字都不用什么预渲染」)。
#      机制上也确实不需要 —— 预热表 `_FONT_WARM_HUD`/`_FONT_WARM_SIZES` 是
#      `(字号, bold)` 对、**按字号预热**, 唯一按整串预烘的是中奖大字那块。
#
# 【v0.7.109 高压文案四项 + 历史详情改用结果弹窗那套 + 成绩曲线加坐标刻度】
#
#   玩家 2026-09-16 两批:
#   ① 「高压 360 秒改为 连续运行 360 秒」→ 随后更正:「**连续高压测试 360 秒**(之前的那个)」
#   ② 「最低 xx·平均 xx 步/秒, **圆点改为逗号**」
#   ③ 「每段采样(括号内很多字)改为 **等间隔连续采样成绩：**xxx」
#   ④ 「走势图更名为 **成绩曲线**」
#   ⑤ 「这个**历史详情打开后, 应该用之前的那个格式**, 而不是再新作一个」
#   ⑥ 「图的高压走势改名为 **高压SOC测试的成绩曲线**」+「纵坐标需要一个**成绩点**」
#      +「横坐标是**次数** 也需要标记几个数值」+「这个曲线是不是**太粗**了, 看看之前那个
#      帧率曲线用哪个粗细, 这个也用那个粗细」
#
#   · ①②③④ 四句文案直接改。⚠️ 「连续高压测试 N 秒」最终定为**全工程唯一的写法**
#     (与历史详情、空态那句一致)。
#   · ⑤ 是**结构性**的: 详情原来有自己的「成绩/频率/过程」三段版式 ✗, 与结果弹窗是
#     **两套说法**(同一个数一个写「平均 A / 最低 B」、另一个写「最低 B，平均 A」)。
#     ⇒ 抽出 **`_hp_result_text(d, opt_lines)`** —— 结果弹窗与历史详情**共用这一份**,
#       现场那条路按**记录的字段名**组一个 dict 再传(两边同一条渲染路径)。
#       ⚠️ `opt_lines`(锁核/提优先级)是 android **运行时状态**, 记录里没存 ⇒ 详情传空,
#          **不印假值**。
#   · ⑥ `SpeedCurve` 重画: 左侧留 46dp 画**纵轴成绩点**(最高/中/最低三条刻度线+数字),
#     下方留 18dp 画**横轴次数**(1 / 中 / 末); 线宽由 1.2 改成与 `FpsCurve` **完全一致**的
#     `width=1.15, joint="round"`。⚠️ 刻度文字**不能沿用 `FpsCurve._label`** —— 它写死
#     深灰(配它的**浅底**), 而这张是**深底**, 沿用会看不见 ⇒ 自己那份用 `COL_SUB`。
#
#   ⚠️⚠️ **本轮我自己踩了一个静默删除**: 重写 `SpeedCurve` 时用的是「从 `class SpeedCurve`
#      到 `def _bench_score_text`」两点之间的**整段替换**, 而 **`_bench_result_title` 正好夹在
#      中间** —— 被一起删掉了, 而 `py_compile` **完全查不出来**(NameError 只在运行时抛)。
#      抓到它的是 `temp/_benchdetail.py` 的 **B0**(「成绩弹窗能开」, 当初就是为这类
#      `_rec` 未定义的坑补的门禁)。已补回, 并在那句 docstring 上写死了教训;
#      同时**对着备份核了一遍顶层函数/类清单**(备份里有、现在没有的: 无 ✓)。
#
#   验证: --selftest OK · py_compile OK · 纯 LF · 字体子集复核缺字形 **0** ·
#        `temp/_benchdetail.py` 30 项 · `temp/_hpcurve.py` 6 项 · `temp/_verify.py` 41 项 ·
#        `temp/_physprog.py` 7 项 全绿 + 曲线截图看过(刻度与线宽都对上了)。
#
# 【v0.7.108 重置弹珠数后**让出 0.1 秒**再开采样(玩家指出的真问题)】
#
#   玩家 2026-09-16: 「跑分期间屏幕不是黑的啊, 模拟跑的时候, 是要看帧率的, 如果弹出
#   消息是影响的, 应该是**静默直接改珠子**。然后开始跑模拟帧率, **改珠子的这个操作
#   耗时不算帧率变化? 延时执行?**」
#
#   ✅ **玩家质疑得对, 上一版不保险**:
#      · 渲染采样那 5 发球期间屏幕**确实是正常游戏画面**(黑屏只在物理波), 而那段
#        **正是在量帧率** —— 这点玩家说得对(我上一条答成「屏幕是黑的」是错的)。
#      · 重置珠子会刷新 HUD 上几个标签 ⇒ **文字纹理重建是下一帧才发生的**。
#      · 而 `_await_prebake` 在预热早就完成时(玩过一会儿再跑分, **最常见**)会
#        **当场**调 `_start_benchmark()` ⇒ 重置与「开始收样本」落在**同一帧**
#        ⇒ 那次重建**正好落进采样窗口** ✗✗
#   ⇒ 改成重置后 **`Clock.schedule_once(..., 0.1)`**(约 3 帧)再开始采样 ✓
#     (对整场 44.5 秒可忽略)。**不弹消息**(按玩家要求静默改)。
#   ⚠️ 采样窗口的定义点是 `_start_benchmark` 里的 `_flip_times = []` —— 它是渲染统计
#      的**唯一数据源**, 所以「在它之前发生的任何事都不算进帧率」。
#   ⚠️ `_start_bench_test` 唯一的调用点是菜单按钮的 `on_release`(**主线程**) ⇒
#      `Clock.schedule_once` 安全(那段「本函数跑在工作线程上」的注释是**过期描述**)。
#
#   验证: --selftest OK · py_compile OK · 纯 LF · 字体子集复核缺字形 **0** ·
#        `temp/_physprog.py` **7 项全绿**(新增 P7: 重置后必须延时再开采样 —— 谁改回
#        「当场开始」它会当场红) · `temp/_benchdetail.py` 30 项 · `temp/_verify.py` 42 项。
#
# 【v0.7.107 进度条化 + 物理演算进度 x/88 + 跑分前后弹珠数归零】
#
#   玩家 2026-09-16 三条(同一批):
#   ① 「物理验算的 x/44 改为 x/88, 每 0.5 秒更新一次进度, 主要是 44 太难听了」
#   ② 「这个不是物理验算 x/88 秒, 而是 物理验算进度 x/88」
#   ③ 「不对, 我点击开始模拟的时候, 弹珠数量不是从 1000 开始的,
#      每次开始模拟前, 不是要重置一次弹珠数量吗?」
#
#   ⚠️⚠️ **本版第一次 push 忘了 bump 版本** —— 那个 changelog 补丁脚本自己语法错了、
#      `buildozer.spec` 一个字没改, 而 commit 照常提交了 `main.py` ⇒ 推出去的包版本号
#      还停在 0.7.106。**现在补上**(所以这一条 changelog 覆盖了两个已推的 commit)。
#
#   ① 进度单位换成「半秒一格」: 分子分母同时 ×2 ⇒ 数字**每 0.5 秒跳一次**
#      (0.25 秒轮询照样够用)。
#      ⚠️ 分母取 `int(秒数) * 2`: 总秒数是 **44.5** ⇒ `int(44.5)*2 = **88**`
#         (正好落在 88 上, **不是凑的**); 直接写 `int(44.5*2)` 会得 **89**。
#      ⚠️ 分子仍**封顶**(实际会跑到 89 个半秒)。
#   ② 文案 `物理演算 d/d秒` -> **`物理演算进度 d/d`** —— 加「进度」、**去掉「秒」**。
#      去掉秒字后「半秒一格」就不在字面上了, 那正是玩家要的(**进度条**, 不是秒表)。
#   ③ 弹珠数**跑分开始前与结束后各重置一次**, 重置成 `START_BEADS` = **1000**。
#      ⚠️ **上一版我做错了方向**: 做成了「快照 + 原样放回」—— 那样跑的时候弹珠数
#         照样从当前值往上滚, 只是跑完还回来。玩家要的是**开始就归零**。
#      ⚠️ **投中数(`plays`/`hits`)仍然「记下再放回」、不归零** —— 玩家只说「弹珠数量」,
#         而那两项是他**真实的累计投中数**(还写进配置), 归零等于把他的记录抹了。
#      ⚠️ 依然**不走 `reset_balance()`**: 那个会解锁输入、播 `cash` 音效、写状态栏。
#      新方法 `_bench_set_balance()` / `_bench_counters_begin()` / `_bench_counters_end()`。
#      ⚠️ 三个 `_anim_*` 必须与 `balance` 一起同步, 否则余额会从旧值开始滚动。
#
#   验证: --selftest OK · py_compile OK · 纯 LF · 字体子集复核缺字形 **0** ·
#        `temp/_physprog.py` 6 项全绿(23 种文案 · P6 冻结检查量出**最长不更新 0.55 秒**
#        = 正好是半秒节拍) · `temp/_benchdetail.py` **30 项**全绿(B9 系列改成新语义:
#        开始前 balance=1000 · 结束后 balance=1000 · 投中数放回 48/48 · 幂等 ·
#        没走 `reset_balance` 那条会解锁输入的路) · `temp/_verify.py` 42 项全绿。
#
# 【v0.7.106 进度文案: 「物理跑分」->「**物理演算**」】
#
#   玩家 2026-09-16: 「这个**物理跑分太粗俗了**, 能不能换个**文雅点的名字**」。
#   玩家从候选里选了 `物理演算`(「演算」比「跑分」雅, 而两个字把「物理」保留了;
#   与旁边那行「SOC高压测试 d/N秒」并列也齐)。
#
#   · 进度行 `物理跑分 d/d秒` -> `物理演算 d/d秒`。
#   · 黑屏刚上来时那句初始白字也一并改了 —— 它原来写的是
#     `"物理跑分 0/%d" % SOC_SAMPLE_RUNS` ⇒ **既是旧名字、又是旧格式**
#     (`0/5` 是轮数, 而进度现在是按秒的 —— 一直没人发现的不一致)。
#     现在只写名字**不拼数字**(0.25 秒后 `_prog_tick` 就会用真进度盖掉;
#     而总秒数到那时还没算出来)。
#   ⚠️ **日志里**那些「物理跑分…」**不改** —— 那是诊断行, 精确比雅致重要
#     (且旧日志里已经是这个词)。
#
#   验证: --selftest OK · py_compile OK · 纯 LF · 字体子集复核缺字形 **0** ·
#        `temp/_physprog.py` 6 项 · `temp/_benchdetail.py` 29 项 · `temp/_verify.py` 42 项 全绿。
#
# 【v0.7.105 跑分前后**弹珠数/投中数**原样放回】
#
#   玩家 2026-09-16: 「跑分前(模拟测试)前需**重置弹珠数量**, 跑分完成后
#   也需要重置这个数量」。
#
#   ⚠️ **为什么是“快照 + 还原”而不是直接调 `reset_balance()`**:
#      · 它会把 `plays`/`hits` **清零** —— 那是玩家**真实的累计投中数**(而且会写进配置)
#        ⇒ 跑一次测试就抹掉 ✗。
#      · 它还会 `_set_controls_enabled(True)`(**解锁输入**)、播 `cash` 音效、写状态栏
#        「已重置」 —— 跑分期间这三样**全是错的**。
#
#   · 新增 `_snap_bench_counters()` / `_restore_bench_counters()`(幂等)。
#   · 快照在 `_start_bench_test` 开头(渲染采样发 5 发球**之前**)。
#   · 还原在 `_run_benchmark` 的 `finally`(异常路径也罩得住;
#     ⚠️ 必须在 `_bench_fps_lock_off` **之后** —— 门禁 L6 查的是 `finally:` 首行),
#     `_bench_done` 开头再兜一次(幂等)。
#   · 还原时**同步 `display_balance` / 三个 `_anim_*`** —— 不同步的话余额会从
#     被污染的值开始滚动(`reset_balance` 里也是这么写的)。
#
#   ⚠️ 跑分期间弹珠数**一定会涨**: 固定盘面 `BENCH_BOARD` 每发都中奖,
#      5 发下来能翻好几倍(玩家截图实证「弹珠 164000 / 累计48投48中」)。
#      ⇒ 净效果 = **这个测试完全不碰你的弹珠数与投中数**。
#
#   验证: --selftest OK · py_compile OK · 纯 LF · 字体子集复核缺字形 **0** ·
#        `temp/_benchdetail.py` **29 项全绿**(B9/B9a/B9b/B9c: 跑完弹珠数回到跑前 ·
#        投中数也回退 · 幂等 · 没走 `reset_balance` 那条会解锁输入的路)。
#
# 【v0.7.104 历史详情补上诊断块 + SOC 高压新增「走势图」】
#
#   玩家 2026-09-16 两条:
#   ① 「普通测试的那个**是不是漏了一块内容啊, 下面的灰色字体**」
#   ② 「soc高压测试可以**搞个图**吗, 也就 300 个数据作用,
#      **点击额外的按钮**显示, 类似之前的帧曲线」
#
#   ① 那块灰字就是 `_bench_low_summary_text()` 的输出(采样窗口 / 卡顿帧 /
#     慢帧 / 慢帧分布 / 最慢一帧), 它原来**只读 `self._bench_diag`**
#     ⇒ 记录里没存就印不出来 ⇒ 详情弹窗就缺了一块。
#     · 记录里**新增 `diag`**(`_bench_collect_diag()` 的输出, 本来就是 JSON 友好的;
#       体积约 1~3KB/条, 与 SOC 那张表每条存 360 个窗口同一量级)。
#     · `_bench_low_summary_text(self, d=None)` —— **加一个可选入参**
#       (不传就读 `self._bench_diag`, 现场那条路**一个字不用改**)。
#     · `_show_bench_detail` 里**照现场同样的样式**再加一个灰标签;
#       老记录没有 `diag` ⇒ 那段是空串 ⇒ **那个标签整个不 add**(不印空壳)。
#   ② 新增 `SpeedCurve`(小型控件) + `_show_hp_curve(windows)`, 在**结果弹窗**与
#     **历史详情**两处各加一个「走势图」按钮。数据就是记录里的
#     `windows`(逐秒一个, **实测 321 个** —— 直接从真实那份 JSON 里数出来的)。
#     ⚠️ **与 `FpsCurve` 分开写是故意的**: 那张的纵轴是**帧率**(由帧间隔换算)
#        且带一条帧率上限线; 这张的纵轴是**步/秒** —— 硬套得把值取倒数,
#        纵轴就变成"越快越靠下", 读图的人会理解反。
#     ⚠️ 只画**折线 + 中位参考线**; 点数上千也只用**一条 `Line`**
#        (与点数无关地便宜), 别按点建 `Rectangle`。
#     ⚠️ 不够 2 个点就**不建按钮**也不开弹窗(一条直线没信息)。
#
#   验证: --selftest OK · py_compile OK · 纯 LF · 字体子集复核缺字形 **0** ·
#        `temp/_hpcurve.py` **6 项全绿**(C1 按钮 · C2 点数 321 · C3 图上真有 2 条 Line ·
#        C4/C4b/C5 点数不够与老记录都不建按钮) + 截图看过 ·
#        `temp/_benchdetail.py` 25 项(新增 D8 详情里有诊断块) ·
#        `temp/_verify.py` 42 项 全绿。
#
# 【v0.7.103 成绩面板: 删掉「稳定性」那行, 改为「N 轮分数分别为：…」】
#
#   玩家 2026-09-16: 「这样的话, **稳定性这一块可以删了**, 改为
#   **5 轮分数分别为:**」。
#
#   现在读作:
#       平均每轮 14811 步模拟，平均差系数 39.86%
#       5 轮分数分别为：14811,14547,33766,15200,14900
#
#   · 删掉原来那行「稳定性：A～B 步/秒，样本是：…」—— 极值从逐轮
#     分数**一看就有**, 没必要再单列。
#     「平均差系数」留在上一行(那是玩家上一轮点名要的)。
#   · 「N 轮」的 N **按实际算**(`len(_runs)`), 不写死 5。
#   · `phys_min` / `phys_max` **不再参与面板渲染**(仍然照旧存进记录与日志,
#     复盘时还能用)。
#
#   验证: --selftest OK · py_compile OK · 纯 LF · 字体子集复核缺字形 **0** ·
#        `temp/_benchdetail.py` 24 项 · `temp/_verify.py` 42 项 全绿。
#   ⚠️ 探针本身修了一个漏洞: `_benchdetail.py` 之前**没打桩
#      `_save_bench_history`** —— 每跑一次都往真实那份 JSON 里追加假记录
#      (实测攒到 52 条), 而"逐行点详情"的循环因此超时 ⇒ D3 假红。
#      现已打桩 + 把历史钉成空表。
#
# 【v0.7.102 成绩面板版式改成玩家给的式样 + 高压样本从 12 点提到 20 点】
#
#   玩家 2026-09-16: 「这个格式改为：平均每轮x步模拟，平均差系数x.xx% [回车]
#   稳定性：xxx~xxx步/秒，样本是：xxx,xxx（采集多次样本，按时间顺序，至少20次
#   （如果只能采集5个。就用5个），**括号内容是备注，不发**）。
#   另外之前的高压力测试的样本之前只显示10个，现在也提高到20个」
#
#   · 那两行改成:
#       平均每轮 14811 步模拟，平均差系数 39.86%
#       稳定性：14547～33766 步/秒，样本是：14811,14547,33766,15200,14900
#     「样本是」列**逐轮样本**、**按时间顺序**、逗号分隔, **取前 20 个**。
#     ⚠️ 本测试每轮一个样本 ⇒ 常规就是 5 个(玩家也说了"只能采集 5 个就用 5 个")。
#     ⚠️ 老记录没有 `phys_fps_runs` ⇒ 印「—」, 不拿别的数回填。
#   · SOC 高压的样本点数 **12 -> 20**(两处同步: `_hp_summary_text` 与
#     `_show_hp_detail`)。「等间隔取 N 点」里那个 N 是按实际算的, 自动跟着变。
#   · 括号里那句("至少 20 次"等)是玩家给我看的备注, **没有打进面板**。
#
#   验证: --selftest OK · py_compile OK · 纯 LF · 字体子集复核缺字形 **0** ·
#        `temp/_benchdetail.py` 24 项 · `temp/_verify.py` 42 项 · `temp/_physprog.py` 6 项 全绿。
#
# 【v0.7.101 运算速度改平均值口径 + 那行措辞微调】
#
#   玩家 2026-09-16 三条:
#   ① 「这个**运算速度取平均值**」(原来是中位数)
#   ② 「把**平均需改为平均**」
#   ③ 「**持续时间的单位从 x.x 秒改为 x.xx 秒**」
#
#   · ① `phys_fps` 由 `_phys_sorted[len//2]`(中位数) 改成
#     **`sum(fps_list)/len(fps_list)`**。
#     ⚠️ 这**一个数牵连很广**: 成绩面板那行 / 历史表第三列 / `cost_ms`(每发计算用时) /
#        `phys_norm`(归一化) **全从它来** —— 改它是**换口径**, 不只是换显示。
#        老记录里存的是当年的中位数 ⇒ **跨版本比成绩别把两者混着看**
#        (这句写进了注释)。
#     跟着改的三处口径: 面板那行「5 轮中位每秒」→「5 轮**平均**每秒」;
#       历史表第三列表头「中位分/平均差系数」→「**平均分**/平均差系数」;
#       脚注「中位分：…的中位数」→「平均分：…的**平均值**」。
#       (连带把注释里 6 处「中位分」与那句"`phys_fps` 是中位数, 不是均值"
#        一并改正 —— 那句话现在是**反的**, 留着会误导。)
#   · ② 那行第一段「每次飞行平均**需** 256.5 步运算」→「每次飞行平均 256.5 步运算」。
#   · ③ 秒那一项 `%.1f` → **`%.2f`**(步运算与次数仍为一位小数)。
#
#   验证: --selftest OK · py_compile OK · 纯 LF · 字体子集复核缺字形 **0** ·
#        `temp/_benchdetail.py` **24 项全绿** · `temp/_physprog.py` 6 项全绿 ·
#        `temp/check_desc.py` 不受影响(菜单没动)。
#
# 【v0.7.100 菜单文案: 换成更短的说法, 6 行在 360dp 上全部一行放得下】
#
#   v0.7.99 把那两行合并后是 **312px**, 而 360dp 只有 261px
#   ⇒ 窄屏会折行。玩家定稿换成更短的说法:
#     旧: `物理引擎是用 Python 写的，吃单核浮点算力。`  (312px)
#     新: `Python 写的引擎，吃单核浮点算力。`        (**249px**)
#   ⇒ `check_desc`: 6 行全部 `360:OK`, 最宽 256px / 余量 5px。
#   ⚠️ `tools/android_part_ui.py` 已同步(否则重跑生成器会盖回来)。
#
#   验证: --selftest OK · py_compile OK · 纯 LF · `temp/check_desc.py` 全行 OK。
#
# 【v0.7.99 成绩面板: 飞行时长只算「飞行」那段 + 三条整句并一行 + 版本进标题 + 菜单文案合并】
#
#   玩家 2026-09-16 四条:
#   ① 「蓄力时间也不算飞行时间, **只有弹珠开始运动到刚落地才算飞行时间**」
#   ② 「去掉前面的文字, 保留后面的, 后面的文字**放 1 行**」
#   ③ 「把版本号**放在性能测试后面** 中间有一个空格」+ 「**之前界面中不要加版本号**」
#   ④ 「**整合为** 物理引擎用 python 写的，吃单核浮点算力。」
#
#   · ① `_finish_render_sample` 的切段**只累加 `飞行` 这一种标签** ——
#     `_bench_tag()` 的 `飞行` 恰好就是 `state == "flying"` 的整个区间
#     = 开始运动 → 刚落地 ✓。上一版还算了 `落袋`(落地后那几下弹跳)
#     —— 那**在落地之后**, 且持续到球停稳 ⇒ 会把每发再撑大好几倍。
#     (`蓄力 / 待机 / 装杯 / 哑火` 一律不算)。
#   · ② 删掉「每次发射 / 计算用时·飞行用时·富余」那两行(与下面三条是同一批数),
#     三条并成**一行**, 措辞改成玩家给的: `飞行平均持续` / `飞行期间可完成`。
#   · ③ 新函数 `_bench_result_title()`(与 `_soc_result_title`/`_startup_title` 同一套):
#     标题 = `性能测试 v0.x.x`; `_bench_score_text` 正文首行**只留设备/系统/Python**。
#     ⚠️ 玩家明说**详情弹窗里也不要加版本号** ⇒ 不做开关, 正文一路不带。
#   · ④ 菜单说明那两行合并成 `物理引擎是用 Python 写的，吃单核浮点算力。`
#     ⚠️ **这一条会在窄屏上折行**: `check_desc` 量出 **312px**,
#        而 360dp 只有 **261px**(400dp 也只有 296) ⇒ 360/400dp 上都折成两行。
#        说明标签是 `_auto_h` 自动撑高的 ⇒ **只是变高, 不会被裁**。
#        ⚠️ 而且**这两行原本是故意手写 `
` 拆开的**(tools 源里有注释:
#           232+192=424px > 261px, 交给 Kivy 自动折会把断点落在空格上)。
#           玩家本次要求合并 ⇒ 已合并, 并在此记下这个对价。
#
#   现在的成绩面板:
#       性能测试 v0.7.99
#       HIH-D-34991 / Windows / Python 3.11.4
#       运算速度：5 轮中位每秒 33684 步模拟
#       稳定性：33000～34000 步/秒 · 平均差系数 1.23%
#       每次飞行平均需 256.1 步运算，飞行平均持续 1.0 秒，飞行期间可完成 128.9 次飞行模拟
#
#   验证: --selftest OK · py_compile OK · 纯 LF · 字体子集复核缺字形 **0** ·
#        `temp/_benchdetail.py` **24 项全绿**(B1/B1b 合一行且旧两行已删 · B2 「—」不回填 ·
#        B3 三个数真值 · B5/B5a/B5b 切段 + 装杯排除 · B6 正文无版本 · B7 标题带版本) ·
#        `temp/_physprog.py` 6 项全绿 · `temp/check_desc.py` 量出上面那个折行代价。
#
# 【v0.7.98 每发飞行时长: 排除装杯演出(玩家: 不包含落珠动画)】
#
#   玩家 2026-09-16: 「这个每次飞行所需时间**是不包含落珠动画**的时间
#   你说平均要六秒多 肯定是错了」。
#   ✅ 判断对。上一版(v0.7.97)的切段是"从这一发发射到下一发能发",
#   而**跑分用的固定盘面(`BENCH_BOARD`)每一发都中奖 ⇒ 每一发都会演装杯**,
#   而下一发要等 `state == ready` ⇒ 按"两次发射的间隔"切出来的段**里面全是装杯时间**
#   (实测能把每发从 ~0.5 秒撑到 6 秒+)。
#   ⇒ 只累加"球真的在动"的帧(`_bench_tag()` 的 `飞行` + `落袋`),
#     把 `装杯 / 蓄力 / 待机 / 哑火` 排除。
#     「落袋」是飞行的尾巴(球还在动), 「装杯」才是玩家说的那个演出。
#   ⚠️ 门禁 B5b: 夹具里**故意夹 3 帧装杯(各 2000ms)** —— 不过滤的话每段会是 6500ms。
#
#   验证: --selftest OK · py_compile OK · 纯 LF · 字体子集复核缺字形 **0** ·
#        `temp/_benchdetail.py` **24 项全绿**(B5/B5a/B5b) · `temp/_physprog.py` 6 项全绿。
#
# 【v0.7.97 两处纠错: 每发飞行时长切段(五次之和->平均) + 跑分进度把等待也算进去】
#
#   玩家 2026-09-16 连报两条, **两条都是真 bug**。
#
#   一 · 「这个倍率计算你是不是搞错了 你算的是五次飞行时间和 可能应该取平均」
#   ✅ **玩家看对了**。`_finish_render_sample` 里切段用的判据是 `_f[8] == 0`
#   (`_SINCE_LAUNCH` 为 0 = 发射那帧), 但 `_SINCE_LAUNCH[0] = 0` 写在 `launch()` 里,
#   而 `_SINCE_LAUNCH[0] += 1` 在**另一处**(15262 / 16256 行, 与 `_bench_frames.append`
#   的 11992 行不在一处), 先后不保证 ⇒ 发射那帧被记下来时计数器
#   **已经是 1** ⇒ `== 0` **一次都不成立** ⇒ 整场只切出一段(= 整个采样窗口)
#   ⇒ 印出来就是「五次之和」。
#   ⇒ 判据改成「**计数器回落**」(`_sl <= _prev_sl`): 0 跟 1 都认得出,
#     而飞行中只增不减, 不会误切。门禁 B5/B5a: 用 **app 真实会产生的形态**
#     (计数器从 1 起、没有 0) 喂进去, 断言切成 **3 段各 500ms**;
#     旧判据下会切出**一段 1500ms**(就是那个 bug 的形状)。
#
#   二 · 「跑分的24秒上限有问题 经常出现每秒不更新的情况 空闲的时候也要更新
#        所以你是不是漏考虑了等待时间」
#   ✅ **也是真的**。分母原来只有 CPU 秒(≈24.5), 而样本之间有 `gap_sec`
#   (5 秒 × 4 次 = **20 秒**)**不烧 CPU** ⇒ 那 20 秒数字一动不动。
#   ⇒ 分母加上等待总量, 分子加上等待耗时 —— 两边都是**玩家真在等的时间**。
#   ⚠️ **第一版修错了**(探针 P6 抓出来的): 只在 `sleep` **结束之后**把时长
#      累加进 `_PHYS_SLEPT` ⇒ 睡的那 5 秒里它一动不动 ⇒ **照样冻**(实测冻 **2.80 秒**)。
#      正解是把「正在等待的起点」也发给读的一侧, 由它按墙钟实时累加;
#      结束时**先清起点再并入**累计值(顺序反了会重复算一段)。
#   ⇒ 实测最长不更新从 **2.80 秒**降到 **0.75 秒**(正常节律: 整秒 +
#      0.25 秒轮询, 本来就要 1.1~1.4 秒才变一次)。分母从 24.5 变成
#      **44.5 秒**, 与真实墙钟对得上。
#
#   验证: --selftest OK · py_compile OK · 纯 LF · 字体子集复核缺字形 **0** ·
#        `temp/_physprog.py` **6 项全绿**(P6 **冻结检查**: 同一数字最长连续
#        ≤1.6 秒 —— 阈值按分辨力定: 3 秒的 gap 下旧行为会冻 ~3 秒) ·
#        `temp/_benchdetail.py` **23 项全绿**(B5/B5a 切段) · `temp/_phys_hash.py` 物理逐位不变。
#
# 【v0.7.96 成绩面板追加三条整句: 步运算 / 秒 / 能完成几次】
#
#   玩家 2026-09-16: 「这个界面**额外显示** 每次飞行平均需 x 步运算,
#   每次飞行平均需 x 秒, 每次飞行时间内可以完成 x 次飞行模拟。这里面的数值
#   用的还是**这五次飞行的平均值**, 可以取**一位小数点**」。
#   玩家明确选了**叠加**(不是替换上面那两行)。
#
#   现在的样子:
#       每次发射：需 412 步模拟
#       　　计算用时 20.3 ms　飞行用时 510.0 ms　富余 25.1 倍
#       每次飞行平均需 412.0 步运算
#       每次飞行平均需 0.5 秒
#       每次飞行时间内可以完成 25.1 次飞行模拟
#
#   · 三个数就是上面那三个(每发平均步数 / 飞行用时 / 倍率), 换个**整句**说法
#     + 带单位。· `avg_frames` 的存法由 `int()` 改成 **`round(..., 1)`**
#     —— 取整就永远印 `.0`。老记录存的整数读出来补 `.0`, 不影响
#     (`_bench_score_text` 是唯一读者)。
#   · 任一取不到印「—」, **绝不回填**。
#
#   验证: --selftest OK · py_compile OK · 纯 LF · 字体子集复核缺字形 **0** ·
#        `temp/_benchdetail.py` **21 项全绿**(新增 B4/B4a/B4b/B4c: 三条都在 ·
#        步运算一位小数 · 秒 = 飞行用时÷1000 · 次数 == 倍率)。
#
# 【v0.7.95 「每次发射」那行: 拆两行 + 计算用时/飞行用时/富余倍率】
#
#   玩家 2026-09-16: 「每次发射的后续文字调整 计算用时 x.x ms, 飞行用时 x.x。溢出倍率 x.x」
#   + 「倍率是**飞行除以计算**」+ 「飞行那个是**五次平均用时**」。
#
#   · 那行**拆成两行**(三个数挤一行会糊成一团):
#       每次发射：需 412 步模拟
#       　　计算用时 20.3 ms　飞行用时 510.0 ms　富余 25.1 倍
#   · **飞行用时 = 渲染采样那 5 发的实测时长均值**。**不新埋点** —— 用 `_bench_frames`
#     里那对现成的逐帧数据切段: [0] = 本帧间隔(ms), [8] = `_SINCE_LAUNCH`(发射那帧为 0)
#     ⇒ 每次归零就是新的一发, 段内帧间隔求和。见 `_finish_render_sample`。
#     ⚠️ **不能拿"两次 launch 的间隔"代替**: 里面还含下一发的蓄力延时(0.1s)+tick 节奏
#        (0.1s), 会系统性多算 ~0.15 秒。
#   · **富余 = 飞行 ÷ 计算**: >1 有余量、=1 刚好、<1 **算不过来**(现象是掉帧/球变慢,
#     **不会崩**)。
#     ⚠️ 不叫「溢出倍率」: 数值 >1 表示有余量, 而"溢出"字面上像出事了。
#     ⚠️ 这个余量是**跑分时**测的(物理线程已提优先级 + 屏幕黑屏), 是**最理想情况的上限**;
#        真玩起来主线程还要画帧、演装杯, 实际比它**更小**。
#   · 任一项拿不到一律印「—」, **绝不回填**(老记录没有 `flight_ms`/`margin`)。
#
#   验证: --selftest OK · py_compile OK · 纯 LF · 字体子集 AST 复核缺字形 **0** ·
#        `temp/_benchdetail.py` **17 项全绿**(新增 B1 拆两行 / B2 无数据印「—」不回填 /
#        B3 有数据时 `飞行 510.0 ms　富余 25.1 倍` = 510.0 ÷ 20.3)·
#        其余 14 项(详情接线 / 同源渲染 / 只有最新那条有曲线 / 几何 / 时间去年代)不回归。
#
# 【v0.7.94 跑分四项: 菜单文案纠错 + 波1 进度改按秒 + 时间去年代 + 测试历史加详情按钮】
#
#   玩家 2026-09-16 一次给四条, 逐条落实。
#
#   一 · 菜单文案**纠错**(玩家: 「模拟测试中弹珠好像是发射5颗, 但是界面说发射3颗」)
#   **玩家没看错, 是文案错了**: `BENCH_TARGET_LAUNCHES = 5`(858-859 行)一直发 5 颗。
#   「3」的来历(git 溯源): b6eb2e6 那次把 `SOC_SAMPLE_RUNS` 5->3(**轮数**),
#   顺手把说明里的「5 颗」改成「3 颗」—— **把轮数误当成了颗数**, 而发球数从没动过。
#   之后轮数又回到 5, 那句「3 颗球」却没人改回来。
#   · 「1. 自动发 3 颗球，测屏幕渲染帧率」 -> 「1. **累计发射 5 颗弹珠**，测屏幕渲染帧率」
#   · 「2. 物理引擎全力跑，测每秒模拟步数」 -> 「2. **通过后台跑物理引擎测试 CPU 性能**」
#   ⚠️ **父项目 `tools/android_part_ui.py:2439` 里是另一份写死的旧文案** —— 本次已同步,
#      否则重跑生成器会把「3 颗」原样盖回来(tools/ 与 main.py 早已分叉)。
#
#   二 · 波 1(物理跑分)进度**从"第几轮/共 5 轮"改成按秒**
#   玩家: 「后续物理跑分的时候只有五个颗粒度, 能不能改成**秒为颗粒度或进度**」。
#   · `_burst` 的循环里发布 `_PHYS_PROG`(整场已烧掉的 CPU 秒) —— 发布点选在**每颗球**
#     那一层(内层 `range(4000)` 才是每步)。**绝不能挪进内层**: v0.7.74 的教训就是
#     "热循环每步多加一点"直接把跑分拖下去了。
#   · 循环形状由 `while cpu_clock() < deadline` 改成 `while True + 一次取值`,
#     **调用次数与判据完全相同** ⇒ 发球序列一个数都不变(`temp/_phys_hash.py` 复验通过,
#     hash 仍是 `4d7a7e07…`)。
#   · 文案 `物理跑分 d/5` -> `物理跑分 d/d秒`, 分母 = `warmup + (runs+1)*head + runs*sample`
#     的 **CPU 秒**总数(实测约 24.5 秒 ⇒ 25 种文案, 原来 5 种)。
#   ⚠️ 为什么是 **CPU 秒**不是墙钟: 与 SOC 那段同一条规矩 ——"**和循环条件是不是同一根
#      尺子**"。波 1 的循环条件就是 CPU 秒。代价: 样本间的 `gap_sec`(5 秒 × 4 次)不烧
#      CPU ⇒ 那几秒数字不动, 那是**事实**不是卡住。
#
#   三 · 历史表时间列**去掉年份**(玩家: 「去掉时间中的年」)
#   `2026-09-15 14:07` -> `09-15 14:07`。
#   ⚠️ 这个格**来回改过四轮**(详见 `_hist_stamp` 的 docstring)。这次**不是**重犯老路:
#      以前是**宽度不够**逼出来的(时间列 110px 而完整格式要 113px, 字号被压到 13.16sp),
#      这次是**显示偏好**, 而且顺带把宽度账彻底松开 ⇒ `09-15 14:07` 只要 76px,
#      **三列数据全都放得下 ⇒ 全表字号回到 sp(14) 上限**(一个列宽常量都不用改)。
#   ⚠️ **只改显示**: 盘上存的一直是 `%Y-%m-%d %H:%M`, 旧记录一条都不用动。
#
#   四 · 「测试历史（渲染 / SoC）」右侧**新增「详情」按钮**
#   玩家: 「右侧新增一个详情按钮, 点击打开的就是**跑分后的弹窗**」。
#   · 新增 `_show_bench_detail(r)`: 正文走**新的共用渲染** `_bench_score_text(rec)`,
#     与跑完那一刻的成绩面板**同一份代码** ⇒ 两边永远一样(两处各写一份迟早脱钩)。
#     为此把 `_bench_done` 的成绩串也改成调它 —— 传进去的就是**刚存进历史的那个 dict**。
#   · 列宽 110/92/88 -> **80/66/92/50 = 288**(时间列去年份后只要 76px, 腾出的一列给按钮)。
#     ⚠️ 加列最容易挤坏的是**那个按钮**(旧事故: 详情被挤成 25px), 这次按钮固定 50dp(探针钉着)。
#   · 记录补三格 `render_10low` / `render_p99` / `render_p90`(原来只印在跑完那一刻的面板上,
#     历史里没有 ⇒ 详情就少三行)。体积可忽略, 老记录没有 ⇒ 自动退回三值版,**不回填**。
#   ⚠️⚠️ **帧率曲线只在"刚跑完那一次"有**(玩家 2026-09-16 定案: 「我已经放弃在历史记录中
#      显示帧率曲线了 … 只有当前跑的那次有这个」+「**如果能找到数据就[有], 否则没有**」)。
#      ⇒ **不存任何逐帧数据**(原始 `_render_gaps_ms` 有 1500~3800 个数, 100 条要 1~2.5MB,
#        而那份 JSON 是启动期同步读的)。判据 = `r is bench_history[-1]` 且内存里那份非空:
#        数据在就给按钮, 不在就不给(**不给点开是空图的按钮**)。
#
#   验证: --selftest OK · py_compile OK · 纯 LF · 字体子集 AST 复核缺字形 **0** ·
#        `temp/_physprog.py` **5 项全绿**(9 种文案 vs 旧格式上限 3 种 · 分子单调不减 ·
#        不超分母 · 分母 == 按实参算的 CPU 秒)·
#        `temp/_benchdetail.py` **14 项全绿**(含 B0: 成绩弹窗不抛 —— 这条是补门禁:
#        本轮真踩过一次 `_rec` 未定义的 NameError, `py_compile` 查不出来;
#        D1c/D1d/D1e 几何: 每行居中 0/11 偏 · 行宽 288 · 按钮 50dp 没被拉伸;
#        D1b: 每个详情按钮都对上一条记录; D3: **只有最新那条**有曲线按钮;
#        D2: 正文 == `_bench_score_text(记录)` **同源**; D4: 曲线点数 == 内存里那份;
#        D6: 找不到数据时不建按钮)·
#        `temp/_phys_hash.py` 物理逐位不变 · 截图逐张看过。
#        ⚠️ 探针自己踩了三次坑(`children` 逆序当正序 / 全角括号当半角 / 锚点空格),
#           每次红都是**探针的错**; 现在几何判据一律按 `inner` 对齐坐标系。
#
# 【v0.7.93 高压口径统一成「连续高压测试 N 秒」】
#
#   玩家 2026-09-16: 「连压360秒 改为连续高压测试360秒; 历史界面的 高压360秒 也改为
#   连续高压测试360秒」。
#   同一个东西原来有**三种说法**: 空态写「连压 N 秒」、历史详情写「高压 N 秒」、
#   模拟测试结果面板写「高压 N 秒」 ⇒ 现在统一成「**连续高压测试 N 秒**」。
#
#   改动(三处):
#   · `_show_hp_history` 空态: `连压 N 秒即可产生一条` -> `连续高压测试 N 秒即可产生一条`
#   · `_show_hp_detail`: `…   高压 N 秒` -> `…   连续高压测试 N 秒`
#     ⚠️ 这行因此约 31 字, **手机上会折成两行**(内容区约 285px)。可以接受: 那段正文是
#        `_auto_h` 撑高的多行标签, 折行只会长高、**不会被裁**(探针有断言钉着)。
#   · `_bench_done`(模拟测试结果面板)那行 `高压 N 秒：首…末…` -> `连续高压测试 N 秒：…`
#     ⚠️ 这一处**玩家没点名**, 是按"同一个短语的第三个出现点"一并改的 —— 只改两处又会
#        变成同一个东西两种说法。已当面说明, 不同意可一句话回退。
#
#   ⚠️ 别把「连压 N 分钟」改回**硬编码字面量**(那是 2026-09-15 踩过的坑, 见
#      `_bench_menu_desc` 上方的注释): 菜单文案里那句仍然走 `SOC_SUSTAIN_WALL_SEC` 常量。
#
#   验证: --selftest OK · py_compile OK · 纯 LF ·
#        `temp/_verify.py` **42 项断言全绿**(新增两条: 空态那句 / 详情那句)+ 截图看过 ·
#        字体子集 AST 复核: 会显示的字符串里缺字形 **0**。
#
# 【v0.7.92 高压面板/结果弹窗逐行审阅: 跑分改平均值 + 措辞改人话 + 采样行说明 + 标题带版本】
#
#   玩家 2026-09-16 一连串逐行审阅(对着截图问「这句话是什么意思」), 逐条落实:
#
#   一 · 跑分口径: **中位数 -> 平均数**
#   玩家: 「soc高压测试历史记录中显示的跑分 从中位数改为平均数」。
#   论证与他 2026-09-15 给 CPU 频率定的那条**同款**(见 `_hp_freq_line`): 每秒窗口的
#   步/秒是**双峰**样本(空转窗口 + 满窗口), 中位数必然落在其中一个峰上 ⇒ 要么像
#   "全程很慢"、要么像"全程满血", 两个都不代表这一段。
#   · 记录新增 `mean`(**旧 `median` 照旧存着**, 只是不再显示 —— 别删)。
#   · 显示一律走新函数 `_hp_score()`: 新记录读 `mean`, **旧记录拿 `windows` 现算**
#     (两者本来就是同一次测试的同一批逐秒样本, 算得出来就**不印「—」**)。
#   · 三处口径同步(列头 `_HP_COLS` / 脚注 / 详情正文) + **结果弹窗同一口径** ——
#     频率那边就是因为两处不一样被玩家逮到过, 别重犯。
#
#   二 · 措辞改"人话"(玩家: 「我不是这一块的专家, 你看看怎么写得可以让人看懂」)
#   · `跑分线程优先级：已提升 -4 → -8` -> `跑分线程已提权：比画面渲染更优先（-4 → -8）`
#     (-4/-8 是 `Process.setThreadPriority` 的编号, 外人看不懂; **数字留在括号里** ——
#      它是"系统到底答没答应"的唯一硬证据)
#   · `性能核：已锁定 cpu6,cpu7` -> `跑分只跑这几个核：cpu6、cpu7`
#   · 没提权时不再只说"未提升", 而是说**后果**: `跑分线程没提权：可能被画面抢 CPU（原因）`
#   · **非安卓时这两行整行不出现** —— PC 上它们永远是恒定值, 而这块面板的成文规则是
#     "有唯一预期值的, 只在偏离时才有信息"(同 `audio_detail` 里"不适用的行直接不出现";
#      玩家 2026-09-11 就说过「这几个不适用听起来有点奇怪」)。
#   · 删掉 `高压 360 秒（背靠背不停）` —— 内部行话, 且详情弹窗里早就删了。
#
#   三 · 采样行**说清自己是什么**
#   玩家: 「这个是随机的 还是按照时间顺序的 **需要说出来**」。
#   答案是**按时间顺序、等间隔抽的点**(`range(0, len(v), len(v)//12)`) —— 不是随机、
#   也不是全部 360 段, 而面板上原来一个字都没交代。现在前缀写明:
#   `每段采样（按时间顺序，左起最早，等间隔取 N 点）：`, **点数按实际算**(不写死 12)。
#
#   四 · 结果弹窗标题带版本、正文不带
#   玩家: 「把版本号**放入标题**中吧 加个空格, **这个地方就不要版本号了**」。
#   ⇒ 新函数 `_soc_result_title()`(与 `_startup_title` 同一套做法):
#     `SOC高压测试 v0.x.x`, 拿不到版本时退化成纯名字; 正文那行只留**设备/系统/Python**
#     (它们不是标题能表达的东西, 且跨机器比成绩时靠的就是这一段)。
#   ⚠️ **历史详情弹窗里那行 `设备 版本` 照旧保留** —— 那是**记录诞生时**的版本, 是历史数据。
#
#   五 · 用词统一成「平均值」
#   玩家: 「把平均数和均值 都改成平均值」。会显示的字符串里已无「平均数」「均值」:
#   · SOC 列头 `平均值`; SOC 脚注 `平均值：物理引擎每秒模拟步数的平均值`(2026-09-16 重写:
#     原文「高压那段各个 1 秒窗口速度的平均数」写的是**实现细节**、没说这个数是什么)
#   · 「平均差 ÷ 平均值」
#   · **隔壁「测试历史」脚注那句话逐字相同, 一并统一**(只改一边就是同一个概念两个词)
#
#   六 · 启动信息
#   · `音效加载 41 / 41` / `语音加载 61 / 61`(已加载 / 满编)。玩家 2026-09-16:
#     「格式应该是 ：x/y 大概是已经加载几个 累计几个」。
#     为此在 **PC 的烘焙路径**(`_bake_pcm`)也记下满编数 —— 以前只有安卓的 `_bake_named`
#     记 `_n_bank`/`_expected`, PC 上**一个都没记**(那正是"PC 从来没印过计数"的根)。
#     ⚠️ 两边**可能不等**: 读 wav 失败的那几个会被跳过, 那个差额就是这个数存在的意义。
#     `bank` 为空时**一行都不印**(否则「音效加载 0 / 0」会被读成全军覆没)。
#
#   验证: --selftest OK · py_compile OK · 纯 LF ·
#        `temp/_verify.py` **40 项断言全绿** + 逐张截图看过。探针特意覆盖几个**容易假绿**处:
#        · 造**偏斜**样本(平均 10556 / 中位 10000) —— 均匀样本测不出到底换没换口径
#        · 旧记录(无 mean)现算 / 新记录直接读 mean / 无样本印「—」三条路各一条
#        · **模拟 `platform = "android"`** 才测得到那两行(PC 上恒为空串)
#        · 清空流程的**阴性对照**: 点「取消」记录仍在 12 条 → 点「确定清空」才变 0 条
#        · 字体子集: 用 AST 抽**所有会显示的字符串**逐字比对 ⇒ 缺字形 **0**
#        ⚠️ 探针把 `_save_hp_history` **打桩**, 绝不动盘上真实记录(清空不可逆)。
#        ⚠️ 探针本身改错三次(把 Kivy `children` 的**逆序**当正序、全角括号当半角、
#           锚点少写一个空格), 每次红都是**探针的错** —— 判据已改成与列序无关。
#
# 【v0.7.91 SOC 高压历史面板照搬参照面板 + 清空日志; 启动信息补回计数、删冗余版本行】
#
#   玩家 2026-09-16(看真机截图): 「这个界面肯定不行吧 你**直接把另外一个跑分的历史log
#   给抄袭过来**吧 顺便带一个 log 清空功能」。
#
#   改动一 · SOC 高压历史面板按 `_show_bench_history` **逐项照搬**
#   (以前两张表每一条都不一样, 那正是"看着不像一套东西"的原因):
#   · **删掉整套 `_vn` 行数推算**, 换成 `ScrollView(size_hint=(1, 1))` —— 面板高度改由
#     弹窗**固定**给(`0.7 * _vh`), 与记录数无关(记录少留白、多了滚动)。
#   · **删掉末尾那次 `_popup_fit_content`** —— 它会把弹窗收到"内容最小高度", 而
#     `ScrollView` 对 `minimum_height` 的贡献是 **0** ⇒ 一旦它发力**整个列表被压成 0 行**。
#     隔壁那张表本来就没有这次调用, 这正是它高度稳定的原因。
#   · **表头独立排版**(按自己文字宽度分列、整行居中; 不再与数据行共用列宽), 表头**不调**
#     `_fit_line`/`_fit1`; 并加**全表共用一个字号**(逐数据列量出上限再取最小)。
#   · 行高 28 -> **26dp**、脚注 `_auto_h(foot, dp(44))`、弹窗宽 0.92 -> **0.96**。
#   · 列宽 116/46/76/46 -> **110/58/62/58 = 288dp**: 旧的中位数那格只比文字宽 **3px**,
#     真机上时间戳与它糊成一片(玩家截图为证 `2026-09-15 23:06:34652`)。
#
#   改动二 · 新增 `_clear_hp_history`(与 `_clear_bench_history` 同款)
#   「清空历史」只在**有记录**时出现; 确认框写清条数与「不可恢复」; **只清
#   `plinko_hp_history.json` 这一张**, 不动模拟测试历史(两张表分开是玩家 9-15 定的案);
#   清完**当场重开面板**让玩家看到空态。
#
#   改动三 · 启动信息
#   · **补上「音效加载 N 个」「语音加载 N 个」**。玩家报「之前有…你怎么给删了」,
#     核实结论是 **PC 上从来没有过**: 那两个数只长在安卓 named 分支(`_n_bank`/`_expected`),
#     而 PC 走 `_bake_pcm`、**那两个字段根本没被赋值**。查证命令:
#       `git log -S'rows = ["音频后端' -- main.py`(⇒ 那行来自 v0.6.34)
#       `git log -S"音效加载" -- main.py`(⇒ 全历史零命中) ⇒ **没删过, 是没写过。**
#     实现: 按 `self.bank` **现算**(合成音 = bank 总数 − `_voice_files()` 数), 不重新合成;
#     `bank` 为空时一行都不印(否则「音效加载 0 个」会被读成全军覆没)。
#   · **删掉「游戏版本」那一行**: 标题 `_startup_title()` 里就是同一个版本号, 一屏印两遍。
#
#   验证: --selftest OK · py_compile OK · 两个文件纯 LF ·
#        `temp/_verify.py` **19 项断言全绿** + 逐张截图看过:
#        弹窗居中 0.0px · 表头独立宽度 158 且居中 0.0px · 滚动区 size_hint=(1,1) ·
#        12/12 行居中 0.0px · 行宽 288 · 行高 26dp · 有「清空历史」+「关闭」·
#        确认框点「取消」记录仍在 12 条 / 点「确定清空」才变 0 条 · 清空后当场回空态、
#        且空态下**没有**清空按钮 · PC 上「音效加载 41 个 / 语音加载 61 个」·「游戏版本」已消失。
#        ⚠️ 探针把 `_save_hp_history` **打桩**, 绝不动盘上真实记录(清空不可逆)。
#
# 【v0.7.90 SOC 高压历史面板: 表格居中 + 详情分三块 + 菜单文案】
#
#   玩家在 Lenovo Y700(2560x1600 平板, **竖屏**)上打开「SOC高压测试历史」后报
#   「**布局偏右 没有居中**」。
#
#   根因(读码得到): 那张表的列宽是「**时间列吃剩余**(`size_hint_x=1`), 另三列固定」,
#   而**整张表没有任何居中设定**。v0.7.87 那次"提高时间列宽度"在手机上刚好
#   (360dp 屏时间列约 126dp), 但平板上弹窗宽 0.92*1600 ≈ 1472px, 时间列吃掉约 1080px,
#   剩下三列全挤在**右边缘** ⇒ 看着就是"整张表偏右"。
#   对照: 隔壁「测试历史（渲染 / SoC）」一直是**固定列宽 + `pos_hint={'center_x':0.5}`**,
#   本来就居中 —— 两张表一个居中一个不居中, 玩家一眼就看出来了。
#
#   改动:
#   · `_show_hp_history`: 改成与隔壁**同一套写法** —— 固定列宽 + `size_hint_x=None` +
#     `width=_table_w` + `pos_hint={'center_x':0.5}`, 表头与每一行共用同一个 `_table_w`。
#     列宽**量出来的**(`text_px(文本, sp(14))`): 时间 113 · 中位数 42(表头) ·
#     平均差系数 **70(表头 5 字, 最长项)** · 详情按钮文字 28 ⇒ 留余量 116/46/76/46 = 284dp。
#     ⚠️ 表头不许被单独缩字号(否则又变回玩家报过的"字体不均衡")。
#     ⚠️ 窄屏按比例收(`_k`), 免得 `size_hint_x=None` 的子控件溢出弹窗。
#   · `_show_hp_detail`: 正文**分三块**(成绩 / 频率 / 过程) + 组内缩进两格。
#     玩家原话「一堆看不懂的东西」—— 病根不是字太多, 是**没有分组**。内容一条不删。
#   · `_vn` 下限 3 -> **10**, 固定预留 240 -> **200**dp; 去掉 `min(len(...))`
#     —— 玩家定案「窗口的高度是固定的」, 记录不足就下面留白。`_vn` 上移到空态分支之前,
#     空态与列表共用同一个高度。
#   · 菜单文案: 「连压 %d 分钟, 看持续性能衰减」->「**考验调度和散热能力**」。
#
#   ⚠️ 记一笔**排除掉的误判**: 曾把"上一版只显示 4 行"归因到 `_vn` 的固定预留上
#      (说"平板横屏可用高只有 400dp") —— **算不出来**: `_veq()` 是**排过序的**
#      (`return (w, h) if w <= h else (h, w)`), 横竖都返回「短边 × 长边」, 所以
#      Y700 上 `_veq()[1]` **恒为 2560px**(≈1280dp), 公式给 **30 行上下, 横拿竖拿一样**。
#      当时只显示 4 条的**真实原因更可能是记录本来就只有 4 条**(存储上限 **100**,
#      见 `_load_hp_history`/`_save_hp_history`)。以后见到"条数不对"**先数记录, 别先改公式**。
#
#   验证: --selftest OK · py_compile OK ·
#        `temp/_hp_shot.py` 几何断言全绿(弹窗居中 0.0px · 表头居中 0.0px · 宽 284 ·
#        **12/12 行居中 0.0px** · 详情三块齐 · 高度撑开到 342 > h0 220) + 逐帧截图看过。
#        ⚠️ 探针第一版有一条**假红**: 数据行的父容器 `inner` 原点在**滚动坐标系**里
#        (`inner.x=0` 而 `sv.x=49.6`), 拿 `row.center_x` 直接减 `content.center_x` 恒差
#        一个偏移 ⇒ 12/12 报"偏了"。**红的是探针, 不是产品。**
#
# 【v0.7.89 「开启声音」的提示音也删掉(避免误触) —— 声音开关变成静默切换】
#
#   接 v0.7.88(删了「关闭声音」那条)。玩家 2026-09-16 追加:
#   「开启声音也不要了。避免误触」。
#   理由与上一版同源: 在需要安静的场合**误触**一下, 如果弹出一句「开启中奖音效」,
#   那一下同样会突然出声 —— 两个方向都该是哑的。
#
#   改动:
#   · `toggle_mute`: 去掉 `sfx.play("voice_mode_sfx")` ⇒ **两个方向都不播任何音**,
#     反馈只走按钮本身(绿底「音效已开」/ 深底「音效已关」)。
#   · `voice/voice_mode_sfx.wav` 从仓库删除。语音总数 62 -> 61。
#   · `tools/generate_voice.py` 清单同步(那一整段现在只剩注释)。
#   · README 语音数 62 -> 61(三处)。
#   · 设计页: 「关声音就是立刻关」那张卡改写成「切换声音是静默的」, 语音数同步 61。
#
#   ⚠️ 至此 `_refresh_mute_btn` 是声音开关**唯一**的反馈通道 —— 动它之前想清楚:
#      按下去如果既不响也不变色, 玩家会以为按钮坏了。
#   ⚠️ `Sfx.play` 的互斥前缀名单里还留着 `voice_mode_`(无害: 那两条已经没人播)。
#      没删是因为它同时管着 `voice_rtp_` / `voice_bet_`, 单独摘一个前缀反而更乱。
#
#   验证: --selftest OK · py_compile OK · 行尾纯 LF ·
#        `voice/*.wav` = 61 个 · `grep voice_mode_sfx main.py` 只剩注释。
#
# 【v0.7.88 删掉「关闭声音」的提示语音(也不加载)】
#
#   玩家 2026-09-16: 「删掉关闭声音的这个音效, 也不加载」。
#   **动机很具体**(玩家原话): 「刚才在办公室, 我不想人听见, 我点了这个关闭声音,
#   结果声音贼大」—— 他想静音, 按下去之后它**先大声念了一句**「关闭声音」,
#   在需要安静的场合反而更吵。一个"关声音"的按钮不该先制造声音。
#
#   改动:
#   · `toggle_mute`: 去掉 `sfx.play("voice_mode_off")` 与
#     `Clock.schedule_once(self._apply_sound_off, 1.0)` —— 改成**立即静音**。
#     那个 1 秒延迟本是留给播报收尾的(顺带成了"切回来就取消关闭"的反悔窗口);
#     语音删掉后它失去了理由, 留着会变成"我明明点了关、声音还在响一秒"。
#   · `_apply_sound_off` 方法整个删除(只被上面那一处调用)。
#   · `voice/voice_mode_off.wav` **从仓库删除** —— 语音是按目录(`os.listdir`)
#     全量加载的, 文件不在就**不会加载**(这就是玩家要的"也不加载")。语音数 63 -> 62。
#   · `tools/generate_voice.py` 清单同步删掉该条(否则重新生成又会造出来)。
#   · README 的语音数 63 -> 62(三处)。
#   · 设计页: 「关声音可以反悔」那张卡重写成「关声音就是立刻关」, 语音数同步 62。
#
#   保留: `voice_mode_sfx`(「开启中奖音效」)—— 打开声音时那一句照播,
#        那时玩家要的就是声音, 不存在"吵到自己"的问题。
#
#   验证: --selftest OK · py_compile OK · 行尾纯 LF ·
#        `grep voice_mode_off main.py` 只剩注释 · `voice/*.wav` = 62 个。
#
# 【v0.7.87 SOC 高压历史界面: 高度 / 列宽 / 缺字】
#
#   玩家 2026-09-16 报的两条:
#   ① 「SOC 高压历史的界面需要优化: 1、需要把高度大幅提高, 同 1 页显示更多历史数据
#      2、详情按钮变窄很多, 把时间内容宽度大幅度提高。让字体能均衡显示」
#      · **显示行数**: 写死的 8 行 -> **按视口算**(0.96*_vh 减固定部分约 240dp, 每行 30dp)
#        ⇒ 360x660 上约 13 行、320x560 上约 9 行。留自适应是为了小屏不把内容顶出弹窗
#        (见 `_popup_fit_content` 里"字飘在游戏画面上"那一段)。
#      · **列宽重排**: 「详情」从"吃剩余"(实测 ~86dp, 而俩字只要 ~30dp) 改成**固定 46dp**;
#        时间列改成**吃剩余**(360dp 机上 84 -> 约 126dp, **+50%**)。旧布局下时间列只有
#        84dp 却要放 14sp 下宽 113px 的完整时间戳 ⇒ 时间列字号被压到 10.4sp、与另两列的
#        14sp 不齐 —— 玩家报的"字体不均衡"就是它。现在三列同档。
#      · **滚动区高度公式** `28*_vn+10` -> `30*_vn+10`: 行高 28 之外还有 `inner` 的
#        `spacing=2`, n 行真实内容高 = 28n+2(n-1)。旧式在 12 行时容器 346px 而内容
#        358px, **最后一行被裁一半**(截图实证)。
#   ② 「有一个字显示为 xx」 —— 根因是**字体子集缺字**: `fonts/NotoSansSC-Medium.otf`
#      只覆盖 1602 个码位, 而界面文案里用到了「群」(行 14036 脚注"一个离群值")。
#      按玩家给的方案「把括号内的字都删了」删掉那段口径沿革(解释保留在代码注释里) ——
#      一举两得: 缺字消失 + 脚注从 3 行回到 2 行, 高度让给列表。
#      ⚠️ 顺带清掉另外 7 处**界面文案**里的缺字(同样会显示成方块, 只是玩家还没撞上):
#        「性能簇」->「性能核组」x3 · 「性能簇亲和性」->「核组绑定」· 「只碰寄存器」->
#        「不碰内存」· 「厉害」->「明显」· 「吃亏」->「变慢」· 「CPU 亲和性」->
#        「CPU 绑定核」x2 · 跑分诊断行首的 `↑` ->「上面」。
#      ⚠️⚠️ **字体子集已落后于 main.py**: 全文扫描 22 个码位缺字形, 其中 8 处在界面文案里。
#        本轮全部用**改文案**规避(零风险、不增包体积)。若以后还要加新字, 正解是重跑子集,
#        对拍口径见 `temp/subset_full_probe.py`: 1602 字 x 5 字号 x bold 两种 = 16020 次渲染,
#        像素差 0 处。**加文案时先扫一遍缺字**(脚本见 `temp/` 下的字体探针)。
#
#   验证: --selftest OK(无异常) · py_compile OK · 行尾仍是纯 LF ·
#        `temp/_hpsoc_shot.py` 三张截图(12 行全显示 / 详情弹窗 / 空态) ·
#        界面文案缺字复扫 = 0(只剩不可见的 U+FE0F)。
#
# 【v0.7.86 修 P0: 波 2 跑完黑屏不撤(玩家报「卡在成绩核算中, 不回主界面」)】
#
#   **玩家报的**: 「soc高压测试结束后, 会卡在『成绩核算中：1秒』处, 不会回到主界面」。
#   玩家随后附的高压结果弹窗截图**内容完整、有「关闭」按钮** ⇒ 弹窗本身正常,
#   卡住发生在**关掉弹窗之后**。
#
#   根因: **`_hp_done`(波 2 结束)整个漏了 `_hide_bench_dim()`**。
#     黑屏(置灰层+白字)是 v0.7.82 才启用的, 当时只把撤屏加进了 `_bench_done`(波 1),
#     波 2 这条路径一个字没加 ⇒ 高压跑完黑屏永久留在屏幕上, 白字停在最后一次进度。
#     玩家看到的就是"黑屏 + 一句话不动"。
#
#   修法(三处, 缺一不可):
#     ① `_hp_done` 开头加 `_hide_bench_dim()` —— 正常结束路径。
#     ② `_run_hp_test` / `_run_benchmark` 的 `finally` 各加一句兜底
#        `Clock.schedule_once(lambda dt: self._hide_bench_dim(), 0)` —— **异常路径**:
#        跑分中途抛异常时, 那个 `Clock.schedule_once(self._hp_done)` 根本走不到,
#        只修 ① 的话黑屏照样永久留着。
#        ⚠️ 兜底**不能放 finally 第一行** —— 门禁 L6 照"finally 首行"查 `_bench_fps_lock_off(`
#           配对(与波 1 同一规矩)。
#     ③ 更正 `_bench_done` 里那条**过期注释**("兼容旧路径: 当前跑分不再置灰") ——
#        那句话在 v0.7.82 之后就错了, 会让人以为这里的撤屏可有可无。
#
#   ⚠️ **顺带删掉「成绩核算中：x秒」那个显示分支**(玩家: 「如果这个显示机制没有用,
#      是一切的错误根源, 你就把他给删掉」)。
#      · **它不是根源** —— 根源是上面 ① 那条漏了的撤屏; 它只是"黑屏留着时白字停在最后
#        那句文案"里的**那句文案**。
#      · **但它确实没用**: 核算实测只要 51ms(见 `temp/hp_tail_probe.py`), 而轮询 0.25 秒
#        才一次 ⇒ **多数情况下这段文案连一帧都画不出来**。删掉零损失。
#      · 现在超点就**封顶**显示 360/360。删掉"跳动"也顺带消掉一个误导源 —— 它看起来像
#        "还在干活", 实际可能已经卡住, 而**这正是玩家把它当成病根的原因**。
#
#   参考数据(玩家附的波 2 结果弹窗, TB323FU / v0.7.83 / 360 秒):
#     首 37441 → 末 31740 步/秒(降 15%) · 最低 31648 · 中位 32467 · **平均差系数 1.27%**
#     性能核已锁定 cpu6,cpu7 · 跑分线程优先级已提升 -4 → -8 · CPU 平均 4315MHz
#     每段采样 37441/33181/33056/32866/32714/32399/32410/32423/32149/32263/31903/31792/31816
#     (衰减曲线平滑, 无跳变 —— 这台机器的散热表现正常)
#
#   验证: `bench_instr_probe` 23/23 · `hp_instr_probe` 8/8 · `benchhist_probe` 21/21 ·
#   `_bench_screen_probe` 11/11(含新增的 T5/T5b: 两条结束路径都有撤屏 + 两处 finally 兜底) ·
#   `--selftest` OK · 物理逐位哈希不变(`4d7a7e07...4fe4b7cb`)。
#
# 【v0.7.85 波 2(高压)也降到 10fps —— 两波同档】
#
#   玩家 2026-09-15 澄清: 「我这里是 360 秒的高压测试 SOC高压测试 fps 你不是说改为 10fps
#   吗 怎么是 42fps」。
#
#   我原来只把**波 1** 改成 10fps, 波 2 留 60(依据是玩家更早那句「压力测试不用改」)。
#   但玩家说的"这个画面"是**包含帧率**的 —— 黑屏 + 白字 + 低帧率是一整套, 两波都该用。
#   ⇒ 现在 `_BENCH_FPS_FORCE` 也改成 10, **两波统一**。
#   (实测证据: 玩家在高压期间看到 **42fps** —— 闸门是 60、实际只渲染到 42, 说明主线程
#    当时还在跟满载的跑分线程抢 CPU; 降到 10 之后那部分争抢就还回去了。)
#
#   ⚠️ **代价说清(已告知玩家, 玩家仍选统一)**: 波 2 问的是"一直满载会掉多少"(散热能力)。
#      降帧率等于**少画就少发热**, 衰减曲线会好看一些, 但那**不再等于"真实负载下的散热能力"**。
#      玩家更早那句「压力测试不用改」指的是**测量协议**(窗口/轮数/间隔)别动 —— 现在协议
#      确实一个字没动, 动的只是屏幕合成次数。
#
#   验证: `_bench_screen_probe` 7/7 · `bench_instr_probe` 23/23 · `hp_instr_probe` 8/8 ·
#   物理逐位哈希不变(`4d7a7e07...4fe4b7cb`)。
#
# 【v0.7.84 修日志文案(帧率上限印的是常量不是实际值) + Y700五代真机验证】
#
#   ① **文案 bug**: 波 1 的日志行印的是 `_BENCH_FPS_FORCE`(60) 而不是**实际按到的**
#      `_bench_fps_force_now()`。自 v0.7.82 起波 1 用更低的 `_BENCH_FPS_FORCE_PHYS`,
#      于是真机上出现**自相矛盾的一行**:「强制帧率上限 **60** · 实际渲染 12.9fps」。
#      (这也是玩家截图里"帧率显示 60"的来源之一。)
#
#   ② **Y700 五代(TB323FU) / v0.7.83 两份真机日志 —— v0.7.82/83 的三件事全部生效**:
#      日志                     | 逐轮步/秒                        | 中位   | 平均差系数 | 探针波动 | 实际渲染
#      plinko_fps_..._225103   | 20292·20274·20274·20258·20259   | 20273  | **0.05%**  | 1.10 倍  | 13.2 fps
#      plinko_fps_..._225327   | 34620·35204·34921·34885·34955   | 34921  | **0.38%**  | **1.04 倍** | 12.9 fps
#      · **10fps 那档生效**: 实际渲染 12.9/13.2fps(v0.7.81 时是 55fps)。
#      · **探针分块取最大生效**: 探针波动从 221654 那次的 **1.62 倍**降到 **1.04~1.10 倍**。
#      · **线程优先级仍生效**: `已提升 -4 → -8`。
#      · **稳定性是历史最好**: 0.05% / 0.38%(此前 25102RKBEC 上是 0.22% / 1.48%)。
#        ⚠️ 225103 逐轮只差 34 步 —— 那种"几乎完全一致"通常是**已经进入降频后的稳态**。
#
#   ③ ⚠️ **但暴露了真正的病**: 同一台设备、同一个版本, 两份的**中位数差了 42%**
#      (34921 vs 20273), 而且**慢的那份反而更稳**(0.05% vs 0.38%)。**这与参数无关,
#      是设备状态(散热/降频)决定的** —— 正是玩家最早提的「同一个 CPU 跑分要稳」那条。
#      下一步该往"跨次一致性"使劲(温控/预热/冷却门), 而不是继续调窗口结构。
#
#   验证: `_bench_screen_probe` 7/7 · `bench_instr_probe` 23/23 · `hp_instr_probe` 8/8 ·
#   `benchhist_probe` 21/21 · `--selftest` OK(首跑偶发 3σ 假失败, 重跑即过) ·
#   物理逐位哈希不变(`4d7a7e07...4fe4b7cb`)。
#
# 【v0.7.83 修 v0.7.82 的真机卡死: 黑屏/白字必须建在主线程】
#
#   **玩家 2026-09-15 真机报的**: 「我刚开始跑帧率就显示这个。而且一直卡住不动了」
#   (截图: 黑屏出来了、白字停在「物理跑分 0/5」一动不动)。
#
#   根因: v0.7.82 我把 `_show_bench_dim()` / `_set_bench_msg()` 写进了
#   **`_run_benchmark` / `_run_hp_test` 的开头**, 而那两个函数跑在**工作线程**上
#   (`threading.Thread(target=self._run_benchmark)`); 它们要碰 Kivy 的 canvas 指令与
#   **CoreLabel 的文字光栅化** —— **跨线程做这个会卡死**。
#
#   修法: 把这两句搬到**主线程那一侧**的启动器 `_wait_idle_then_bench` /
#   `_wait_idle_then_hp` 里(它们是 Clock 回调), 工作线程体里只留一句注释说明为什么不在那儿。
#   ⚠️ **别搬回去** —— 线程体里再出现 `_show_bench_dim(` / `_set_bench_msg(` 就是回归。
#
#   ⚠️⚠️ **为什么上一版的门禁全绿却漏了它**: `temp/_e2e_bench.py` 直接调
#      `benchmark_trajectories`, **根本不经过 GUI 那条路径**; 而 `bench_instr_probe`
#      是直接调 `rw._run_benchmark()`(线程体), 也不走启动器。**两条路都绕开了 bug 所在处。**
#
#   ⇒ 新增门禁 `temp/_bench_screen_probe.py`(**7 条**), 走**真 GUI 启动路径**:
#      T1 结构: 两个工作线程体里**不许**出现建屏/写白字(阴性对照: 搬回去当场变红)
#      T2 结构: 两个主线程启动器里**必须**有它们
#      T3 行为: 黑屏**真的出现**;  T3b 白字**真的在更新**(不是停在 0/N)
#      T4 行为: 跑分期间主线程**还在动**(帧计数在涨) —— 卡死的直接判据
#      另: 本探针顺带量到 `_BENCH_FPS_FORCE_PHYS=10` **在桌面确实生效**(1.6 秒涨 21 帧 ≈ 13fps)。
#
#   验证: `_bench_screen_probe` **7/7** · `bench_instr_probe` 23/23 · `hp_instr_probe` 8/8 ·
#   `benchhist_probe` 21/21 · `--selftest` OK · 物理逐位哈希不变(`4d7a7e07...4fe4b7cb`)。
#
# 【v0.7.82 跑分黑屏+白字 · 探针改「分块取最大」(仍不碰球)】
#
#   一、**跑分黑屏 + 白字**(玩家 2026-09-15:「你直接黑屏就行, 上面写几个字就可以」+
#       「如果顺利的话, 压力测试也使用这个画面」)。
#       · 置灰层 0.72 → **1.0**(纯黑)。它原来根本没被调用(注释写着"当前跑分不再置灰"),
#         现在在**波 1 与波 2 开始时都接上**。
#       · 白字**不是控件**: `RootWidget.canvas.after` 盖住整棵子树, 盘面里那个
#         `_bench_badge` 标签会被黑屏一起盖掉 ⇒ 白字改用 **CoreLabel 的纹理**画在
#         同一个 `canvas.after` 里、**矩形之后**(顺序即层序)。内容 = 进度
#         ("物理跑分 3/5" / "SOC高压测试 120/360秒"), 由 `_prog_tick` 每 0.25 秒刷。
#       · **波 1 帧率 60 → 10fps**(新增 `_BENCH_FPS_FORCE_PHYS`; 波 2 与渲染测量不动,
#         按玩家「压力测试不用改」)。
#         ⚠️ 为什么能这么低: 跑分是从 `state==ready` 起的, **本来就没球没演出**,
#            "去掉动作"这半现状已满足, 负载全在每帧的 GL 合成上; 而白字 0.25 秒才变一次
#            ⇒ **4fps 就够**。玩家:「其实帧率可以更低」。
#         ⚠️ **更正一条我写错的注释**: 原文写"1fps 会被系统读成 app 卡死了而去降频" ——
#            **不准确**。2026-09-15 查证: Android 判 ANR **只有时间判据**(主线程 5 秒内
#            没处理掉输入事件, `InputDispatcher` 给每个事件算 timeoutTime), **判据里没有
#            帧率这一项**。低帧率只能**间接**引发(单帧太重 ⇒ 主线程忙于画帧 ⇒ 攒够 5 秒),
#            而**降帧率恰恰是往"不 ANR"的方向走**。真正要守的是**用户可感知的卡顿线**
#            100~200ms/帧(5~10fps) —— 10fps 踩在下沿, 且跑分期间黑屏无动画。
#
#   二、**探针改「分块取最大」**(`_speed_probe` / `_alloc_probe` **同构**, 别只改一个)。
#       血证: 真机 `plinko_fps_20260915_221654` 的第 2 轮**探针报 19.47M** 而其余四轮
#       28.0~31.6M(**-34%**), 可**同一轮**的实际步/秒只低 **2.3%** —— 同样一次瞬时干扰,
#       摊进 50ms 占 100%、摊进计分段 4000ms 只占 1.25% ⇒ **那个 -34% 是尺子自己被干扰,
#       不是核心真的慢**。
#       做法: 50ms 切成 `_PROBE_BLOCKS=4` 段, **返回最快那一段**(干扰只会让测量变慢、
#       不会变快 ⇒ 上界量该用最大值估计)。总成本不变。
#       ⚠️ `_PROBE_BLOCKS` **不是从"哪个块数最稳"的排名挑的** —— 实测两轮
#          (`temp/_probe_blocks.py`, 每档 30 次) **排名完全相反**(一次 6 块最好/8 块最差,
#          另一次反过来), 说明桌面分辨不出。改按**机制约束**取值: 下界 2(抗干扰),
#          上界 ~5(每块 >=10ms 才有 ~500 轮计数)。两次都复现的只有"中位随块数上升
#          (+6~9%)" = 机制确实生效。
#       ⚠️ 副作用: 探针值系统性偏高 ⇒ **归一化跑分会变小**, 这是一次**口径切换**,
#          不是设备变快了。
#
#   三、**修掉一个真 bug**(自测抓到的): 我用区间替换改 `_speed_probe` 时, 把夹在
#       `_speed_probe` 与 `_alloc_probe` **之间的 `class _ProbeObj` 一起删掉了** ⇒
#       `_alloc_probe` 抛 NameError。⚠️ **端到端跑分测试当时是"通过"的** —— 因为
#       `benchmark_trajectories` 里那句调用被 `try/except` 包着, 异常被静默吞成 0.0。
#       **只有 `bench_instr_probe` 的 K11 抓到了它**(分配探针全是 0)。
#       教训: 探针静默归零是**看不见**的, K11 这条必须留着。
#
#   验证: `bench_instr_probe` **23/23** · `hp_instr_probe` 8/8 · `benchhist_probe` 21/21 ·
#   `--selftest` OK · 端到端跑分无异常 · 物理逐位哈希不变(`4d7a7e07...4fe4b7cb`)。
#   ⚠️ 探针那条断言(K7/L6/L9)跟着改动更新过, 都是**断言过期**不是功能坏了。
#
# 【v0.7.81 波 1 窗口改「掐头 0.5s + 计分 4s + 留尾」(桌面实测定参, 仍不碰球)】
#
#   玩家 2026-09-15: 「单次跑分时间理论上别超过 6 秒, 4-5 秒比较合适(考虑这是移动设备)」
#   + 「不要只看评分, 还要看具体得分变化了多少」「如果不足 0.5% 是不是也没有意义」。
#
#   参数(单轮窗口 5.0 → **4.5 秒**, 落在玩家要的区间里):
#     · 掐头 1.0 → **0.5 秒**(去 DVFS 爬坡)
#     · 计分 3.0 → **4.0 秒**(样本比原来多 33%)
#     · 掐尾 1.0 → **0.0**(**留尾** —— 这条是数据推翻直觉的地方, 见下)
#
#   依据: `temp/_window_fair.py` **一次采集、离线切不同起点**的赛马(5 秒窗口内、计分固定
#   4 秒 ⇒ **样本数完全相同**, 消掉系统漂移; 上一版让计分时长跟着缩短, 于是"样本变少"的
#   代价盖过了"掐头"的收益, 得出了相反的错误结论 —— 那个漏洞是玩家指出的):
#
#     方案              μ(得分)   平均差    评分      μ变化      平均差变化
#     不掐              32510    316.6   103.2      —          —
#     掐 0.25s          32544    291.0   112.0    +0.11%     −8.07%
#     掐 0.50s          32584    296.7   110.8    +0.23%     −6.26%
#     掐 0.75s          32554    257.0   127.3    +0.14%    −18.81%
#     掐 1.00s          32563    295.5   111.0    +0.16%     −6.67%
#
#   两条结论:
#   ① **得分 μ 全部不足 0.5%**(最大 +0.23%, 噪声级) —— 按玩家的判据, "得分"这一项
#      **没有意义**。这一整套买到的是**同设备更稳**, 不是"跑得更高"。如实记账。
#   ② **平均差降 6~19%**, 远超 0.5% —— 稳定性是真改善。而掐多少(0.25/0.5/1.0s)
#      **数据分不出来**(评分 110.8~112.0 全在噪声内; 0.75s 那档 127.3 像 6 次采样的
#      离群值, 不拿来定参)。既然一样, 就取省时间的 0.5。
#   ③ **"留尾"是数据推翻直觉的地方**: 另一批 8 秒窗口的公平赛马显示"越靠后取越稳"
#      (取 0~4s 平均差 1058 → 取 3~7s 降到 793) ⇒ 尾部是最稳的一段, 掐掉是**净损失**。
#      所以 `_BENCH_TAIL_SEC = 0.0`, 与"掐头去尾"的直觉相反。
#
#   ⚠️ 全是**桌面**数据。真机 DVFS 比桌面激进得多, 幅度肯定不同 —— 最终裁决要真机跑一次。
#
#   顺带考证(回答玩家「我记得 geekbench6.1 是 5 秒？」): **Geekbench 6.1 的 5 秒是
#   workload 之间的「间隔」, 不是样本时长** —— 它把间隔从 2 秒加到 5 秒(6.0 是 2 秒),
#   理由正是"减少温控、降低逐次波动"。项目里 `SOC_SAMPLE_GAP_SEC = 5.0` 用的就是它。
#   官方**没有公开**每个 workload 跑多久(查过, internals PDF 403), 所以样本时长是我们
#   自己定的, 不受 Geekbench 约束。
#
#   验证: `--selftest` OK · 物理逐位哈希不变(`4d7a7e07...4fe4b7cb`) ·
#   `benchhist_probe` 21/21 · `hp_instr_probe` 8/8。
#
# 【v0.7.80 模拟历史面板: 表头独立排版 + 表头改名「中位分/平均差系数」】
#
#   玩家 2026-09-15 定稿(附截图): 「这个标题和附录需要优化 —— 标题中从平均差系数改为
#   中位分/平均差系数; 备注调整, 删掉波动和后续」。
#
#   ① **表头独立排版**(这条是根, 另两条都靠它才成立)。玩家原话:
#      「你让表头和表格内的内容不对齐就可以了, 时间才 2 个字, 内容那么长」。
#      表头「时间」只要 ~26px, 而数据「2026-09-15 14:07」要 **113px** —— 本来就不该共用
#      一套宽度。共用的时候, 表头里最长的那列会把**全表字号**往下压, 整张表的字都变小,
#      那比"列没对齐"严重得多。现在表头按**自己文字的宽度**分列、整行居中; 数据行照旧用
#      `_HW`。代价是表头与数据不逐列对齐 —— **玩家已确认接受**。
#
#   ② **表头文字**: `时间 / 平均/1%Low帧 / 平均差系数` → `时间 / 平均/1%Low帧 /
#      中位分/平均差系数`(玩家把「中位跑分」缩成「中位分」)。
#      ⚠️ 口径**仍是平均差系数** —— 玩家在选择题里选的, 不是标准差系数(两个是不同的量)。
#
#   ③ **脚注**: 删掉「波动：(最大跑分-最小跑分)/中位数跑分」那一行(旧口径已被取代),
#      换成「平均差系数：平均差 ÷ 均值, 越小越稳」。表头里出现了新词, 脚注就得解释它。
#
#   ④ 顺手修掉**两个字号 bug**(都是我自己在这次改动里引入、被探针当场逮住的):
#      · 第一版: 表头宽度被当成"该列的可用宽度"去算字号 ⇒ 第一列表头「时间」只有 30dp,
#        却要塞下该列最长的内容「2026-09-15 14:07」(113px) ⇒ 全表字号崩到 **5.88sp**。
#      · 第二版: 字号只按 `_HW` 算了, 但"最长内容"仍把**表头**算进去 ⇒ 新表头 9 个字
#        把全表拖到 **10.64sp**。
#      ⇒ 现在: 字号按**数据列宽**算, "最长内容"**只从数据行取**; 表头有自己的宽度。
#      实测字号回到 **13.16sp**(改动前的水位), `benchhist_probe` F1~F4 全绿。
#      ⚠️ 这段的教训写进注释了: 表头是标签, 它的宽度/文字都不该约束数据的字号。
#
#   验证: `benchhist_probe` 21/21(F1 不裁 / F2 不折行 / F3 字号 13.16 / F4 全表一个字号) ·
#   `hp_instr_probe` 8/8 · `--selftest` OK · 物理逐位哈希不变(`4d7a7e07...4fe4b7cb`) ·
#   截图人工核对(表头与数据视觉齐整、脚注两行到位、旧记录印「—」未回填)。
#
# 【v0.7.79 稳定性读数改用「平均差系数」, 高压删掉「归一化」列(仍不碰球)】
#
#   玩家 2026-09-15 定稿: 跑分与高压里的**「波动」全部改用「平均差系数」**, 高压的
#   **「归一化」列删掉**。
#
#   ① **平均差系数 = 平均差 ÷ 均值**(新增 `_mad_coef`)。旧「波动」的口径是
#      `(max-min)/中位` —— **只看两个极端点**, 一个离群值就能把它整个带飞(那段旧注释
#      自己都写着"跑分次数少时一个异常值就能把它带飞")。平均差用上**每一个样本**,
#      对离群值的敏感度远低于极差; 相比 σ 也低一档(平均差不平方)。
#      换掉的地方: 波 1 状态栏 + 逐轮日志 + 模拟历史面板表头与数据 / 波 2 结果弹窗 +
#      历史面板 + 详情弹窗。
#
#   ② **高压的「归一化」列删掉**(显示层面)。`norm` 与 `spread` 两个字段**照旧存进记录**
#      —— 老记录要能读、以后要复盘。**别顺手把它们从记录里删掉。**
#      ⇒ 高压历史面板从 5 列变 4 列(时间/中位数/平均差系数/详情), 「详情」按钮宽度反而
#      **从 55px 涨到 71px**(加列最容易挤坏的就是那个按钮, 这是往回走)。
#
#   ③ 模拟历史面板第三列表头 `中位跑分 / 波动` → `平均差系数`。⚠️ 表头**必须用这个短词**:
#      试过 `跑分/平均差系数`, 全表字号被从 **13.16sp 压到 11.48sp**(玩家看得见的回归)。
#      改这几个字之前先跑 `temp/benchhist_probe.py` 看 F3/F4 的字号。
#
#   ④ **窗口三段结构**(波 1, 见 v0.7.78): `_BENCH_HEAD_SEC` / `_BENCH_TAIL_SEC` 保持 1.0 秒。
#      ⚠️ **桌面的离线赛马说"不掐最好"**(不掐 1/CV 56.8 > 掐1 53.9 > 掐2去2 48.5 >
#         掐6去6 33.4) —— 但**原因是样本变少导致 σ 升**(μ 只降 0.1~0.7%), 而不是
#         "掐错了"。**桌面根本没有爬坡可掐**(实测窗口内末/首 = 1.026)。
#         真机的间接证据是另一回事: 7 份日志里 5 份、锁核后 3/3 的**最低轮都是第 2 轮**
#         (低 2.3~2.9%), 那正是轮首 DVFS 爬坡的形状。**这个病桌面测不出来** ⇒ 参数留给
#         真机裁决: 若真机上第 2 轮仍明显偏低, 把这两个值调大; 若各轮已经齐平, 调回 0。
#
#   ⑤ 顺手修掉探针里的一处**假绿灯**: `hp_instr_probe` 的行筛选器只认"无数据"和纯数字,
#      换列之后旧记录那格印的是「—」⇒ 一半数据格根本没被收进 `_rows`, 于是 **H5
#      (每一列都没被裁) 恒真**。修完数据格从少数变成 13 个, H5 才真的在检查。
#
#   验证(**逐位**): `temp/_phys_hash.py` 200 个固定种子, 每帧 x/y/vx/vy 精确到 17 位有效
#   数字连同落格号喂 sha256 —— 改前改后**哈希完全相同**(`4d7a7e07...4fe4b7cb`)。
#   **弹珠物理一个 bit 都没变**。`--selftest` OK(四档 RTP 全过 / 卡死 0)。
#   探针: `benchhist_probe` 21/21 · `hp_instr_probe` 8/8。
#
# 【v0.7.78 跑分度量: 线程优先级 + 剔除起跑段(弹珠逻辑一行未改)】
#
#   目标(玩家 2026-09-15 定稿): **评分 = 1 / 变异系数 = 平均值 ÷ 标准差**, 越高越好。
#   它同时是两条 —— 同一台设备重复跑要**稳**(标准差小), 更好的 CPU 要跑得**高**(平均高)。
#   ⚠️ 所以这一版**只动测量侧**: 弹珠的运行逻辑(物理/碰撞/发射/球路)一行都没碰。
#
#   ① **跑分/高压工作线程提调度优先级**(新增 `_bench_raise_thread_priority`):
#      `_bench_pin_fast_cpus` 管的是"允许跑在哪些核", 它**不保证抢得到 CPU**; 而跑分线程和
#      Kivy 主线程在同一个进程里抢 CPU, 进程内的调度权重是自己能改的(App 改本进程线程的
#      优先级不需要 root)。用 `Process.setThreadPriority(THREAD_PRIORITY_URGENT_DISPLAY)`
#      提升, 并用 `getThreadPriority(0)` **读回验证** —— 系统可以静默拒绝, 没变成负数就如实
#      记"未提升", 不印假成功。只作用于这一颗线程(主线程与正常游戏不受影响), `finally` 恢复。
#
#   ② **剔除起跑段**(`benchmark_trajectories._run_once`, 新增 `_BENCH_RAMP_CPU_SEC = 0.4`):
#      轮与轮之间有 5 秒间隔, 大核在那几秒里已经落到低频 OPP, 于是每个窗口的**开头**那几百
#      毫秒是在 DVFS 爬坡 —— 把它算进分母等于拿"降温"换了"降分", 两头都亏。做法是先跑一小段
#      **不计成绩、不占分母**的同种负载把频率顶到位, 再起计时。
#      判据来自真机: 7 份日志里 **5 份的最低轮都是第 2 轮**(锁核后的 3 份日志里 3/3)。
#      ⚠️ 预热用**独立的随机流** —— 否则会消耗主序列的随机数、让"跑哪些球"变掉, 历史就不可比了。
#
#   ③ 顺手更正一条**过期注释**: `_BENCH_PIN_FAST_CORES` 旁原写"正常游戏、渲染线程和高压
#      测试都不改亲和性"—— 实际 `_run_hp_test` / 渲染采样 / `_run_benchmark` **三处都调了**
#      `_bench_pin_fast_cpus()`。照那句旧注释做判断会得出错误结论(1号专家复核时抓到)。
#
#   验证(**逐位, 不是抽样**): `android/temp/_phys_hash.py` 对 200 个固定种子各跑一发, 把每帧
#   的 x/y/vx/vy 精确到 **17 位有效数字**连同落格号喂进 sha256 —— 改前改后**哈希完全相同**
#   (`4d7a7e07...4fe4b7cb`)。**弹珠物理一个 bit 都没变**。`--selftest` OK(四档 RTP 全过 / 卡死 0)。
#
#   ⚠️ ①②的**真机收益要靠这次出包实测**: 桌面没有手机那种激进的 DVFS, "剔除起跑段"在桌面测不出
#      差别; `setThreadPriority` 在桌面直接走"非安卓"降级分支。跑完看日志里那两行
#      (「物理跑分工作线程性能核锁定」与「物理跑分工作线程优先级」)是否显示已生效。
#
# 【v0.7.77 物理热路径优化: 桌面实测吞吐 +29.8%, 物理逐位不变】
#
#   目标: 抬高「中位数跑分 × (1 − 0.5 × 波动比例)」这个评分(玩家 2026-09-15 定稿的判据)。
#   做法全是**纯查找/内联优化**, 一行物理公式都没动 —— 收益是**真吞吐**, 不是把数字做漂亮:
#   `advance_flight` 是 GUI 与 selftest 共用的同一份代码, 所以真机游戏同步变快。
#
#   ① `physics_step`: 把 `G/VMAX/FLOOR/BALL_R/WALL_E/E/_ARC_REACH/SUBSTEPS/math.hypot`
#      绑成**默认参数**(全文件只在定义处赋值一次, 已逐条核对), 每子步的 LOAD_GLOBAL 换成
#      LOAD_FAST; `geo["walls"/"deflectors"/"peg_rows"/"dividers"]` 提到函数开头取局部。
#      ⚠️ 那四个 geo 列表**只能在函数内取局部** —— `self.geo["deflectors"]` 会被**每一发的
#         弧面抖动重新赋值**(见 launch_ball 附近的 arc_dy), 提到 advance_flight 那层就错。
#      ⚠️ `_ARC_FRAME` **没绑**: 它是全局可变的(advance_flight 里每帧 +=1)。
#
#   ② `_collide_rect`: `max(rx1, min(b.x, rx2))` 双轴 clamp **内联成条件式** + 缓存 b.x/b.y。
#      profile 实测 `min`/`max` 各被调 **417 万次**、合计占物理热路径 **8.5%**, 是全链路最大
#      的一块**可内联**开销。本函数内所有读取都在写回之前 ⇒ 缓存与逐次读取等价。
#
#   ③ `walls` / `dividers` 循环加 **x 向 AABB 粗筛**: 左右两面墙的 y 覆盖**全程**(0~660),
#      原来那条 y 粗筛对它们**永不生效** ⇒ 每子步都白调一次 `_collide_rect`。球心到矩形的
#      最近点距离 >= 两个 AABB 的间距, 所以两轴都不重叠 ⇒ **必然不碰** —— 这是**超集判据**。
#
#   验证(**逐位, 不是抽样**): `android/temp/_phys_hash.py` 对 200 个固定种子各跑一发, 把
#   每一帧的 x/y/vx/vy 精确到 **17 位有效数字**连同落格号喂进 sha256 —— 改前改后**哈希完全
#   相同**(`4d7a7e07...4fe4b7cb`), 落格分布也一致。**物理一个 bit 都没变**。
#
#   收益(**桌面交替 A/B, 3 组**, 消除系统漂移): 原版中位 33255 → 优化版 43161, **+29.8%**;
#   profile 总 cumtime 8.136s → 6.774s(前两处, 第三处另计)。
#   `--selftest` 连续 3 次: 2 次 OK · 1 次偶发 3σ 假失败(项目既有现象, 重跑即过)。
#
#   ⚠️ 这是**桌面 x86 上的相对收益**。真机 ARM 上省掉的同样是 CPython 解释器开销
#      (LOAD_GLOBAL / 内置函数调用), 但物理步在那边更受内存带宽限制, 相对收益可能小一些。
#      要不要出包验证由玩家定。
#
# 【v0.7.76 回退 0.7.74/0.7.75 的测试分核与 GIL 让出, 保留测试历史表格居中】
#
#   为什么回退 —— 真机实测跑分大幅下降, 且**不是**"为流畅付出的合理代价", 是两处实现问题:
#
#   ① `_bench_gil_yield` 挂在**每一个物理步**上。物理跑分是纯 Python 热循环, 每步多一次
#      函数调用 + 一次取模; 每 128 步再 `time.sleep(0)` 主动让出执行机会。跑分测的就是吞吐,
#      这笔开销直接吃步/秒, 还附带线程切换与缓存失效 ⇒ **不光标低分数, 波动也更大**。
#
#   ② `benchmark_sustained`(波 2 / SOC 高压)的 `for _ in range(4000)` **缩进掉出了
#      `while` 循环体** —— 那一秒窗口内只疯狂 `launch_ball`、**从不模拟飞行**, 退出后只模拟
#      最后一颗球。⇒ 0.7.74/0.7.75 的 6 分钟高压衰减曲线**整条是废的**, 不只是"分数低"。
#      实测对照: 正常一轮 203 发 / 55066 步、逐窗步/秒 ~34000; 坏状态下只剩 4 发 / 874 步、
#      逐窗步/秒 401~780(崩掉 50~80 倍)。0.7.73 里这段缩进是正确的。
#
#   做法: 逻辑**完整回退到 v0.7.73**(以 4a4ad43 的 main.py 为基底重建, 天然修掉 ② ),
#   只保留 0.7.74 的界面修复 —— 「测试历史（渲染 / SoC）」表头行与数据行按同一张
#   290dp 固定宽度表格整体水平居中。跑分口径回到已知最高、且与历史数据可比。
#   代价: 测试期间界面帧率回到约 54fps, "保持接近 60 帧"这个目标放弃(用户 2026-09-15 已知情)。
#
#   判据: `git diff 4a4ad43 -- main.py` **恰好只剩 2 个 hunk**(表头行 + 数据行居中), 多一个都不行。
#   回归: py_compile · --selftest OK(四档 RTP 全过) · 高压缩进专项 PASS
#        (含**阴性对照**: 把缩进改回坏的样子实测 FAIL, 逐窗步/秒 596 vs 正常 34026) ·
#        bench_instr_probe 23/23 · hp_instr_probe 8/8 · benchhist_probe 21/21。
#
# 【v0.7.75 跑分期间维持主界面接近60帧 + 历史面板居中修复】
#
#   这版把本次优化用中文完整说明：
#   · 测试时，主界面渲染线程临时保留中核，物理跑分和 SOC 高压线程锁定最高性能核；
#     测试结束或异常时恢复系统原来的 CPU 亲和性。
#   · 物理计算循环每处理 128 个模拟步主动让出一次 GIL，给 Kivy 主线程稳定的执行机会，
#     目标是让测试期间实际帧率保持在 55~60 帧附近；60 仍是目标帧率上限，不是假装保证值。
#   · 修复 Y700 与红米 K90 Pro Max 上「测试历史（渲染 / SoC）」表格整体偏左的问题，
#     表头和数据行现在按同一张固定宽度表格整体水平居中。
#
#   回归：py_compile；历史面板 21/21；跑分链路 23/23；高压链路 8/8。
#
# 【v0.7.74 测试分核与 GIL 让出 + 测试历史表格居中】
#
#   ① 物理跑分 / SOC 高压：工作线程继续锁最高性能簇(cpu6/7)，主渲染线程在测试期间保留中核，
#      两边结束或异常都恢复原亲和性；把“60”从上限请求落实成主线程有稳定调度空间。
#   ② 物理循环与高压循环每 128 个模拟步主动让出一次 GIL，避免 Python 工作线程连续饿住 Kivy，
#      目标是让测试期间实际渲染稳定在 55~60fps；日志/结果同时记录锁核是否真正生效。
#   ③ 「测试历史（渲染 / SoC）」的固定三列表头与数据行统一按 290dp 表格整体水平居中，
#      修复 Y700 与红米 K90 Pro Max 上宽度变化导致的表格偏左。
#
#   回归：py_compile；bench_instr_probe 23/23；hp_instr_probe 8/8。
#
# 【v0.7.73 帧率上限弹窗: 说明文字再精简两字, 字号 11sp -> 12sp】
#
#   玩家 2026-09-15 定稿: 「字体增大, 优化为 实际帧率上限=min(屏幕支持，系统设定，本窗口设定）」
#
#   ① 文案把「系统全局设定」缩成「系统设定」(省 2 字 = 22dp), 腾出来的宽度**全换成字号**。
#   ② 字号 11sp -> **12sp**: 实测 272dp(原 11sp 版 249dp)。
#      弹窗 0.96 下可用宽 360dp 屏 290dp / 393dp 屏 321dp ⇒ 一行放得下。
#   ⚠️ 为什么停在 12sp: 0.96 弹窗下 **360dp 屏的字号上限就是 12.9sp** —— 13sp 要 296dp,
#      已经超了 290dp。要 13sp+ 只有两条路: ①只保 393dp 及以上的屏(321dp, 能到 14.9sp);
#      ②弹窗推到 0.98(360dp 屏只剩 1dp 余量, 风险自负)。玩家要更大字号时从这两条里挑。
#   ⚠️ 标点仍是半角(`,` `()` 而非 `，` `（）`): 全角版 304dp@12sp 会在 360dp 屏折行。
#
# 【v0.7.72 帧率上限弹窗的说明文字: 列全三者 + 压到一行】
#
#   玩家 2026-09-15 两轮修正后的定稿: 「实际帧率上限=min(屏幕支持，系统全局设定，本窗口设定）。
#   如果你觉得放不下, 就缩小字体」。
#
#   ① v0.7.71 里我把它砍成了「实际帧率上限 = 三者取最小」(17sp) —— 一行是放得下了,
#      但**没说是哪三者**, 玩家当场退回:「三者都tmd是谁啊」。
#   ② ⚠️ 列全三者之后**一行放不下**: 玩家写法实测 **356dp**(全角标点), 而弹窗可用宽只有
#      261dp(360dp 屏, 0.88 弹窗) / 290dp(393dp 屏)。按玩家指示**缩字号**解决, 三条并用:
#        · 弹窗 0.88 -> **0.96**  (可用宽: 360dp 屏 290dp / 393dp 屏 321dp);
#        · 标点统一**半角** (`,` `()` 而非 `，` `（）`): 356dp -> 322dp@13sp
#          (项目里 `_apply_fps_cap` 的 docstring 本来就是 `min(屏幕支持, Android系统上限,
#           用户设定)` 这种半角写法, 风格一致);
#        · 字号 13sp -> **11sp**: 实测 **271dp**。
#      ⇒ 360dp / 393dp / 412dp 屏都能一行放下。320dp 老屏(可用 251)会折, 但折后每行完整。
#   ③ ⚠️ 代价说清楚: 11sp **比最早那版 13sp 还小 2sp** —— 这是「三者要列全」和「必须一行」
#      两头挤出来的结果。两行版能到 15sp, 玩家已否, 不再试。
#
#   ⚠️ 判据一律是**宽度实测**(`CoreLabel.get_extents`, 同一个 otf; 桌面 density=1 ⇒ px == 真机 dp),
#      **不是桌面截图** —— 桌面窗 540px 比真机宽 60%, 只看截图会漏掉真机上的折行。
#
# 【v0.7.71 跑分测量锁核(三条线程) + 帧率上限弹窗文案砍到一行放大字号】
#
#   来由: 真机日志反复出现「同机同档, 跑分能差 69%」——
#     红米 K90 Pro Max 三次同开"均衡": 最大频率 2624/2880/2875(**只差 9%**),
#     而物理跑分 12617/19316/21340(**差 69%**)。逐核之后大核**全程钉在 2880**、
#     前 6 核只差 6%, 跑分仍差 59% ⇒ 「最大频率」这一列在这个问题上**没有分辨力**。
#   排查到最后, 剩下的可控变量之一是**线程在快/慢簇之间迁移**。本轮就是把它消掉。
#   ⚠️ **锁核不是给游戏提速** —— 它是量尺的校准: 消除迁移这个测量变量, 让两次跑分可比。
#      日常游玩**不锁**(正常游戏主循环、渲染线程的调度完全不受影响)。
#
#   ① 【对应 commit fa24723】物理跑分工作线程锁核:
#      新增 `_bench_cpu_max_khz` / `_bench_pin_fast_cpus` / `_bench_restore_cpu_affinity`。
#      用 `cpuinfo_max_freq` 识别最高性能簇(不用会随负载抖的 `scaling_cur_freq`),
#      `sched_setaffinity` 把**当前线程**收窄到该簇, 跑完在 `finally` 里恢复原集合。
#      ⚠️ 读不到拓扑 / 系统不接受 / 本来就是同构核 —— 一律**不锁**, 原因如实记进
#      `_BENCH_CPU_PIN` 并印进跑分日志。**不编数**。
#
#   ② 【对应 commit 1f9219f】同一把尺子推广到另外两条测量线程:
#      · **渲染帧率采样窗口**(阶段 1, 跑在主线程): 进采样前锁、采样一结束立刻恢复;
#      · **SOC 高压测试**(波 2 工作线程): 锁在性能簇, 免得把「温控衰减」和「迁到慢核」
#        混成一件事 —— 两者的曲线形状一样, 不锁就分不开。
#      · ⚠️ 顺带修一个计时口径: 高压的**墙钟起点挪到锁核之后** —— 拓扑探测那一段时间
#        不该算进测试时长(原来盖在探测之前, 等于多算了)。
#      · 高压结果弹窗新增一行 `性能核：已锁定 cpuX,cpuY` / `性能核：未锁定（原因）`,
#        与跑分日志同一套口径。
#
#   ③ **帧率上限设定弹窗的说明文字**(玩家 2026-09-15 报「你这个字这么小, 精简下,
#      是不是1行就能显示得下」):
#      原来是一整句 34 字 `实际帧率上限 = min（屏幕支持，安卓系统设定的上限，当前设定的上限）`
#      —— 实测 **427dp**, 而弹窗可用宽只有 419dp(540 桌面) / 290dp(393 真机) ⇒ **必然自动折行**,
#      折点还落在「…当前设定 / 的上限）」上: 真机上那行只剩「的上限）」三个字的孤行。
#      现在: 文案砍到 11 字 `实际帧率上限 = 三者取最小`, 字号 13sp -> **17sp** ⇒ 实测 **205dp**。
#      余量: 393dp 屏可用 290dp / 360dp 屏 261dp / 320dp 老屏 226dp —— 三种都放得下。
#      ⚠️ 代价是**不再列举是哪三者**(弹窗标题 + 上面那根滑条已经给够上下文);
#         想保留列举的话宽度翻到 245dp, 字号就只能停在 13sp == 玩家嫌小的那个。
#      ⚠️ 判据是**宽度实测**(`CoreLabel.get_extents`, 同一个 otf; 桌面 density=1 ⇒ px == 真机 dp),
#         **不是桌面截图** —— 桌面窗 540px 比真机宽 60%, 只看截图会漏掉真机上的折行。
#
# 【v0.7.68 模拟历史第五次定稿: 去掉「归一化」列 + 全表统一字号 + 时间恢复完整年月日】
#
#   ⚠️ 玩家 2026-09-15 三条一起下:「去掉归一化列」「项目内的字体大小改成相同」
#      「时间现在可以恢复年月日+时间格式了」。
#
#   🔴 **一、去掉「归一化」列。**
#     · 它进了面板两版(v0.7.63 起), 现在**退出显示**。理由是真机数据:
#       实测跨设置的值是 **535 / 603 / 597 / 572** —— **还不稳定, 不配当"公平秤"**。
#       留着它不但误导, 还占着宽度把**完整时间戳**挤掉了。
#     · ⚠️ **`phys_norm` 照旧存在 JSON 里**(老记录要能读、以后要复盘), 只是不再显示。
#       **别顺手把它从记录里删掉。** 门禁 B6 钉着"面板列里没有它 + 记录里还有它"。
#
#   🟡 **二、全表统一字号(表头 == 数据行)。**
#     · **病**: 之前是**逐列各自 `_fit_uniform`**, 而表头 `平均/1%Low帧`(96px)**比数据**
#       `120.2/90.3`(69px)**长得多** ⇒ 表头被缩到 ~12sp 而数据行留在 14sp,
#       **同一张表里两种字号** —— 玩家截图指出的就是这个。
#     · **修法**: 逐列量出"这一列最多能放多大"(`fit_font_size` 走同一套阶梯),
#       取**最小的那个**发给**所有**格子(表头 + 每一行的每一格)。
#     · ⚠️ **不能把所有格子塞进一个 `_fit_uniform`** —— 它取的是"组里最窄那列的宽度",
#       会被最窄列(88px)拖死, 整表缩到 ~11sp。
#     · ⚠️ 表头**不再走 `_fit_line`/`_fit1`** —— 那两个是"单列自己缩", 一缩就出两种字号。
#       门禁 `[15]` 那条判据跟着改成钉 `_fs_all`(见 fx_probe 里那段说明)。
#
#   🟢 **三、时间恢复完整年月日 + 时间。**
#     · 演进: ①「只有月日」→ ②「年月日+时间」(放不下, 11.48sp) → ③「本年省年份」(13.16sp)
#       → ④ **删掉「归一化」列腾出宽度 ⇒ 恢复完整格式**(现在)。
#     · `_hist_stamp` 就是 `time.strftime("%Y-%m-%d %H:%M", ...)`, 认不出原样返回。
#       **只改显示** —— 盘上本来存的就是这个格式, 旧记录一条都不用动。
#     · 三列列宽 **110 / 92 / 88**(量出来的: 各列最宽内容 113 / 96 / 89 = 298px,
#       内容区约 290px ⇒ 按比例配), 全表落在一个字号上。
#
#   实测(探针 `[360]`/`[540]` 两处直接量建好的 Label): **全表只有一个字号 = 12.32sp**,
#     列宽 110/92/88, **没有一格被裁、没有一格折行**。
#
#   回归: --selftest **OK** · py_compile **OK** · fx_probe **5 项既有失败**(贴图 mipmap,
#         预先存在) · `temp/benchhist_probe.py` **21/21** · `temp/bench_instr_probe.py` **23/23**
#         · `temp/hp_instr_probe.py` **8/8**。
#   ⚠️ 探针这轮**自己错了两次**, 都记在注释里:
#      ① `_cells()` 原来只按"`size_hint_x is None` 的 Label"捞, 把弹窗里别的 Label
#         (标题/脚注)也捞进来 ⇒ F4 报"出现 5 种字号" —— **红的是探针**;
#         改成**结构判据**: 一个 `BoxLayout` 且**全部孩子都是固定列宽的 Label**、列数对得上。
#      ② 新加的 **F4**(全表只有一个字号)就是这个截图的专用门禁 —— 没有它,
#         "表头比数据行小一档"能一路绿过去。
#   ⚠️ `fx_probe` 本轮改了 **2 条门禁**(「以归一化收尾」和「表头走 `_fit_line`」) ——
#      都是**改成钉新行为**, 不是删掉。
#

# 【v0.7.67 真机验证 v0.7.66: 压渲染确实见效(+12.4%), 但"只压 Kivy 那一半"是错的 —— 补上对系统的请求】
#
#   🔴 **一、玩家的方案被真机证实了。**(K90 Pro Max, v0.7.66, 游戏120+OS120)
#
#     **同一个配置、同一台机器**, 只差"渲染负载"这一个变量:
#
#       ① v0.7.62  跑分那段实际渲染 **120.3fps**  步/秒 **18943**  探针 35.42M
#       A v0.7.66  跑分那段实际渲染 ** 70.5fps**  步/秒 **21291**  探针 35.66M
#                                        ⇒ 步/秒 **+12.4%**, 而探针只变 **+0.7%**
#
#     ⇒ **"渲染在拖物理"至此有了同机同设置的直接对照**, 不再是跨配置推断。
#     ⚠️ 顺带**再次否掉了"归一化就够了"**: 归一化也跟着从 535 涨到 597(+11.6%)
#       ⇒ 那一块**归一化确实抓不住** —— 与 v0.7.66 里那段自我修正一致。
#
#   🟡 **二、但日志当场抓到"没按住", 根因是 v0.7.66 漏了一半。**
#
#     · 日志原话: `强制帧率上限 60 · 那一段**实际渲染**: 61.4 · 67.8 · 70.5 · 70.8 · 71.5 fps`
#       ⇒ 实际**高于** 60(**没按住**)。而"游戏60+OS60"那一次就按住了(57.0)。
#     · **根因**: `_apply_fps_cap()` 里有两件事 —— ①压 `Clock._max_fps`; ②调
#       `_request_android_high_hz()` **告诉系统"我要多少 Hz"**。v0.7.66 的闸门只做了①,
#       而②里 `target = min(屏幕, 系统, **玩家档位**)` **照旧用玩家档位**。
#       ⇒ 跑分期间 app **一边告诉系统"我要 120Hz"、一边只画 60** ——
#         那正是 ② 那次"**设备整个塌一次**"的同款不一致状态。**这是我引入的 bug。**
#     · **修法**: 闸门同时挂进 `_request_android_high_hz()`, 把 `target` 也按到
#       `_BENCH_FPS_FORCE`。门禁 **L9** 钉这条(它**只能在源码里断言** ——
#       该函数在非安卓上直接 early return, 桌面跑不到那一段)。
#     · ⚠️ `setFrameRate` 是**请求**, 系统可以拒绝(省电/温控/用户强制 120) ——
#       所以**日志里「实际渲染」那一行永远是最终判据**, 不能拿"我请求了 60"当"已经按住了"。
#
#   🟢 **三、归一化**还没有**变成稳定的跨设置指标**(实测 535 / 603 / 597 / 572)。
#     · 所以**别急着用归一化那一列做跨设置比较** —— 它比"原始步/秒"好, 但还不是终局。
#     · 现在最靠得住的用法: **在同一个环境下比**(手机和游戏都设 60, 让"实际渲染"落在 57 附近),
#       或者看**同一配置跨版本/跨设备**的变化。
#
#   回归: --selftest **OK** · py_compile **OK** · fx_probe **5 项既有失败**(贴图 mipmap,
#         预先存在) · `temp/bench_instr_probe.py` **23/23**(新增 L9) ·
#         `temp/hp_instr_probe.py` **8/8** · `temp/benchhist_probe.py` **19/19**。
#
# 【v0.7.66 跑分/高压期间**强制把帧率按到 60**(玩家定案: 与其事后归一化, 不如直接压住渲染)】
#
#   🔴 **一、玩家提出的方案**(原话:「跑分/压力测试/测物理性能的时候, 强制把游戏刷新率
#      下降到 60FPS, 岂不是最准的?」)。**采纳。**
#
#     · **为什么需要**(这一条推翻了我上一轮的说法): 我原以为"归一化(步/秒÷探针)已经够了",
#       但**实测不是** —— 真机同一个 K90:
#         ① 渲染 120.3fps · 探针 35.42M · 归一化 **535**
#         ③ 渲染  80.4fps · 探针 35.80M · 归一化 **603**
#       两次的**探针只差 1.1%**(核心速度一样), 而归一化差 **12.7%**
#       ⇒ **归一化之外还有一块是它抓不住的**。把渲染本身压住, 才是直接治那一块。
#     · **为什么是 60 而不是更低**: 60 是**正常状态**(app 自己的档位里就有它), 系统不会把
#       "1fps"读成"这个 app 卡死了"而去降频/限后台; UI 也还活着(进度条照常刷)。
#     · **闸门挂在 `_apply_fps_cap()` 里** —— 那是"帧率上限"**唯一的生效点**。
#       散在各调用点的话, 跑分途中任何一次 `_apply_fps_cap`(弹窗关闭 / 切回前台)
#       都会把上限**悄悄恢复**, 而日志上看不出来。可重入计数(`_BENCH_FPS_LOCK`)。
#     · ⚠️ **两波都在 `finally` 里松手** —— 中途抛异常也必须还原, 否则玩家**永远卡在 60fps**,
#       而画面上看不出来。门禁 L6 专门钉这条(用正则找 `finally:` 紧跟的那一行)。
#     · ⚠️ **渲染窗口那一档不受影响** —— 它测的就是渲染帧率, 压它就没意义了。
#
#   🟡 **二、"请求 60" 不等于 "拿到 60" —— 所以必须记实际值。**
#     · 真机实测过: ③ 请求 60、系统 120, **实际渲染 80.4fps**。所以日志里印的是
#       `强制帧率上限 60 · 那一段实际渲染: X · Y · Z fps (中位 W)` ——
#       **实际明显高于 60 就说明没按住**, 那次成绩的环境仍混着玩家的设定。
#     · 桌面探针实测: 强制 60 时实际渲染 27.2 / 38.8 / 36.8fps(主线程在跟跑分抢),
#       **低于**强制值 ⇒ 判语写的是"按住了"。⚠️ 第一版判语写死了"高于 = 没按住",
#       拿到 36.8 还印"没按住" —— **判语必须看数, 不能看方向**。
#     · 实际值由 `_FRAME_CALLS`(`_frame` 的调用计数)的增量 ÷ 墙钟算出来, 逐轮记。
#
#   ⚠️⚠️ **三、必须说清的两件事。**
#     1. **这一版的效果还没在真机上验证过。** 它**可能帮忙, 也可能帮倒忙** ——
#        ② 那次"设备整个塌一次"就是**帧率设定不一致**引起的(app 要 120、系统 60)。
#        强制 60 也是一种"与玩家设定不一致"的状态。**下一轮真机日志会直接回答**:
#        看 `实际渲染` 那一行有没有低于 60、以及归一化值在三次之间是不是变稳了。
#     2. **新旧成绩不能直接比。** v0.7.65 及以前的记录都是**没强制帧率**时测的;
#        要比较, 请用同一版本重测。
#
#   回归: --selftest **OK** · py_compile **OK** · fx_probe **5 项既有失败**(贴图 mipmap,
#         预先存在) · `temp/bench_instr_probe.py` **22/22**(新增 L1~L8 钉这道闸门:
#         抬基准到 120 才测得出效果 / 锁开着时 `_apply_fps_cap` 不许顶回去 / 可重入 /
#         异常路径靠正则找 `finally` / 实际渲染帧率要有值) · `temp/hp_instr_probe.py` **8/8**
#         · `temp/benchhist_probe.py` **19/19**。
#   ⚠️ 那个探针**自己错了三次**, 都记在注释里: ①在主线程直接调 `_run_benchmark` ⇒
#      主线程被占住、一帧都不渲染 ⇒ 实际帧率记成 0; ②桌面基准本来就是 60 ⇒ 不抬到 120
#      的话"压到 60"是空操作; ③`finally` 的文本断言把缩进写死了。**三次红的都是探针。**
#

# 【v0.7.65 模拟历史删掉「CPU平均频率」列 + 排版回归准表格 + 网上查证: sysfs 骗人是已知行为】
#
#   🔴 **一、网上查证的结果(玩家 2026-09-15:「你去互联网上, 其他人看看有没有遇到此类问题」)。**
#
#     **① `scaling_cur_freq` 不反映实际频率 —— 这是 Qualcomm 官方承认的已知行为。**
#        Qualcomm 支持论坛(QCS8550 那贴)的原话:
#          「LMH mitigations are HW controlled and cpu freq is reduced at HW level.
#            **sysfs nodes will not reflect changed freq.**」
#        内核邮件列表里 Konrad Dybcio 说得更细: LMH 平台上 OSM/EPSS **不知道真实的降频后频率**,
#        CPUFreq 框架设进去的值和"DCVS + LMH 限流之后真正的输出"是两回事。
#        ⇒ **我们实测到的那件事(scaling_cur_freq 报 2712 而探针低 16%)不是野现象, 是设计如此。**
#        论坛给的"正确读法"是 debugfs 的 `clk_measure` 节点或 `dcvsh_freq_limit` —— 都不是
#        普通应用能读的。**所以我们的纯算术探针反而是应用侧唯一可用的替身。**
#
#     **② "跑分不稳"本身是学界记录过的现象。**
#        ACM HotMobile '17 的论文 *Freeze It If You Can*: AnTuTu 的子项**波动可达 50%**,
#        15 次跑分的最高/最低总分差约 **14%**、部分子项超 **55%**; 主因是
#        **CPU 调频设置、温度、后台服务、可用内存**。论文的建议是"固定频率、控温、杀后台",
#        做了之后波动可压到均值的 ~1%。⇒ **我们的结论(要控制变量、要看归一化)与它一致。**
#
#     **③ Python 侧的方法论也支持我们这套做法。**
#        CPython 的 issue 36051 明确要求基准测试**把"GIL 释放/获取的开销"与"缓存效应"分开**;
#        多篇资料指出这类负载常常**先撞内存带宽再撞核心数**。
#        ⇒ 我们的"**纯算术探针(只碰寄存器) + 对象分配探针(碰分配器/内存)配对 + 反向对照**"
#          正是这套方法论。
#
#     ⚠️ **一句话: 没有人"解决"过它** —— 文献和厂商给的答案都是"控制环境 + 别信 sysfs",
#        而这正是本项目已经做了的。**这条线到此为止。**
#
#   🟡 **二、模拟历史: 删掉「CPU平均频率」那一列**(玩家 2026-09-15 定案)。
#     · 理由就是上面第①条 —— 那一列**报的是调频器的目标值, 不是实际值**, 留着是误导。
#       ⚠️ **`phys_freq_mean` 照旧存在 JSON 里**(老记录要能读、以后要复盘), 只是不再显示。
#       别顺手把它从记录里删掉。
#
#   🟢 **三、排版回归"准表格"**(玩家:「排版回归准表格, 不要用空格来分割」)。
#     · 这一格**版式改过四轮**, 结论写进 `_HIST_COLS` 上面那段注释了, 别再从头试:
#       ① 固定四列 → ② 空格分隔(四列各付一遍余量要 333px, 内容区只有 299px) →
#       ③ 加第五列「归一化」(字号掉到 11.48sp) → ④ **删频率列 + 回归固定列宽对齐**(现在)。
#     · **为什么现在能对齐**: 删掉频率列把宽度让了出来。四列按各自最宽内容
#       (时间 113 / 帧 96(**表头比数据长**) / 跑分·波动 89 / 归一化 42 = **340px**)
#       配进内容区, 每列再由自己的 `_fit_uniform` 缩字号。
#     · ⚠️ 列宽是**量出来的**(`text_px` 非粗体 14sp), 最后落在 `_HW = 96/82/76/36 = 290px`,
#       并把**弹窗从 0.92 加宽到 0.96**(0.92 只有 275px, 装不下 340px 的列需求)。
#       实测最小字号: 360dp **11.12sp** / 540dp **11.48sp**。
#     · ⚠️⚠️ **表头与数据行必须共用 `_HW`** —— 分两处写一定会漂, 探针 B3 钉着这条。
#     · ⚠️ 表头文案(`平均/1%Low帧` 等)**一个字没改** —— 唯一没动的那条路, 因为它是玩家
#       2026-09-14 点名要的。**要缩就先缩别处。**
#
#   回归: --selftest **OK** · py_compile **OK** · fx_probe **5 项既有失败**(贴图 mipmap,
#         预先存在) · `temp/benchhist_probe.py` **19/19**(整份重写: 判据从"行文本形状"改成
#         "**固定列宽的单元格**", 新增 F0 一条 —— 找不到单元格就说明版式退回去了) ·
#         `temp/bench_instr_probe.py` **14/14** · `temp/hp_instr_probe.py` **8/8**。
#   ⚠️ `fx_probe` 本轮**改了 4 条门禁 + 1 条匹配式** —— 它们钉的正是这次有意改掉的东西
#      (「CPU平均频率」列、空格分隔、`_HIST_SEP`/`_HIST_HEAD` 共用、`_fit_line(head` 那个
#      变量名)。**每条都是改成"钉新行为", 不是删掉。**
#

# 【v0.7.64 高压(SOC)测试同步修订: 也采纯算术探针 + 历史面板加「归一化」列】
#
#   ⚠️ 玩家 2026-09-15:「**高压测试的跑分也应该同步修订**」。
#
#   🔴 **一、为什么高压也要归一化。**
#     · 高压(波 2)的"步/秒"**口径与波 1 逐字相同**(同一条确定性输入序列、`thread_time`
#       当分母), 而波 1 已经被实测证明:**同样的核心速度下, 渲染 120fps 时物理步比 80fps
#       慢 13.9%**(探针只差 1.1%) ⇒ 那是**渲染抢内存/缓存**。
#       ⇒ 高压的**绝对值**同样被污染, 跨设置/跨设备比高压成绩**只能用归一化那个数**。
#     · ⚠️ 高压期间主线程的渲染负载**基本恒定**(没有球在飞) ⇒ **降幅比例**受影响小,
#       但"首窗/末窗的绝对值"和别的测试放在一起比时必须归一化。
#
#   🟡 **二、`benchmark_sustained` 每窗后采一次纯算术探针**(`_SUST_SPEED`)。
#     · 与波 1 **分开存**(`_SUST_SPEED` vs `_BENCH_SPEED`): 两波是**不同的两段时间**
#       (波 1 有间隔、波 2 一秒不停), 混在一起就分不出是哪一段的。
#     · ⚠️ **每窗都采**(360 秒里约 360 次、每次 0.05 CPU 秒 ⇒ 累计 18 秒, 占比 5%)。
#       这是**有意的**: 探针必须**全程陪着**, 才追得上温控造成的漂移。
#     · ⚠️ 归一化取**首窗**(峰值), **不是中位** —— 高压问的是"一上来能跑多快",
#       中位会被后面的温控衰减拉低。
#
#   🟢 **三、高压历史面板加第五列「归一化」+ 逐窗探针进 JSON。**
#     · 新字段: `norm`(首窗步/秒÷首窗探针×1e6) 与 `speed_runs`(逐窗探针)。
#       `speed_runs` 留着是为了事后分清"**步/秒在掉是机器整体慢了还是物理本身衰减**"。
#     · ⚠️ **旧记录没有这两个字段** ⇒ 印「无数据」, **绝不拿中位数回填**(印假数)。
#     · ⚠️⚠️ **加列把「详情」按钮挤坏过一次, 记下来**: 第一版只把文案列的余量压了, 结果
#       **按钮只剩 25px**(「详情」两字在 14sp 下就要 28px), 表头 `归一化详情` 都黏在一起。
#       ⇒ 三处一起改才修好: ①时间列改用 `_hist_stamp`(本年 `09-14 09:00` 只要 76px,
#       与模拟历史面板同一条规矩); ②其余列按实测内容压到 88/46/44/42; ③弹窗 0.86 → 0.92。
#       现在按钮 **55px**。
#       ⚠️ **门禁当时有洞**: `H4` 只判"按钮宽度 > 0", 25px 时照样全绿 —— **是截图抓到的**。
#       新加的 **H6** 判"按钮宽度 >= `text_px('详情',14)+12`", 这才是真正该钉的那条。
#
#   ⚠️ 日志新增四行(高压那段): 逐窗纯算术探针 / **归一化** / 逐窗"步/秒÷探针" / 读法。
#      最后那串"步/秒÷探针"是**判断真衰减**用的: 它**平**而步/秒在掉 ⇒ 机器整体慢了;
#      它**跟着掉** ⇒ 渲染/内存那一侧变了。
#
#   回归: --selftest **OK** · py_compile **OK** · fx_probe **5 项既有失败**(贴图 mipmap,
#         预先存在) · `temp/hp_instr_probe.py` **8/8** · `temp/bench_instr_probe.py` **14/14**
#         · `temp/benchhist_probe.py` **20/20**。
#
# 【v0.7.63 跑分之谜定案: 跨帧率设定比跑分没意义(归一化列 + 分配探针 + 结论写进代码)】
#
#   🔴 **一、结论(玩家要的 C): "中位跑分"会被渲染帧率污染, 跨帧率设定比较没意义。**
#
#     红米 K90 Pro Max 三次同开「均衡」, 只改"手机帧率上限 / 游戏帧率上限"(v0.7.62 的仪表):
#
#       ① 游戏120+OS120  渲染120.3fps  步/秒 **18943**  纯算术探针 35.42M  大核 2880
#       ② 游戏120+OS 60  渲染 60.1fps  步/秒 **12793**  纯算术探针 30.00M  大核 2712
#       ③ 游戏 60+OS120  渲染 80.4fps  步/秒 **21580**  纯算术探针 35.80M  大核 2880
#
#     · **① 和 ③ 是一组干净对照**: 两次的**纯算术探针只差 1.1%**(35.42 vs 35.80M)、
#       大核频率也一样钉在 2880 ⇒ **核心速度是一样的**, 而物理步**差 13.9%**。
#       ⇒ **那 14% 与 CPU 频率无关** —— 只能是内存/缓存那一侧(探针只碰寄存器,
#         `advance_flight` 要碰对象头/属性字典/小对象分配)。
#     · 加上归一化(把核心速度因子除掉): ① 535 / ② 426 / ③ 603 ⇒ **核心速度只解释了
#       差异的一小部分**。
#     · `cpuinfo_cur_freq`(实际频率) **这台机器不给读** —— 三份日志都是「没读到」。
#       但**不需要它了**: 纯算术探针是**硬件速度的直接测量**, 比任何 sysfs 都可信。
#     · ⚠️ **`scaling_cur_freq` 被证伪**: ② 的大核报 2712MHz(与 ① 的 2661 只差 3%),
#       而同一轮的探针**低了 16%** ⇒ 它报的是**调频器的目标值**, 不是实际值。
#     · ② 那一轮**第 5 轮的探针崩到 15009390(同轮其他值的 42%)** ⇒ 那次设备整个塌过一次,
#       `本轮内 探针最大/最小 = 2.36 倍` 就是它 —— **② 波动 35.9% 的来源**。
#       (② 正是"app 要 120Hz、屏幕只有 60Hz"那个打架状态。**相关性有, 因果没有实测**,
#        别再往下写死结论。)
#
#   🟡 **二、A: 历史面板加第五列「归一化」+ JSON 三个新字段。**
#     · **定义**: 中位步/秒 ÷ 中位纯算术探针 × 1e6。**跨帧率设定比跑分只能用这个数**。
#     · ⚠️ 它**不是**"机器快不快"的绝对指标(那要看步/秒本身) —— **两个一起看**。
#     · ⚠️ **v0.7.62 以前的记录没有它** ⇒ 印「无数据」, **绝不拿步/秒回填**(那是印假数),
#       与频率那一列同一条规矩。
#     · ⚠️⚠️ **代价是实测的, 不是估的**: 五列之后 360dp 上 `_fit_uniform` 落到 **11.48sp**
#       (四列时是 **13.16sp**) —— **正好是玩家上次特意避开的那个字号**。
#       备选也量过(可用宽 275.2px): 砍掉频率列换归一化 **294px**(比原四列的 330px 还窄,
#       因为 `3902Mhz` 比 `603` 宽); 短表头 197px 但数据行 360px 接手, 白缩。
#       **玩家 2026-09-15 明确选了保留五列**(频率那一列他要看) —— **别再自作主张删它。**
#     · 新增 JSON 字段: `phys_norm` / `phys_speed_runs` / `phys_alloc_runs`。
#
#   🟢 **三、B: 「对象分配探针」(`_alloc_probe`) —— 把内存那一侧单独量出来。**
#     · 一个**固定工作量**的小对象"分配-写属性-丢弃"循环(走 CPython 分配器, 碰内存),
#       和 `_speed_probe`(**只碰寄存器**)**配成一对**, 每轮各跑一次, 紧跟在该轮物理之后。
#       · **两个一起掉** ⇒ 核心速度整体慢了;  **只有分配探针掉** ⇒ 内存/分配器被抢。
#       日志印 **各自相对第 1 轮的倍率** + `分配/算术` 那个比值, 判读写在行里。
#     · 两个探针都在 `_run_once` **之外**跑(自己计时), **不占"步/秒"的分母**。
#
#   回归: --selftest **OK** · py_compile **OK** · fx_probe **5 项既有失败**(贴图 mipmap,
#         预先存在) · `temp/benchhist_probe.py` **20/20**(含第五列旧记录 → 无数据) ·
#         `temp/bench_instr_probe.py` **14/14**。
#   ⚠️ 两个探针的门禁里都有**反向对照**: K9/K10 钉"判读逻辑"(步/秒抖而探针不动 ⇒ 必须判成
#      「不是核心速度」; 两个一起抖 ⇒ 必须判成「就是核心速度」), K13 钉"只有分配探针掉时
#      比值那行必须跟着变"。**所以那些结论不是写死的字符串。**
#

# 【v0.7.62 跑分之谜第三轮: 排除三个假设 + 加"实际频率"与"纯算术探针"两把新尺子】
#
#   🔴 **一、v0.7.61 那三件仪表上真机之后, 一次排除了三个假设。**
#
#     红米 K90 Pro Max, 三次同开「均衡」, 只改两个设定(玩家的分组):
#
#       ① 游戏120 + OS120   渲染 120.3fps   跑分 **18450**   逐轮 22839·18450·18025·19731·18393
#       ② 游戏120 + OS 60   渲染  60.1fps   跑分 **13596**   逐轮 17432·13386·14502·13596·12548
#       ③ 游戏 60 + OS120   渲染  80.4fps   跑分 **21585**   逐轮 23986·21788·21073·21586·21304
#
#     **最多与最少差 59%。排除了三个假设:**
#       ① **"被限在慢核上"** —— 亲和性三次**完全一样**(`0,1,2,3,4,5,6,7`, 全 8 核)。
#          ⚠️ 顺带**验证了一件之前标"未验证"的事**: `os.sched_getaffinity` **在安卓上确实
#             读得到** —— 这条口子通了。
#       ② **"频率掉下去了"** —— 逐核频率: 大核(cpu6/7)在 ①③ **全程钉在 2880**, ② 也只低
#          6%; 前 6 核三次差 6% 以内。**频率解释不了 59%。**
#       ③ **"等 GIL 没排除干净"** —— 新增桌面实验 `temp/gil_denominator_probe.py`:
#          同一段纯 Python 负载, 主线程**满载 GIL 压力**下, CPU 秒口径只掉 **1.3%**
#          (墙钟口径掉到 **0.490 倍**, 证明实验本身有分辨力)。
#          ⇒ `thread_time` **确实**排除了 GIL 等待, `_run_benchmark` 那句注释是对的。
#
#   🟡 **二、剩下两种可能, 而且以前**分不开**——本轮加的两把尺子就是分它的。**
#       ㈠ **sysfs 在骗人**: 高通平台上 `scaling_cur_freq` 报的是**调频器的目标值**,
#          实际时钟可以被 EPSS/温控按下去**而不回写** ⇒ "频率差不多"可能是假象。
#       ㈡ **缓存/内存被渲染抢了**: `advance_flight` 是纯 Python, 每一步都在碰**对象头 /
#          属性字典 / 小对象分配**, 所以它对**缓存与内存子系统**同样敏感 —— 而逐核频率
#          管不着这个。
#
#       **尺子一: 实际频率**(`cpuinfo_cur_freq`)。`_cpufreq_cores()` 现在每核多读一个
#       `cpuinfo_cur_freq`(直接读硬件那一侧), 日志印
#       `实际/请求 = X.XX 倍` —— **明显小于 1 ⇒ `scaling_cur_freq` 报的不是实际值,
#       前面所有「频率差不多」的结论都得作废**。读不到就如实印「没读到」, 不编数。
#
#       **尺子二: 纯算术探针**(`_speed_probe`)。一个**只碰寄存器**的定工作量循环,
#       用本线程 CPU 时间计时, **每轮跑一次**(紧跟在那一轮物理之后: 同样的热状态、
#       同样的调度处境; 它在 `_run_once` **之外**, 不占"步/秒"的分母)。
#       日志印**各自相对第 1 轮的倍率**, 两串一比就出结论:
#         · 步/秒掉得**明显比探针多** ⇒ **缓存/内存被抢**(不是核心速度)
#         · 两个**掉得差不多** ⇒ **核心真的慢了**(那 `scaling_cur_freq` 就是在骗人)
#       日志里**自带这句判读**, 因为这一行是给"下一次有人来查这个问题"看的。
#       ⚠️ 两个量**量纲不同**(一个是"步"、一个是"轮"), 所以**不能直接相除** ——
#          第一版就是这么印的(`步/探针 = 0.001`), 完全读不出东西。现在印的是倍率。
#
#   回归: --selftest **OK** · py_compile **OK** · fx_probe **5 项既有失败**(贴图 mipmap,
#         预先存在) · `temp/bench_instr_probe.py` **11/11**。
#   ⚠️ 那个探针里 **K9/K10 是一对反向对照**, 专门钉**判读逻辑**本身:
#      K9 塞"步/秒抖 2 倍、探针纹丝不动" -> **必须**判成「不是核心速度的问题」;
#      K10 塞"两个一起抖"               -> **必须**判成「就是核心速度在变」。
#      两条都做过, 所以那行结论不是写死的字符串。
#   ⚠️ **未验证项(照旧要说清)**: `cpuinfo_cur_freq` 在**这台 K90 上给不给读**只有真机
#      日志能回答; 桌面读不到, 只测到"如实印没读到"那条分支。
#
# 【v0.7.61 给"物理跑分为什么差 69%"配仪表: 逐轮步/秒 + 逐核频率 + CPU 亲和性】
#
#   🔴 **一、起因是一次被推翻的结论**(玩家在红米 K90 Pro Max 上实测)。
#
#     · 同一台机器、同样开「均衡」模式, **只改两个设定**("手机帧率上限" / "游戏帧率上限"):
#
#         手机   游戏   渲染实测        物理跑分    波动
#          60    120    80.4 / 62.3     12617    30.4%
#         120    120   120.3 / 88.9     19316    20.1%
#         120     60    60.1 / 43.0     21340    13.4%
#
#       跑分**差 69%**, 而那一列「CPU 平均频率」只差 **9%**(2624/2880/2875)。
#       同一台 Y700 做同样的对比, **差距很小** ⇒ 这是**设备相关**的, 不是普遍规律。
#     · ⚠️⚠️ **代码里原来写着错的结论**。`_run_benchmark` 的注释里, 上一轮已经记过一次
#       "同一台设备 60 与 120 跑分差接近 2 倍", 当时写的是:
#       「而这两个 benchmark 按构造与刷新率无关 ⇒ 那 2 倍**只能来自 DVFS**(核心频率)」。
#       **这组新数据把它推翻了** —— 频率只差 9%, 跑分差 69%。本轮先**把那条结论删掉**,
#       换成"能确定哪一半、另一半待测", 并装上能测它的仪表。**不许再猜一个填进去。**
#     · 能确定的那一半: 跑分跑在**独立线程**上、分母是 `time.thread_time`(**这条线程自己的
#       CPU 时间**, 不是墙钟) ⇒ "等 vsync / 等 GIL"都不进分母, **按构造与刷新率无关**。
#       实测第 2 vs 第 3 组只差 10%, 与"渲染 120fps 比 60fps 更扰缓存/GIL"相符 —— 这一档是
#       设计内的。
#       第 1 组比第 3 组低 **40%**、且**波动 30.4%**, 而它渲染出来是 **80.4fps**(既不是 60
#       也不是 120) ⇒ 那一次是**手机锁 60、游戏要 120, 系统在来回抢刷新率**, 设备处在
#       不稳定态。这一档**老仪表解释不了**。
#
#   🟡 **二、问题出在"尺子"上: 「CPU 平均频率」是各核最大值。**
#     · `_cpufreq_mhz()` 读 `cpu0..7/scaling_cur_freq` 取 **max**。而"跑分那条线程跑在哪个核
#       上"是 **OS 调度**决定的 —— **大核 2.8GHz 的时候, 线程完全可能正在 1.5GHz 的中核上**
#       (K90 是"2 大 + 6 中"的架构)。⇒ **这一列在这个问题上没有分辨力**,
#       "三次频率差不多"**推不出**"跑分线程跑得一样快"。
#
#   🟢 **三、本轮加的三件仪表**(都是"把猜变成量", 不动任何游戏逻辑):
#     1. **逐轮步/秒**印进日志(`_phys_fps_runs`)—— 中位数把 5 轮压成一个数, 而
#        "五轮一直低"和"中途掉一轮"是**两种病**(前者频率/核心, 后者温控/系统干预)。
#        同时写进历史记录 JSON。
#     2. **逐核频率**(`_cpufreq_cores` / `_freq_core_stats`)—— 采样线程每 0.5 秒顺带把
#        **每个核**的频率累加(均值/最低/最高), 日志印成
#        `cpu0 2745(2600~2900) · cpu1 ...`。这样"大核 2.8G 空转、线程在 1.5G 中核"一眼可见。
#        ⚠️ 这里**不做** `_cpufreq_mhz()` 那个"连续两核读不到就停"的提前退出 ——
#          逐核要的就是**完整一张表**, 中间缺一个会让人以为"那个核不存在"。
#     3. **CPU 亲和性**(`os.sched_getaffinity(0)`, **每轮采一次**)—— 唯一能从应用侧读到
#        "这条线程被限在哪几个核"的口子。每轮都采是因为它**可能中途变**(系统按负载收窄
#        cpuset), 只看开头会漏。
#     · ⚠️ 三件都遵守同一条工程规矩: **采不到就如实印「没采到」(非安卓 / 读不到 sysfs),
#       绝不编数**。桌面探针实测: 逐核返回空表、亲和性全 `None`, 而日志照常生成。
#
#   ⚠️ **未验证项(必须说清)**: 第 3 件(`sched_getaffinity`)在 Windows 上**根本不存在**,
#      只测到"不支持时如实留空"这条分支。**它到底能不能在 Android 上读到, 只有真机日志
#      能回答** —— 下一轮日志里那一行如果印「没采到」, 就是这条路走不通, 得换法子。
#
#   回归: --selftest **OK** · py_compile **OK** · fx_probe **5 项既有失败**(贴图 mipmap,
#         预先存在) · `temp/bench_instr_probe.py` **7/7**。
#   ⚠️ 那个探针自己错过两次(都记在它的注释里): ①`_bench_frame_log()` 在**没有渲染窗口
#      数据**时第 3 行就 `return ""`, 于是拿到空串、判据全红; ②合成帧数据的**下标要按
#      `_bench_frames` 的真实形状**(不是输出列的次序) —— `_rec[7]` 是主线程ms, 给了字符串
#      就 `ValueError`。**两次红的都是探针, 不是产品。**
#   ⚠️ K7 是**阴性对照**: 手工塞 `1000/2000/3000` 进去, 那一行**必须**印出"波动 100%" ——
#      否则它可能只是印了个常数, 那不叫仪表。
#
# 【v0.7.60 弹窗期不钉字体(把 24 个钉子名额留给游戏 HUD, 不被设置界面吃掉)】
#
#   🔴 **一、"弹窗开着的时候不钉"** —— 玩家点出来的一条, 数据支持它。
#
#     · **起因**: 玩家指出「每轮游戏次数设定」「隐藏返还率」这些**都是设置界面**,
#       打游戏时根本看不到, 不该跟游戏 HUD 抢资源。顺着这条查下去, 结论比他说的更严重。
#     · **实测**(`temp/menu_font_probe.py`, 同一个进程取 S0/S1 两张快照做差):
#       把 main.py 里**全部 13 个弹窗**开一遍, 会开出 **42 个**游戏里从不出现的字号 ——
#         彩蛋「弹珠返回发射槽」12 · 隐藏返还率的「关闭隐藏」7 · 模拟历史表头 7 ·
#         SOC高压测试历史 6 · 帧率上限设定 6 · 弹珠发射模拟测试 4 · 测试历史标题 1
#       其中 **19 个当场就够到了钉子门槛(冷开满 3 次)** —— 而 `_PIN_MAX` 一共只有 24 个。
#       ⇒ **点一遍设置界面, 79% 的钉子预算就废了。**
#     · ⚠️ 它们**没有被预热**(预热表 52 项里一个都没有, 判据 B 实测通过) —— 所以问题
#       **不在"白烘了", 而在"白钉了"**: 钉子名额是**游戏 HUD 的救命资源**。
#     · **做法**: `RotPopup` 自己维护「现在开着几个弹窗」(`open`/`dismiss` 各记一笔),
#       `text_px` 里到门槛时若弹窗开着就**不钉**, 并把这个跳过次数计进日志。
#       ⚠️ 闸门挂在 `RotPopup` 上而不是 `self._popup` 工厂上 —— 实测**有 3 处**直接
#          `RotPopup(...)` 绕开了工厂(`_show_hp_history` / `_show_hp_detail` /
#          `_show_bench_history`), 挂工厂会漏掉它们。
#       ⚠️ 每个实例一个开关去重, 不直接加减: `open()` 有早退分支、`dismiss()` 也会被
#          重复调 —— 计数偏大 = 钉子被**永久静默禁用**, 偏小只是少钉几个。往小的一侧取。
#     · **效果(端到端实测)**: 开遍全部弹窗之后 **菜单独有的 42 个字号一个都没被钉**
#       (拦截 103 次); 同期被钉的 6 个**全是游戏 HUD**(`音效按钮` x5 / `轮次按钮` x1,
#       后者是全 app 冷开第一名 37 次)—— **正是该钉的**。
#
#   ⚠️⚠️ **探针自己错过一次, 记下来**: 第一版只开了 **6 个**面板就下结论, 漏了 7 个
#      (含玩家点名的「每轮游戏次数设定」和「隐藏返还率」)。补全之后数字**翻了一倍多**:
#        冷开过的字号 82 -> **100**   ·   菜单独有 24 -> **42**   ·   够门槛的 6 -> **19**
#      **教训: "把功能都过一遍"这种判据, 清单必须从代码里枚举出来(数 `Popup` 构造点),
#      不能凭印象列。**
#   ⚠️ 另一条探针自身的坑: 打开报告 UI 会置 `_FS_MUTE_UNTIL` 静音 1.5 秒, 而记账整段在
#      静音期内是跳过的 ⇒ 拿被静音的账去数"菜单用了哪些字号"会得到 **0 个**。
#      探针里必须把静音期设成 0 长度(`M._FS_MUTE_SEC = 0.0`)。
#
#   回归: --selftest **OK** · py_compile **OK** · fx_probe **5 项既有失败**(贴图 mipmap,
#         预先存在) · `temp/popup_gate_probe.py` **8/8**(含"重复 dismiss 不会把计数弄成
#         负数"与"关掉之后钉得上"这对阴性对照) · `temp/menu_font_probe.py` **5/5** ·
#         `temp/fontpin_probe.py` **11/11**。
#   ⚠️ 日志行新增一栏: `因弹窗开着而跳过 N 次` —— 不许静默, 否则闸门卡死也看不出来。
#
# 【v0.7.59 字体子集化(8.0MB -> 0.4MB, 冷开 30ms -> 0.39ms) + 钉子门槛抬到"第 3 次冷开"】
#
#   🔴 **一、字体子集化** —— 本批最重的一刀, 把"冷字号"这个问题的量级直接改了。
#
#     · **做法**: 用 `fontTools.subset` 从 8,345,436 字节的完整 NotoSansSC-Medium 里
#       只留 `main.py` 真正会用到的字形, 输出 **417,324 字节**(−95%)。
#       字符清单 = `main.py` **全文**出现的字符(含注释/文档串) ∪ 旧清单 ——
#       `temp/_subset_chars_v2.txt`, 共 1606 个。
#     · **实测收益(同一支探针, 只换字体文件, 前后各跑一次)**:
#         冷开 **29.977 ms -> 0.390 ms**(**77 倍**)  ·  热命中 0.016 ms 不变
#         于是真机日志里那帧 `帧719 56.5ms[字号 49.9ms]` 的元凶 ≈ 变成 **0.6ms**。
#       ⚠️ 这是**桌面**数字; 真机比例未知, 但机理是"解析字形表, 代价 ∝ 文件大小",
#          所以下一轮真机日志的「最慢的冷字号测量」那一行是它的直接读数。
#     · ⚠️⚠️ **验证用"全字集逐字节对拍", 不是抽 25 个样例**(`temp/subset_full_probe.py`):
#         **1602 个字符 x 5 个字号(含 45.1875 / 57.2375 这种小数尾巴) x bold 两种
#          = 16020 次渲染, 像素差异 0 处**;  9 项字体度量(unitsPerEm/ascender/descender/
#         lineGap/xHeight/capHeight…)与原字体**完全相同**;
#         **阴性对照**(故意删掉 40 个汉字映射的字体)抽查 1520 次检出 50 处 ⇒ 对拍有分辨力。
#       另一道 `temp/font_smoke_shot.py` 专门堵一个洞: 上面那个探针是**自己另外注册**
#       `OrigFont`/`SubFont` 去比的, **没走 main 的注册路径** —— 万一 main 那行
#       `LabelBase.register` 静默失败, 它照样全绿而真实界面全是豆腐块。
#       所以又比了一次 **main 注册的 `Roboto` == 原字体**(60 次逐字节) + 四个中文界面截图。
#     · ⚠️ **旧子集(13:21 那版 355KB)其实也够用, 但那是运气**: 它缺 258 个字符,
#       好在**全在注释/文档串**里(真正会渲染的字符串字面量共 769 个字符, 它一个不缺)。
#       新清单收全文, 以后**只改注释也不会漏**; 代价 +62KB(417KB), 相对省下的 7.9MB 可忽略。
#     · ⚠️ **`✅`(U+2705) 在 main.py 里有, 但两个字体都没有** —— 它是 emoji, NotoSansSC 不含,
#       只出现在 main.py:807 的注释里、永远不渲染。这是**既有状态, 非本次引入**。
#
#   🟡 **二、钉子门槛从"第 2 次冷开"抬到"第 3 次"**(`_PIN_AFTER = 3`)。
#     · **依据是真机数据**(v0.7.56 一份 5.4 分钟的进程): **93 个字号 / 307 次冷开 /
#       复冷过的 54 个**, 而 `_PIN_MAX` 只有 **24** 个名额 ⇒ 按"谁先复冷谁占坑"来分,
#       **名额不够、而且分错了人**: 那 54 个里绝大多数只复冷 2~5 次(省 1 次冷开),
#       真正的慢性病是少数 —— `57.24B x53` · `39.16 x31` · `45.19B x28` · `48.20B x11`。
#       抬到 3 之后名额就花在它们身上, 代价只是各自多冷开一次。
#     · ⚠️ **别再调回 2**: 那是"名额不够 + 分错人", 不是"更早保护"。
#     · 日志行改口径: `触发(复冷) N 次` -> **`达到门槛(冷开满 3 次)的有 N 个`** ——
#       改名是因为门槛一抬, 那个数就不再等于"复冷过的个数"了, 沿用旧标签会读错。
#     · 门禁: `temp/fontpin_probe.py` **11/11**, 新增 **B0**(第 2 次冷开**不该**钉、第 3 次才钉)
#       + **C/D 分辨力 13.9 倍**。⚠️ D 那条的阈值**从写死的 5ms 改成相对的** ——
#       子集化把冷开打到 0.39ms, 写死毫秒的门禁会永远红。
#
#   ⚪ **三、计划里那条"让弹窗别再污染字体缓存"—— 查清了, 但决定不做**。
#     · 查清: 93 个字号里有 **29 个**来自三个弹窗标题 —— 它们都走
#       `_fit_line(Label(...), 19/20/20)`(基准 sp(19/20) ≈ 57~60pt), 然后靠
#       `fit_font_size` 的阶梯**一路缩到放得下**, 缩到的正是 v0.7.32 为压到 64 上限
#       而**故意不烘**的 `FIT_FINE` 档。帧率曲线 12 档 / 性能测试 12 档 / 帧率上限设定 5 档。
#     · **为什么不做**: 子集化之后单次冷开只要 **0.39ms** ⇒ 三个弹窗加起来约 **11ms**,
#       而且**一次访问只付一次**。为 11ms 去动三个弹窗的标题排版, 风险(字号/换行/裁切)
#       远大于收益。**而且它真正的后果 —— "把游戏的字体挤出去导致复冷" —— 已经被上面
#       那条钉子从另一头堵住了。**
#
#   回归: --selftest **OK** · py_compile **OK** · fx_probe **5 项既有失败**(贴图 mipmap,
#         预先存在) · `temp/subset_full_probe.py` **4/4** · `temp/font_smoke_shot.py` **2/2**
#         · `temp/fontpin_probe.py` **11/11** · `temp/kivy_fontcache_probe.py` **12/12**。
#   ⚠️ 原字体备份在 `temp/NotoSansSC-Medium.ORIG.otf`(未跟踪, 不进包)。
#      要回滚: 把它拷回 `fonts/NotoSansSC-Medium.otf` 即可。
#
# 【v0.7.58 模拟历史: 时间戳「本年省年份」+ 字段分隔改 2 个空格(把字号从 11.48sp 救回 13.16sp)】
#
#   ⚠️ 这一版是 **v0.7.57 的直接延续**, 起因是 v0.7.57 里那句「看起来宽度足够」被实测证伪。
#
#   🔴 **一、时间戳: 本年省掉年份**(玩家 2026-09-15 看完实测数字后选的)。
#     · 演进: ①「只有月日」→ ②「年月日 + 时间(看起来宽度足够)」→ ③ **实测证明②不够**。
#       v0.7.57 那一版整行 **364px**, 而 360dp 上弹窗内容区只有 **275.2px** ⇒ `_fit_uniform`
#       把字号从 14sp 压到 **11.48sp** —— 已经**低于 Android 正文建议下限 12sp**。
#     · 现在: **本年 `09-15 14:07` / 往年 `2025-11-03 20:41`**。
#       实测(`temp/benchhist_probe.py` 的 F 判据**直接量建好的 Label**):
#       360dp **11.48sp → 13.16sp**, 540dp 仍是 14.00sp, 两档都**一行、不折、不裁**。
#     · ⚠️ **"省年份"没有引入"09-15 分不出今年去年"这个歧义** —— 只有**本年**才省,
#       往年的行照旧带年份。历史留 100 条, 这张表是"最近几天"的读数, 绝大多数行是本年。
#     · ⚠️ **代价(实话说)**: `_fit_uniform` 是**按最宽那行**给整组定字号的, 而往年的行
#       比本年的长 5 个字符 ⇒ **只要历史里有一条往年的, 整表就按那一条缩**, 回到约 11.5sp。
#       这是"最坏情况 = 现状", 不是变差; 但别指望它永远是 13.16sp。
#
#   🟡 **二、字段分隔从 1 个空格改成 2 个**(玩家 2026-09-15 定案)。
#     · 起因: 比例字体里空格很窄, 1 个空格时 `10:03 59.8` 在屏幕上读起来就是
#       `10:03:59.8` —— **时间那一格本身就带冒号**, 两个字段黏成一串。
#     · ⚠️ 表头与数据行**共用** `_HIST_SEP`, 所以表头也一起变宽了; 那点宽度已经算进
#       上面的字号账里(所以最终是 13.16sp 而不是预估的 12.8sp)。
#
#   回归: --selftest **OK** · py_compile **OK** · fx_probe **5 项既有失败**(贴图 mipmap,
#         预先存在, 与本次无关) · `temp/benchhist_probe.py` **18/18**
#         (含 360dp/540dp **两处实量** + **把真文本打出来**, 不再靠截图肉眼认年份)。
#
# 【v0.7.57 字体「钉子」(治冷字号反复重开) + 模拟历史时间戳改回完整年月日时间 + 频率读不到印「无数据」】
#
#   🔴 **一、字体「钉子」** —— 本批最重的一刀, 治的是"同一个字号被**反复**冷开"。
#
#     · **病**: Kivy 的字体缓存是 **64 项 FIFO**(`_text_sdl2.pyx` 的 `sdl2_cache` /
#       `sdl2_cache_order`), 而本 app 一轮跑下来要开 **86~93 个不同字号** ⇒ 队列一直
#       在被挤爆。真机日志里 `fs=45.1875` 那一次冷开 **47.0ms**(第 1、4 轮的帧 724);
#       桌面普查: **每轮 150 次冷开里有 22 次是"复冷"**(同一个字号第二次被冷开)。
#     · **修法**: 命中只在 dict 上查、**淘汰只从 order 队头取** ⇒ 只要把某个 fontid
#       **从 order 里 `remove` 掉且不 append**, 它永远排不进淘汰队列, dict 里那份就
#       再也不会被 `del`。**触发时机 = 复冷那一刻**(同一个 (字号,bold) 第二次被冷开),
#       所以不需要任何预设名单, 数量自限, 钉的正是受害的那几个。
#     · ⚠️ **绝不 append** —— order 出现重复项时 Kivy 淘汰那句 `del sdl2_cache[popid]`
#       会 KeyError **崩在渲染路径上**。所以只准 remove, 且钉之前断言 order 无重复
#       (断言不过就整条停用并**计数上报**, 失败次数印进日志)。
#     · ⚠️ **上限 `_PIN_MAX=24`** 是按 RSS 预算倒推的(每个 fontid 实测约 0.88MB,
#       24 个 ≈ +21MB), **不是"能钉多少钉多少"**。
#     · ⚠️⚠️ **定位必须在 `gc.freeze()` 之前**(实测踩到): `gc.freeze()` 把当时活着的
#       对象全移进永久代, 而 **`gc.get_objects()` 不返回永久代里的东西** —— 冻结之后
#       实测只剩 **11 个**对象(整进程 5.4 万个都被冻住) ⇒ 队列根本扫不到, **钉子静默失效**。
#       现在放在 prebake 链里、预热表烘完之后、freeze 之前(那时队列里有货, 判据认得出)。
#     · ⚠️ **"静默失效"是这块最大的风险**, 所以日志里把「定没定到 / 钉了几个 / 失败几次 /
#       复冷几次 / 形状命中的 list 有几个」全摊开 —— 定不到时还会**额外印一行**
#       「钉子没生效, 冷字号会照旧被驱逐, 这是已知的失效, 不是没问题」。
#
#     **验证(两道, 都做过阴性对照)**:
#       · `temp/kivy_fontcache_probe.py`(**最小复现**, 12/12 全绿): 定位到队列 / 新开
#         字号 ⇒ 队列 +1 / 上限 64 生效 / 队头被淘汰 / 摘掉 fontid 后灌 80 个新字号
#         仍是热命中。**冷开 29.98ms vs 热开 0.029ms(1000 倍)**。
#       · `temp/fontpin_probe.py`(**出货路径**, 9/9 全绿): A 定位成功(队列长 60,
#         形状命中的 list 共 **1** 个) · B 复冷触发钉子(钉成功 1 · 失败 0 · 复冷 1) ·
#         **C 钉住的 0.041ms / D 对照 30.807ms ⇒ 分辨力 742 倍**。
#         ⚠️ D 那条阴性对照是**必须**的: 没有它, C 可能只是"它本来就没被驱逐"的假绿。
#         ⚠️ **上一版探针在这里红过, 而红的是探针不是钉子**: 它写的是
#         `min(量3次)`, 而**冷开那一次自己会把 fontid 重新插回队尾** ⇒ 第 2、3 遍
#         已经是热的(实测 29.98 / 0.023 / 0.021) ⇒ 永远取到快的那个。
#         现在**每个字号只量第一次**, 并把这条写进探针注释。
#
#   🟡 **二、模拟历史的时间戳改回完整「年月日 + 时间」**(玩家 2026-09-15 定案)。
#     · 玩家**先**要「只有月日 2 个数值」, 看过实际排版之后**又**改回
#       「**日期改为年月日时间**(看起来宽度足够)」—— 连时间一起要。
#     · ⚠️ **"宽度足够"这个前提实测不成立**, 但玩家选的就是这个: 整行约 **364px**,
#       而 360dp 上弹窗内容区只有 **275.2px** ⇒ `_fit_uniform` 把字号缩到 **11.48sp**
#       (540dp 上 **14.00sp** 不缩)。**仍然一行、不折、不裁**, 只是 360dp 上比
#       "只有月日"那版(272px, 满 14sp)小了一档多。11.48sp 已压在 Android 正文
#       建议下限(12sp)之下, **再往这一格加字就要重算**。
#     · 认不出的格式**原样返回**, 不吞信息也不抛。
#
#   🟢 **三、「读不到频率」印「无数据」而不是 `—`**(玩家 2026-09-15:「如果无/读不到
#     频率 改为 **无数据** 而不是一个 `-`」)。破折号会被读成"这一格坏了",
#     「无数据」才是"没采到"的正常态。
#     ⚠️ 只改**频率那一格**; 跑分/波动那格的缺失标记**没动**(玩家没提, 别顺手改)。
#
#   回归: --selftest **OK** · py_compile **OK** · fx_probe **5 项既有失败**(贴图 mipmap,
#         预先存在, 与本次无关) · `temp/fontpin_probe.py` **9/9** ·
#         `temp/kivy_fontcache_probe.py` **12/12** · `temp/benchhist_probe.py` **16/16**
#         (含 360dp / 540dp 两处**实量**: 一行不折 + 最小字号 11.48 / 14.00)。
#   ⚠️ **未验证项**: 真机上"钉住几个 / 复冷几次"只有设备日志能回答 —— 桌面这轮的
#       22 次复冷是**普查值**, 真机字号集合更大(FPS 上限滑条 + 各种 sp 档)。
#   ⚠️ 本轮**没有**动字体文件(见下): 字体子集化(8.0MB -> 0.34MB)验完了但**换不进去**,
#       有 3 个 `main.py` 预览进程占着字体文件句柄(`Device or resource busy`), 没杀。
#
# 【v0.7.56 查看模拟历史: 时间精简为月日 + 新增「CPU平均频率」列 + 清空历史 + 频率改"跑分时平均"】
#
#   ⚠️ 上一版 **v0.7.55**(帧率上限设定)是另一个 AI 推的, **没有写 changelog 块** ——
#      所以本块直接接在 v0.7.54 之后。
#
#   🔴 **一、时间精简为月日**(玩家:「把时间精简为日期, 只有月日 2 个数值, 比如 09-14」)。
#     · **只改显示** —— 盘上照旧存 `%Y-%m-%d %H:%M`, 旧记录一条都不用动, 别处要完整时间
#       也还拿得到(`_hist_stamp`)。
#     · ⚠️ **跨年必须带年份**: 光看 `09-14` 分不出今年去年, 而历史留 100 条、天天跑跨年是
#       迟早的事。所以**本年 `09-15` / 非本年 `25-09-15`** —— 一年以内仍是玩家要的短样子。
#     · 认不出的格式**原样返回**, 不吞信息也不抛。
#
#   🟡 **二、新增「CPU平均频率」列**(玩家:「新增一个列，CPU平均频率（显示的是非压力测试
#     跑分时的数据）显示为 xxxMhz」+ 样例 `08-25 59.8/53.0 4256Mhz 14685/5.8%`)。
#     · **字段顺序按样例**: 时间 / 平均·1%Low帧 / **CPU平均频率** / 中位跑分·波动
#       —— 频率在**第三位**, 不是最后。单位写 `Mhz`(玩家两次都这么写)。
#     · 取的是**波 1(物理跑分)**那一段, **不是** SOC 高压段。主测试早就不跑高压段了,
#       `sust_freq_*` 在主测试记录里本来就是死的。
#     · ⚠️⚠️ **玩家两次强调「cpu频率不能用中位数」** ⇒ 这一列**只准用平均**;
#       **旧记录**(那之前只存了 `phys_freq_p50`)**显示 `—`**, **绝不拿中位数回落** ——
#       拿别的数顶就是印假数。
#     · ⚠️ **"跑分时"的平均 —— 采样间隙里的空闲**不算数**(玩家:「必须用跑分时的平均频率
#       (空闲的时候可以不计)」)**。为此新增 `_FREQ_GATE`: `benchmark_trajectories` 在
#       `sleep(gap_sec)` **前后关/开门**, 采样线程只在这道门开着时才 append。
#       不关门的话, 那 **20 秒(5 秒 x 4 次)休息期**的低频会把平均值拖下水。
#       ⚠️ `benchmark_sustained`(SOC 高压)**没有间隙**, 门对它恒真 —— 互不干扰。
#
#   🟢 **三、日志里的"代表值"一律改平均**(玩家:「所有'代表值'都改平均」)。
#     · 新增 `cpufreq_mean` / `phys_freq_mean`, 并印进日志。
#     · 「跑分那段的 CPU **平均**频率: N Mhz(最低/最高/采样数; **采样间隙已剔除**)」。
#     · 渲染窗口那一段改成「CPU 频率(**渲染窗口**那一段, 不是跑分段): **平均** ... (中位 ...)」
#       —— 中位降到括号里, 与最低/最高并列成**分布的一项**, 不再冒充代表值;
#       并**明写"这一段天生偏低(大部分时间在等 vsync)"**, 否则会被读成"跑分时只有这么点"。
#     · ⚠️ **平均取不到就印「没采到」, 绝不拿中位数顶**(`fx_probe` 有阴性对照钉着)。
#
#   🔵 **四、清空历史 + 二次确认**(玩家:「下面新增一个按钮，清空历史」)。
#     · **只在有记录时**才放出这个按钮(空列表上放一个"清空"是没意义的热区, 而且它离
#       「关闭」只有 8dp, 误触不可逆)。确认框**写清条数 + 「不可恢复」**。
#     · ⚠️ **只清「模拟测试历史」**(`plinko_bench_history.json`) —— SOC 高压历史是**另一张表**,
#       两张分开是玩家 2026-09-15 定过的案, 别一起清。确认框里也明说了这句。
#     · 清完**当场重开面板** → 玩家立刻看到空态, 而不是盯着一份已经删掉的旧表格。
#
#   🟣 **五、表格不再强制列对齐**(玩家:「如果放不下的话，有个最简单的办法，数据用空格分割，
#     不强制要求对齐了」)。
#     · 起因: 加第四列后, 四列按"各自最宽那串"算要 **333px**, 而 360dp 的内容区只有
#       **299.2px** ⇒ 硬排就得整表缩到 ~11.7sp。
#     · 实测(`temp/hist_colwidth_probe.py`): 同一行改成**空格分隔**后只要 **272px**,
#       **满 14sp 就放得下** —— 省下来的正是"每列的余量各付一遍"。
#     · 表头与数据行**共用同一个分隔常量**(`_HIST_COLS` / `_HIST_SEP`), 从结构上杜绝
#       "两边分隔不一致"。弹窗宽 `0.86 -> 0.92`(所有列都受益)。
#     · ⚠️ 代价是**上下不对齐**(比例字体), 这是玩家明确接受的取舍。
#
#   ⚪ **六、菜单按钮配色改成 3 色**(玩家:「这几个按钮的颜色好乱啊。要不这样，右边的按钮用
#     左边按钮的颜色？」「这样就3种颜色了」)。**同一行同色**: 模拟系 `COL_FIRE` /
#     SOC 系 `COL_SOC` / 信息系 `COL_BTN`。⚠️ 代价是同行里分不出"行动/历史" —— 玩家选的。
#     ⚠️ `COL_DARKRED`/`COL_SOC_DIM`/`COL_BTN_OFF` 在别处(取消/返回/关闭)**仍在用, 别删**。
#
#   回归: --selftest OK · py_compile OK · fx_probe **5 项既有失败**(贴图 mipmap, 预先存在)
#         · `temp/benchhist_probe.py` **11/11 全绿**(含"行形状 == 玩家样例"逐字相同、
#         旧记录 → `—`、清空后**盘上文件也空**、频率门默认开)
#         · **截图两张**(360 宽 / 540 宽): 表头一行、数据行全部一行、无折行无裁字
#   ⚠️ `fx_probe` 本轮**改了 5 条旧门禁**(它们钉的是被这一版有意改掉的东西): 三条日志行文案、
#      一条固定列宽 `dp(118), dp(94)`、一条表头引号; 另**新增 6 条**钉新口径。
#   ⚠️ **未验证项**: `_FREQ_GATE` 的真实效果(间隙里到底采到多少次)只有真机日志能回答 ——
#      桌面那次缩短版跑分里间隙只有 0.3 秒, 滤掉的样本本来就少。
#
# 【v0.7.54 修尺子(第 0 批) + 「尾」三分段(第 2 批) —— 全部来自对抗性评审】
#
#   这一版**不改玩法、不改画质、不动物理**, 只做两件事: **把量具修准**, 和**把一根量不出
#   东西的尺子切开**。起因是一轮完整的对抗性评审(发散 2 人 x 2 轮 + 压测 2 人 x 3 轮),
#   它的头号结论是: **测量仪器本身已经坏到会误导决策**, 在修好之前, 后面每一次构建的读数
#   都不可信。
#
#   🔴 **一、两套 ★ 口径都印全**(第 0 批)。原来日志第 4 行印「长停顿门槛 12.12 毫秒(中位帧
#     x 2 ... 命中 N 帧)」、第 6 行印「★ 低于中位帧率 55% ... N 帧」—— **两个不同的量,
#     都叫 ★, 并存在同一份日志里**。实测第 2 轮: 前者 3 帧、后者 8 帧, **差 167%**;
#     而**第 1 轮两者都给 1**, 所以只看一轮永远发现不了"它们不是一个东西"。
#     (这次评审的简报里就是这么混起来的, 两位专家各自独立抓到了同一个错。)
#     · 现在印成 `# ★口径A 长停顿「帧间隔 ≥ 中位×2」...` 与
#       `# ★口径B 低于「中位帧率 55%」(= 帧间隔 ≥ X 毫秒 = 中位×1.818) ...`,
#       **各自带名 + 带换算**, 再也不会被当成同一个数。
#
#   🟡 **二、字体缓存的判据改比「实际开过几个 fontid」**(第 0 批)。原来那行拿
#     `_wc`(=预热表项数 52) 去比 64 ⇒ **恒印「有余量」**, 而**紧跟着的下一行**印的是
#     "开过 86 个 ⇒ 必然发生过淘汰" —— **同一份日志两行结论相反**, 且乐观的那行在视线更前面。
#     病灶不是"没有警报", 是**判据量错了对象**: 占住 64 个槽位的是实际开过的 fontid 数。
#     · 现在判据行**排在预热项数那一行之前**, 并且预热表那行自己标注「**这一行只说明我烘了
#       几项, 不是判据**」。`fx_probe` 新增两条断言钉住这个顺序与口径。
#
#   🟢 **三、报告 UI 自己的字号不计入账本**(第 0 批)。玩家要导出日志, 就**必须**打开
#     「帧率曲线」弹窗并按「保存日志」—— 而那个弹窗**自己的标题**走 `_fit_line`
#     → `_fit_font_size_slow` 一次走满 13 档 ⇒ **一口气开 12~13 个 fontid**。
#     铁证: 账本尾部那 12 个连降数 = 基准 57.2375 x 阶梯(相邻比值逐位吻合到小数点后四位),
#     而 `57.2375/19 = 45.1875/15 = 39.16/13 = 3.0125` 正是本机密度。
#     ⇒ **"测量动作污染被测对象"**: 保存日志这个动作, 把日志里印的那个计数撑大 12。
#     · 面板构建期间(含**异步的布局收敛**, 所以静音窗口按**时间**算而不是按函数返回)暂停记账。
#     · ⚠️ **不许静默**: 被跳过的次数**照记并印进日志**(`报告 UI 期间静音跳过 N 次`)。
#       本仓库栽过太多次"没印"被读成"没发生"。
#
#   🔵 **四、两个「普查」计数器**(第 0 批)。这是这次评审里两位专家都点名的**数据缺口** ——
#     原来的账本只能回答"开过几种", 回答不了"**该钉几个**"和"**一帧会不会开十几个**"。
#     · `# 冷开次数普查:` **每个 fontid 一共冷开了几次**(按次数降序取前 12 名)。
#       原来只有 `_COLD_FS` 留**最慢的 5 条** —— 那本身就是采样, 定不了 P。
#     · `# 帧内冷开分布:` 本帧冷开 N 次 → 这种帧有多少。原来只有 `(N次/M测)` 那一栏,
#       而它**只在「字号」挤进该帧最大两个子步骤时才印**(21059 帧里只印过 2 帧)——
#       拿它当普查是**采样当普查**, 评审里为此吵了一整轮。
#     · 桌面首跑读数(缩短版跑分, 1920 帧): **帧内冷开 0次 1908 / 1次 12**
#       ⇒ **最大就是 1 次, 没有任何一帧开过 2 个以上** —— 这一条当场判了"一次 fit 走满
#       12 档开 12 个 fontid"在实测里**没有发生过**。设备上的普查数才是最终答案。
#
#   🟣 **五、「尾」三分段**(第 2 批)。老实现只有一个「尾」= `_frame` 跑完 → 进 `flip` 之间的
#     **整段墙钟残差**, 是一个 lump —— 而评审里两位专家就"参考带帧多出来的那 2~3ms 去哪了"
#     吵了一整轮, 谁都切不开它。
#     · 新增 `_clock_wrap()` 给 `Clock.tick` 的**结束**盖时间戳, 于是变成三段:
#       `尾·回调`(`_frame` 跑完 → Clock 本轮跑完) · `尾·空档`(Clock 跑完 → 进 flip) ·
#       `_FRAME_SWAP`(flip 自身, 早就有了)。**两段之和 == 老那一格**, 语义不丢。
#     · ⚠️ 拿不到 Clock 标记时**原样退回老那一格「尾」** —— 绝不因为少一个标记就把账丢了。
#     · ⚠️ 只在采样期记(`_TEXUPD_ACTIVE`)。
#
#   回归: --selftest OK · py_compile OK · fx_probe **5 项既有失败**(预先存在, 全是贴图 mipmap)
#         · `temp/batch0_probe.py` **8/8 全绿**(含"判据行在预热表行之前"、"静音计数印进日志"、
#         "逐帧记录里出现过 尾·回调/尾·空档")
#   ⚠️ **未验证项**: 静音窗口在真机上到底能覆盖几次(桌面只跳过 1 次 —— 桌面弹窗更宽、
#      标题第一档就放得下, 复现不出设备上那 12 个)。设备上的普查数是这一版的主要交付物。
#
# 【v0.7.53 SOC 高压改按"真实时间"跑 360 秒 + 跑分参数按 Geekbench 6.1 + 仪器/文案修正】
#
#   🔴 **一、SOC 高压: 口径从"CPU 秒"改成"真实时间(墙钟)秒"**。玩家 2026-09-15 三条要求:
#     「真的至少测试 300 秒 / 测试结束后就要弹窗 / 面板显示的数别太违和」, 并在选择题里选定
#     「显示 360 秒、到 360 必弹」这一档。
#     · 病根: 面板报的是工作线程跑掉的 **CPU 秒**, 而主线程每帧要抢 GIL 渲染, 工作线程只拿到
#       约 3/4 的核 —— 桌面实测比值 **1.31**(20 秒 CPU 的活要 26.2 秒墙钟,
#       `temp/hp_tail_probe.py`) ⇒ 旧口径"压 300 秒"实际要玩家等 **约 393 秒**, 而面板早在
#       300/300 就顶格了 ⇒ **面板在骗人**(玩家原话:「跑了 300/300 秒后, 很久都没有弹出窗口」)。
#     · **为什么不去调数字凑**: 想让"面板显示 360、到 360 必弹"就得把工作量改成 360/1.31≈275 秒,
#       但 **1.31 是这台电脑的比值、不是常数**; 手机若为 1.5, 275 秒的活要跑 413 秒 ⇒ 面板到
#       360/360 时活儿还剩 53 秒 —— **同一个 bug 原样回来**。猜比值只在测过的机器上成立。
#     · 解法: **让循环条件本身就是墙钟**(`benchmark_sustained` 的 `total_wall_sec`) ——
#       分母 = 真实秒数目标、分子 = 秒表, 数学上必然在到点那一帧结束, 换任何设备都对得齐。
#     · ⚠️ 代价(已当面告知玩家): ①**工作量随设备浮动**(桌面约干 275 CPU 秒的活) ——
#       "步/秒"是**速率**(分母是各窗口自己消耗的 CPU 秒), 仍然可比; 而且受热时间对所有设备
#       都恰好 360 秒, **对测衰减反而更公平**。②旧记录的 `sec`=300 是旧口径(CPU 工作量),
#       新记录 =360 是新口径(真实时长), **新老不直接可比**; 步/秒那一列不受影响。
#     · ⚠️ **采样窗口仍是 1 CPU 秒** —— 它决定"步/秒"的分母, 没跟着改, 以后也别改。
#     · ⚠️ **不截断最后一个窗口**: 外循环在窗口开始前判条件, 所以最后一个窗口会多做 ≤1 个窗口
#       的活(≈1.31 秒) ⇒ 总墙钟落在 [360, 361.4]。为了"精确到点"去截断会让它的分母变小、
#       样本变噪 —— 而**末窗口正是衰减曲线的关键读数**(`_hp_stats` 的"末")。
#     · ⚠️ **常量改名**(这次 bug 的本质就是"两种秒混用一个名字", 名字必须能分):
#         `SOC_SUSTAIN_SEC`        → `SOC_SUSTAIN_WALL_SEC`      (真实时间, 决定**跑多久**)
#         `SOC_SUSTAIN_WINDOW_SEC` → `SOC_SUSTAIN_WINDOW_CPU_SEC`(采样窗口, 仍是 **CPU 秒**)
#         形参 `benchmark_sustained(total_cpu_sec=)` → `total_wall_sec=`
#       全文件 10 处引用一并改净(grep 复核 0 残留)。
#     · 删**死字段** `_hp_t0`(自 v0.7.52 起只有赋值、无人读取)与 `benchmark_sustained` 的
#       `on_window` 形参(进度改用墙钟后已无调用方 —— 与 v0.7.51 删 `_bench_save_log` 参数
#       同一个处置)。新的 `_hp_wall0` 由**工作线程在开跑那一刻盖章**, 不含"等球落地"那段。
#
#   🟡 **二、过点文案「成绩核算中：x秒」**(玩家 2026-09-15:「如果时间超过 6 分钟, 如果还没有
#     弹出窗口, text 上写『成绩核算中：x秒』, 这个 x 代表进度」)。
#     · 判据只认墙钟一条(`已过秒数 >= 360`), **不需要额外的标志位**, 也就没有跨线程状态要同步。
#     · ⚠️ 它是**保险丝、不是常态**: 桌面实测"核算 + 建弹窗"只要 **51 毫秒**(写历史 JSON 1ms +
#       拼摘要 2ms + 建弹窗 50ms), 而进度每 0.25 秒才刷一次 ⇒ **多数情况下这段文案一帧都不会
#       被画出来**。真会看到它的场合是"核算真慢了"或"主线程被卡住" —— 那时玩家看到的是一个
#       **在跳动的计数**, 而不是不动的 360/360(后者读起来就是死机)。
#
#   🟢 **三、跑分参数按 Geekbench 6.1**(玩家:「跑分测试那个 34 秒太短了, 你改为 geekbench6.1
#     的标准吧」「主要是担心 pc 和手机不一样」; 来回试了几版后由玩家自己定稿)。
#     · ⚠️ **唯一有公开依据的只有"间隔"**: Geekbench 6.1 把 workload 间隔**从 2 秒加到 5 秒**
#       (6.0 是 2 秒 —— 我们之前用的正是 6.0 的值), 官方理由是"**减少温控、降低逐次波动**"。
#     · ⚠️ **样本时长与轮数是我们自己定的, 不是 Geekbench 的规范** —— 官方**没有公开**每个
#       workload 跑多久(查过, internals PDF 403)。所以"按 6.1 的标准"能唯一确定的只有那个间隔。
#     · `SOC_SAMPLE_CPU_SEC 1.0 → 3.0` · `SOC_SAMPLE_RUNS 3 → 5` · `SOC_SAMPLE_GAP_SEC 2.0 → 5.0`。
#       ⚠️ 为什么轮数取**奇数**: `_bench_done` 取中位数用的是 `sorted(v)[len(v)//2]` ——
#       n 为偶数时那是 `sorted[n/2]`, 下面 4 个上面 3 个, 是**上中位数**(报出来偏悲观);
#       奇数时下面上面一样多, 才是真正居中的那个。
#     · 账(桌面实测比值 1.31): 跑 = 预热 1.5 + 5x3.0 = **16.5 CPU 秒 ≈ 22 秒真实时间**;
#       休息 = 4x5.0 = **20 秒**; 物理段合计 ≈ **42 秒**; **主测试总计 ≈ 67 秒**
#       (菜单文案同步改成「模拟测试约 67 秒」)。
#     · ⚠️ **本次未跑端到端实测**(只做了编译 + 按比值算账) —— **不许当已验证**。
#
#   🔵 **四、仪器与文案修正**
#     · **日志补一行「内存冻结」**: 这行原来**只在成绩面板上显示**, txt 里没有 ⇒ 离线看日志时
#       **无从判断**"这一轮的 gen-2 到底是冻结没生效、还是冻结之后新造的对象又攒够了"。
#       实测代价: 四轮 v0.7.50 日志里有一轮撞到 **21.2 毫秒的 gen-2**, 而账面上它与另外三轮
#       看不出任何差别。⚠️ `gc_frozen == 0` 也**必须印**(印成"没冻结")—— 那正是本仓库栽过的
#       "静默": 冻结链没跑到时日志一片安静, 看起来和"冻结成功"一模一样。
#     · **`_hp_freq_line` 口径 中位 → 平均**: 同一个测试, **结果弹窗印中位、历史详情印平均** ——
#       两处口径不一致(玩家就是这么发现的:「压力测试的 cpu 频率 …… 这个地方应该取平均值吧」)。
#       现在两处都是平均; 中位照旧印在**详情**的「频率分布」那一行, 也照旧存在记录里
#       (`freq_p50`)。理由是频率采样是**双峰**的(空载 1133MHz / 睿频 4608MHz): 中位数必然落在
#       其中一个峰上, 而平均值对应的是平均功耗/发热, 正是这个高压测试想回答的问题。
#     · **跑分菜单说明抽成 `_bench_menu_desc()`**: 末句「连压 N 分钟」原来是**硬编码字面量**,
#       改 `SOC_SUSTAIN_WALL_SEC` 时**不会跟着变**(本轮把口径从 300 秒改成 360 秒时差点漏掉,
#       那就会印"连压 5 分钟"而实际跑 6 分钟)。抽成函数还让 `temp/check_desc.py` 能读到
#       **出货那一份** —— 它原来从 `tools/android_part_ui.py` 正则捞字面量, 而 `tools/` 与
#       `android/main.py` **早已分叉**, 量到的是别的版本。
#     · **按钮改名 `SOC测试历史` → `高压测试历史`**(玩家 2026-09-15:「soc高压测试不改
#       把soc测试历史改为高压测试历史」)。`SOC高压测试` 不动, 配色不动。
#
#   回归: --selftest OK · py_compile OK · `temp/hp_wall_probe.py` **17 条全绿**(含阴性对照:
#         面板停在 7 而真实跑到 11.12 秒时, "核算中→弹窗 干等 2.99 秒"被判红) ·
#         `temp/gc_log_probe.py` **4/4 全绿** · 菜单截图确认改名生效。
#   ⚠️ **未验证项(不许当已验证)**: 第 三 项(跑分参数)与第 四 项里的频率口径, 均未端到端实测。
#
# 【v0.7.52 修 SOC 高压"进度到 300/300 却很久不弹窗"(进度按墙钟算, 活的按 CPU 时间算)】
#
#   🔴 **玩家报的**:「PC 高压测试跑了 300/300 秒后, 很久都没有弹出窗口」+ 截图
#      (状态栏停在 `SOC高压测试 300/300秒`, 持续很久)。
#
#   **不是卡死, 是进度条在撒谎。** 两套时间不是一回事:
#     · 进度按 **墙钟** 算(`time.time() - _hp_t0`);
#     · 而活儿按 **CPU 时间** 跑(`benchmark_sustained` 用 `thread_time` 当分母)。
#   实测(**桌面, 同一条链**):
#     · 单独跑 20 秒 CPU -> 墙钟 20.2 秒 (1.01 倍, 干净);
#     · 放进 app 里跑(工作线程) -> **1.28 倍** —— 主线程每帧都要醒一次抢 GIL。
#   ⇒ 300 秒的活儿要 ~380 秒墙钟, 而墙钟 300 秒时进度就顶到 300/300 了
#     ⇒ **看到"跑完了"其实还剩两成没干完**。手机上 165Hz 醒得更勤, 这个差只会更大。
#
#   **改法**: 进度改由**干活的那条线程自己报**(它才知道自己跑完多少 CPU 秒)。
#     `benchmark_sustained` 新增 `on_window(elapsed_cpu, total)` 回调(跑在工作线程上,
#     **只写普通属性、绝不碰界面**), `_prog_text` 读它; `_hp_prog` 每窗口更新一次。
#     ⚠️ 拿不到汇报时显示 `0/N`(真实的"还没开始"), **不拿墙钟凑数** —— 那正是原来的谎。
#
#   **顺手排除的两个嫌疑**(都做了 A/B, 免得改错地方):
#     · 「进度更新把工作线程挤死了」—— **不是**: 同一个 10 秒工作量, 进度开着 12.0 秒、
#       关掉 13.0 秒(关掉还慢一点, 纯噪声)。
#     · 「`benchmark_sustained` 本身有毛病」—— **不是**: 单独跑 20 秒 CPU 只用 20.2 秒墙钟。
#
#   ⚠️ **一个要玩家知道的后果**: 高压测试的**实际墙钟时长会明显超过 300 秒**(桌面实测 1.28 倍
#      ≈ 6.5 分钟; 手机可能更多)。进度条现在如实反映这件事 —— 数字是按 CPU 秒走的,
#      会比秒表慢。要不要把 300 调小, 是玩家的决定。
#
#   门禁: `temp/hp_hang_probe.py` 三条 —— 单独跑的墙钟/CPU 比值 / 进度**逐秒真的在涨且按 CPU 秒** /
#     弹窗时刻与进度对得上。另: `temp/hp_ab_probe.py` 做"进度开/关"的 A/B(结论: 无差别)。
#   回归: --selftest OK · fx_probe 5 项既有失败(预先存在)
#
# 【v0.7.51 SOC 高压历史(4列+详情) + 进度显示 + 菜单按系配色改名】
#
#   🔴 **一、进度显示 —— 只在"全力跑"的两个阶段, 渲染窗口一个字都不显示。**
#     玩家 2026-09-15:「那 25 秒测试帧率的不显示任何进度消息, 后面全力测试 SOC 的时候才显示」。
#     · 物理段 -> `物理跑分 d/3`;  SOC 高压段 -> `SOC高压测试 d/300秒`
#     · ⚠️ **渲染窗口那 25 秒正在测 1%Low** —— 在那里面写标签就是往被测帧上加活儿, 一律不显示。
#       我第一版给它的进度串做了**启动期预烘**(怕未命中的文字重建当场造卡顿帧),
#       玩家一句话把整条路撤了: 不写就不污染, 连那 6 次预烘也一起省掉(暖链里已查证为 0)。
#     · 进度由主线程 `Clock.schedule_interval(0.25)` 轮询; 工作线程**只写普通属性**,
#       绝不碰界面。文案不变就不写标签(每写一次都是一次文字重排)。
#     · `benchmark_trajectories` 新增 `on_sample` 回调(跑在工作线程上, 只写属性)。
#
#   🟡 **二、SOC 高压测试的独立历史**(玩家:「高压测试也专门搞个log记录。有4列，时间、中位数、
#     波动、按钮。点击按钮可以看到详细成绩和SOC平均频率」)。
#     · 新面板「SOC高压测试历史」: **时间 / 中位数 / 波动 / [详情]** —— 每行一个详情按钮。
#     · 详情弹窗: 中位(+波动) / 首→末(降幅) / 最低 / **SOC 平均频率** / 频率分布 / 逐窗曲线。
#     · 与「测试历史」**两张表分开存**(`plinko_hp_history.json`): 一个测峰值、一个测衰减,
#       挤一张表只会互相污染(一半格子是 0/—)。
#     · 结果里新增 `mean` 频率(玩家点名要"平均频率")。
#     · ⚠️ **ScrollView 必须给显式高度**(实测): 它不是带 `minimum_height` 的 Layout ⇒
#       `_popup_fit_content` 算内容最小高度时读到 0 ⇒ 弹窗只按"标题+表头+脚注+按钮"收,
#       **滚动区被压成 0, 一行数据都显示不出来**(截图逮到的: 表头在、下面是空的)。
#       现在按行数算死(最多 8 行)。**这是"门禁绿、画面坏"的又一例。**
#
#   🟢 **三、删掉 SOC 高压的「保存日志」**(玩家:「删掉高压测试的写入文件功能 / 保存文件功能」)。
#     `_hp_save` / `_hp_log_text` 一并删除; 当初为它做的 `_bench_save_log(text, prefix)`
#     参数化也**还原成原签名**(没有调用方了, 留着就是死参数)。**历史记录(JSON)照旧落盘。**
#
#   🔵 **四、菜单重排 + 改名 + 按系配色**(玩家:「把开始测试和查看历史按钮放在一起，都是在前2名，
#     名字改为开始模拟测试，查看模拟历史。这几个按钮的颜色你看看怎么改下，分几个系？」)。
#     顺序与配色(3 系, 同类同色系, 行动亮 / 历史暗):
#       开始模拟测试 `COL_FIRE` 红      查看模拟历史 `COL_DARKRED` 暗砖红   ← 模拟系
#       SOC高压测试 `COL_SOC` 琥珀       SOC测试历史 `COL_SOC_DIM` 暗琥珀    ← SOC 系
#       启动信息   `COL_BTN` 中性蓝(它不是测试, 不占两个测试系的颜色)
#     ⚠️ 新增 `COL_SOC`/`COL_SOC_DIM` 而不是复用 `COL_METER`(#f0b000 太亮, 白字读不清)。
#     ⚠️ 用户可见文案统一成「模拟测试」;**`测试历史（渲染 / SoC）` 不动** ——
#       它描述的是内容, 而且 `fx_probe` 拿它当切片锚点钉着(改了会把门禁打红)。
#
#   回归: --selftest OK · fx_probe 5 项既有失败(预先存在) · 菜单/历史/详情**截图逐一确认**
#
# 【v0.7.50 高压测试独立成按钮(5 分钟) + 主测试缩到 34 秒(3 样本)】
#
#   🔴 **一、高压测试拆出来, 独立一个按钮**(玩家 2026-09-15:「新增高压测试按钮,
#      原有测试是原有时间」->「高压时间增加到五分钟」)。
#     · 入口: 长按标题 3 秒的性能测试菜单里, 新增 `高压测试`(四个按钮: 开始测试 / 高压测试 /
#       查看历史 / 启动信息)。
#     · 时长 **300 秒(5 分钟)**, 窗口之间**一秒都不停**, 测**持续性能衰减**。
#     · 跑完弹结果弹窗(首/末/最低/中位 + 降幅 + 每段采样 + **高压那段的 CPU 频率**),
#       并有「保存日志」—— 存成 `plinko_hp_*.txt`, **逐窗全部列出**(120+ 行, 不抽稀)。
#     · ⚠️ 与性能测试**互斥**: 一个在跑时另一个进不去。
#     · ⚠️ `_bench_save_log` 参数化成 `(text=None, prefix="plinko_fps")` —— 高压测试
#       **复用那套四级降级落盘**(MediaStore -> 外部私有目录 -> 内部目录 -> 剪贴板),
#       一个字都不重写: 复制一份必然漂移, 而那个功能的全部承诺就是"去 Download 拿得到完整文件"。
#       默认值下行为与改之前**逐字相同**。
#
#   🟡 **二、主测试缩到 ≈34 秒**: 波 1 的样本 5 -> **3**(间隔仍是 2 秒)。
#     它的物理段 = 1.5 预热 + 3x1 + 2x2 = **8.5 秒**, 加渲染窗口 25 秒 ≈ 34 秒。
#     ⚠️ 主测试**不再跑高压段**。这带来一个必须处理的副作用: 上一次高压测试的结果会**留在内存里**,
#       不主动清的话面板和日志会把**旧的高压数**当成这一次的印出来 —— 那是"印假数", 比没有更糟。
#       所以 `_run_benchmark` 里**显式 `self._sust_fps = None`**(探针②钉着)。
#
#   ⚠️ **三、关于"Geekbench 标准时间"**: 查了公开资料(官方 internals PDF 返回 403, 靠多来源交叉),
#     唯一能确定的"标准时间"是 **workload 之间的间隔: 6.0 是 2 秒, 6.1 起是 5 秒**;
#     每个 workload 具体跑多久公开资料里没有。玩家最终定的是**缩短**(3 样本 + 2 秒),
#     所以这里保持 2 秒间隔、只减样本数。
#
#   门禁: `temp/hp_probe.py` 五条全过 —— 常量对(300/3/2) / 主测试真的清残留 /
#     高压流程端到端(假函数秒回, 不真等 5 分钟)出得了摘要与日志 / `_bench_save_log`
#     能存任意文本且文件名前缀对、**回读内容逐字一致** / 阴性对照(清空数据 => 摘要为空串)。
#     另: 菜单弹窗**截图确认**(4 个按钮、说明更新、无裁切)。
#
#   ⚠️ 补丁纪律(这一版零事故, 因为按 v0.7.48 那两条教训做了):
#     写文件一律 `newline=""`; 长锚点带中文+缩进手抄必错, 改用**短片段定位 + 索引插**;
#     改完跑 `temp/def_inventory.py` 对账 —— **443 -> 457**, 新增的正好是本次加的那 14 个,
#     **无丢失**。
#
#   回归: --selftest OK · fx_probe 5 项既有失败(预先存在) · 高压探针 5/5 PASS
#
# 【v0.7.49 字体缓存账本: 冷字号到底被谁挤掉的(实测: 不是弹窗, 是逐档试字号)】
#
#   🔴 **玩家问"还能不能优化 low 帧" -> 剩下的靶子里有一个是"冷字号被挤出字体缓存"。**
#     这一版**只加测量, 不改行为** —— 因为一查代码, 我原本想的两条修法都已经被证伪/禁止了。
#
#   **背景**: 真机冷字号榜里一直有「**预热时量过=是** 却仍冷」的条目(帧724 那种 20~43 毫秒的
#     单帧尖峰)。语义是"预热真的开过它, 现在又冷了" ⇒ **被 LRU 挤掉了**。Kivy/SDL2 的字体
#     缓存只有 **64 项**。我原来的猜想是**弹窗**干的(跑分面板/测试历史面板里一堆
#     12sp/15sp/16sp/17sp/20sp)。
#
#   ⚠️ **账本一开就把猜想推翻了, 而且是在桌面上、还没跑分、还没开过任何弹窗的时候**:
#       `# 字体缓存账本: 到现在一共开过 **58** 个不同字号 (上限约 64) **必然发生过 LRU 淘汰**`
#       最近开过的那 12 个是 **10.64 · 9.80 · 16.00 · 15.04 · 14.08 · 13.12 · 12.16 · 11.20 …**
#       —— **一条一条往下走的阶梯**。
#     ⇒ 那是 `_fit_font_size_slow` 在**逐档试** `FIT_SCALES + FIT_FINE`(最多 13 档),
#       **每试一档就开一个 fontid**。而预热表是 52 项 ⇒ **52 + 58 早就越过 64**, 淘汰是结构性的。
#
#   **改法(纯测量)**: `text_px` 的**冷测量分支就是"开了一个 fontid"的那一刻**, 在那里记账:
#     · `_FS_OPEN`  —— 按开的时间顺序记 `(序号, 字号, bold, 调用方/文案前10字)`
#     · `_FS_OPEN_SET` —— 去重后 = **开过的不同字号数**(>64 ⇒ 淘汰必然, 不需要再猜)
#     日志里紧跟"字号预热表"那行印出来, 并且**对每条冷字号回溯**"它发生时已开过多少个不同
#     字号"和"它之前最近开过的 8 个" —— 直接看出是被谁挤的。
#     取不到就整段不印(不印假数)。
#   门禁 `temp/fsledger_probe.py` 三条全过: 真的在记 / 日志印得出 / 阴性对照(清空账本 =>
#     印"还没撑爆", 判据有分辨力)。
#
#   ⚠️ **为什么这一版不改行为** —— 我想过的两条修法都已经被这个工程自己挡在门外:
#     · **"先按比例估一个字号、再验一次"**: `_fit_font_size_slow` 里白纸黑字写着
#       「实测字宽**不随字号线性变**(同一串在 14.95 和 14.05 下量出来都是 139px, 字形步进被
#        取整)……**估计这条路是死的, 别再试**」。
#     · **改二分**: `FIT_FINE` 那段明写「**绝不能改回二分**」, 附真机 42 毫秒/次的证据。
#     剩下的路是"减档位"或"给窄控件加宽"(`每轮100次`/`音效按钮` 是深走的两个),
#     **都动布局与字号集合, 属观感相关 —— 先看真机那两个数再定, 不凭桌面数字改布局。**
#
#   ⚠️ **桌面数字不外推**: 桌面 `sp(N)=N`, 手机是 `N x 密度` ⇒ 字号集合完全不同。
#     真正要看的是真机日志里那句「到今天一共开过 N 个不同字号」。
#
#   回归: --selftest OK · fx_probe 5 项既有失败(预先存在) · 账本探针 3/3 PASS
#
# 【v0.7.48 跑分改两波(性能 + 60 秒高压衰减) + 飘字也预烘 + 一次补丁事故的教训】
#
#   🔴 **一、跑分改成两波**(玩家 2026-09-15 定案:「我也想顺便看看高压下的性能, 所以应该是两波」)。
#
#     波 1 —— **测性能(峰值)**: 1.5 秒预热 + **5 个 1 秒样本, 样本之间停 2 秒**, 取中位。
#        ⚠️ **样本之间那个间隔是照 Geekbench 6 抄的**: 它的 workload 之间有间隔, 6.0 是 2 秒、
#           **6.1 起加到 5 秒**, 官方理由白纸黑字写着「**减少温控、降低逐次波动**」。
#           没有间隔就是"背靠背烤机", 后几轮被自己烤热 ⇒ 测到的是持续性能, 不是峰值。
#     波 2 —— **测高压(衰减)**: 窗口之间**一秒都不停**, 整整压 **60 秒**, 出首窗/末窗/最低 + 降幅。
#     ⚠️⚠️ **顺序不能反**: 波 2 会把这台机器烤热, 反过来的话波 1 就不再是峰值了。
#     ⚠️ **两波各自采一遍 CPU 频率**(分开存) —— 这样"波 2 掉了 40%"能直接对上"频率掉了多少",
#        一眼分清是温控还是别的原因。日志里那行「CPU 频率(采样期)」采的是**渲染窗口**,
#        与跑分不是同一段时间, **不能拿来解释跑分**。
#     面板加一行(没有数据整行不印): `高压 60 秒：首 14800 → 末 9200 步/秒（降 38%）· 最低 8900`
#     JSON 里把逐窗值和两波的频率都留着。
#     门禁 `temp/twowave_probe.py` 四条全过(含"墙钟真的比 CPU 时间多出间隔"这一条)。
#
#   🟡 **二、飘字(`center_toast`)也预烘**(玩家:「未中奖是不是也需要预烘 —— 我说的是飘字」)。
#     · **「未中」本来就已经烘了** —— 它是 `_bigtext_warm_items` 的第一条(走中奖大字那条路);
#       **飘字是另一条路**, 与大字同一个病: `center_toast` 里是 `Label(...)` **每次现建新标签**
#       ⇒ 按标签缓存永远空, 只有全局缓存能救, "第一次"仍要付 8~11 毫秒。
#     · 补上 7 条可枚举的(4 条固定 + 3 条轮次档位)。`重放失败：%s` 带异常文本, **枚举不了**。
#     · ⚠️ **每类标签要各自的模板**: 指纹含 `halign`, 飘字是 `halign="center"`,
#       拿大字的模板去烘 ⇒ 一条都命中不了, 而且是**静默**的。新增 `_warm_tmpl(key, factory)`。
#     · ⚠️ **去重键必须含文案**: 原来写的 `(字号, 颜色)` 把「先等这一发落定」「重放失败：…」
#       「弹珠数量不足…」(全是 COL_FIRE + 同字号) **合并成一条** ⇒ 只烘了第一条,
#       其余照旧冷开**且不报错**。实测 7 条只烘进 3 条。
#     · **常驻标签(余额/统计/轮次按钮)不用补**: 它们有**按标签缓存**, 同一个值第二次起就命中;
#       而余额/统计的值域是**无限的**, 枚举不了。状态栏那十来句早就在 `_build_texwarm` 里。
#     门禁 `temp/warm_verify2.py` 三条全过 —— ⚠️ 它比 v0.7.46 那次**严格**: 那次我在**同一个
#       回调里就关了计数**, 而 Kivy 的文字重排是 Clock **延后**执行的 ⇒ 那批根本没被数到(假绿)。
#       现在**跨帧等 1.2 秒再读**, 并且加了阴性对照(没预烘过的文案必须产生一次重建)。
#     实测: 7 次真中奖 + 7 条飘字 ⇒ **重建来源 = {} · 命中 28 / 未命中 0**。
#
#   ⚠️⚠️ **三、一次补丁事故, 记在这里免得再犯。**
#     我改 `_run_benchmark` 时用了"**从 `_run_benchmark` 整段替换到 `_bench_done`**"的写法,
#     而那一段里还坐着 `_device_info` / `_build_info` / `_bench_save_board` / `_bench_restore_board`
#     **四个方法** ⇒ **全被静默删掉了**, 而调用点还在 ⇒ 一开跑分就 AttributeError。
#     第一次我只补了"我记得的那两个", 第二次才按名单查出另外两个。
#     **教训**: 按两个锚点整段替换之前, **必须先列出那一段里所有的 `def`**, 确认没有别人的代码;
#     改完**再对一次账**。为此留了 `temp/def_inventory.py`(和 HEAD 比方法/类的增减)。
#     本次对账结果: 443 -> 448, 新增的正好是本次加的那 5 个, **无丢失**。
#
#   ⚠️ **四、桌面数据不作性能结论**(玩家:「桌面不随便」)。
#     桌面实测同机两次跑分 **32396 vs 13823(2.34 倍)**、高压衰减 **52% vs 4%** —— 那不是散热
#     (桌面散热顶级), 是 Windows 的电源/调频。**桌面探针只用来看"机制通不通"**,
#     性能结论一律以真机日志为准。
#
#   回归: --selftest OK · fx_probe 5 项既有失败(预先存在) · 三个桌面探针全过
#
# 【v0.7.47 物理跑分: 把它和"跑分那段的 CPU 频率"一起印进日志(查 60Hz/120Hz 差 2 倍)】
#
#   🔴 **玩家报的**:「同一台安卓设备, 显示上限设成 60 与 120 时, 性能跑分差异巨大 ——
#      120 的接近 60 的 2 倍」。
#
#   **先说清楚: `benchmark_trajectories` 本身是干净的**, 按构造它与刷新率无关 ——
#     · 跑在**工作线程**上(`_wait_idle_then_bench` 还先等主线程空闲);
#     · 用 `time.thread_time`(**该线程自己的 CPU 时间**)当分母, 不是墙钟 ⇒ 等 GIL、
#       渲染、系统调度的空档都被排除;
#     · 每轮用**实际消耗**的 CPU 时间作分母(`frames / used`)。
#   日志里那句"主线程变忙 ⇒ 物理吞吐掉约 6%"记的就是 GIL 竞争, 量级是 6%, **不是 2 倍**。
#
#   ⇒ 那 2 倍只能来自"**同样的代码、不同的核心频率**": 刷新率变了 ⇒ 应用"看起来"忙不忙变了
#     ⇒ 调频器(DVFS)给的频率变了。日志里那行 CPU 频率的读法早就写着这件事:
#     「调频器按负载升频, 而本应用大部分时间在等 vsync ⇒ 它看着很闲。此时 `主线程ms` 被主频
#       放大, **同一份代码在不同跑分里会差好几倍**」。
#
#   ⚠️⚠️ **但上面这段是推断, 不是实测, 所以这一版只做测量、不改结论** ——
#      原来**根本没法验**: 物理跑分只在面板和 JSON 里, **帧率日志里没有**; 而日志里那行
#      「CPU 频率(采样期)」采的是**渲染窗口那二十几秒**, 与跑分**不是同一段时间**,
#      拿它解释跑分差异是**张冠李戴**。
#
#   **改法(纯测量, 不动跑分本身)**:
#     · `_run_benchmark` 在跑分那 8.5 秒 CPU 时间里**自己起一个 0.5 秒一次的频率采样**
#       (`_cpufreq_mhz`, 与渲染窗口那个采样器**分开**, 互不覆盖);
#     · 日志头新增一行, **紧挨着**跑分印出它那段的频率:
#         `# 物理跑分(中位 14854 步/秒) · **跑分那段的 CPU 频率**: 中位 2496MHz (最低 1133 / 最高 2496, 17 个采样)`
#       采不到时印 `**没采到**(非安卓 / 读不到 sysfs)`, 取不到跑分时**整行不印**(不印假数);
#     · `bench_history` 的 JSON 里也存一格 `phys_freq_p50`(面板不印, 留着事后对账)。
#
#   门禁: `temp/physlog_probe.py` 三条 —— 有频率时印得出 / 没采到时印"没采到" /
#     **阴性对照**: 把跑分归零那一行**整行不印**。全过。
#
#   **下一步(等玩家的两份日志)**: 60 与 120 各跑一次, 两份日志里那两行一对比就知道 ——
#     · 频率明显不同 ⇒ 是 DVFS, **跑分本身没错**, 但"跨刷新率比较跑分"这件事不成立;
#       真要稳, 得靠**更长的预热**把调频器推上去(那会让跑分变慢, 是另一个决定);
#     · 频率一样而跑分仍差 2 倍 ⇒ 那就**不是 DVFS**, 得回头重查跑分链。
#
#   回归: --selftest OK · fx_probe 5 项既有失败(预先存在) · 桌面探针 3/3 PASS
#
# 【v0.7.46 把中奖大字的"第一次"挪到启动期(实测 +18%) + 板面加细分埋点】
#
#   🔴 **一、中奖大字/阴影预烘 —— 这一版的主菜。**
#
#   **病根(6 份真机日志 + 玩家在两个版本上独立复现)**: 中奖大字/阴影是**每次中奖现新建的
#     Label**, 而 `_texex` 存在标签自己身上 ⇒ 每个新标签的缓存都是空的。`_TEXEX_G`(全局缓存)
#     能救它, 但**每种 (文案, 颜色) 第一次出现时仍要付一次真光栅化** —— 真机实测那一帧的
#     `填纹` 是 **8~11 毫秒**, 而 165Hz 一帧只有 6.06 毫秒预算 ⇒ **那一帧必卡**。
#
#   **冷/热两批日志的对比**(同版本 v0.7.44/45、同设备、同配比、中奖演出都是 4 次):
#       冷(第1轮)  1%Low 91.8 / 98.6 · 卡顿帧 8 / 7 · 装杯>8.1ms 105 / 117
#       热(后两轮) 1%Low 114.8 / 112.6 / 116.5 / 107.6 · 卡顿帧 2/5/0/4 · 装杯>8.1ms 26/38/98/266
#     ⇒ 差 **+18%**。而 `重建来源` 从「余额8·统计4·**结算大字4·结算阴影4**」变成
#       「只剩 余额8·统计4」⇒ 热的那几轮**全部命中**。把"第一次"挪到启动期就是白拿这一跳。
#     ⚠️ 已排除的替代解释: **不是** CPU 频率(最好那轮 745-#2 最高频只 2496MHz, 最差那轮
#        745-#1 反而到 4512)、**不是**中奖次数(四份日志的中奖演出段数都是 4, 帧数几乎相同)。
#
#   **改法**: 枚举当前投注档会出现的 `+N` 整串, 用 `_build_texwarm` 现成那条链预烘。
#     · 范围**从代码派生**(`VALUE_SHAPE` / `PRESETS` / `slot_color`), **不许手抄** ——
#       抄的那份迟早脱钩, 而脱钩的表现是"预热白做、还不报错"。
#     · 7 个赔付值 + 未中 = 8 条文案 x (大字 + 黑影) = **16 次**, 每次约 5 毫秒
#       ⇒ **约 80 毫秒**, 分摊在启动预热链里。
#     · 走的是 **Kivy 自己的渲染**(`_warm_one` -> `Label.texture_update` -> 缓存),
#       **零像素风险** —— 不像字形图集要自己拼字。
#     · ⚠️ `_warm_one` 多认一个第 4 项 `font_size`: 中奖大字的字号由 `fit_font_size` 现算,
#       **必须一起设**, 否则烘的是"模板那个字号"的纹理, 而指纹里含 `font_size` ⇒ 一条都命中不了。
#     · ⚠️ 预热模板**必须打 `_texupd_tag`**: `_texupd_wrap` 的入口判据是
#       `self._texupd_tag is not None and self.text`, 没打标的标签**压根不进缓存**,
#       烘了等于没烘, 而且**不报错**。
#
#   门禁: `temp/bigtext_warm_probe.py` 五条, 全过:
#     ① 预热链里真的多了 16 条; ② 链真的跑完(45/45);
#     ③ **指纹比对** —— 照 `big_result_text` 的建法现建一个真 Label, 它的 `_texex_key`
#        能在 `_TEXEX_G` 里查到(大字/阴影都命中);
#     ④ **端到端**走 7 次真中奖, `重建来源` = **{} , 0 次重建**;
#     ⑤ 阴性对照: 没预烘的投注档查不到 ⇒ 判据有分辨力。
#     另: 截图确认中奖大字照常画出来(`temp/bigtext_shot*.png`)。
#   ⚠️ 枚举不全**不会出错**(没预到的组合第一次照旧走老路), 失败模式是"没赚到"。
#
#   🟡 **二、`板面` 加内部细分埋点(`板·球` / `板·钉闪`)—— 为下一轮定位, 本版不改行为。**
#     真机 7 份日志里 `板面` 单帧最大 **7.4 毫秒**(平时 1~3), 那一次把帧推到 14 毫秒。
#     **桌面复现不出来**: 桌面整块最大只 0.987 毫秒, 设备慢约 37 倍, 而且这个差主要在 GL 上。
#     关键线索: `_redraw` 早有独立标签「装杯」, 而 7 份日志里它**一次都没当过最大子步骤**
#     ⇒ 尖峰**不在 `_redraw` 里**, 在 `tick_draw` 的其余部分。这两格就是为把它指名道姓。
#     ⚠️ **曾经把它排在第二优先级是错的** —— 7 份日志合计 `板面` 只有 **129 帧 / 248 毫秒 /
#        单帧最大 7.4 毫秒**(每窗口约 35 毫秒)。它当上"最大子步骤"是因为**那一帧别的都很小**,
#        不是因为它贵。"最大子步骤" ≠ "大"。
#
#   回归: --selftest OK · fx_probe 5 项既有失败(预先存在) · 桌面探针 5/5 + 埋点已生效
#
# 【v0.7.45 修: 跑分结束后 9 个倍率槽全是 x100(盘面没还回去)】
#
#   🔴 **玩家 2026-09-15 报的**:「完成跑分测试后, 下面的倍率档, 都是 *100 这个明显不合理啊」。
#
#   **不是这几版引入的** —— `BENCH_BOARD` 那套是 **v0.7.28** 进去的(`git log -S BENCH_BOARD`),
#   只是以前没人注意到。
#
#   **机制**: 跑分期间 `_auto_launch_tick` 每一发都把 **9 个槽全钉成同一个值**
#     (`BENCH_BOARD = (5, 10, 20, 50, 100)`, 第 5 发 = **100**), 而且它**写回了缓存**
#     `self._boards[self.rtp_target] = self.multipliers`。
#     跑分结束处的代码**把随机数原样还回去了**(`_bench_rng_state`), **却忘了还盘面**
#     ⇒ 跑分一完就是满屏 x100, 一直挂到下一次发射(`park_ball` 会重掷)才自愈 ——
#       中间那段时间看着就是坏的(玩家看到的正是那一段)。
#
#   **改法**: 按同一个函数里现成的"存了再还"写法补上另一半 ——
#     新增 `_bench_save_board()`(`_start_bench_test` 里调, 与 `_bench_saved_status` 并排)
#     与 `_bench_restore_board()`(`_bench_done` 末尾调, **在 `_bench_running = False` 之前** ——
#     之后 `park_ball` 就会重掷, 那时要还的就是"新掷的一份"了, 不是跑分前那份)。
#     还完调一次 `_update_slots()` 只刷 9 个槽(与 `park_ball` 同一个写法, 不整块重画)。
#   ⚠️ 存的是**每个列表的副本**(`list(v)`): `self.multipliers` 与 `self._boards[rtp]`
#      是**同一个 list 对象**(见 `set_rtp`), 只存引用会被原地改动连带污染。
#
#   **两个方法都抽成独立的**(而不是把存/还内联在跑分那两条长链里)是为了**能被探针直接调**
#   —— 跑分整条链太长, 端到端测不起来(第一版探针就是因为存盘藏在 `_start_bench_test` 里,
#   只能验到"没存就没得还"这种废话)。
#
#   门禁: `temp/bench_board_probe.py` 四条, 全部实测:
#     ① 钉死之后倍率槽那一段**真的**变了 —— 11168 个像素(先让 bug 现形);
#     ② `_bench_restore_board()` 之后 `multipliers` == 跑分前那一份;
#     ③ **那一段逐像素与"跑分前"相同 —— 0 个像素差**(这才是玩家看到的东西);
#     ④ 阴性对照: 不存盘时 restore **不还**、槽仍是 x100(11168 像素差) ⇒ 判据有分辨力。
#   ⚠️ 探针夹具上踩了两个坑, 都写进它的文件头了:
#      · **`Window.screenshot()` 截的是"调用那一刻已经画好"的那一帧** —— 改完画布**立刻**截,
#        截到的是改动**之前**那一帧。第一版写成"先截后等", 两张图整体错位一拍、判据全反。
#        正确写法是**先等 0.9 秒再截**。
#      · **不能整屏比**: 顶部自绘的帧率/温度浮层每帧都在变(HUD 区恒定 2000+ 像素差),
#        整屏比永远不为 0。判据只比**倍率槽那一段**(图像行 780..840)。
#
#   回归: --selftest OK · fx_probe 5 项既有失败(预先存在) · 桌面探针 4/4 PASS
#
# 【v0.7.44 撤回 742 加的预热项(真机证据) + 成绩面板文案 + 测试历史配色】
#
#   🔴 **一、撤回 v0.7.42 那 11 项预热 —— 那是上一版自己造的债。**
#
#   v0.7.42 往字号预热表里加了 `sp(36) x (FIT_SCALES[1:] + FIT_FINE)`。方向没错
#   (当时真机抓到的冷字号基准确实全是 57.2375), 但做法违反了自己三十行上方写死的规矩
#   (「只能烘 FIT_SCALES, 不能连 FIT_FINE 一起烘」—— v0.7.31 实测全烘 = 155 项把前面的
#   全挤掉, 低90 从 8 帧退回 10 帧)。三条实测理由:
#     ① 表撑到 **63 项**, 日志自报「**超了! 会互相挤, 必须减字号总数**」(判据 >60);
#     ② 它**一项都没兑现**: 那三条 FIT_FINE 冷字号全印「预热时量过=否」, 且"最近预热档差"
#        是 0.18/1.08/3.37 —— 差这么大说明**表里根本没有那个字号**;
#     ③ 它挤掉的是**游玩路径**上的字号: `累计1投1中(100%)` 那条印着「预热时量过=是 却仍冷」,
#        落在帧724 上 **15.9 毫秒, 占该帧 22.5 毫秒的 71%**。
#   删完 **63 -> 52**, 桌面探针 `temp/warm_table_probe.py` 断言表长 = 52。
#   阴性对照(把那 11 项塞回去): 当场变回 63、判据变红 —— 判据有分辨力。
#   ⚠️ **顺带推翻了我自己在 v0.7.42 给的解释**: 我当时说"预热链没排到尾部那几项",
#      而桌面实测 `_font_prebaked = 63/63` —— **链跑完了**。所以真机上到底为什么没兑现,
#      **尚未定论**, 代码注释已改成如实记录 + 待查方向。**不许照那个猜测改代码。**
#
#   🟡 **二、成绩面板两处文案(玩家 2026-09-15 逐条点检)。**
#     · 「卡顿帧分布 / 慢帧分布」**去掉百分比, 只留 `x/y`**(原话:「去掉百分比 只有 x/y
#       这样的格式」)。排序**仍按比率** —— 分数大小一样时, 比率才是"哪个阶段更容易卡"
#       的正确次序, 按绝对帧数排会把长阶段顶到前面。
#     · 「最慢一帧」**毫秒 -> 帧/秒**(原话:「改为 xx.x fps」)。单位跟着本面板其余几行走
#       **中文「帧/秒」**, 不用 `fps` —— 上面两档门槛印的就是「低于 91 帧/秒」,
#       同一块面板混两种单位正是上一版被打回过的那类排版问题。
#   探针 `temp/panel_text_probe.py`(夹具 = v0.7.42 真机数字)三条判据 + 阴性对照全绿。
#
#   🟢 **三、测试历史面板配色对调**(玩家 2026-09-15:「标题用金色 备注用灰色, 2个颜色换下」)。
#   标题 `COL_TEXT` -> `COL_BALL`(金), 备注两行 `COL_BALL` -> `COL_SUB`(灰)。
#   ⚠️ 这是**有意反转** 2026-09-14 那条「解释文字换成金色」的决定, 注释里已标明新决定,
#      免得下一个人当成漂移又改回去。
#   验证: 像素级(金色只在标题那几行、灰色在最下面两行) + 截图肉眼确认。
#   ⚠️ `fx_probe` 里钉着旧决定的那条断言**跟着改了**, 并做了真·阴性对照(换回旧配色 =>
#      两条新判据变红)。新判据**两侧各钉各的**并互斥 —— 往哪个方向换都会红。
#
#   ⚪ **四、字形图集: 做完了, 但 `_GLYPH_ON = False` 关着, 不进这一版。**
#   代码(图集 / `GlyphLabel` / 预热 / 接线)全部就位且已验证到"**关掉之后与旧版逐像素
#   完全相同**"(同位置分两次截图比对, 差异 0)。卡住的是: 打开后字形的**顶边 AA 多出
#   恰好一行**(墨迹 36 行 -> 37 行, 底边不变), **我没能解释**。已排除: 纹理不同(逐像素
#   相同)、坐标不同(两边矩形都是 y=685/高 70)、过滤(linear 与 nearest 一模一样)、
#   跨位置比较(同位置也是 704)、多块拼接(单字同样)。复现: `temp/glyph_same_pos.py`。
#   收益依据仍在(每次中奖那 4 帧 20.5~22.8 毫秒里的 `填纹` 9~11 毫秒), 但**每一个差异
#   像素都要能解释**才许开 —— 下一位接着查 `TextureRegion` 的 `tex_coords` 是否恒等、
#   以及 Kivy 的 `Rectangle` 用 region 时有没有半像素 inset。
#
#   🔧 **五、`fx_probe` 自己修了一个真 bug**: `check()` 里 `print` 写在 `FAIL.append` **前面**,
#   而 Windows 控制台默认 GBK —— 详情里带 `⚠`/`⇒` 时 `print` 抛 UnicodeEncodeError
#   ⇒ **后面几百条断言一条都不跑**(静默少跑), 看起来像"门禁跑到一半崩了"。
#   已改成**先记账再打印**, 并给 `print` 加 ascii 兜底。
#   判据: 一条断言失败**绝不许影响后面的断言** —— 和"每条都要能失败"一样硬。
#
#   回归: --selftest OK · fx_probe 5 项既有失败(球与玻璃杯贴图, 预先存在) · --smoke
#
# 【v0.7.43 接缝真修: 把生成器设计里专修它的 `over` 层接回来(那是个静默回归)】
#
#   🔴 **找到了病根, 而且它是个回归。**
#
#   **不是**"亮度台阶"(我 v0.7.41 修错了那个, 已回退), **也不是**"叠一层就能盖掉"。
#   逐像素实测(椭圆最左端一列): 88 → **49** → 101 —— 一条 **1 像素的暗口**; 而且
#   **整图 `glass_tumbler.png` 自己就在那一行从 79 跳到 100** ⇒ 硬切是**设计稿里的**,
#   切开只是让它落在一条直线上。
#   ⚠️ 中途试过"用整图盖一条钟形渐变带"(玩家的建议), **扫了 peak 0.0~0.4 全试遍:
#      接缝列相邻行跳变恒为 52~54, 加不加、加多强都一样** —— 叠加只能**缩放**它,
#      消不掉; 要消掉需要"中间值"的墨, 而三张贴图在那一行都没有。该带子已撤除(不做无用功)。
#
#   **真因**: 生成器 `generate_glass_tumbler.py` 179..190 的注释写得很明白 ——
#     「后半环画在 `over`(压暗之后才画)…… 2026-09-13 把压暗去掉之后, **设计稿里原本的
#      硬切直接露了出来** —— 玩家报「玻璃杯的接缝黑白分明」。现在颜色统一, 只在 alpha 上
#      从顶部 62 渐变到分界处 80(**与前半环的 80 严丝合缝**)」
#   ⇒ **`over` 层就是这条接缝的正式修法**, 而 main.py 里**没有它** ——
#     2026-09-14「适配当前基线」时**连代码带 `fx_probe` 的两条断言一起摘掉**,
#     ⇒ **静默回归**(代码没了、门禁也没了、谁都没发现), 玩家这次看到的接缝就是它。
#
#   **改法**: 接回 `over` 层, 并让它**接管杯口那一段补画**(两个一起画 = 同一圈环画两遍)。
#     同一次演出只切一个开关, 接缝列逐行实测:
#       只补画(现状):      88 → **49** → 101               跳变 52   (暗口)
#       over 叠加:         138 → 112 → 101                跳变 49   (**亮过头**, 峰值 139)
#       **over 取代杯口**:  99 → 110 → **111** → 100 → 102 → 100  跳变 49  ← 既无暗口也不过亮
#     逐帧截图对照 6x: 现状那侧远侧弧末端**一个清楚的硬折角**, 新的是**一根连续的弧线**。
#   ⚠️ **只让 over 接管杯口, 杯底那段仍走 12 段补画** —— `over` 的杯底环段 alpha 只有 46
#      (back 同段 71), 整条换过去杯底远半圈会暗约 11%。
#   ⚠️ **必须原样画**(带贴图自带的 alpha): 它是**更白的镜面高光**而不是"更亮的 back",
#      按 α=1 整幅画会 −9% 反向过冲 —— 这也正是 v0.6.84 那次为何看着不对。
#   ⚠️ 需要把 `assets/glass_tumbler_over.png` **纳入 git**(此前一直是未跟踪文件)。
#
#   门禁: 新增 `fx_probe [28]` 五条 —— 它必须能加载(1600x920) / 表里有 `oseam` /
#     次序 = 压暗之后、补画之前 / 有 over 时杯口那 12 段让位 / **含阴性对照**
#     (关掉 over ⇒ oseam 消失且补画回到 24 段)。判据全是**行为**(建表看指令次序),
#     不是文本匹配 —— 本文件栽过三次"判定串被解释它的注释喂饱"。
#     ⚠️ 这一层之所以要专门钉一条, 就是因为**上次它是代码与门禁一起消失的**。
#
#   门禁: fx_probe **353 OK** / 5 项已知(球与玻璃杯贴图, 预先存在)。
#   验证: --selftest OK · fx_probe 353 OK · --smoke · 三态 A/B 截图 + 逐行剖面。
# 【v0.7.42 回退修错的接缝 + 冷字号: 预热被自己的缓存挡掉(真 bug) + 测试历史表头字号】
#
#   ① ⛔ **接缝那件整段回退** —— 见 v0.7.41 段尾的追记。我修的是"远侧比近侧暗 16%",
#      而玩家指出的那条缝其实是**贴图里上下两半没接上留下的 1 像素暗线**。改了等于没改。
#      数值剖面(台阶 10.3%->1.0%)、A/B 截图、门禁、阴性对照**全都做了且全绿** ——
#      **全绿也没用, 因为量错了对象。** 该改动与 `fx_probe [27]` 已一并撤掉。
#
#   ② 🔴 **冷字号: 找到两个真 bug, 都修了。** 740 日志里 `帧724 53.2ms[字号46.8]`
#      一帧就把 1%Low 从 92.7 拖到 91.1 —— 这是现在唯一压着 1%Low 的东西。
#
#      **bug A: 诊断自己印假数。** `_COLD_FS_BASE` / `_COLD_FS_TAG` 只在 `_fit1()` 里写,
#      而 `big_result_text` 这类地方是**直接调 `fit_font_size`** ⇒ `text_px` 读到的基准/标签是
#      **上一次 `_fit1` 的残留**。实证: 740 四条 FINE 冷字号都写「基准57.2375」
#      (= 设备 `sp(36)`), 而全工程只有"未中大字"用 `sp(36)`、它两个字在 0.70 档就放得下 ——
#      **根本走不到 FINE**。⇒ 据那个数改就是盲改。现在由 `_fit_font_size_slow` 现传真值,
#      并加了**文本前 10 个字** —— 一眼看出是哪句文案。
#
#      **bug B(真凶): 预热那一句可能被 `text_px` 自己的缓存挡掉, 等于没烘。**
#      预热走的是 `text_px("未中", fs, bd)`, 而 `text_px` 有自己的 `_FIT_PX` 缓存 ——
#      键一旦命中就**不建 CoreLabel、不开字体**, 预热白做, 而**画面上完全看不出来**。
#      日志「读法」早就写着这条现象(「=否 ⇒ 预热那一句被 text_px 自己的缓存挡掉了」),
#      但因为没人能从画面上看出来, **一直没修**。
#      ⇒ `text_px()` 加 `force=True`(跳过缓存读), 预热改走它。
#      桌面实测复现: 逼到 0.42 档那条路径的冷开记录 **5 条 -> 1 条**。
#
#      **顺带**: `_WARM_DID.add(...)` 原来**无条件**记 ⇒ 日志那栏「预热时量过=是」在
#      "其实没烘"时也照样显示, **自证失效(比没有更糟)**。现在只在 `_FRAME_FIT[1]`
#      (真冷测量计数)确实 +1 时才记。
#
#      **补烘**: 大字基准 `sp(36)` 的**整条 13 档阶梯**进预热表(原来只有 1.0 那一档)。
#      实测 740 四条冷字号全是 `sp(36) x FIT_FINE`, 且桌面复现的尺寸与真机**一字不差**
#      (`24.0397` / `35.4872`)。预热表 52 -> **58 项**(Kivy 上限 ~64)。
#      日志那行现在同时印「与 64 上限的关系」(有余量 / 贴上限 / **超了**)。
#      ⚠️ **只补 `sp(36)`, 没有全都烘上** —— 12 个 HUD 基准 x 13 档 = 156 必然爆 64,
#         "全都烘上"在这台引擎上是**反效果**(v0.7.31 实测: 155 项把前面全挤掉)。
#
#      ⏳ **仍未解决(要取舍, 留给下一轮)**: 修完 bug A/B 后桌面复现里仍有 **1 条**冷开
#      (`fs=18.0 = sp(36)x0.50`), 它在预热表里却仍然冷 ⇒ **被 Kivy 的 LRU 挤掉了**。
#      表 63/64 项**贴着上限**, 运行期任何一次额外开字体都会把最早的挤出去。
#      治本只有「**减字号总数**」(工程自己的读法就这么写) —— 而那是**改观感**的取舍
#      (阶梯档位变粗 / 标签变小), 不该由我单方面拍, 下一轮拿数据再谈。
#
#   ③ **测试历史表头 12sp -> 14sp**(玩家:「这个时间 平均/1%low帧什么的标题字体的大小增加」)。
#      原来表头 12sp **比下面的数据行 15sp 还小一档**, 不像表头。
#      实测: 时间 **14.00sp** / 中位跑分·波动 **14.00sp** / 平均·1%Low帧 **13.16sp**
#      (被 94px 列宽自动缩了一点, 仍高于 12), 三个列头**各一行**(排版高 19~21 < 列高 22)。
#      ⚠️ **别为了"字够大"去加宽 `fps_w`**: 第三列拿的是**剩余宽度**, 加宽它就是把它挤小
#         (窄屏上会缩到 ~10.7sp, 更糟)。表头这一排的总宽固定, 三个列头在抢同一块地。
#      门禁那条也改了: 原来钉 `_fit_line(head, 12)` 的**字面量**, 字号一改就假红,
#        而它真正要守的是"必须走 `_fit_line` 自动缩字号"。现在钉**行为**(基准 >= 14),
#        已做阴性对照(改回 12 -> 红)。
#
#   门禁: fx_probe **348 OK** / 5 项已知(球与玻璃杯贴图, 预先存在)。
#   验证: --selftest OK · fx_probe 348 OK · --smoke SMOKE-OK ·
#         冷字号桌面复现 5 条 -> 1 条(含阴性对照: 没烘过的字号仍能报冷, 判据有分辨力) ·
#         预热表 58 项(未超 64) · 测试历史表头截图与实测字号。
# 【v0.7.41 杯口接缝: 最靠接缝那几段叠第二遍 + 成绩面板加采样窗口/慢帧分布/门槛改帧秒】
#
#   ① 🔴 **杯口接缝**(玩家:「把杯子上边缘的接缝去掉吧」; 走了一轮对抗性评审):
#      病因**不是我们画出来的, 是烘在贴图里的** —— 杯口环那一个椭圆按贴图行 166/920
#      被切成前后两半, 分属 back(α 上限 81) 与 front(α 上限 146) 两张图。后层整层被压暗,
#      补画把它画回来, 但**单遍叠加的 alpha 上限就是那段纹理自身的 alpha**(环上 ~0.31),
#      而近侧半圈在 front 里是 ~0.37 ⇒ **切开线两侧永远差 ~16%**, 那就是接缝。
#      ⚠️ 工程里原先写着「落差消不掉」—— **那句只对「单遍」成立**。Kivy 的 `src_over` 是
#         **乘法复合** `1-(1-a)(1-a*k)`, 画两遍能**超过单遍上限**(0.31 ⇒ 0.42)。
#      改法: `_rim_back_strips()` 在每段 band 的 12 条之后, **把最靠接缝的 3 条再叠一遍**
#        (复用同一批子贴图, 只把 alpha 换成 0.40)。**其余一个字没动**, 指令 +12 条。
#      实测(完整合成链, 按 x 逐列): 接缝台阶 **10.3% -> 1.0%**,
#        而**环顶(远侧中段)63.9 一个数不变** —— 杯口的远近层次感完整保住。
#      ⚠️ α 是**扫出来的**: 台阶只跟 α 走、**跟叠几段无关**(测量窗口只落在最后两段里,
#         取 3 还是 6 段都包含它们)。0.25→+4.4% / 0.30→+3.3% / **0.40→+1.0%** /
#         0.50→−1.2%(反向过冲)。**取 0.40 不取更接近 0 的 0.45**: 让远侧**仍略暗于**近侧,
#         那是真实玻璃该有的样子; 再往上就翻成"远侧比近侧亮"的反自然过冲。宁可欠, 不要过。
#      ⚠️ 评审还推翻了三条我原先的说法(全部有像素级证据): ①台阶烘在原图里; ②`over` 那张
#         闲置图不是"更亮的 back"而是**一道更白的镜面高光**(RGB 240,249,255 vs 190,216,229),
#         整幅盖上去会 **−9% 反向过冲** —— **这正是 v0.6.84 那批被玩家否掉的机制**;
#         ③它其实有**两段**(还有杯底环 rows 691..811), 整条换源会让杯底远半圈掉 11%。
#         ⇒ 因此**没有采用 `over` 层**, 也就不必把那个被否过的资产拖进来。
#   ❌❌ **【2026-09-14 追记 —— 这一段修错了东西, 原样留着当教训】**
#      玩家收到包之后指认:「**这个接缝还在的**」—— 并指出红点标的位置才是接缝所在。
#      逐像素复量(玩家截图的椭圆最左端一列):
#          y 84..94   亮度 93~99   (上段)
#          y 95       亮度 52      ← **一条 1 像素的暗线**
#          y 96..107  亮度 93~95   (下段)
#      **两段的亮度本来就一样(~95), 中间夹着一条 1 像素的暗线。** 那不是"亮度台阶",
#      是**贴图在 row 166/167 处上下两半没接上**、露出底色的一条缝; 而环在最左最右是
#      近乎竖直的, 所以这 1 像素的缝横切过去就是一条看得见的线。
#      我整轮都在配平"远侧比近侧暗 16%"——**那是环的远近渐变, 另一个现象**, 所以改了等于没改。
#      ⇒ **该改动已整段回退**(`RIM_BAND_TAIL_*` 两个常量与那段叠画循环都撤了,
#        `fx_probe [27]` 一并摘掉)。教训: **"量到有差异"不等于"量到了玩家说的那个差异"** ——
#        改之前要先让玩家指认位置, 或者至少把**接缝本身**的剖面画出来看形状, 不能只看一个亮度差。
#      门禁 `fx_probe [27]`(4 条, 含**行为断言**: 拿真贴图调 `_rim_back_strips` 数条数与 alpha),
#        **已做阴性对照**(把段数置 0 → [27] 红 3 条)。⚠️ 第一版 4 条里有 3 条是**自指**的
#        (期望值用同一个常量算), 阴性对照只红了 1 条 —— 已改成绝对判据。
#
#   ② **成绩面板**(玩家三次追加, 措辞是他逐条定的):
#      · 「让我知道帧率计算的表演是多少秒, 多少帧」⇒ 第一行 **`采样窗口：X 秒 · N 帧 · 中位 N 帧/秒`**;
#      · 「<55% 让人看得不明不白的」⇒ 门槛不再写百分比。那一行原先**两个 `%` 含义还不一样**
#        (门槛 vs 比例), 读者分不出; 且「卡顿帧的比例和慢帧的比例, 写入后面对应的项目」
#        +「应该是 <xx帧, 这个xx是计算来的」⇒ 改成**算出来的帧/秒**;
#      · 「你应该先写要求, 再写数量, 目前顺序不对」⇒ **门槛进名字后的括号, 数量跟在冒号后**;
#      · 「额外新增一个慢帧分布」⇒ 两条分布并列。
#      最终排版:
#          采样窗口：31.9 秒 · 5233 帧 · 中位 165 帧/秒
#          卡顿帧（低于 91 帧/秒）：9 帧（0.17%）
#          慢帧（低于 124 帧/秒）：168 帧（3.21%）
#          卡顿帧分布：装杯 0.4%（9/2373）
#          慢帧分布：蓄力 6.0%（5/83）· 装杯 5.9%（140/2373）· 飞行 0.8%（21/2631）
#          最慢一帧：22 毫秒（装杯）
#      ⚠️ 三个数**同源**: 中位与两个门槛全从 `d["p50"]` 派生, 与判 `jank_n`/`slow_n` 用的
#         是同一个数 ⇒ "印出来的门槛"与"数出来的帧数"严格一致。门槛比例 0.55/0.75 也收成
#         `JANK_RATE`/`SLOW_RATE` **单一真源**(原先散在四处)。
#      ⚠️ 慢帧的**帧数与分布取自同一个 list**(不许两趟各算一遍 —— 那是 v0.7.40 那个
#         `slow_n` 被覆盖的同族病)。新增日志自证行 `★ 面板窗口 vs 日志窗口`。
#      ⚠️ 顺手清掉三行**死代码**(`_n/_ms/_stages = d.get("low1_*")`, 取值后无人用,
#         `_n` 还在下面被重新定义) —— 是 `fx_probe` 新增的那条断言抓出来的。
#      ⚠️ 顺带修掉门禁里**两条子串幻觉型假绿灯**: `"_th90 = _p50b / 0.5"` 一直匹配着
#         `/ 0.55`(`0.55` 含 `0.5`)、`"/ 0.7"` 匹配着 `/ 0.75` —— 阈值从 50%/70% 改成
#         55%/75% 时它们**照样绿**。现在改成钉常量名。
#
#   门禁: fx_probe **352 OK** / 5 项已知(球与玻璃杯贴图, 预先存在)。
#   验证: --selftest OK · fx_probe 352 OK · --smoke SMOKE-OK ·
#         接缝数值剖面 10.3%->1.0%(环顶不变) · 接缝 A/B 截图 · 面板六行与真日志标准答案逐字一致。
#
#   ⏸️ **暂缓**: 「数字字形图集」这轮没做。挖下去发现一个真陷阱 —— `_texex_key` 里含
#      `text_size`, 而大字的 `text_size` 是 `bind(size=...)` 在**布局之后**才写进去的,
#      同一文案在布局前后落到**两个不同的键**(日志里「结算大字 4 次/局」正好 = 2 键 x 2 Label)
#      ⇒ 预热只要一项对不上就是**静默空转**(画面正常、命中率恒 0)。而真图集要重写大字那条路,
#      它身上挂着淡出/缩放/揭晓时序一堆血泪不变量。**单独一轮做扎实**, 不在本轮硬塞。
# 【v0.7.40 修 v0.7.39 的落杯画面事故(丢 PushMatrix/PopMatrix) + 修正「慢帧<75%」恒等于卡顿帧】
#   ① 🔴 **画面事故**(玩家:「落杯动画一塌糊涂」, 附录像 `video/bug1.mp4`):
#      v0.7.39 的持久指令表把 `_draw_bead` 里那对 `PushMatrix/PopMatrix` **漏掉了**。
#      而 Kivy 的 `Rotate` 是**上下文指令** —— 它作用于**其后所有**指令, 直到被 Pop 掉。
#      后果有两层, 第二层才是致命的:
#        · 第 2 颗球的旋转叠在第 1 颗上, 第 3 颗再叠…… 20 颗球 = 20 个旋转的复合
#          (录像里球被拉成一条**斜串**散出去);
#        · **球层后面紧跟着前层玻璃**(`_fx_post`) ⇒ 那串未 Pop 的旋转**全叠到杯子上**,
#          杯子被拧成一个巨大的斜四边形(录像 #1263 最清楚, 调试截图 `temp/cmp_cup.png`)。
#      修法: `_tbl_build` 的球层循环里补回 `PushMatrix()` / `PopMatrix()`(**红线 4**)。
#      ⚠️ 为什么能出货: 见 v0.7.39 块里那段**追记** —— 当时的验收探针参数没被用上,
#         把同一份文件跟自己比了两遍, 报"逐条一致"(假绿灯)。**探针也要做阴性对照。**
#
#   ② 🔴 **`slow_n` 被原地覆盖**(玩家:「慢帧<75% 的实际帧率数量也是错的吧, 他目前和
#      卡顿帧一直相同」—— 完全正确):
#      `_bench_collect_diag` 里 `out["slow_n"]` **赋值了两次** —— 收尾那段按
#      「帧率 < 中位帧率 75%」算对了(**真机 v0.7.35 = 168 帧**), 后面"归因分析集"
#      那段又用「≥2 倍中位帧时间」(即 <50%) **原地覆盖成 5**; 面板再套一层
#      `max(slow_n, jank_n)` 兜底 ⇒ `max(5, 9) = 9` ⇒ **印出来恒等于卡顿帧**。
#      修法: 分析集改名 `slow2_n` / `slow2_ms`(它的消费者只有那一行日志), 把 `slow_n`
#      留给面板。**用真机逐帧数据当标准答案验收**: 卡顿帧 9 / 慢帧 168 / 长停顿 5 全中。
#      ⚠️ 顺带把日志里两处「慢帧」标签改成「**长停顿**」(门槛行 + 各阶段表列头) ——
#         它们说的是 ≥2 倍中位(<50%)那一档, 而「慢帧」已被面板占成 <75%。同一份日志里
#         同一个词两个含义, 实测两个数差 **33 倍**(168 vs 5), 正是误读的根源。
#         代码里这个现象一直叫"长停顿"(159/2009/9570/9774/10819 行), 跟着它叫。
#      新增自证行(日志头): `# ★ 慢帧「中位帧率 <75%」: N 帧 = 低于55% A + 参考带 B`
#         —— 下次读日志当场能验面板那个数有没有又脱钩。
#
#   门禁: fx_probe **+4 条** = 340 OK / 5 项已知(球与玻璃杯贴图, 预先存在)。
#      [26] 装杯球层矩阵栈: 每球一对 Push/Pop + 栈不倒挂且归零 + **每条 Rotate 都紧跟
#      PushMatrix**(只数个数不够 —— 把 Push 全堆开头、Pop 全堆结尾也满足个数和归零)
#      + 前层玻璃画在深度 0。**已做阴性对照**(摘掉那两行 → [26] 相关四条全红)。
#   验证: --selftest OK · fx_probe 340 OK / 5 已知 · 球层坐标旧 vs 新 8/8 逐条一致 ·
#      三版逐帧截图对照(旧实现正常 / 坏版灰色斜板 + 球斜串 / 修复版杯子形状正常)。

# 【v0.7.39 装杯板面持久指令表(真性能改动) + 成绩面板两档 + 版本号分隔】
#   ① ⚠️ **这是 v0.7.33 之后第一件真动性能代码的改动** —— v0.7.34~38 全是埋点/口径/观感,
#      一帧都没降过。这一件预期清 **2 帧**(帧5192 11.27ms / 帧5197 11.29ms, 板面 3.4/3.3,
#      **只超线 0.16 / 0.18 毫秒**)。
#
#      **做法**: `WinPileFX._redraw` 原来是 `canvas.clear()` + **整块重建** —— 固定 56 条
#      (接地阴影 2 + 后层玻璃 2 + 压暗 2 + 杯口环补画 48 + 前层玻璃 2)每帧重来一遍,
#      球层还有 `5 x N` 条(x100 时整块 684 条)。真机 `板面` 306 帧累计 **639 毫秒**。
#      现在改成三张**持久指令表**: `_tbl_build` 建一次, 之后每帧 `_tbl_apply_fixed` /
#      `_tbl_apply_balls` 只改 `rgba / pos / size / angle`。不可见的球只把 `size` 置 0,
#      **不增删指令**(条数在一局内恒定)。
#      桌面基线(v0.7.35 拆分实测): 杯层 max 0.71 / 中位 0.32; 球层 max 1.64 / 中位 0.36。
#
#   ⚠️⚠️ **红线三条, 一条都不能碰**(全写进代码注释了):
#      · 球的画家序**绝不能拆** —— 必须按 `self._balls` 原始顺序单趟建表。历史账:
#        改成"落定球 + 飞行球两趟"之后实测 x100 一局 4750 帧里 **25.5% 的重叠帧层次错乱**、
#        落定那一帧约 46 颗球单帧掉一半像素。那个写法当年没出事, 唯一原因是
#        "发牌序恰好 = z 降序"这个巧合。
#      · 玻璃五层的相对顺序不许动: 后层玻璃 < 压暗 < 杯口环补画 < 弹珠 < 前层玻璃。
#      · **每次 mode 迁移必须显式作废表**(`_tbl_drop`) —— 少了它退出装杯后杯子/压暗会
#        **永久留在画布上**, 而 `canvas.clear()` 是唯一能擦掉它们的机制(原来每帧都 clear,
#        所以这个问题从来没暴露过)。
#      另: 重建判据里球列表比**对象身份**不比长度 —— 新一局长度可能一样(8 颗换 8 颗),
#      只比长度会拿上一局的球继续画。
#
#   ❌❌ **【2026-09-14 追记 —— 这一段当时是假的, 原样留着当教训】**
#      当时写的是: 「改用 `temp/pile_tbl_probe.py` 把两版指令流 dump 出来逐条比,
#      **结果新旧各 164 条指令逐条相同** ⇒ 红线 1/2 双双成立」。
#      **那是假绿灯。** 那个探针的 `SRC = sys.argv[1]` **赋了值却从没被用过** ——
#      它永远 `import main`, 于是所谓"新旧对比"其实是把**同一份文件跟自己比了两遍**,
#      当然一模一样。而真实差异(球层少了 `PushMatrix/PopMatrix`, 每球差 2 条)就这么被吞了,
#      直接导致 v0.7.39 带着画面事故出货 —— 玩家实测「**落杯动画一塌糊涂**」。
#      **教训写死在这里**:
#        · 探针**必须能被它自己的参数改变行为**; 写完先用两个**已知不同**的输入跑一遍,
#          确认它真的会报差异(这就是探针的阴性对照, 和门禁的阴性对照同等重要)。
#        · 数"条数相同"**不能证明"结构相同"** —— 结构断言要另外写(fx_probe [26])。
#        · 结论里出现的数字, 必须是那次真跑出来的; 「我以为它跑的是 A」不算。
#
#   ✅ **验收方式(2026-09-14 重做, 这次是真的)**:
#      · `temp/tblball_diff.py`: 用 `importlib` 按**参数指定**的文件加载(参数真的被用了),
#        夹具用**已落定的球**(坐标跨进程确定), 只比球层矩形。
#        结果: **旧 8 条 / 新 8 条, 逐条不同 0 处** ⇒ 持久表没有改动任何一颗球的几何。
#      · `fx_probe [26]`: 结构断言(Push/Pop 配对 + 包住每条 Rotate + 前层玻璃在深度 0)。
#      · 逐帧截图三版对照(`temp/win_shot_all.py`): 旧实现/坏版/修复版同拍。
#
#   ② **成绩面板印两档**(玩家定稿, 名字也是他定的「改名字即可」):
#        `卡顿帧（<55%）：共 N 帧` 与 `慢帧（<75%）：共 M 帧`
#      阈值都是**相对本机中位帧率**的百分比。⚠️ **慢帧 ⊇ 卡顿帧**(同一个集合的两档),
#      所以 `M >= N` 恒成立; 老 diag 字典没有 `slow_n` 时直接印 0 会出现
#      「卡顿帧 6 / 慢帧 0」这种**自相矛盾**的两行(探针夹具上当场撞到过) ⇒ 加了不变量兜底。
#
#   ③ **版本行加分隔符**: `NCO-AL00 / Android 15  v0.7.37` -> `NCO-AL00 / Android 15 / v0.7.37`
#      (玩家: 「安卓版本和这个游戏版本也可以加一个符号 /」)。三段并列: 机器 / 安卓 / 游戏版本。
#
#   门禁: fx_probe 336 条(+6: 持久指令表四条 + 面板两档两条; 另更新了几条被取代的旧断言)。
#   验证: --selftest OK · fx_probe 336 OK / 5 项已知(球与玻璃杯贴图, 预先存在)。
version = 0.8.62
# ⚠️ **版本号实际一直在用 `0.8.x`**(玩家 2026-09-17 定: 这次 bump 到 `0.8.21`)。
#    下面这段"换成 `1.0`"的方案**已作废**, 留着只为解释历史, **别再照着改**:
#    2026-09-16 14:47 的 `84b8a57` 曾把值改成 `1.0` 并写下这段注释, **11 分钟后**
#    `83d0092`(v0.8.0) 又把值退了回来, 注释漏删 ⇒ 值与注释互相矛盾了一天多。
#    上面的 changelog 里 `v0.7.x` 那些是**历史记录**, 不改。
#
# --- 以下为已作废的旧方案原文(仅存档) ---
# ⚠️⚠️ **版本号方案从这一版起换成 `1.0` / `1.1` / `1.2` …**(玩家 2026-09-16 定:
#    「把版本号改为 1.0（后续是 1.1, 1.2, 1.1000）之类」)。
#
# ⚠️⚠️ **但 `version` 只决定 versionName(显示的那个字符串), 真正管"能不能装上去"的是
#    `android:versionCode`** —— 而它**没在本文件里定义过**, 一直是由 buildozer/p4a
#    自己推的。buildozer 只在 `android.numeric_version` **有值**时才把 `--numeric-version`
#    传给 p4a(见 buildozer 1.5.0 `targets/android.py`: `if numeric_version:`), 所以在此之前
#    那个数是 p4a 按它自己的规矩给的 —— **很可能与 `version` 字符串有关**。
#    ⇒ 版本号从 `0.7.111` 退回 `1.0`, 万一 versionCode 也跟着退, Android 会**拒绝安装**
#      (`INSTALL_FAILED_VERSION_DOWNGRADE`), 装新版就得先卸载 —— 那会把玩家攒的历史
#      (`plinko_hp_history.json` / 性能测试历史 / 弹珠数)一起清掉。
#    ⇒ 所以这里**把 versionCode 显式钉成一个很大的常数**: 不管旧包推到多少, 新包永远
#      比它大, 永远装得上。**别删这一行**, 也别改成跟 `version` 联动的写法。
#    (`1_000_000` 远大于任何 `0.7.x` 能推出来的数; 安卓上限 2_100_000_000, 不碰线。)
android.numeric_version = 1000000




requirements = python3,kivy==2.3.0,pyjnius

# 锁定 python-for-android 到 2024 年的 tag,绕开新版默认下载 Python 3.14 alpha 的问题
p4a.branch = v2024.01.21
# 构建后 hook: 往 manifest 主 activity 强制 screenOrientation=fullSensor + resizeableActivity=true
p4a.hook = p4a/hook.py

# 四方向随重力: 正竖/倒竖180°/横拿全支持。横拿时 app 内切左盘面+右控制列分栏(盘面竖直满屏),
# 不再锁竖屏——锁竖屏会被 12L+ 大屏 letterbox/ZUI 关进半屏兼容盒(画面缩小的病根)
orientation = portrait, portrait-reverse, landscape, landscape-reverse
# 显式 manifest 方向: fullSensor(四方向随重力, Android原生值), 与 hook 注入一致
android.manifest.orientation = fullSensor
# 全屏沉浸(仅运行时实现): 状态栏/导航栏由 main.py _enter_immersive() 负责隐藏。
# fullscreen 保持=0(与已知可跑的 0.5.0 一致, 不折腾): 0.5.1(fullscreen=1)与
# 0.5.2(=0)当时都闪退, 但真凶是 _enter_immersive 的零参签名 bug(见 BUILD_APK.md
# 弯路 3.22), fullscreen=1 本身未被证明有问题, 只是也没必要开。
fullscreen = 0

android.permissions = VIBRATE

# v0.8.61: 给 APK 加一个**自己写的 Java 源目录**(`java/com/plinko/SoundGate.java`)。
# ⚠️ 这是本项目第一次往包里塞自制 Java。已核 buildozer 1.5.0 的 `execute_build_package`:
#    它读 `app.android.add_src` ⇒ 转成 p4a 的 `--add-source` ⇒ bootstrap 的 build.py 定义了这个
#    选项并喂给 gradle。**类取不到不会崩**: `_attach_gates` 全包 try/except, 拿不到就静默退回
#    老探针(日志里印「闸门 无(类取不到: …)」)。备选路是 `p4a/hook.py` 往 dist 里写文件。
# ⚠️ 那个 .java **通篇只写 ASCII** —— CI 上 javac 的默认源编码不保证是 UTF-8, 注释里一个
#    中文字节就可能把整个 APK 编崩。
android.add_src = java

# targetSdk=33(2026-08-17): 30 的兼容模式在 12L+ 大屏(sw>=600dp)会被塞固定比例
# letterbox 盒(ZUI 近正方形半屏盒, 实测 fullSensor 四方向也躲不开, 像素取证确认)。
# 33+ 声明全方向+resizeable 才给全屏窗口; 锁竖屏时代(sensorPortrait 需兼容模式)已结束。
android.api = 33
android.minapi = 21
android.ndk = 25b

android.archs = arm64-v8a,armeabi-v7a

android.allow_backup = True

icon.filename = %(source.dir)s/icon.png

# 必须和 presplash.png 边缘同色(#0b1220),避免闪屏黑闪
android.presplash_color = #0b1220
presplash.filename = %(source.dir)s/presplash.png


[buildozer]

log_level = 2
warn_on_root = 1
