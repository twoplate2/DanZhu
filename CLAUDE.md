# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 这个仓库是什么

跳跳的弹珠机 Android 版 (Kivy 2.3, 竖屏)。PC 版 `plinko.py`(tkinter) 的移植。
push 到 `main` → GitHub Actions 云构建 → Artifacts 下载 APK。
**打包/构建配置要动之前, 先读 `BUILD_APK.md`**。

## 命令

```
python main.py              # 桌面预览(540×960)
python main.py --selftest   # 无界面门禁自测(改完必跑; 偶发 3σ 假失败, 重跑一次)
python main.py --smoke      # 自动冒烟 + 截图到 %TEMP%/plinko_smoke
python main.py --landscape  # 桌面模拟横屏反旋转(1740x1000 横窗, 验证横屏布局用)
python main.py --nosound    # 静音启动
python -m py_compile main.py
python ../tools/fx_probe.py # 中奖玻璃杯自检(纹理/堆形/缓存/时长/曲线/**揭晓投递语义**)
python ../tools/build_android_main.py --check   # 校验 main.py 与 tools/ 源同步
```

⚠️ **出包必须 bump `buildozer.spec` 的 `version`**: 它是"装的是哪个包"的唯一肉眼证据
(APK 文件名含版本)。改完代码要 commit+push 才会触发云构建(`.github/workflows/build-apk.yml`,
也支持 `workflow_dispatch` 手动触发; 跑一轮 25~90 分钟)。

**测中奖务必先切 360% 档**: 默认 80% 档盘面抽不出 x50/x100, 拿固定盘面测等于没测到
真正会出问题的高倍率(颗数最多、锁输入最久、最容易掉帧)。

## main.py 是生成物

`main.py` 由**父项目** `tools/build_android_main.py` 生成:
- 常量/几何/物理/音效合成/selftest **原样抽取**自父项目 `plinko.py`
- 中奖杯球堆 `tools/pile3d.py`(纯 stdlib) + 表现层 `tools/android_part_pile.py`
- Kivy 手写段来自 `tools/android_part_{head,backends,ui}.py`

顺序: `[head, b1..b7, backends, pile3d, android_part_pile, ui]`(新两段插在 `ui` 之前,
因为 GameArea/RootWidget 要用到它们)。**`tools/` 与 `../wingui/` 都不在 git 里**,
仓库里只有生成物, 所以改完必须重跑生成器 —— 用 `--check` 兜底。

本仓库只含生成结果, **不含生成器和源文件**。因此:
- **在父项目环境里**: 改源文件 → `python tools/build_android_main.py` → `python android/main.py --selftest` → 回本仓库 commit。**不要手改 main.py**。
- **只有本仓库时**: 直接改 main.py 可行, 但父项目重新生成会覆盖。重大改动必须回父项目做。

## main.py 内部结构

1. **常量+几何**: 520×660 逻辑坐标系(y 向下)。发射槽在井底(PLUNGER_Y=FLOOR-BALL_R-2, 弹簧 Z 字形露出), 发射速度 1077~1114, G=1000
2. **物理层**: `Ball`(__slots__ class, 含 `_rng` 撞钉扰动流), `physics_step`, `launch_ball`, `advance_flight`(纯被动: 重力+碰撞, 零干预), `benchmark_trajectories`
   - **三段式轨迹**: 竖直上升(vx=0)→弧面缓动带球转向(35°接触段, ARC_EASE_FRAMES=3, 出口速度=入射速度不耗能)→抛体进钉阵
   - **4 旋钮离散表**: 弧面角度/力度、天花板角度/力度, 10 个力度档各一套「离散值+概率」表(`KNOB_*` 四张表), 每发按力度档采样(`_sample_table` + `_power_band`), boost 限幅 [0.65,1.25](加速≤25%/减速≤35%)
   - **力度互补 + 总体均匀**: 弱蓄首钉偏右/满蓄首钉偏左, 随机力度时首钉/落袋总体铺满 9 槽(绕开弱蓄档打不散的物理瓶颈)
   - **天花板 2 号弹射器**: 撞顶后按力度档采样角度旋转 + 力度缩放, 撞顶保底 vy≥180 防"吸住", 事件位记 EV_CEIL(已修死事件位)
   - **弧面** = 根部R8弧 + 35°接触段15px + R400微弯弧30px(切线连续无转折), 碰撞半径=视觉半径(ARC_VISUAL=1.4, 球与弧面相切)
   - **彻底被动**: 无引导/无修订/无预定槽, 球完全被动下落, 结算==物理落格恒成立(零穿帮)
   - **盘面固定格数**: `roll_multipliers` 按 `K_DIST` 表掷"本局几个格子有奖"(`K_DIST[rtp]`),
     再 `random.sample` 选位置、用 `VALUE_DIST[rtp]` 填倍率。四档实测中奖率 **24.1% / 35.6% /
     43.4% / 78.7%**(现 360% 档 = 6/7/8 格)。**RTP 由 `_solve_p2` 闭式反解配平, 改表不用手调**;
     中奖率与"每次中多少"是**同一个旋钮的两端**(返还是档位, 恒等)。
     ⚠️ 别用 `E[K]/9` 估中奖率(坏盘重抽对 k 有偏, 偏低约 1pp), 要看准确值就跑 MC。
   - **碰钉回弹**: PEG_BOUNCE_VY_MAX=280(弹高≤39px<行距55) + E_SLOW=0.70/E_FAST=0.40 + PEG_FRICTION=0.95/vy0.97 + PEG_SPRINT=0.7软化(末段保留横速, 落袋干净靠 ALIGN 收尾)
   - **弧面抖动**: 每发 ±6px 垂直平移(arc_dy), 档内首钉散布 ±10~15px
   - **卡死兜底看位移**: 球位置不动(≤1px/帧)超 MAX_FALL_SEC(4s)才强制 settle
   - **遗传算法优化器**: `../scratch/optimize_knobs.py`(**在父项目, 不在本仓库**), 三阶段 GA(每档独立→联合480维→降维arc-only120维+种子初始化), 评分=落袋总体均匀×0.7+首钉总体均匀×0.3
   - **benchmark_trajectories(duration)**: 纯 CPU 性能测试(返回 3 元组 flights/frames/fps_list)
3. **音效**: 41 合成 PCM + 57 edge-tts 语音(voice/*.wav)。`Sfx.play()`: gain 10档缓存、按名节流
   - ⚠️ **节流键是音效名**(`self._last.get(name)`), 所以**"加变体"等于把聚合速率乘上变体数** ——
     按 `peg0..5` 的惯例给某个音加 4 个变体, 速率上限会悄悄从 10/s 变成 40/s。
     `SFX_VOICES=8`, 满了的行为**不是丢音, 是把还在响的流当场掐断**(随机断头音, 桌面测不出来)。
   - ⚠️ **合成顺序即音色**: 所有配方共用一条 `_ARNG` 随机流, 新增音效**一律追加在 `iter_bank()`
     末尾**, 插在中间会平移其后所有音效的波形。
   - **全局语音互斥**: voice_rtp_/voice_bet_/voice_mode_ 按上一句**真实时长**判。click throttle 0.08s。flight 跳过 bake/prime
   - **弧面接触音**: EV_ARC 事件播 rail 0.18(轻金属"擦"声, throttle 0.05)——转向瞬间的听觉反馈
   - **装杯落杯音用的就是 `bounce`**(主游戏"球落进倍率槽"那个音, 一个波形成本)。2026-09-10 试过
     换成 4 个变体的合成"玻璃音", **用户判"这是电子音, 不是小球撞击的声音", 已全部回退** ——
     教训写在 `plinko.py` 的 `_sfx_cup` 原址注释里: **撞击感来自攻击瞬间的宽带噪声, 不是分音**;
     主导成分只要是几个正弦分音 + 长衰减, 无论基频放哪都是"音调"。要照 `_sfx_tink`(撞钉)那样改。
4. **后端**: `_SoundPoolOut`(Android) > `_WaveOut`(winmm) > `_KivySoundOut` > 静音
5. **Kivy UI**: `GameArea`(FloatLayout) + `RootWidget`(BoxLayout 6行)
   - 弹簧: Z字形, k=120/damp=3.2 阻尼振荡回弹, 视觉倍率45, 过冲clamp=-0.25
   - 状态机: ready→charging→flying/misfire→landing→landed, 累加器驱动
   - 发射: frozen_power 保存力度, 音量分级(0.60→0.80), 震动(哑火8ms/正常14ms)
   - 落地: LAND_E=0.42, LAND_BOUNCE_MIN_VY=220, LAND_BOUNCE_MAX_VY=220(回弹vy上限), ±8%随机, 不瞬移
   - **回弹是纯竖直的**(用户定稿): 进入 landing 状态时立刻 `b.vx = 0`。横向只剩 LAND_K 弹簧，而弹簧只会把球拉向**本槽中心**，拉不出去 —— 落定期间球不可能横移出本槽。
     ⚠️ 病根在这儿：landing 循环里只有重力/横向弹簧/地板，**没有隔板碰撞**，而球带着飞行末段的横速入槽，回弹期能横着滑过隔板停到隔壁槽（结算槽仍是本槽 = "钱算对了、球停错地方"）。清零横速是治本的解法，比补隔板碰撞干净。
   - **结算提前到"第一次触地"那一刻**：`settle(i)` 直接在 `landed` 分支里调（原来要等回弹落定）。实测原来 88.7% 的落定是走 0.5s 超时兜底结束的、平均比触地晚 0.3~0.5s。**回弹照播**（landing 状态只是不再拦着结算），但中奖演出/揭晓从触地就开始排。
     ⚠️ 改这里要保住两条：`_settled` 守重复；landing 分支的 `landed_at` 仍要按"回弹结束"重设，否则 LAND_HOLD 会从触地时刻开始算。
   - 飞行中灰化: `_set_controls_enabled(False)` 时按钮+标签文字统一变暗
   - 中奖大字: life=BIG_TEXT_LIFE=1.8s(见 android_part_pile.py 顶部), font_size 仅值变时写
   - 满蓄力: 每0.60s轻响 charge_full(0.40)
   - 防沉迷: balance/round_plays/plays/hits 持久化, 启动自动处理打满状态
   - 返回键拦截(key 27), 声音状态一致, 轮次结束语音兜底 total+3.0s
   - **性能测试(隐藏)**: 长按标题3s→10s benchmark→Popup弹窗(设备/帧数/次秒 + **版本 · 构建日期**)
  - 那一行的日期取 `main.py` 的 **mtime**, 不烘进源码: 生成器是纯字符串拼接, 烘日期会让
    `--check` 每次都报不同步。p4a 把 app 目录塞进 APK 的 `private.tar` 时, 条目时间戳 = 构建机
    上文件的写入时间(CI 是新拉代码后立刻构建), 首次运行解包保留 mtime, 所以它约等于构建日。
    版本走 PackageManager 的 `versionName`。`fx_probe [13]` 钉住『不抛异常 + 格式 YYYY-MM-DD』。

## 语音播报

57条语音(含若干切换提示与数字片段)。两态开关: 音效已开(含语音播报,默认)→音效已关；
**声音设置不持久化**(不进配置文件, 每次启动都是已开)。
voice_lose 有意不接入(合成 lose 音更中性)。
语音互斥仅对 UI 交互生效, 结果/轮次序列不受影响。

> **RTP 语音已补齐（2026-08-16）**：`voice_rtp_80/120/200/360` 四条齐全，孤儿 `voice_rtp_100` 已删。切档四档都有语音。（2026-09-10: 最高档 300% -> 360%）

## 中奖玻璃杯表现

中奖时结算后停 0.5s(让槽位白闪/绿灯先被看见)→ **整屏压暗(板面 + HUD 五行)** → 玻璃杯浮在画面
**中下部**、弹珠从板面上方雨点般落下堆满杯子 → **装满静止 hold_for(倍率) 秒**(回味) → 0.25s 淡出 →
解锁。倍率 = 颗数, 投注档 = 球色(1绿/10蓝/50红/100紫)。

**压暗的两处真源与绘制次序**(2026-09-11 修"背景不够暗"; 改之前板上全绿但画面是坏的):

- **覆盖面**: `WinPileFX` 的压暗矩形 = GameArea 的矩形, 而 RootWidget.children 顺序是
  `[顶栏, 返奖档, 投注档, GameArea, 信息行, 底行]`(后 add 的后画) ⇒ **信息行和底行画在
  GameArea 之上, 板面压暗永远盖不到它们**; 横屏反旋转时五行正好落在画面两侧, 于是
  "板面已黑、两边还亮"(实测左条 36.6 / 右条 29.6 vs 压暗后板面 14~17)。
  现在用**两块矩形盖住"整块屏幕减去游戏区"**: `RootWidget._build_hud_dim()` 在
  `self.canvas.after` 里放上下两块, `_relayout_hud_dim()` 每帧摆位,
  `_sync_hud_dim()` 每帧从 `WinPileFX.dim_alpha(HUD_ALPHA)` 取值统一写。
  ⚠️ **视口必须读 `self.parent`(App.build 里那个 AnchorLayout), 不能按 `self.x/width` 反推** ——
  Kivy 是单一坐标系(`LandLayer._to_eq` 的注释写明整棵树的 pos 就是"等效竖屏物理坐标"),
  那份矩形恰好就是等效视口、横屏时铺满整块物理屏。第一版按 `-self.x` 反推, 实测宽窗口横屏下
  **右边 122px、顶边 56px 原样亮着**, 且 `game_area.y<0` 时下块被 `max(0.0, ga.y)` 算成高度 0。
  ⚠️ **别再退回"每行各盖一块"**: 五行之间有 `spacing=dp(10)`、底部 `padding` 12dp, 那些地方是
  窗口背景, 没有控件去盖 —— 第一版就是这么写的, 62px 的缝成了全屏最亮的东西(缝 20.6 vs
  相邻被压暗的行 16.8~19.6, 横屏时是 **6 条贯通全高的竖亮线**)。也别写"相邻两块首尾相接":
  一旦重叠, 0.68 叠 0.68 变 0.90, 缝从"更亮"翻成"更暗"。
  ⚠️ **两者不能用两个独立常数**: `HUD_ALPHA = DIM_ALPHA`(0.68) 是刻意的, 同屏两档会打架。
  实测: 五行 65.1/42.8/40.9/36.0/45.2 → 18.7/12.6/12.4/14.3/12.0, 缝 20.6 → 11.7, 演出后**精确复原**。
- **绘制次序**: `_redraw` 必须是 `后层玻璃 → 压暗 → 杯口环补画 → 球 → 前层玻璃`。
  原来压暗画在**后层玻璃之前**, 而后层玻璃(球堆区覆盖 95% 面积、alpha 均值 22.6/255、
  近 (166,194,210))会把刚压暗的板面**提亮回去** —— 实测杯内只压掉 12%、杯外板面压掉 46%,
  **玩家盯着的杯子周围反而是全画面压暗最失败的地方**。改成后层玻璃之后再压, 杯子区域
  亮度 34.0 → 25.8。放到**前层玻璃之后**则是错的(会把弹珠本身也压暗)。
  ⚠️ **代价是后半个杯口环一起被压暗** —— 实测峰值 68.0 → **29.0(−57%)**, 而前半圈在 front 层
  纹丝不动 ⇒ 前后对比从 1.76:1 拉到 **4.1:1**, 那半圈读起来像"没画出来"(玩家 2026-09-11 报
  "上边缘的内侧和外侧颜色差异较大")。所以多一步 `_rim_band_strips()`: 把 back 里
  **后半个杯口环那一条**(`RIM_BACK_FRAC` = 贴图顶上 166/920, 几何切)补画到压暗之上。
  ⚠️ **补画必须分段做纵向渐变, 不能整条一个 alpha**(第一版就是整条, 已被玩家打回:
  "明暗可以不一样, 但是目前的方案是没有过渡, 只有两个明暗层次")。后层被压暗、前层不压暗,
  两者在杯子左右两侧**直接拼上** —— 整条补画只是把台阶从 `25 vs 120` 挪成 `80 vs 120`, 还是硬切。
  现在横切成 `RIM_BAND_STRIPS=12` 段, 每段一个 alpha, 从顶部 `RIM_BAND_ALPHA_TOP=0.55`
  线性升到分界处的 1.0 —— 相邻段只差 4%(看不出台阶), 且分界两侧都接近 1.0(接缝消失)。
  实测沿环一圈: 远侧中心 ~42 → 两侧 ~70 → 近侧 ~120, 连续变化。
  ⚠️ 补画**不能按"alpha>60 的像素"挑**: back 里杯体和杯底也有 alpha>60 的像素(各约 1.1 万),
  那样会把薄纱一起捞回来, 等于把"板面被杯子提亮"的元凶放回去。必须按贴图行段切。
  ⚠️ 补画位置也钉死了: 在**压暗之后**(压暗之前 = 白补)、**弹珠之前**(之后 = 球被它盖住)。
- **回退路径**(分层图加载失败只画整图)以前画在压暗**之后** ⇒ 回退局里杯子比正常局亮一截、
  前后遮挡也没了。现在把整图当"后层"用(画在压暗之前), 正常路径零影响。
- `WinPileFX.dim_alpha(peak=DIM_ALPHA)` 是压暗的**唯一取值口**，返回的是**板面本帧真正画上去的那个数**
  （`_redraw` 每帧把 `a_dim` 写进 `_a_dim_now`，`dim_alpha` 只做换算，**不自己再取一次时间**）。
  `idle` 直接返回 0（退场分支读 `_settled_at`，而 idle 时那是上一局残值，交给 `_layers()` 会被
  clamp 成 1 ⇒ HUD 永远全黑）。
  ⚠️ **两边不能各自 `time.time()` 采样两次**：曲线最陡处约 `DIM_ALPHA*3/ENTER_DIM_DUR ≈ 12.75 alpha/s`，
  两次采样差 10ms 就差 0.13 个 alpha；更要命的是**退场结束那一帧** `tick()` 已把 mode 切成 idle、
  `_redraw` 不再画板面，HUD 若自己取时间就会比板面**多黑一整帧**（专家组实测指出，`fx_probe [11]` 已钉住）。
  配套：`RootWidget._frame` 里 `_sync_hud_dim()` 必须排在 **`game_area.tick_draw()` 之后**
  （演出是靠 tick_draw → `win_fx.tick()` → `_redraw` 推进的，放前面就又晚一帧）。
- **Kivy 语义(已用最小样例实测)**: `child.canvas.after` 画在该子控件**自己的 canvas 与
  它全部子控件之上**; `parent.canvas.after` 画在整棵子树之上。这是"在一行 HUD 上盖一层
  而不影响 GameArea"能成立的原因。⚠️ `_bench_dim_*` 用的是 `RootWidget.canvas.after` ——
  它会盖住 GameArea 里的**中奖大字**, 所以**不能**拿它做 HUD 压暗("大字留上方"是用户定案的)。

- `WinPileFX` 是 `GameArea` 的子控件(_restack_overlays 每帧重画的次序保证它是
  「板面 < 杯子 < 中奖大字」三层里的中间层)。帧推进由 `GameArea.tick_draw()` 调,
  **自己不持有任何 Clock** —— 切后台回来动画直接跳终态而不是卡住(busy 立刻转 False)。
- 运行期零物理: 球堆由 `pile3d.build_pile` 在生成期一次算完, 运行期只做斜投影 + 纯时基插值。
- **绝不软锁**: `busy()` 有 `FX_MAX_SEC=9s` 硬兜底, `play_win` 全程 try/except。
  输入锁一旦卡住玩家只能杀进程, 这是本模块唯一能出线上事故的地方。
- 时序(实测): ×2 2.56s / ×5 3.15s / ×20 4.33s / ×100 4.75s。含 0.5s 起播延迟与 hold_for(m)+0.25s 尾巴。
- **两个收尾时刻必须分开**(`_make_balls()` 里一起算出来):
  - `_last_touch` = 最后一颗球**第一次触地** → **揭晓基准**(揭晓 = 它 + `REVEAL_DELAY`)。
  - `_last_settle` = 最后一颗球**回弹停住** → **退场计时起点**, 这一刻杯子才装满静止。
  ⚠️ 曾经把这俩合成一个, 结果退场从"最后一颗刚触地"就计时 —— 球还在弹杯子就在淡出,
  玩家报"落进容器后消失得太快, 没有回味"。两者实测相差 **0.185~0.323s**(均值 0.254)。
- **揭晓 = `_last_touch + REVEAL_DELAY`(现 0.4s, 用户定案)**。语义是"让最后几下落地的声音
  先播完, 不被中奖琶音盖掉" —— 中奖琶音是全库最响的音(-1.4dBFS、带混响、最长 1.8s),
  原来挂在触地当刻, 一进来就把最后两三下落地声全盖住(玩家: "我甚至听不到弹珠落地的声音")。
  最后一颗球的**末声**在 `触地 + t1`(`t1 ∈ [0.13, 0.20]`), 所以 0.3 就够, 0.4 是留余量。
  `fx_probe [5]` 有两条门禁钉住:"揭晓 == 触地 + REVEAL_DELAY" 与 "揭晓晚于末声"。
- ⚠️⚠️ **揭晓判定绝不能挂在某个 mode 分支里**(2026-09-10 严重回归, 86% 的中奖局丢音)。
  完整血泪见 `WinPileFX.tick()` 末尾与 `_pump_reveal()` 的注释, 这里是结论:
  - 揭晓时刻(触地+0.4)通常**晚于** `win→result` 的切换点(`_last_settle`, 触地+0.185~0.323)。
    把判定写在 `if self.mode == "win"` 里 ⇒ mode 先跳走 ⇒ **这个一次性事件再也判不到**。
  - 后果不是"少一个音": `_on_done` 一路挂到**下一局**才被 `play_win` 的重入保护放出来 ——
    上一局的数字/语音画在新一局开头, 而新一局自己的大字被 `_reveal_done` 吞掉(258/300 局);
    **"中奖后不再发射"的局没有下一局 ⇒ 中奖音永久丢失**(这才是玩家说的"完全不播放")。
  - `busy()` 第一行对 `idle` 短路 ⇒ 9s 硬兜底**救不了**这条路。
  - **正确形状**: 所有 mode 迁移走完, 末尾统一调 `_pump_reveal(now)` —— 只认绝对时刻 +
    `_done_fired` 单向闩, **与 mode 解耦**。`not self._balls` 那条是**排空**分支
    (result→idle 那帧先清空 `_balls`, 紧接着走到这里), 保证欠的账必被放掉。
  - **幂等闸只能有一处真源**: 现在在 `settle()` 的 `_on_settled` 里, **同时盖住语音**
    (原来 `_reveal_win` 有闸而语音裸奔); 另有 `_win_seq` 序号丢弃上一局的迟到回调;
    兜底 deadline 与主路径**共用同一个闭包** `_settle_cb`(兜底自己另写一份 ⇒ 那份没有语音)。
    `_reveal_win` 内部**不再**放闸 —— 两处各判同一个东西必然静默脱钩(`hold_for` 已踩过一次)。
- ⚠️ **门禁盲区** `fx_probe [10]`: 一次性事件的 bug 要用"**投递语义**"门禁抓, 不是数值门禁。
  `[1]~[9]` 全在测值域(公式/区间/时长上界), 全绿也照样 86% 丢音。`[10]` 用受控时钟逐帧驱动
  真 `tick()`, 断言"每局恰好放一次 / 3 帧内 / 收尾不留挂账"。**改 `tick()` 或 `settle()` 的
  揭晓相关逻辑后必跑它。**
  ⚠️ `tick()` 的 result 段**也在调 `_advance_balls()`** —— 不推进的话 `_ball_screen` 对未落定球
  是按 tt 插值的, 它们会**冻在半空**而不是落到堆里。
  ⚠️ `_advance_balls()` 里的落定判据必须是 `t >= b["end"]`(**绝对时刻**), 不能写成 `tt >= b["end"]` ——
  `end` 本身是 `t0 + f + t1 + t2` 算出来的绝对时刻, 拿减过 t0 的 `tt` 去比会永远比不到, 球永远落不定(会停在半空)。
- **进场/退场是分层动画，不是一根透明度**（`WinPileFX._layers()` 返回 `(压暗alpha, 道具alpha, 缩放k, 位移dy)`）：
  - 进场错峰：压暗 0.30 起、杯子 0.32 起（0.18s），0.50 落位正好接上雨钟；杯子从**上方 +56 逻辑px** 降入、0.92→1.0 放大。
    位移 22→56 是因为实测玻璃贴图极透（后层 alpha 上限 81/255、前层 147/255），**透明度这条通道传不了信息**，22px 只有杯高的 7%、读不出来。
  - **装满后的静止时长(回味)按倍率分档**(用户定案)。真源**只有 `hold_for(m)` 一个函数**：
    `HOLD_TIERS=(2,3,5,10,20,50,100)` + `HOLD_BASE=0.6` + `HOLD_STEP=0.1`
    => **x2 0.6 / x3 0.7 / x5 0.8 / x10 0.9 / x20 1.0 / x50 1.1 / x100 1.2**（秒）。
    为什么按**档位**线性而不是按倍率线性：倍率逐档约翻倍，按档位 +0.1 等价于按 log(倍率) 线性 ——
    行业里「庆祝时长随奖额变化」是写进专利的标准做法（US20070010315A1：10 分→2s、100 分→10s）。
    为什么地板是 0.6：x2 占中奖场次 **55%**（x2+x3 占 80%），而 0.35→0.40 这种 +0.05s 低于可辨差
    （~10~15%），**改了等于没改** —— 玩家为此回来过两次。整场庆祝仍短于打一发的飞行（中位 4.24s）。
  - 退场：静止 `hold_for(m)` 秒 → **透明度走线性**（每帧均匀掉 ~1/15）+ **位移走二次缓出**（上浮 52px、缩到 0.90），`RESULT_FADE=0.25`（**恒定，不随档位变** —— 离开的手感要一致）；压暗层早 0.04s 撤（"灯先亮回来、道具后撤走"）。
    ⚠️ **退场的透明度绝不能再改回三次缓入**（原来 `a=1-((td-0.20)/0.25)**3`）：那个曲线到窗口 79% 处才走完一半，实测 td=0.30 时 alpha 还有 0.936、只上浮 2.4px，最后 0.15s 从 0.90 崩到 0 —— 观感是"杵着不动 → 啪一下没了"，就是用户报的"突然消失"。位移也别改回缓入：规范的"退场加速"前提是元素全程可见，这里 alpha 同步在掉，**感知优先于语义**。
  - **整场雨前移**（`RAIN_ANCHOR=0.12` / `RAIN_SHIFT_MAX=0.55`，在 `_make_balls()` 末尾）：杯子 0.50 就位，而球要 `t0+f_enter` 才被画出来（老实现实测 0.94~1.05），中间 0.44~0.55s 屏幕上一个像素都不动 → 玩家读成"演出结束了"。按**本局实测的最早入画时刻**整场平移（不能钉死，每局在 0.29~0.86 之间波动），一帧画面都不改。别用调 `RAIN_HEADROOM`/`FALL_GRAVITY` 来达到同样目的——那两个在动画面（雨点会变成整齐球阵 / 从"飘"改成"砸"）。
  - ⚠️ **停留时长只能有一处真源：`hold_for(m)`**。`expected_sec` / `tick()` 的解锁判据 / `_layers()` 退场分支(3 处) / `ui` 的 `_reveal_deadline` / `fx_probe` 全从它取值 —— **别在别处写死任何秒数**（`_reveal_deadline` 曾经硬编码 0.45，尾巴从 0.45 改到 0.60 时**静默**脱钩，兜底 deadline 提前触发导致数字/语音提前剧透）。
  - ⚠️ `_layers()` 读的是实例字段 `self._hold`（`play_win` 里按倍率设好）；而 `fx_probe` 走 `WinPileFX.__new__` 绕开 `play_win`，所以 `__init__` **必须**给 `self._hold` 一个默认值，否则探针直接炸。
  - ⚠️ **位移只能朝上**：杯底在逻辑 578，槽区隔板顶在 606。进场起手 +56、退场终态 +52，最高处杯底 522，都还在隔板顶之上（`fx_probe` 有断言）。**往下位移超过 28 就压住倍率槽**。
  - ⚠️ **`ENTER_CUP_AT + ENTER_CUP_DUR <= WINDUP`**：`tick()` 在 `now >= _t0` 处硬切到 win 分支并返回常量 `(1,1,1,0)`，进场曲线没跑完会被当场截断成瞬移。现在是 0.32+0.18 = 0.50，严丝合缝。
  - ⚠️ **缩放必须 bw/bh 同比例**（`_map` 的 x 走 `_abw`、y 走 `_abh`、球半径走 `_abw`），只缩一个球会变椭圆或从杯口冒出去。动画只写 `_ab*/` 临时字段，**绝不写回 `_bx/_by`**。
  - ⚠️ `_layers()` **必须按 `mode` 分支**：`_settled_at` 初值是 0.0，退场曲线不看 mode 会算出天文数字 → clamp 成 1 → 整场装杯按"退场终态"渲染。
    （`fx_probe` 的 [6] 现在专门管这段曲线：单调性 / 解锁那刻归零 / 进场收在 (1,1,1,0) / AT+DUR<=WINDUP / 几何红线。以前这块是**零自动覆盖**，改坏了完全静默。）
  - ⚠️ `tick()` 的 pending 分支**不能有 `if self._dirty` 掐帧**，否则错峰动画整段只画一帧。
- 球纹理 d=128 纯 Python 合成, 由 `prebake_step` 启动后分帧预烘(当前投注档优先);
  球堆缓存 LRU 上限 12。

## 验证标准

RTP≈档位±0.05、卡死=0、撞钉音>90%、哑火零泄漏、
**弧面接触率≈100%**、冲顶x跨度≥20px、首钉75~110帧、转向每发都有、
首钉前无碰撞段折角≤4°/相邻帧差≤1°、音效体检0异常。
**回弹感硬门禁**(玩家可见口径, 不用"1px"糊弄): 像素弹高≥10px 占比≥25%(实测26.9%)、≥20px≥5%(实测6.1%)、max≤45px。
**悬念率哨兵**(软目标): 末段变向≥15° 或 落格≠越行槽 占比≥35%(实测 100%)、混沌帽(落格偏离入口≥5槽)≤10%(实测 1%)。
**下落节奏门禁**(专家组标定口径: power=0.8 + 固定种子): 碰钉减速比 0.45~0.80
(防黏滞<0.45/防穿阵>0.80) + 滞留帧≤30/发 + 行穿行≥0.15s。
**横向稳定性**: 底部区域 vx 从<80 突增>280 的次数=0(防"凭空横向移动"反物理)。
**压暗覆盖与绘制次序**(`fx_probe [11]`, 2026-09-11 加): 两块压暗矩形是否**恰好盖满"整块屏幕
减去游戏区"**(零重叠、零缺口) / 视口变宽时是否跟着铺满 / `_sync_hud_dim` 排在 `tick_draw()` **之后** /
`dim_alpha` 的 idle 归零与"演出结束那帧已归零" / **绘制次序 后层玻璃 < 压暗 < 杯口环补画 < 弹珠 <
前层玻璃**(遍历真 `_redraw()` 的指令表定位, 不是文本匹配)。
⚠️ 接线断言必须用**几何**判据, 别用"数某个调用出现几次" —— 实测把 `self._row_dim(top)` 复制五遍
照样数出 5; 也别只比对 back/dim/front 三个下标: 夹具里不放球的话, 把压暗挪到"已落定球之后"
(球沉进背景)照样全绿。这两个盲区都是专家用变异体打出来的。
⚠️ 文本断言一律读 **`android/main.py`**(出货那个文件), 不是 `tools/` 下的源 —— 生成器没重跑时
查源会给出包假绿灯(`--check` 管同步, 是另一条命令)。
⚠️ 这一节存在的理由和 `[10]` 一样: `[6]` 只测 `_layers()` 的**数值曲线**, 而上述三件事
全在曲线之外 —— 曲线全绿, 画面照样是"板面黑了、两边还亮"或"杯内被玻璃提亮回去"。
⚠️ 写这类门禁要**做阴性对照**(把改动改回坏的, 确认它真的会红)。实测踩过:
`dim_alpha` 的 idle 守卫那条, 夹具用 `_settled_at=0.0` 时**删掉守卫也不会红**
(退场曲线恰好也收到 0) —— 夹具必须给"刚过去的时刻"才有区分力。
**落珠震动**(`fx_probe [12]`, 2026-09-11 加): 装杯期每一次落地音都必须对应**恰好一次**震动, 且
**时刻逐个相同**(逐次比对, 不是只数个数), 两次震动间隔不小于 `BOUNCE_THROTTLE`。
GUI 真发验证: 落格==结算槽 0 穿帮。
## 已知陷阱（改代码时注意）

- **`start_charge()` 必须立即调 `_set_controls_enabled(False)`**：否则充电窗口(0.5~1.0s)内 bet 按钮可点，多点触控可切下注额导致余额变负。`launch()` 才禁用为时已晚。
- **哑火分支的累加器必须与飞行分支同构**：`_misfire_frames` 递增和 `advance_misfire()` 都必须在 `while self._accumulator >= FIXED_DT` 循环**内部**，每物理步+1 而非每渲染帧+1。否则 120Hz 上超时误杀、30Hz 上动画变慢。
- **stall 位移检测必须在累加器循环内每步做**：循环外看净位移会漏检（多步子步位移矢量抵消）。帧计数不用墙钟，与 selftest 一致。
- **预演/真发确定性 = 同种子各自新建 rng，不是共享 rng 实例**：共享实例串行时，预演消耗后 rng 状态已变，真发轨迹不同。同种子(launch_ball(power, random.Random(seed)))逐帧一致。
- **弧面延长只能沿出口切线方向（大半径微弯）**：向左弯会接近球抛体路径、产生二次接触，向右弯成钩子。改弧面形状必须重跑接触率/二次接触门禁。
  ⚠️ 这条原注释里的具体角度(`25°`、与抛体路径夹角 `12.8°`)是**按旧的 ARC_OUT_ANGLE=25° 算的**,
  而该常量**已改成 35°**(见 `ARC_OUT_ANGLE` 处注释: 25°→35° 修复落格偏置)—— 那几个角度**需要按 35° 重新推导**,
  不要照抄旧值。
- **弧面碰撞半径必须=视觉半径(ARC_VISUAL=1.4)**：球渲染 12.6 比碰撞 9 大，弧面不用视觉半径会看到球"嵌进"弧面。
- **修订注入必须在累加器循环内逐物理帧消费 events(清位), 不能等 tick 末 _play_events**：events 是位或累积, 一次 tick 推进多物理帧时残留位会让撞钉计数虚高, 注入点错位 → 落格偏差(实测 35%)。正确做法: 循环内每帧 `tick_ev |= b.events` 收集给音效 + 计数/注入 + `b.events=0; b.amp.clear()`(与预演 `_sim_flight` 同构)。(注: 修订机制已删, 此条为历史教训, 事件消费约定仍有效)
- **plinko.py 与 main.py 架构不同**：plinko.py 用 `tkinter.after(FIXED_DT)` 定时间隔（无累加器），改物理/引导逻辑时两边要分别评估是否受影响。改物理/音效/selftest → 改 plinko.py 再重跑生成器; 改 Android GUI(每轮/弹簧/弧面绘制) → 改 tools/android_part_ui.py(不经过 plinko.py)。两版 GUI 差异(PC 无防沉迷、Android 有)是设计使然, 不是漂移。
- **`GameArea._redraw()` 的 `canvas.clear()` 会把子控件的 canvas 一起摘掉**（叠加层"指令都在、屏幕没有"，不报错）。每次重画板面后必须 `_restack_overlays()` 把中奖杯与大字的 canvas 按序重新 add 回末尾。判据: `canvas.indexof(child.canvas) == -1`。详见 BUILD_APK.md §3.24。
- **Kivy 子控件的 canvas 是绝对(窗口)坐标，父级不做平移**：子控件里画东西要用 `self.x/self.y + 偏移`，不要按"父级局部帧"减父容器坐标。详见 §3.25。
- **`canvas.after` 的绘制层级（实测语义，决定了"能不能盖住谁"）**：`child.canvas.after` 画在该子控件**自己的 canvas 与它全部子控件之上**；`parent.canvas.after` 画在**整棵子树之上**。所以
  - 要在"一行 HUD 上盖一层、又别碰游戏区" → 挂**那一行自己的** `canvas.after`（`_row_dim` 这么干的）；坐标沿用 `_row_bg` 的 `pos=row.pos`，同一种写法同一个坐标系，不引入新假设。
  - ⚠️ `canvas.after` 里的矩形**必须在子控件都加完之后**再建，别依赖"Kivy 会把它插到 after 组之前"的次序细节。
  - ⚠️ 别用 `RootWidget.canvas.after` 做全屏压暗 —— 它盖得住五行，**也盖得住 GameArea 里的中奖大字**。
- **生成器不做任何去重，同名模块级变量静默覆盖**：新分片的名字一律加前缀。实踩: `_BALL_TEX` 撞上 ui 段的同名变量被重置成 `None` → 首次中奖 AttributeError。详见 §3.26。
- **测中奖先切 360% 档**：80% 档的盘面抽不出 x50/x100，用固定盘面测会漏掉高倍率（颗数最多、锁最久、最易掉帧）的真实场景。
- **彩蛋（弹珠返回发射槽）走 `_easter_hold` 全套锁**：它不设 `_land_hold`，不锁的话 `park_ball` 会在 `landed_at+0.6s` 就跑掉——重掷盘面、state 回 `ready`、按钮恢复，然后装杯才播。锁从 `settle()` 一路持续到**弹窗关掉**（`_easter_finish`）。
  - **顺序：先播 ×2 装杯 → 全部落定 → 弹对话框**（`_on_easter_settled`）→ 玩家点确定 → 解锁。`_on_easter_settled` 有 `_easter_hold` 守卫：玩家若中途按重置又发了一发（`launch` 会清锁），就不再弹窗盖在新一局上。
  - 这条路径**不设状态栏文字**（结果全交给弹窗说）；播报就一条：语音档播 `voice_easter_{1,10,50,100}`（按投注档穷举），否则播 ×2 轻赢琶音。
- **装杯的落地音只靠节流限速，不按颗数截断**（`BOUNCE_THROTTLE=0.10`）。曾有个 `BOUNCE_BUDGET=24` 的"只前 N 颗播"上限，是照搬"SoundPool 8 条流会被 coin 抢"的估计加的 —— 但 coin 早已挪到揭晓之后也不在装杯期响，而 `bounce` 音只有 0.11s，10/秒的重叠也就 1~2 条流。实测代价：×20 后半段静音 1.4s、×50 静音 1.8s、×100 静音 2.3s（弹珠还在落、声音没了）。
  ⚠️ **同一个教训适用于震动**：`_vibrate_tick` 也**不设次数上限**，10 次/秒 × 2~4 秒照震。
- **落珠震动与落地音共用同一次节流判定**（2026-09-11 接的玩家需求："弹珠落入容器的时候设备应该震动，震动和落地音效同步"）。实现在 `WinPileFX._bounce`：**挂在 `sfx.play(...)` 的返回值上**，`if play(...): _vibrate_tick(gain)`。
  ⚠️ **绝不能让震动自己再判一次时间/自己计数** —— 两处各判必然漂移，玩家会"听到响但手上没感觉"（或反过来），而那正是"同步"要消灭的。要改成独立开关的话，把节流从 `Sfx.play` 挪到 `_bounce`，仍然只能有一处。
  ⚠️ 副作用（有意为之）：`音效已关` 时 `play` 返回 False ⇒ **也不震**。本作只有一个反馈开关，关了声音的人多半也不想在会议里被震四秒。想解耦就改上面那条。
  ⚠️ 参数刻意做得**又短又不满**：10~18ms / 振幅 80~220（随撞击强弱走）。脉冲一长就糊成"持续嗡嗡"而不是"珠子一颗颗落进杯子"；振幅不满格是因为这是"雨点"不是"大奖震一下"（后者是 settle 那次 45~300ms 的长震）。手机马达启动时间约 10~20ms，所以 10ms 是下限。
  ⚠️ 只在 Android 生效（`platform != "android"` 直接 return），桌面 `--smoke`/`--selftest` 都测不到真震动，只能靠 `fx_probe [12]` 钉住"调用的次数与时刻"。
- **中奖杯尺寸改 `CUP_W` 时先量 PNG 的内容边界**：玻璃图 **1600 宽**里真实内容只占 x 48~1552（= design 800 里的 24~776），所以杯宽最多能到 553（内容刚好贴满 520 的画布）。底缘用 `CUP_BOTTOM` 锚定（不是杯顶），这样改杯高只往上长，不会压到倍率槽。
  ⚠️ 贴图是 **1600×920（design 的 2 倍）**，2026-09-11 从 800×460 提上来的：玩家平板上杯子的屏幕宽是 1612px，1× 贴图等于放大 2.02× 采样、杯壁的结构边/高光发糊（玩家报的"模糊"）。生成器 `wingui/assets/generate_glass_tumbler.py` 本来就是 `SCALE=8` 超采样到 6400×3680，**提分辨率只改了 `_save()` 的 `OUT` 降采样目标，画法一行没动**。几何仍是 design 800×460（`pile3d`/`_map` 与贴图边长无关），所以改尺寸不用碰任何布局；但 `fx_probe [2]` 钉了 `(1600, 920)`，换回 1× 会当场变红。
  ⚠️ **别再往"提高弹珠纹理 d"上想**：两轮专家各自实测，`d=128→256` 的收益是 **中位差 1/255、均方根 2.21/255**（≈0），代价确定（纯 Python 合成耗时 ×4）。球贴图的边缘羽化写的是**半径百分比**（`dist<=0.97` 后 3%），软边在屏幕上的宽度**与纹理边长无关**。
- **揭晓合流：中奖的数字/余额/语音一起在"最后一颗球**第一次触地**"那一刻给**（`_reveal_win`；判据见上面的 `_last_touch`）。t=0 只落账务 + 槽闪 + 灯 + 顶栏中间态（`命中 xN · 结算中…`）。以前大字 t=0 就报、语音等装杯播完才念，中间隔一整场（×2≈2.0s、×100≈3.7s），数字等于提前剧透。
  - **余额必须真冻结**：只把三行赋值搬到 `_reveal_win` 是不够的 —— `_anim_start_time` 还是旧值，`elapsed >= _anim_dur` 会走 else 分支 0.1 秒内追平。要靠 `_anim_pending` 让 `_frame` 整段跳过。
  - `elapsed` 要 `max(0.0, ...)` clamp：负值时噪声项 `(1-t)` > 1 反而放大，会抖出一帧偏移。
  - **`on_done` 里必须同时调 `_reveal_win` 和 `_play_win_voice`**（顺序：先立大字再响）。只挂语音的话揭晓会靠 deadline 兜底才出来，两者差 0.2s。
  - 兜底 deadline = `settle + expected_sec(m) - (hold_for(m) + RESULT_FADE)`。`expected_sec` 现在**直接读本局真实球数据**（`_last_settle + 0.05`），所以是严格上界。
    ⚠️ 老实现按 `FALL_MAX` 估，注释写着"实测比真实 T 高 0.55~0.80s，所以当兜底是安全的"——**那句是错的**：每颗球的下落时长带 `×u(0.88,1.12)` 随机，可以超过 `FALL_MAX`。实测 2000 局/档，x100 有 3.9%、x50 有 1.6% 的局里估得比真实**短**，兜底会提前触发 → 大字/余额/语音在最后一颗球落定前冒出来（正是当初把揭晓挪到 T 想消灭的"数字提前剧透"）。`fx_probe` 的 [5] 现在是单向门禁：只许偏高，不许偏低。
  - 大字生命 `BIG_TEXT_LIFE=1.8s`（原 3.0s）—— 揭晓在 `_last_touch`，而解锁在它之后约 0.6~0.9s，3.0s 会飘进下一局的蓄力期，且 `TEXT_CY_WIN=150` 恰等于 `PEG_TOP=150`。
- **槽位指示灯是「中了就绿 / 未中红」，不按倍率取色**（与 PC 版同一套）。按倍率取色会有两个毛病：`slot_color(5)` 恰好 `== COL_FIRE`（x5 中奖和未中一模一样），而且倍率槽色块本来就是 `slot_color(该格倍率)`，灯再上同色是重复信息。

## git commit 注意

本仓库的 shell 环境是 bash。**不要用 PowerShell 的 here-string 语法 `@'...'@`** 来写多行 commit message——bash 会把 `@` 当作文本内容，导致 GitHub 提交记录只显示一个 `@`。正确做法：

```
git commit -m "第一行标题" -m "第二行正文"   # 多行最简单
# 或
git commit -m "$(cat <<'EOF'
标题行
正文行
EOF
)"
```
