# 跑分波动优化记录（2026-09-15）

## 现象与判断

v0.7.67 的物理跑分阶段已经把实际渲染稳定压在约 57fps：120/120 为 57.5fps，60/60 为 56.5fps。因此两组原始分数中位数仅相差 0.9%（16740 / 16592），帧率配置本身不再是当前波动的主因。

但 120/120 的五轮为 `22264 / 16741 / 21566 / 16197 / 16640`，范围 6067（相对中位数 36.2%）；60/60 为 `17699 / 16296 / 17611 / 16332 / 16592`，范围 1403（8.5%）。前者仍然不能视为稳定的性能测量。

日志已有两项直接证据表明问题落在跑分线程的执行位置与设备状态，而不是球路或显示帧率：

- 五轮均使用相同随机种子和输入；
- 120/120 的纯算术探针从 35.72M 降至 22.77M（1.58 倍），说明同一工作线程实际拿到的核心性能发生了明显变化。

现有 `sched_getaffinity` 只证明线程“允许”使用 cpu0~7，不能证明每一时刻实际跑在哪个核；同时本机无法读取 `cpuinfo_cur_freq`，也不能用 sysfs 的请求频率代替真实频率。

## 本次优化

物理跑分开始前，读取当前线程允许使用的 CPU 集合，并从其中按 `cpuinfo_max_freq`（不可读时回退 `scaling_max_freq`）识别最高性能簇。仅在确认设备存在异构频率簇时，将**跑分工作线程**收窄到该最高性能簇；跑分结束或异常时恢复原亲和性。

这样不会改变正常游戏的调度策略，只消除跑分线程在性能核与较慢核心之间迁移这一可控变量。实现必须：

1. 无权限、sysfs 不可读、单一频率簇时安全降级，不强行锁核；
2. 把“是否锁定、原集合、目标集合、频率依据/失败原因”写进帧率日志，避免把请求当成事实；
3. 与现有 60fps 强制锁及其 `finally` 恢复路径并列，异常时不残留线程状态。

## 预期与验证

预期是缩小同一轮五个样本的离散度，尤其消除性能核/中核迁移造成的低谷；它不能消除温控、后台任务和内存带宽竞争，因此不承诺固定数值提升。

真机验收应各跑至少三次 120/120 与 60/60：确认日志显示“已锁定”，两组跑分期实际渲染均在 56~58fps，并比较每次五轮的 `(最大-最小)/中位数`。若锁核失败或波动仍主要伴随分配探针下降，应改查后台内存带宽竞争，而不是继续调整帧率。
