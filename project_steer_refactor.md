# 弹珠机 steer 重构进度(2026-08-03, 修订机制重写完成)

## 三个问题的修复状态

### 1. 发射失败后重新发射(已完成)
`relaunch_stalled` 全部删除(PC+Android+selftest), UI stall 卡死改为"踢球"
(沿接触法线向下踢, vy=max(|vy|+80,400)+水平扰动150, 最多 STALL_MAX_RETRY 次,
仍不动强制结算)。文件: plinko.py + tools/android_part_ui.py。

### 2. 平飞/横向跳(已完成)
根因 = 球碰钉反弹后 vx 大 + 行间横穿。修复:
- 反射横速限幅(PEG_REFLECT_VX_MAX=300)
- 碰钉后禁止向上(vy<0→0)
- 保底下落(PEG_BOUNCE_VY_MIN=200)
代价: 修订前命中率从 45.2%(VY_MIN=80 时)降到 ~44% —— 已被修订机制重写消化(见下)。

### 3. 钉子吸引力(已完成)
steer_ball 删除场内每帧拉拽(飞行中零干预, 无碰撞段 vx 变化=0 实测),
引导改为碰钉时一次性(PEG_STEER_K 系数)。near-miss 诱饵(TEASE)已全部移除。

## 修订机制重写(2026-08-03 完成, 替代"重掷盘面")

**问题**: 旧修订 = 落格≠预定槽 → 重掷盘面换种子重预演(≤10次)。副作用:
① 发射瞬间盘面倍率变了(玩家看到的不是投注时那张盘); ② 命中率门禁(>45%)依赖
碰钉引导原始命中率, VY_MIN=200 后 44.0% 卡线红。

**新机制** (`solve_landing`, plinko.py + android/main.py 同步):
1. 同种子预演, 落格==预定槽 → 直接发
2. 落格≠预定槽 → 在**最后碰撞点**(570 排钉, 距槽口 46px)注入 dvx(二分 ±800,
   注入点前碰撞序列与预演逐帧一致, 只改最后一段无钉区出射速度)
3. 仍失败 → 换种子重来 —— **盘面/倍率一律不动**
4. 真发: launch_ball(power, Random(seed), tx), `_frame` 累加器循环内计数撞钉,
   第 k 次碰撞帧末 `b.vx += dvx`

实测: 修订成功率 100%(300/300 selftest 门禁; 500 发重放 0 失败; GUI 真发 20 发 0 穿帮),
平均预演 ~5 次, 盘面零改动。修订前命中率 44% 不再影响玩家体验(修订不挑落格),
门禁降为宽松哨兵(>35%)。

**门禁改造**: (2b') 段从"修订前命中率>45%"改为"修订求解成功率>=99.5% + 平均预演<=7 +
盘面不动(代码保证)"。

## 当前参数(plinko.py, 与 android/main.py 生成物同步, 2026-08-03 下落节奏调整后)

| 参数 | 值 | 说明 |
|------|-----|------|
| G | 1000.0 | 重力(1200→1000: 恢复弹珠机节奏) |
| E_SLOW / E_FAST | 0.55 / 0.25 | 碰钉恢复系数(0.35→0.55: 碰钉反弹明显=弹跳感) |
| PEG_BOUNCE_VY_MIN | 100.0 | 碰钉后保底下落(200→100: 允许自然减速, 防滑翔) |
| LAUNCH_MIN/MAX | 1077/1114 | 发射速度(随 G 回调, apex 57/17 不变) |
| MISFIRE_V_MIN/MAX | 475/935 | 哑火(apex 524/200, 留余量) |
| PEG_STEER_K | 0.8 | 碰钉一次性引导系数 |
| PEG_STEER_MAX | 200.0 | 碰钉引导单次修正上限 |
| PEG_REFLECT_VX_MAX | 300.0 | 反射横速上限 |
| FLIGHT_ENV | 新 16 点 | G=1000 重测: 碰弧面 0.65s / 谷 0.95s / 首钉 1.33s |
| STEER_MIN/MAX/DVX_MAX/VX_MAX | — | 已删除(死代码清理完成) |

## 下落节奏修复(2026-08-03 下午, 用户投诉"下落加速极快")

**症状**: 球"嗖嗖"穿过钉阵 —— 行穿行 0.17s/行, 碰钉后 vy 中位被保底 200 推着,
碰钉前后 vy 比 p50=1.00(碰钉不减速), 40% 碰钉后反而更快。

**根因**: G=1200(高重力)+ E_SLOW=0.35(低恢复, 反弹小)+ PEG_BOUNCE_VY_MIN=200
(碰钉后 vy 保底 200, 抹平自然减速)—— 三重叠加让球在钉阵里单调加速。

**修复**(G=1000 / E_SLOW=0.55 / VY_MIN=100, LAUNCH/MISFIRE 随 G 联动, FLIGHT_ENV 重测):
- 行穿行 0.17→0.22s, 碰钉减速比 0.90→0.83, 碰钉后 vy p50 200→100
- 飞行音: FLIGHT_ENV 重测(碰弧面 0.65s / 谷 0.95s / 首钉 1.33s), FLIGHT_GRAIN_END 0.65
- 门禁: 首钉 55~75→70~90 帧, 转向 45~60→50~65 帧
- **新增"下落节奏门禁"**(防再犯): 行穿行 p50≥0.18s + 碰钉减速比 p50≤0.90
- 副作用: 修订前命中率 43.8%→49.1%(球慢, 碰钉引导更有效), 修订仍 100%, 卡死=0

## 实战教训(新增)

- **修订注入必须在累加器循环内逐物理帧消费 events**: events 位或累积,
  一次 tick 推多帧时残留位让撞钉计数虚高 → 注入点错位 → 落格偏差 35%(实测)。
  循环内每帧收集+清位, 与预演 _sim_flight 同构。
- **修订前命中率的物理极限**: 最后碰撞点距槽口仅 46px(~0.1s), dvx ±800 最多横移
  ~2 槽; 初落格与目标隔 ≥2 槽时单点注入无解(撞隔板反弹/够不着)—— 换种子解决。

## selftest 门禁(2026-08-03 双端全绿)

RTP≈档位±0.05、卡死=0、撞钉音>90%、天花板<10%、哑火零泄漏、弧面接触率≈100%、
冲顶x跨度≥20px、首钉55~75帧、转向每发都有、折角≤4°/相邻≤1°、
**修订求解 100%(300/300, 平均预演≤7)**, 修订前命中率>35% 哨兵, 音效体检0异常。

## 待办
- 真机/实机观感: 注入 dvx 后"碰最后一颗钉横移入槽"的视觉是否自然(±800 上限场景)
- android 仓库 commit + push(CLAUDE.md/main.py/project_steer_refactor.md,
  danzhu_mark.md 删除需确认)
- 父项目文档已更新(CLAUDE.md 架构/参数表/验证标准)

## 其他关键背景
- 弧面 = 右壁口部弧形导轨(根部R8弧+25°接触段+R400微弯弧), ARC_E=0.5,
  碰撞半径=视觉半径(ARC_VISUAL=1.4)
- 预发射修订: 同种子预演 + 定向修正(最后碰撞点注入 dvx)+ 换种子兜底, 盘面不动, 落格==结算槽
- main.py 是生成物: plinko.py(物理源) + tools/android_part_{head,backends,ui}.py,
  改完重跑生成器
- 历史教训见 histroy.md 第五/六轮
