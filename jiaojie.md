# 交接文档：真机 1%Low 偏低（性能优化）问题

> 生成：2026-09-14。供其他 AI 接手继续排查。
> 涉及文件：`android/main.py`（**出货文件**，在 git）、`android/buildozer.spec`（打包配置，在 git）、
> `tools/android_part_*.py`（源分片，**不在 git**）、`tools/fx_probe.py`（门禁探针，**不在 git**）、
> `android/temp/*.py`（排查用探针，不在 git）。
>
> 每条论断都标了来源：**【实测】**（作者跑了代码/看了面板数） / **【读码】**（读了源码） / **【推断】**。
> 请优先信前两类。**本文档不写没验过的结论。**

---

## 0. 一句话现状

**症状**：真机上 **1%Low 只有 31~35fps**，而中位帧间隔是 12.4ms（80fps）。

**已经做到什么**：把一记 26.8 毫秒的 GC 全量回收干掉了（现在是 0.0），把发声/震动/守卫/落盘
四条阻塞路径搬出了主线程，把中位从 7.8fps 拉到 40（那是 v0.6.65 的功劳）。

**现在卡在哪**：真机最慢那几帧的账**对不上** ——

```
38毫秒(待机·实算22.6·主线程20.4·自算9.5)|重掷9.2|板面0.5
38毫秒(飞行·实算24.6·主线程21.2·自算0.3)|板面0.3      ← 我们的 _frame 几乎没干活
34毫秒(装杯·实算20.1·主线程17.3·自算4.5)|板面4.5|字号0.6
```

帧基线 18.8ms、主线程**典型只烧 2.8ms** ⇒ 慢帧多烧约 **17.6ms 主线程 CPU**，其中：

| 来源 | 量级 | 状态 |
|---|---|---|
| `重掷`（`park_ball` 整块盘面重建） | **约 9ms** | **v0.6.91 已改成增量**（桌面 0.529→0.020ms） |
| 其余（Kivy 自己的渲染/GL，跑在 `_frame` **外面**） | **约 8.6ms** | ❌ **还没定位 —— 这是下一步** |

---

## 1. ⚠️ 红线（违反会出事，按严重度排）

### 1.1 **绝对不要跑 `tools/build_android_main.py`**（不带 `--check` 的那个）

它会把 `android/main.py` **整个重写成生成物**，而 `tools/` 与出货文件**自 2026-09-13 起已经分叉**：
`tools/` 里躺着**一批被玩家否掉的观感改动**（金属渐变墙 `wall_ramp`、玻璃 `over` 独立层、
钉子改名 `_peg_nodes`、刻面球 `_facet_bake`、`slot_texture`、mipmap、冰蓝闪光）。
跑一次生成器，这批量会**一起发出去**。

**2026-09-14 已经因此污染了 v0.6.84**（一个评审子代理跑了它，没加 `--check`），
后来 `git checkout <上一个干净提交> -- main.py` 回滚 + 重打改动才修好。

- 要跑只跑 `--check`（它只校验、不写）。
- **改 `main.py` 之后要手工同步到 `tools/` 对应分片**（否则将来重跑生成器会丢改动）。
- 动手前先备份：`cp main.py temp/main_backup_<说明>.py`。

### 1.2 版本号会撞车

APK 文件名含版本号 ⇒ **两个包同版本号会互相覆盖云构建产物**。
2026-09-14 就撞过一次（别人提交了 v0.6.88，我又提交了一版 0.6.88）。
**push 前先 `git log --oneline -3` 看最新版本号是什么。**

### 1.3 出包必须 bump `buildozer.spec` 的 `version`

那是"装的是哪个包"的唯一肉眼证据。

### 1.4 已知坏版本（别装）

| 版本 | 状态 |
|---|---|
| 0.6.82 | **坏**：跑分一按就崩（漏模块级定义）+ 面板静默变空（帧记录索引写反） |
| 0.6.84 | **污染**：带 9/13 那批被否的观感 |
| **0.6.91** | **当前最新、可用** |

---

## 2. 硬事实（真机跑分面板读数，**【实测】**）

设备：**联想 Y700 五代 TB323FU / Android 16**；另外用户在 **NCO-AL00 / Android 15** 模拟器上也跑。

### 2.1 帧率

| 量 | 值 |
|---|---|
| 中位帧间隔 | **12.4ms（80fps）** ← 注意这是"vsync 没咬住"的自由跑结果，不是上限设定的 |
| 1%Low | **31~35fps**（最差约 9 帧的均值） |
| 10%Low | 41~47 |
| p90 / p99 | 44~51 / 38~43 |
| 各阶段中位 | 飞行/装杯/落袋/蓄力/待机 **基本一样**（12.4~19.0，随上限设置变） |

⚠️ **`各阶段中位数一样` 的含义**：帧间隔与"我们画了多少东西"无关（待机约 123 条绘制指令、
装杯约 253 条）。**但注意各阶段帧数差两个量级**（飞行 ~500~700 帧 vs 落袋 ~16 帧），
帧数少的那几档中位数没有可比性 —— 面板 v0.6.86 起已带上帧数。

### 2.2 每帧 CPU 的三个数（**必须一起看**）

```
瓶颈: 大头在等 · 每帧实算 3.7 / 主线程 2.8（其中 _frame 0.5，最坏 9.5）/ 帧间隔 18.8 毫秒
```

- **实算** = `time.process_time()` 差 = **全进程** CPU（含发声/震动/守卫那几条工作线程）
- **主线程** = `time.thread_time()` 差 = **本线程**（Kivy 渲染 + 我们所有回调）
- **`_frame` 自算** = `perf_counter` 包住 `_frame` = **只有我们的帧函数**（**不含** Kivy 渲染、
  也**不含**用 Clock 延后的那些回调 —— 这一格是**半截的**，别单看它）

**分流规则**：
| 现象 | 结论 |
|---|---|
| 实算大 · 主线程小 | 工作线程在忙 |
| **主线程大 · `_frame` 小** | **Kivy 渲染 / 延迟文字重排 在吃**（慢帧就是这一档） |
| `_frame` 大 | 我们自己的代码 |

### 2.3 其他实测

- **GC**：已 `gc.freeze()`（启动预热跑完后冻结 ~5.9 万常驻对象）。冻结前后各强制一次全量回收：
  **2.8~7.2ms → 0.0ms**。窗口内 GC 总量 8~11ms，最坏一次 3~5ms（gen-1）。改造前是 39ms / 最坏 26.8ms（gen-2）。
- **内核态占比 6~20%**（`/proc/self/stat` 的 utime/stime）⇒ **JNI/Binder/文件写那一族基本可以排除**。
- **`文字重排` 33.4 → 8.8 次/秒**（淡出修复之后）。
- **`节拍` 比值 ≈ 1.00**（`_frame` 调用次数 / 采样帧数）⇒ **没有"白送的呈现"**。
- **`慢帧` 那一行**：v0.6.86 起门槛自适应（`max(25ms, 2×中位)`）。**在那之前门槛硬编码 90ms，
  而真机最慢帧只有 40ms ⇒ 那四行永远不打印**（一个专抓卡顿的面板把最该看的东西静默掉了）。
- **屏幕刷新率读数本身会变**：同一台机器，0.6.89 那份面板读到 **144Hz**、0.6.90 读到 **165Hz**。

---

## 3. 已验证的机制（都能复现，改动前先读这几条）

### 3.1 ⭐ Kivy 把颜色**烘进**字形纹理 ⇒ 淡出/变色一律走"画布那条 Color"

`Label` 画布里是一条 `Color`（**恒为 (1,1,1,1)**）+ `BindTexture` + `Rectangle`。
写 `label.color` 会触发 `_trigger_texture_update`（Kivy 用 Clock **延后**执行）
= **重测字形 + 重光栅化整段文字 + 重建纹理 + 上传**。

**【实测】**（`temp/canvascolor_probe.py`）：
```
写 label.color 一拍内 texture_update: 1 次
写画布那条 Color 的 alpha:            0 次
截图确认像素真的变淡了（+200 的金色确实压暗）
```
**【实测】**（`temp/fade_verify_probe.py`）：中奖大字淡出 **40 帧期间 texture_update 增量 = 0**，
画布 alpha 有 20 个不同值且单调不增。

⇒ **v0.6.88 起**：中奖大字与飘字的淡出都改写画布那条 `Color` 的 rgba（拿不到就退回老路）。
**因为不花钱了，改成每帧写，淡出更顺**（不再是 20 档台阶）。
⚠️ 加**新**的文字淡出/变色效果时，照这个模式写，别写 `label.color`。

### 3.2 ⭐ 帧率上限**不能随便设**（cap 必须是 vsync 周期的整数分之一）

**【实测】**（`temp/fpscap_measure.py`，同一台桌面机 + 同一个 60Hz 屏）：

| 上限 | 中位帧间隔 | 中位帧率 |
|---|---|---|
| **0（不设）** | **16.59 ms** | **60.3** |
| 60 | 16.65 ms | 60.1 |
| **120** | **19.33 ms** | **51.7** ← **比不设还慢** |
| 240 | 18.14 ms | 55.1 |

**机制**：Kivy 的 `Clock._max_fps` 让主循环**睡到 1/cap 秒**。cap=120 ⇒ 睡 8.33ms，
而 vsync 一格是 16.67ms（60Hz）/ **6.06ms（165Hz）** —— **睡过了一格** ⇒ 每帧都错过、滑到下一格。

**结论**：`FPS_CAP_MAX = 0`（不设上限），Kivy 不睡，循环由 vsync 自己钉住。
**以后再想"加个上限"之前，先回答"这个数能不能整除屏幕的 vsync 周期"；不能整除就别加。**
⚠️ 屏幕刷新率**只用于面板显示**（`_FPS_INFO[0]`），**不参与任何计算**（它的读数会变，见 §2.3）。

### 3.3 ⭐ 字体：碰一个新字号 = 重开一次 TTF 字形表（桌面 26ms）

- `fit_font_size` 是**阶梯 + 二分**，会去试好几个字号；**每一档都是一个新的精确字号**，
  而 Kivy 的字体缓存**按精确字号索引**。
- 中奖大字的文字是 `+N`，位数一多第一档就放不下 ⇒ 必然往下走 ⇒
  **`big_result_text` 是在 `_frame` 里面跑的**（`tick_draw → win_fx.tick → _pump_reveal → _reveal_win`），
  所以那几档冷开**直接记在 `_frame` 头上**。
- ⇒ **预热必须覆盖整个阶梯**（v0.6.83 起：4 档 → **22 档**，`_FONT_WARM_SIZES`）。
- ⚠️ **别把字号量化到网格**（v0.6.80 试过，v0.6.84 撤回）：真机上 `sp(N)` 是 `N×密度` 的**非整数**，
  量化后与启动期预热/布局烘出来的字号**对不上** ⇒ 每次挑字号都冷开 ⇒
  **主线程变忙 ⇒ 物理 benchmark 那条线程拿到的 GIL 变少 ⇒ 纯 CPU 吞吐掉 6%**
  （玩家实测各 10 次：0.6.79 是 32000+ 步/秒，0.6.81 只有 30000+）。
  **桌面测不出这个副作用** —— 桌面上 `sp(48)` 正好 = 48.0，量化是空操作。

### 3.4 帧累加器必须有单帧上限

**【实测】**（`temp/accum_clamp_probe.py`）：真机观测过"单帧 dt 625ms → 一帧跑了 **37 个物理步**"。
现在 `MAX_STEPS_PER_FRAME = 4`（`_clamp_accum`），灌 625ms 只补 **4 步**（阴性对照：不钳位补 **37 步**）。
**代价是主动丢时间**（那一帧球走得比墙钟慢一点），这是刻意的取舍。

### 3.5 `park_ball` 重掷盘面：只需更新 9 个倍率槽

重掷时**几何一点没变**（钉子/隔板/墙/槽底/力条全在原位），变的只有 9 个槽的颜色与文字。

**【实测】**（`temp/slotincr_verify.py`）：
```
增量 _update_slots 中位 0.020 毫秒  vs  完整 _redraw() 0.529 毫秒   → 快 26 倍
24 轮里两者画出来的东西逐项相同（按可见状态比）
结构对不上时正确退回完整重画（阴性对照）
```
⚠️ `_redraw` 里现在给**全部 9 个槽**都建 Color+Rectangle（空的 `size=(0,0)` 藏起来）——
因为"哪几个槽有奖"每次重掷都会变（`roll_multipliers` 重新 `random.sample` 位置），
只给非空槽建的话条数会变，就没法原地更新。

### 3.6 其他已验证的

- **发声/震动/方向守卫/存档落盘**都在各自的工作线程上（主线程只 `put_nowait`）。
  `_vibrate_double`（彩蛋双震）原本是**同步 JNI**，v0.6.81 也搬走了。
- **系统栏重申**（`setSystemUiVisibility`）跑在 **Java UI 线程**，周期 0.7s → 2.5s。
  ⚠️ 它的计时块里**原来含两次 `autoclass`**（v0.6.87 才把 `_t0` 挪到后面）——
  在那之前面板上那个数是**在测探针自己的查表开销**。
- **弹簧（含末尾阻尼回弹）不吃性能** —— 消融实测（`temp/spring_ablation_probe.py`）：
  冻住弹簧后 `tick_draw` 中位数一模一样（0.0457 vs 0.0458）。**别再动它。**
- **`_frame` 自算的"半截"性质**：它只包 `_frame`，**不含** Kivy 渲染、也**不含**用 Clock 延后的
  `Label.texture_update`、守卫、自动发射…… 所以"自算很小"**不等于**"我们没干活"。

---

## 4. 已排除（死命题清单 —— **禁止以原样复活**）

| 方向 | 排除依据 |
|---|---|
| 画布指令**条数**本身 | 桌面标定 0.257µs/条；345 条全删只省约 3%；钉子只占全板 43%，74 条 Ellipse 合并成 1 条 Mesh 的收益上限 2% |
| 填充率 / overdraw | 装杯比待机多画一倍指令，中位帧间隔一模一样 |
| 球纹理 d=128→256 | 两轮独立实测中位差 1/255（≈0），代价确定（纯 Python 合成 ×4） |
| 降低渲染分辨率 | 同上性质的"减片元"路线 |
| **"GC 调优平均开销不变"** | **这条作废了** —— 它对**平均帧**成立，对 **1%Low 不成立**（1%Low 只看最坏几帧）。GC 冻结就是靠它落地的 |
| 每帧路径上有主线程阻塞调用 | 逐条查过；事件路径上的已全部搬走 |
| "把呈现节拍钉到显示网格能改善 1%Low" | 面板专家用应用真实配置在桌面复现：maxfps=60 给出的是 p50 16.65 / **p99/p50 = 1.02 的无尾部分布** ⇒ 尾部不是节拍造成的 |
| "每发才做一次的活压在最坏帧上" | 最差 20 帧**距上次发射 0~496 帧、中位 259，铺开** ⇒ 不成立（但 `park_ball` 那一笔**是**在慢帧上，见 §0） |
| "慢帧都是纯等 swap"（CPU 时间为 0） | 那几帧实算 20~24ms，CPU 确实烧掉了 |
| "采样窗口短 ⇒ 版本间差异只是噪声" | 31 vs 33 在噪声内，但 7.8 → 31 这种量级差不是 |
| MSAA 解释尾部 | 它解释的是**中位**，解释不了三倍帧（`multisamples=2` 是 Kivy 默认，main.py 从不覆盖） |

---

## 5. 还没解决的（**下一步在这**）

### 5.1 慢帧里约 8.6ms 的"主线程 CPU 在 `_frame` 外面"

**已知**：那是 Kivy 自己的渲染（canvas 遍历 + GL 提交）+ 用 Clock 延后的 `texture_update`。
**不知道**：为什么单帧能到 20ms（工程早期量过 `on_draw` 固定开销约 1ms 桌面 / 3.2ms 真机）。

**最该做的下一步**：**给 Kivy 自己的 `on_draw` 装计时**，把它与"其它 Clock 回调 + 延迟文字重排"
分开。现在面板只知道"主线程 20.4ms 里有 9.5 是 `_frame`"，剩下 11ms 是一团。

### 5.2 `文字重排` 还剩 8.8 次/秒

那 8.8 次是：飘字每 3 秒重造一个（`_bench_toast_tick`）+ 正常的文字变化（余额/状态/统计）。
**飘字"测试设备性能中"那个只在跑分期间存在** ⇒ 它是**跑分自己给自己的成绩上刑**（玩家平时见不到）。
玩家 2026-09-14 说过"先不删了，看看能不能优化" —— 已优化（淡出零重建），**没删**。

### 5.3 `主线程 CPU` / `_FRAME_THR` 这一格曾经是**硬编码 0.0**

`_FRAME_THR[0]` 一度**只有读、没有写**（那一笔编辑当时整脚本没落盘），面板照常打印"主线程0.0"
且格式串看起来完全健康 —— **一个专抓静默的面板用一个常量冒充测量值**。
v0.6.86 修好。**同类事故在本轮发生过三次**（漏定义 / 索引写反 / 加字段推位），
所以：**帧记录是位置元组，每加一个字段都要同步所有按下标取值的地方**，
而且**必须跑 `temp/benchpath_probe.py`**（见 §7）。

---

## 6. 版本历史（本轮的，按时间）

| 版本 | 内容 | 状态 |
|---|---|---|
| 0.6.65 | **发声搬出主线程** → 1%Low **7.8 → 40** | 里程碑 |
| 0.6.66/0.6.69 | 震动搬出主线程 + 启动预热 | |
| 0.6.70 | 字体预热分帧（119ms → 33ms 的最慢一步） | |
| 0.6.71 | 方向守卫/沉浸重申搬出主线程 | |
| 0.6.73 | **GC 冻结**（全量回收 26.8ms → 0.0） | |
| 0.6.75-0.6.77 | 面板加仪表 + 存档落盘搬出主线程 + 语音目录缓存 + `SFX_VOICES` 16→8 | |
| 0.6.78 | 帧累加器单帧上限 | |
| 0.6.79/0.6.80 | 余额滚动去掉 / 字号量化 | **0.6.80 是负收益，0.6.84 撤回** |
| 0.6.81-0.6.83 | 双震搬线程 / 面板仪表 / 预热扩到 22 档 | |
| 0.6.84 | ❌ **污染**（9/13 那批观感） | 别装 |
| 0.6.85 | 回滚重做 | |
| 0.6.86/0.6.87 | 修面板三处错 + 加"节拍/CPU构成/最差20帧位置" | |
| 0.6.88 | **画布 Color 淡出**（纹理重建 40 次 → 0） | |
| 0.6.89/0.6.90 | 帧率上限两版（跟屏幕走 / 固定 120）—— **都错** | |
| **0.6.91** | 上限改"不设" + **重掷改增量** | 最新 |

---

## 7. 探针清单（`android/temp/`，**不在 git**）

改之前先跑对应的那个；它们比人眼可靠。

| 探针 | 验什么 |
|---|---|
| **`benchpath_probe.py`** | **真调 `_start_bench_test()` + 真走 `_on_flip` + 真渲染面板**。抓"只跑跑分才会炸"的 bug（本轮抓过两次）。**改跑分相关代码必跑。** |
| `panel_render_probe.py` | 面板全字段渲染（非空 + 新字段都在） |
| `panel_err2.py` | 把 `_bench_diag_text` 的 `except` 拆掉，让"静默返回空串"的真异常露出来 |
| `fade_verify_probe.py` | 大字淡出零重建 + 画布 alpha 真的在变 |
| `canvascolor_probe.py` | `label.color` vs 画布 Color 的重建次数（+ 截图） |
| `slotincr_verify.py` | 槽位增量 vs 完整重画的**等价性** + 加速比 + 兜底路径 |
| `fpscap_measure.py` | 上限 4 档 × vsync 2 种 的帧间隔对照 |
| `frame_substep_probe.py` | `_frame` 子步骤归属（板面/重掷/字号/装杯） |
| `frame_gc_attrib_probe.py` | `_frame` 大帧是不是 GC |
| `texupd_who_probe.py` | 谁在反复重排文字（按控件+文字归类） |
| `accum_clamp_probe.py` | 单帧补步上限（含阴性对照） |
| `spring_ablation_probe.py` | 弹簧消融（结论：不吃性能） |
| `vibdouble_wiring_probe.py` | 双震真的走队列了 |
| `gc_freeze_wiring_probe.py` | 冻结接线 + **泄漏检查** |
| `warmladder_probe.py` | 预热覆盖整个字号阶梯 |

---

## 8. 门禁与提交流程

```bash
cd android
python -m py_compile main.py
python main.py --selftest        # ⚠️ 偶发 3σ 假失败 —— 失败先重跑一次再说
python main.py --smoke
python ../tools/fx_probe.py      # ⚠️ **已知 5 项红**（9/13 时代口径：期望主球 128×128、
                                 #    期望玻璃/球开 mipmap）。这 5 项是**预期的**，别当回归
python temp/benchpath_probe.py   # 改了跑分/面板相关就必跑
```

然后 **手工同步 `tools/` 对应分片**（见 §1.1），bump `buildozer.spec` 的 `version`，
`git add buildozer.spec main.py`（**别用 `git add -A`**，会把 temp/ 与一堆草稿一起提交），
commit（消息用 `-F 文件`，**别用反引号** —— bash 会把它当命令替换吃掉），push（触发云构建 25~90 分钟）。

---

## 9. 最便宜的下一步（按性价比排序）

1. **装 v0.6.91 跑一次跑分**，看三行：
   - `节拍： … 帧率上限 不设(0)` —— 平均帧率应从 53 回到 60+（165Hz 屏上更高）
   - `最慢三帧 … |重掷?.?` —— `重掷` 应从 9.2 掉到 0.x
   - `1%Low` —— 那 9 帧里省了 9ms，该跟着动一点
2. **给 Kivy 自己的 `on_draw` 装计时**（§5.1）—— 这是唯一还没定位的那 8.6ms。
3. 若 `最差20帧` 显示挤在某处 → 顺着那个子步骤标签挖。
4. ⚠️ `multisamples=2`（2x MSAA 全屏）是**每帧固定**带宽成本，且与"各阶段中位一致"自洽。
   **关掉它是观感决定**（边缘会有锯齿），**要玩家点头**，别自己动。
5. ⚠️ 系统栏重申周期现在是 2.5s。想再拉长/暂停来验证它对 1%Low 的影响，是**一行改动**，
   但它管的是"宽屏设备横拿抢 fullSensor"与"系统栏复活后重新隐藏"，**砍坏了没有门禁会红**
   （`--selftest` / `fx_probe` 都不走 android 分支）。

---

## 10. 用户的工作方式（省得你踩）

- **他要先选模式**：说「对抗性审查 / 头脑风暴 / 多专家评审」这类词时，
  **先弹窗让他选**（走 `对抗性评审` skill），**不要直接开 workflow**。
- **面板要的是真机数据**，而一轮云构建 25~90 分钟 ⇒ 尽量把仪表一次加齐、一次跑分多问几个问题。
- 他会**自己带数据回来**（跑分面板截图、多次跑的对比）。他说过"我测试了 10 次" —— 他的实测**算数**。
- **说话通俗**：先讲玩家视角，再给数字；别堆术语。
- **别让他测试**：能桌面复现的就桌面复现（本轮好几个结论都是桌面 A/B 出来的）。
  但要注意：**桌面上测不到真机特有的副作用**（§3.3 的字号量化就是这么骗过我的，栽过两次）。
