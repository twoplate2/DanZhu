# 1% Low 后续优化交接

更新：2026-09-14。本文只记录“真实呈现长帧”的优化路线，不把纯物理模拟吞吐、降低分辨率、关闭抗锯齿、删动画或隐藏正常游戏 UI 当作优化方案。

## 已确认的边界

- 当前渲染跑分会保留真实游戏负载：落珠、装杯、状态栏、余额、统计和正常音画逻辑都照常运行；跑分专用的中央红字已移除，避免额外测试 UI 污染成绩。
- 物理固定为 `60 步/秒`，显示/交互可按高刷新率更新。截图中的 SoC 中位约 `35035 步/秒`、一发约 `264 步`，故每物理步约 `0.0285ms`，实际物理总负载约 `1.71ms/秒`。物理绝对性能不是 1% Low 瓶颈。
- Android 已支持最高请求 165Hz（`0.7.12`）。最终是否真在 165Hz，必须看跑分面板中的“屏幕 Hz / Android 模式请求 Hz”，系统仍可因省电、温控或用户设置拒绝请求。
- 当前 1% Low 由 `Window.on_flip` 的逐帧真实间隔统计；平均帧率高而 1% Low 低，意味着应处理少量长帧，而不是继续提高平均模拟吞吐。
- 已经做过、不要重复的项目：异步 SoundPool/震动/配置落盘；启动预热；GC freeze；余额不逐帧滚字；装满静止时不重建杯子画布；落定后仅更新倍率槽而不重绘整块盘面；跑分时 SoC 阶段不计入渲染成绩。

## 先收集的真机证据

每个候选方案都必须先或后给出同设备、同设置下至少 3 次完整跑分。165Hz 的单帧预算为 `6.06ms`，不能只看 FPS 数字；应同时比较帧间隔毫秒、平均值、1% Low 和波动。

结果页目前应优先看以下四行：

1. `1% Low 定位`：最慢 1% 的帧数、平均耗时及所处分阶段。
2. `最慢帧`：阶段、主线程耗时、游戏逻辑耗时。
3. `低于 60 FPS 的帧`：用于判断是否仍有肉眼可见的重卡。
4. `文字纹理`：文字纹理重建总数与来源。

若要新增一项，只能新增能改变下一步决策的数据；音频累计耗时、总 CPU 耗时、无归因的计数器等默认不展示。

## 可行方案一：分离“游戏逻辑”与“框架/呈现”长帧

### 目标

先确认 1% 最慢帧究竟主要在飞行、装杯、落袋/重掷，还是逻辑本身很小而卡在 Kivy/GL/系统合成器。

### 做法

- 保留逐帧 `on_flip` 原始采样，不分桶、不平滑。
- 将最慢 1% 按阶段聚合，而不是只报最慢 3 帧。
- 仅对最慢帧记录：主线程 CPU、`_frame` 自身耗时、板面/装杯/字号等已有子步骤耗时。
- 若“主线程明显大于游戏逻辑”，将结论写为“框架/渲染路径候选”，不武断写成 GPU 或 CPU。

### 风险与验收

低风险，仅改善归因。验收不是分数变高，而是下一步能够明确选择飞行路径或装杯路径，且不增加跑分本身的每帧重负载。

## 可行方案二：正常 HUD 的预制纹理切换

### 触发条件

若最慢 1% 中文字纹理重建占比高，且来源是状态栏、余额、统计等正常游戏 HUD。

### 做法

不隐藏正常 UI，也不冻结其状态；把有限状态文案（例如入场、弹跳、即将入袋、发射、未中）预热为固定纹理，运行时只交换纹理/画布指令，而不是让 `Label` 重新排版、光栅化、上传。

余额与统计仍显示真实数值；只有确有高频变化并被数据证明造成长帧时，才考虑数字字符纹理缓存。

### 风险与验收

中低风险。风险是字体缩放、横竖屏布局和无障碍字体下的裁切；必须截图验证 360dp、小字/大字和横屏。验收条件是纹理重建来源显著下降，且正常游玩文字、颜色、更新时机完全不变。

## 可行方案三：装杯动画的动态画布减负

### 触发条件

若“1% Low 定位”表明装杯占最慢 1% 的主要部分，且最慢帧中 `装杯` 子步骤稳定偏高。

### 做法

保持玻璃、球贴图、球数量、透明度、旋转、落地和回弹的视觉效果不变；评估把每帧 `canvas.clear() + 重建指令` 改为“每局建立一次指令、每帧只更新会动球的位置/旋转/透明度”。

不能假定收益很大：常见倍率下同时可见的活动球数量有限，装满静止阶段已经不重绘。先做 A/B 探针，按倍率统计 `_redraw` 的 p50/p95/p99 和活动球数，再决定是否进入实现。

### 风险与验收

中风险。必须验证：进场、遮挡顺序、玻璃前后层、回弹、退场、切后台恢复、玩家点击关闭、x2/x3/x20/x100 和横竖屏截图。只有装杯阶段 1% 帧明显改善且无视觉差异才保留。

## 可行方案四：盘面状态切换的零重建化

### 触发条件

若长帧集中在落袋、待机或下一发前，且子步骤显示板面重绘/重掷偏高。

### 做法

现有代码已将“仅倍率槽变化”改为 `_update_slots()`。下一步只检查仍然触发整块 `GameArea._redraw()` 的真实场景：尺寸变化、首帧、重置、切换档位、异常兜底。

对可证明几何不变的路径继续改为原位更新颜色、文本、灯效和球位置；对尺寸或布局变化保留完整重绘，不能为了分数跳过正确刷新。

### 风险与验收

低到中风险。需要记录“完整重绘原因”，并在重置、改倍率、转屏、返回前台、连发后的落袋边界逐项截图验证。验收为落袋后的尖峰减少，而不是平均 FPS 虚高。

## 可行方案五：减少每帧临时对象与 GC 压力

### 触发条件

若最慢帧附近存在 GC 回调，或分配计数显示飞行/装杯期有稳定的短命对象峰值。

### 做法

- 优先消除每帧 `list(...)`、临时 dict、格式化字符串和重复坐标元组；改为复用小容器或只在状态变化时创建。
- 只动热点路径：`_frame`、`tick_draw`、`WinPileFX.tick/_redraw`。不对普通配置、弹窗或历史列表做无意义微优化。
- 保留现有 GC freeze 策略；若新方案要调整 GC 阈值，必须先证明长帧就是 GC，不能通过“延后回收”把卡顿转移到别处。

### 风险与验收

中低风险。验收包括内存长期运行、切后台恢复、连续多轮游戏和 3 次跑分；任何内存持续增长、退出卡顿或更大的单次 GC 都应回退。

## 可行方案六：Android 合成器与高刷模式的稳定性

### 触发条件

若游戏逻辑和主线程耗时都低，但 `on_flip` 仍出现周期性长帧；或面板显示请求 165Hz 而实际屏幕仍回到 60/120Hz。

### 做法

- 检查跑分开始、弹窗开关、回前台后实际屏幕 Hz 是否变化。
- 保持窗口的 `setFrameRate` 与 `preferredDisplayModeId` 请求；只在生命周期切换时重申，避免每帧 JNI 调用。
- 若长帧呈严格周期性，增加“慢帧相邻间隔 / 生命周期事件”这一项诊断，定位是否为系统栏、功耗策略或合成器节拍，而不是猜测游戏代码。

### 风险与验收

低风险，主要是平台诊断与生命周期修正。验收是实际模式稳定在设备允许的最高档，且不因弹窗、旋转、回前台掉回低刷。

## 明确不做的方案

- 不关闭 MSAA、不降低分辨率、不删玻璃/阴影/球贴图/动画来换帧率。
- 不通过隐藏状态栏、余额、统计或关闭正常音画反馈来伪造更高跑分。
- 不再优化纯弹道模拟吞吐；它已经远低于 120/165Hz 的帧预算。
- 不把 SoC “完整弹道吞吐”误标为解除帧率上限后的游戏 FPS。
- 不以桌面微基准直接宣称 Android 1% Low 已改善；必须由同机真机跑分验证。

## 建议执行顺序

1. 在 165Hz 实际生效的设备上跑 3 次，先看最慢 1% 的阶段归属。
2. 若文字纹理明显进入最慢帧，执行方案二。
3. 若装杯为主，先做方案三的 A/B 探针，再决定是否实现。
4. 若落袋/待机为主，执行方案四。
5. 若逻辑耗时小但仍周期性掉帧，执行方案六；若有 GC 证据，再执行方案五。

每次只落地一个可验证变更，保留前后同设备数据与截图，避免多个改动叠加后无法归因。
