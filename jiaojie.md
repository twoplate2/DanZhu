# 交接文档：弹珠机奖励表优化（×50/×100 + 本局内抽卡分布规律）

> 生成日期：2026-08-31。本文档供其他 AI 审核本次改动。涉及文件：父项目 `plinko.py`、`tools/android_part_ui.py`、`tools/android_part_backends.py`、`tools/generate_voice.py`，及生成物 `android/main.py`。

## 一、目的

玩家（作者本人实测）发现两个体验问题：

1. **高档「全是 ×2」像没有低保**：300% 档中奖率 60%，但中奖时 76% 是 ×2，连续中奖全是 ×2 的概率高达 44%，「落袋到哪都翻倍」。
2. **偶尔「一局堆 2~3 个 ×50/×100」观感失衡**（爆表盘）。

本质：**同一局内 K 个有奖格的倍率是纯独立随机采样**，没有分布规律。本次优化把它改造成「抽卡式分布」——降 ×2、软保底（别全 ×2）、软冷却（别堆大倍率），同时 **RTP 严格 = 档位**（80/120/200/300%）。

## 二、需求来源（玩家原话要点）

- 120% 档「连续 4 次大亏」不符合直觉（早期问题，已通过提中奖率缓解）。
- 200%/300% 档新增 ×50/×100 倍率。
- 「运气差时一堆 ×2 不行」「没有低保」「×2 太低」。
- 同 1 局中倍率分布要有规律：软保底（别全 ×2）+ 软冷却（别堆大倍率，如「出 ×10 后 ×10+ 概率减半」）。
- 中奖率、倍率分布、保底/冷却参数**都可以改**；唯一硬约束是 **RTP = 档位**；数值「不离谱就行」。

## 三、完整改动清单（本次会话累计）

### 1. 倍率生成改为「档位感知」（结构性改动）
- `REWARD_EV`（单值 3.35）→ `VALUE_DIST` 四档倍率分布表 + `REWARD_EV` 按档计算。
- `_reward_value(rtp, big_cooldown=False)`：按档位查表采样；`big_cooldown=True` 时 ≥×10 概率乘 `_COOLDOWN_FACTOR`。
- `roll_multipliers(rtp)`：本局内加软冷却 + 软保底（见下）。
- 新增 ×50/×100（仅 200%/300% 档）；保留 ×3；各倍率非零可感知。

### 2. 本局内抽卡分布规律（核心）
- **软保底**：盘面非零格**全是 ×2** 时，以概率 `_PITY_Q` 把最小格升级到分档阈值 `_PITY_G`。
- **软冷却**：本局已出 ≥×10 后，后续采样时 ≥×10 的概率乘 `_COOLDOWN_FACTOR`（软冷却，不硬封顶）。

### 3. 颜色/音效/震动/语音同步 ×50/×100
- 新增 `COL_X50=#ff8c00`、`COL_X100=#ff1493`；灯色/槽背景/中奖大字/震动/音效 tier 全部接上。
- `WIN_TIERS` 追加 tier5(×50)/tier6(×100)；预热名单加 win5/win6。
- 语音 `voice_win2500/5000/10000` 三条（edge-tts 已生成）。

### 4. UI 调整
- 轮次结束弹窗不再自动消失：语音播完不动弹窗，玩家点「确定」才重置关闭（去掉 `on_done=_auto_reset` + 兜底定时器）。
- 轮次结束弹窗「剩余 N 个弹珠」的 N 用 `COL_BALL` 金色（Label 开 `markup=True`）。

### 5. 回退金雨
- 从 `tools/android_part_ui.py` 移除整套「中奖金雨」代码（用户之前已回退），同步到 main.py 无金雨状态。

## 四、当前数值（最终态，`plinko.py`）

### VALUE_DIST（每档倍率概率，和=1）
| 档位 | ×2 | ×3 | ×5 | ×10 | ×20 | ×50 | ×100 |
|---|---|---|---|---|---|---|---|
| 80% | 0.813 | 0.097 | 0.050 | 0.030 | 0.010 | — | — |
| 120% | 0.764 | 0.094 | 0.080 | 0.045 | 0.017 | — | — |
| 200% | 0.600 | 0.165 | 0.120 | 0.065 | 0.035 | 0.010 | 0.005 |
| 300% | 0.580 | 0.160 | 0.120 | 0.075 | 0.040 | 0.015 | 0.010 |

### KNOB_K（每局有奖格数分布）与中奖率
| 档位 | E[value] | E[K] | KNOB_K | 中奖率 |
|---|---|---|---|---|
| 80% | 2.667 | 2.70 | (2,0.30),(3,0.70) | 30% |
| 120% | 3.0 | 3.60 | (3,0.40),(4,0.60) | 40% |
| 200% | 4.645 | 3.875 | (3,0.125),(4,0.875) | 43% |
| 300% | 5.54 | 4.873 | (4,0.127),(5,0.873) | 54% |

### 软保底 / 软冷却参数
```python
_PITY_G = {0.80: 3, 1.20: 5, 2.00: 5, 3.00: 10}  # 软保底升级目标倍率(分档)
_PITY_Q = 0.3                                      # 软保底触发概率(只在"全×2"时触发)
_COOLDOWN_FACTOR = 0.85                            # 软冷却系数(已出≥×10后 ×10+概率乘此)
```

### 解析断言
模块顶层校验 `abs(E[K]×E[value]/9 - rtp) < 0.005`（校验 raw VALUE_DIST × KNOB_K，不含软保底/软冷却）。selftest 经验 RTP 门禁：低档 ±0.05、高档（含 ×50/×100，σ 大）±0.15。

## 五、验证结果（2026-08-31 实测）

**有效 RTP**（蒙特卡洛 30 万局，含软保底+软冷却，均匀落格）：
| 档位 | 有效 RTP | 漂移 | 门禁 |
|---|---|---|---|
| 80% | 0.817 | +0.017 | OK |
| 120% | 1.239 | +0.039 | OK |
| 200% | 1.987 | -0.013 | OK |
| 300% | 2.981 | -0.019 | OK |

**效果改善**（20 万局统计）：
- 全 ×2 概率：200% 档 26%→9.85%，300% 档 20%→5.05%。
- 堆 2 个 ×20+ 概率：300% 档 ~3%（软冷却温和，未硬封顶）。

**selftest**：`plinko.py --selftest` 与 `android/main.py --selftest` 全绿（RTP 四档 OK、卡死 0、音效 41 个零异常）。

## 六、已知权衡 / 取舍（审核重点）

1. **软冷却「减半」被改成「×0.85」**：实测「减半」（×0.5）会让 300% 档 RTP 少发约 0.15（超出门禁），因为高档 ×10+ 占期望 60%，减半少发太大；软保底又救不回来（高档「全 ×2」概率仅 5%，触发率太低）。×0.85 才把四档漂移收进 ±0.04。
2. **低档（80/120）保持 ×2 高**：E[value] 低（2.667/3.0）是数学必然，80% 档在保留 ×10/×20 时 ×2 下限 ≈0.88，现 0.813 已贴底。低档是「稳妥档」，强行加保底会超发（80% 档保底 ×10 实测 RTP 飙到 1.6）。
3. **中奖率下降**：200% 档 50%→43%、300% 档 60%→54%。这是「降 ×2」的必然代价（E[value] 升、E[K] 降），玩家「更肥但更少中」。
4. **软保底只在「全 ×2」触发**：早期实现是「盘面无 ≥G」就升级，触发率高达 70%+，超发严重（200% 档 RTP 漂到 2.22）。改成「全 ×2」触发后触发率降到 5~27%，超发可控。

## 七、审核关注点（供其他 AI 检查）

1. **RTP 守恒是否真的平衡**：软保底（超发）+ 软冷却（少发）不是严格成对抵消，而是靠「重推 VALUE_DIST 降 ×2」支付 + 「×0.85 弱冷却」控制少发。请重点核验 `_PITY_G/_PITY_Q/_COOLDOWN_FACTOR` 三参数的组合是否在 ±0.05 内稳定（可用蒙特卡洛复现第六节）。
2. **软保底实现边界**：`roll_multipliers` 里软保底取「最小非零格」升级，`min((i for i,v in enumerate(mult) if v>0), key=lambda i: mult[i])` —— 当盘面有多个 ×2 时升级哪个，逻辑是否正确。
3. **软冷却的 `_reward_value` 重归一**：冷却态 `dist = {m: (p*0.85 if m>=10 else p)}` 后重归一，需确认 `sorted(dist.items())` 累计阈值仍正确（×10 概率减小后阈值顺序没乱）。
4. **解析断言只校验 raw 表**：软保底/软冷却改变「有效 E[value]」但不在断言内，靠 selftest 经验门禁兜底。若后续调参数，务必重跑 selftest 看漂移。
5. **低档保底阈值**：`_PITY_G` 里 120% 档 G=5、200% 档 G=5，是否与「低档保底倍率不能太高」的约束一致（80% 档 G=3 已接近超发边界）。
6. **金雨回退**：`tools/android_part_ui.py` 已从 main.py 提取无金雨版本回退，备份在 `tools/android_part_ui.py.bak`（含金雨，供恢复）。
