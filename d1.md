# 横屏问题归档(弹珠机 Android 版)——已解决

> **状态: 已解决**(2026-08-17, 最终版 `746b45a`)。本文是病根链 + 最终方案 + 经验教训,
> 给下次遇到厂商大屏适配问题的人省时间。

## 一、最终方案(四件套, 缺一不可)

| 层 | 做法 | 位置 |
|----|------|------|
| 安装清单 | `screenOrientation=fullSensor` + `resizeableActivity=true` | `p4a/hook.py` 注入 |
| 版本标签 | `targetSdk 33`(android.api=33) | `buildozer.spec` |
| **运行时方向守卫(解药)** | 每 0.7s + 转屏瞬间 + on_resume 调 `setRequestedOrientation(10 FULL_SENSOR)` | `tools/android_part_ui.py` `PlinkoApp._force_fullsensor/_orient_guard` |
| 横屏反旋转层 | 横窗时按等效竖屏窗口(短边×长边)布局, 以屏幕中心旋转 ±90° 铺满; 触摸逆变换; 弹窗挂层 | `tools/android_part_ui.py` `LandLayer/RotPopup` |

**用户体验定调(用户拍板)**: 横拿平板时画面与竖拿**一模一样**(构图不变、满屏), 玩家扭头看/
转回竖屏玩; 正竖↔倒竖 180° 跟手; 左右横各自跟手(`_land_angle`: rotation=1→+90, 3→-90)。

## 二、病根链(像素取证 + 屏上诊断定位)

1. **锁竖屏 = 死路**: Android 12L+ 大屏(sw≥600dp)上 app 锁竖屏不导致"屏幕不转", 而是被
   letterbox 成兼容盒。联想 ZUI 更给 **1519×1754 半宽盒**(宽=3040/2 精确一半, 高=1904-状态栏)。
2. **manifest 声明对 ZUI 无效**: fullSensor + resizeable + targetSdk33 全上, 真机横拿仍是盒
   (80b6db5 诊断版屏上读数实锤: tSDK=33, rot=1, win=1519x1754)。
3. **真凶: SDL 运行时自报竖屏**: SDL 启动/onResume 按自身 hint 调 `setRequestedOrientation`,
   运行时把 manifest 的 fullSensor 覆盖成竖屏 → ZUI 判定"竖屏 app"塞盒。
   **这台 ZUI 恰恰只认运行时请求**(d1.md 旧版"setRequestedOrientation 在 12L+ 是空操作"的
   专家结论在本机不成立)。
4. 解药 = 方向守卫持续重申 FULL_SENSOR → 真机读数变 `win=3040x1754` 全屏 + `ang=90` 反旋转生效。
5. **targetSdk 30 是帮凶**: 早期为 sensorPortrait 兼容模式降的 30, 在大屏上=固定比例盒,
   方向声明无关。已升 33(hook 里旧的降级逻辑已删)。

## 三、踩坑时间线(10+ 次提交换来的)

- 各种锁竖屏(manifest/portrait/sensorPortrait/运行时循环)→ 全部被 ZUI 盒子化解
- targetSdk 30 兼容模式 → 盒子;fullSensor + 30 → 还是盒子(标签不认白搭)
- 横屏分栏(a3f9d8e, 左盘面右控制列)→ 用户否: "游戏必须保持竖屏形态"
- 画面转 90° 铺满(躺倒, 文字正对玩家)→ 用户否: 要的是"画面与竖拿一模一样, 扭头玩"
- **SDL hint 锁竖屏残留**(KIVY_ORIENTATION 只写竖屏两方向)会覆盖 spec 四方向 → 已同步四方向

## 四、遗留观察点

- **左右横映射若反**(某一边横拿画面上下颠倒): 交换 `_land_angle()` 里 90/-90 两映射(一行)。
- 转屏瞬间"一次重排跳变"无法完全消除(系统真把盒窗换成全屏窗, 必然 relayout);
  已把重申时机压到转屏瞬间+0.7s 守卫, 延迟几百毫秒级。
- 定案后若再动方向相关代码, 真机验证顺序: 竖拿 → 倒拿(180°) → 左横 → 右横 → 触摸命中 → 弹窗。
